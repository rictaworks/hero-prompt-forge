# 画像の出どころ

このディレクトリの画像は、**`app-ui/_ds/veyra-dragon-c89babbb-c5ca-488f-a767-e5ea1062d0f9/assets/` の複製**です。

| ファイル | 元のファイル |
|---|---|
| `hero-background.webp` | `app-ui/_ds/.../assets/hero_background.webp` |
| `contact-background.webp` | `app-ui/_ds/.../assets/contact_background.webp` |

**中身は変えていません。** 名前だけを、この事業の書き方（区切りをハイフン）へ揃えています。

## 出どころ

`app-ui/` は、Claude Design プロジェクト「モック制作の範囲確認」から取り込んだものです（`app-ui/README.md`）。`_ds/` はそのデザインシステム（Veyra Dragon）の一式で、**画像もそこに含まれていたものです。**

## 確かめること

**利用条件が確認できていません**（PR #170 のレビューで指摘されました）。`app-ui/_ds/` にライセンスの記載がありません。

**本番の公開までに、次を確かめてください。**

- この画像を商用の配布物へ含めてよいか
- 表示義務（帰属の明記）があるか
- 差し替えが必要な場合の、代わりの画像

**確認が取れない場合は、AI 生成の画像へ差し替えます**（CLAUDE.md）。

## 外部から読み込みません

**画像は同梱します。** 外部の配信先へ取りに行きません。**画面が外部の都合で崩れないようにするためです。**

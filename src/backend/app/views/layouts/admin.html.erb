<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <title><%= t('admin.headings.title') %></title>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <h1><%= t('admin.headings.title') %></h1>
    </header>
    <main>
      <%= yield %>
    </main>
  </body>
</html>

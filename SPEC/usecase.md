# ユースケース図

**実装から起こした図です。** 実装済みの操作だけを載せます。

```mermaid
graph LR
    利用者(("利用者<br/>（X のフォロワー）"))
    管理者(("開発者<br/>（管理画面）"))
    Bot対策["reCAPTCHA<br/>（Google）"]
    判定["x-follower-gate<br/>（フォロワー判定）"]
    LLM["Gemini<br/>（プロンプト精緻化）"]

    subgraph サービス
        UC1["X でログインします"]
        UC2["プロジェクトを作ります"]
        UC3["生成条件を入力します"]
        UC4["生成をお申し込みします"]
        UC5["進み具合を見ます"]
        UC6["3 案を受け取り、写し取ります"]
        UC7["評価メモを残します"]
        UC8["条件をプリセットへ保存します"]
        UC9["プリセットを呼び出します"]
        UC10["生成履歴を見ます"]
        UC11["同じ条件で作り直します"]
        UC12["判定を取り直します"]
        UC13["利用規約・ポリシーを読みます"]

        UC20["規則辞書の版を作り、公開します"]
        UC21["利用者とプラン値を見ます"]
        UC22["判定を取り直します（代行）"]
        UC23["本日の枠をリセットします"]
        UC24["利用状況を見ます"]
    end

    利用者 --> UC1
    利用者 --> UC2
    利用者 --> UC3
    利用者 --> UC4
    利用者 --> UC5
    利用者 --> UC6
    利用者 --> UC7
    利用者 --> UC8
    利用者 --> UC9
    利用者 --> UC10
    利用者 --> UC11
    利用者 --> UC12
    利用者 --> UC13

    管理者 --> UC20
    管理者 --> UC21
    管理者 --> UC22
    管理者 --> UC23
    管理者 --> UC24

    UC1 -.-> 判定
    UC4 -.-> Bot対策
    UC4 -.-> LLM
    UC22 -.-> 判定
```

## 前提

| 操作 | 誰が行えるか |
|---|---|
| 利用規約・ポリシーを読みます | **誰でも行えます。** ログインを求めません |
| X でログインします | 誰でも試せます。**フォロワーでない方は入れません** |
| 生成のお申し込み | **プラン値が `active` の方だけ**です |
| そのほかの操作 | ログイン済みの方です |
| 管理画面のすべて | **BASIC 認証を通った開発者だけ**です。利用者の権限では届きません |

## 実装していないもの

**この図に無い操作は、実装していません。** 例として、プロジェクトの削除・利用者の削除・案の再生成の個別指定は、経路そのものがありません。

# セキュリティレビュー : 画面 02〜09

対象の issue：#70（履歴・一覧）・#71（入力フォーム）・#152（名前の書き方の案内）・#72（生成中）・#73（結果 3 案）・#74（評価メモ）・#75（プリセット）・#76（縮退・エラー）・#77（管理への導線）

## 1 回目（`main` の取り込み）

- 実施日時（JST）：2026-08-27
- 差分の指紋：`cab388d0f4163694`
- 対象：`main`（PR #170 のマージ）をこの枝へ取り込みます

### 変更の範囲

**この枝が持っていた履歴と、`main` の内容を合わせただけです。** 実装の変更を含みません。

食い違ったのは 4 ファイルで、いずれも PR #170 の側（`main`）が新しいものです。

| ファイル | 解いた方針 |
|---|---|
| `review-records/security-review/feature-issue-69-landing.md` | **`main` の内容を採ります。** 6 回目まで記録された側です |
| `test/pr170/landing.spec.ts` | **`main` の内容を採ります。** 読み込みの完了を待たない直しが入っています |
| `src/frontend/src/components/layout/AppBar.test.tsx` | **両方を残します。** この枝の `linkTo` への差し替えと、`main` の並びを合わせました |
| `src/frontend/src/strings/index.test.ts` | **両方を残します。** この枝の選択肢の除外と、`main` のモックの既定値の検査を合わせました。**同じ検査が二重に入らないことを確かめました** |

### 確認した観点

| 観点 | 該当 | 根拠 |
|---|---|---|
| 秘匿値の混入 | **ありません** | 取り込みだけです。値を書き足していません |
| 守りの後戻り | **ありません** | `main` が足した 3 つの検査（外への出口・モックの既定値・アカウント名）は、すべて残っています |
| 認証・権限 | 該当しません | |
| 外部への通信 | **増えません** | |

### 指摘

指摘はありません。

---

## 2 回目（画面 03〜09 の実装）

- 実施日時（JST）：2026-08-27
- 差分の指紋：`f985ff535ffdece2`
- 対象：#71 ・ #152 ・ #72 ・ #73 ・ #74 ・ #75 ・ #76 ・ #77

### 変更の範囲

| 置き場 | 内容 |
|---|---|
| `src/frontend/src/app/requests/new/` | 入力フォーム（03）です |
| `src/frontend/src/app/requests/[id]/` | 生成中（04）と、縮退・失敗・差し戻し（08）です |
| `src/frontend/src/app/requests/[id]/result/` | 結果 3 案（05）です |
| `src/frontend/src/app/requests/[id]/notes/` | 評価メモ（06）です |
| `src/frontend/src/app/presets/` | プリセット（07）です |
| `src/frontend/src/components/requests/` | 上の 4 画面の中身です |
| `src/frontend/src/components/presets/` | プリセットの一覧です |
| `src/frontend/src/components/exceptions/` | 上限到達と差し戻しの見せ方です |
| `src/frontend/src/config/recaptcha.ts` ・ `src/frontend/src/lib/recaptcha.ts` | 人の操作の合図（reCAPTCHA v3）です |
| `src/frontend/src/hooks/useRequest.ts` | 結果が出るまで状態を取りに行きます |
| `src/frontend/src/components/layout/AppBar.tsx` | **管理への導線を外しました**（#77） |
| `src/frontend/src/config/screens.ts` | 固定の入口と、実装済みかどうかを分けました |
| `src/backend/app/services/prompt_requests/representation.rb` | 案の識別子と、記録済みの評価メモを返します |
| `SPEC/api/README.md` | 入力条件で選べる値と、上の 2 項目を契約へ書きました |

### 確認した観点（`.claude/OWASP10.md`）

| 観点 | 該当 | 根拠 |
|---|---|---|
| **アクセス制御の壊れ** | **該当します。守れています** | 画面が呼ぶのは `/api/v1` だけで、絞り込みはすべてバックエンドが行います。**画面から利用者の識別子を送る経路を作っていません。** 他人の資源は `404` です |
| **秘匿値の露出** | **ありません** | 画面が持つのはサイト鍵（ブラウザへ配ってよい値）だけです。**秘密鍵を持ちません。** バックエンドの場所は `NEXT_PUBLIC_` に置かず、中継はサーバー側が解決します |
| **注入（XSS）** | **該当します。守れています** | 差し戻しの「見つかった語」は利用者が送った文字です。**React が逃がします。** `dangerouslySetInnerHTML` を 1 か所も使っていません |
| **安全でない設計** | **該当します** | 人の操作の合図は**投入の経路だけ**に付けます。閲覧には付けません（`SPEC/api/README.md` の判断に従います） |
| **設定の誤り** | **該当します。守れています** | サイト鍵は、**本番で未設定ならその場で失敗**します。開発・テストでは合図を送りません。**この分かれ方は環境だけで決まります**（`src/frontend/src/config/recaptcha.test.ts` の 6 例で固定しました） |
| **古い部品** | **ありません** | 依存を 1 つも足していません |
| **認証の不備** | **該当しません** | 画面は認証の判定を持ちません。未認証は API が `401` を返し、画面はその文言をそのまま出します |
| **データの完全性** | **該当します** | 外から読み込むのは Google の reCAPTCHA だけです。**投入する画面を開いたときにだけ読み込みます** |
| **記録と監視** | **該当します** | 呼び出しの成否は `Trace` に残ります。**秘匿値と利用者の文章を記録へ渡していません** |
| **要求の偽装（SSRF）** | **該当しません** | 画面から任意の場所へ送る経路がありません |

### 判断したこと

**画面で止めるのは、送る前に分かる誤りだけです。** 必須の 3 項目・長さ・色の書き方に限り、**規則による差し戻しは送ってから伝えます。** 画面の検査を守りの代わりにしません。**守りはバックエンドが持ちます。**

**上限到達と差し戻しの文言は、API が返したものをそのまま出します。** 画面で言い換えると、実際の状態とずれます。**画面が持つのは、API が返さない見出しと、種別から引く説明だけです。**

**評価メモを、案と一緒に返す形へ変えました。** 案ごとに引き直すと、記録の無い案で「見つかりません」を制御の代わりに使うことになります。**返す範囲は広がりません。** すでに案を返せる相手にだけ、その案に付いた記録を返します。

**ネイティブの警告表示（alert 等）を 1 か所も使っていません。** すべて画面の中で伝えます。

### 指摘

指摘はありません。

---

## 3 回目（ブラウザ操作の確認）

- 実施日時（JST）：2026-08-27
- 差分の指紋：`20d73b30548010a6`
- 対象：`test/pr174/screens.spec.ts`

### 変更の範囲

**ブラウザ操作による確認を 24 例足しました。** PR 本文の非エンジニア向けユーザーテスト手順と 1 対 1 で対応します。

### 確認した観点

| 観点 | 該当 | 根拠 |
|---|---|---|
| 秘匿値の混入 | **ありません** | 接続先は `E2E_BASE_URL` から読みます。**本番を指すと、その場で失敗します**（`playwright.config.ts` の既存の守り） |
| 外部への通信 | **ありません** | 対象は開発サーバーだけです |
| 記録の残り方 | **該当します** | 保存するプリセットの名前に時刻を入れます。**利用者の情報を含みません** |
| ネイティブの警告表示 | **検めています** | 画面が `alert` 等を出さないことを、1 例で固定しました |

### 指摘

指摘はありません。

---

## 4 回目（取り込みの跡の始末と、テストの独立）

- 実施日時（JST）：2026-08-27
- 差分の指紋：`779f834ec0dce243`
- 対象：`.env.example` ・ `test/pr174/screens.spec.ts`

### 直したこと

| 指摘 | 対応 |
|---|---|
| **`.env.example` に取り込みの跡が残っています**（pr-checker の実測） | **跡の 3 行を取り除き、`E2E_BASE_URL` を 1 つにしました。** 残したのは `main` の側（`http://localhost:3000`）です。**コンテナの中から見た番号です** |
| **ブラウザ操作の確認が、並べて動かすと落ちます**（同上） | **記録が残ることを確かめる例を、その例だけで完結させました。** 自分で書いてから読み直します。**前の例が保存した内容に頼りません** |

**取り込みの跡が他に残っていないことを、リポジトリ全体で確かめました。** 検出は 0 件です。

### 確認した観点

| 観点 | 該当 | 根拠 |
|---|---|---|
| 秘匿値の混入 | **ありません** | 見本の値は、いずれも手元の番号です |
| **設定の誤り** | **直しました** | 跡が残ったままでは、`.env.example` を写した環境で `E2E_BASE_URL` が二重に定義されます |
| 外部への通信 | **増えません** | |

### 実測

**並べて動かして 24 例すべてが緑になることを確かめました**（21.4 秒）。

### 指摘

指摘はありません。

---

## 5 回目（レビューの指摘への対応）

- 実施日時（JST）：2026-08-27
- 差分の指紋：`8d93d2e4b5680f42`

### 対応した指摘

| 指摘 | 対応 |
|---|---|
| **重大 1**（`.env.example` に取り込みの跡） | **4 回目で直しました** |
| **重大 2**（`app-ui/degraded.html` と見た目が一致しません） | **次回のリセット時刻をモックの大きさへ戻し、日付の添え書き・消費の帰属・返還される場合を足しました。** 縮退には**案の見出しと、組み立てた本文の引用**を足しました。差し戻しには**送っていただいた文章の写しと、見つかった語の印**、および**「入力を修正する」**を足しました |
| **重大 3**（「同じ条件で作る」が同じ条件になりません） | **取り出しの契約へ `inputs` を足しました。** 返す範囲は広がりません（要求した本人が送った入力の写しです）。**一覧には載せません。** 画面は、もとの生成リクエストから条件をそのまま引き継ぎます |
| **要修正 4**（`GET /api/v1/session` が仕様書にありません） | **経路の一覧と、詳しい節を足しました** |
| **要修正 5**（選択肢の一致を誰も検めません） | **画面側の定義を読み取って比べます。** 片方だけを増やすと赤くなります。**読み取れない書き方に変わった場合も落ちます** |
| **要修正 6**（呼び名を 1 つ落とすと画面全体が落ちます） | **呼び名が選択肢を漏れなく覆っていることを検めます。** あわせて**逃がし先（`error.tsx`）**を足しました |
| **要修正 7**（上限到達と差し戻しにテストがありません） | **画面の単体テスト 22 例と、ブラウザ操作の確認 6 例**を足しました |
| **要修正 8**（設定どおりに流すと落ちます） | **設定を実際の流し方へ合わせました。** 相手は 1 台の開発サーバーと 1 つのアカウントで、**枠は 1 日 1 回の共有の資源です。** あわせて、順番に頼る例をすべて独立させました |
| **要修正 9**（環境以外で切り替える抜け道） | **4 つの別の変数を立てても切り替わらないことを固定しました** |
| **要修正 10**（開発向けの近道が素通りします） | **`/admin` で始まる入口が 1 つも無いことを、4 つの画面で確かめます** |
| **要修正 11**（読み込みが一度失敗すると取り直せません） | **持ち回しを解きます。** 書き換わる値も入れ物の中へ収めました |
| **要修正 12**（写し取りが静かに壊れます） | **呼び出しの手前で受け止めます** |
| **要修正 13**（終わった状態でも「Generating」） | **状態に合わせた見出しにしました** |
| **提案 14 ・ 15 ・ 16 ・ 17 ・ 18 ・ 19 ・ 22 ・ 24** | いずれも対応しました |

### 確認した観点（`.claude/OWASP10.md`）

| 観点 | 該当 | 根拠 |
|---|---|---|
| **注入（XSS）** | **該当します。守れています** | **送っていただいた文章と、見つかった語を、そのまま画面へ出すようになりました。** React が逃がします。**`dangerouslySetInnerHTML` を 1 か所も使っていません。** 見つかった語で切り分けるときも、正規の表現で特別な意味を持つ文字を逃がします |
| **秘匿値の露出** | **ありません** | 新しく出すのは、要求した本人が送った入力だけです |
| **アクセス制御の壊れ** | **広がりません** | 契約へ足した `inputs` は、すでに案を返せる相手にだけ返ります。**絞り込みはバックエンドが行います** |
| **設定の誤り** | **強めました** | Bot 対策の分かれ方が環境だけで決まることを、4 つの別の変数で確かめます |
| **安全でない設計** | **強めました** | 上限到達の次回リセット時刻は、**無ければその場で失敗させます。** 既定へ寄せません |
| **記録と監視** | **該当します** | 逃がし先は、記録へ残したうえで画面でお伝えします |
| 古い部品 | **ありません** | 依存を 1 つも足していません |

### 実測

**RSpec 1799 例・Jest 132 例・ブラウザ操作 36 例、いずれも失敗 0 件。**
**ESLint ・ 型検査 ・ ハードコードの検査は、いずれも指摘 0 件です。**
**ブラウザ操作の確認は、設定どおりの実行で 2 回連続して緑です**（枠が残っている状態からも、使い切った状態からも通ります）。

### 指摘

指摘はありません。

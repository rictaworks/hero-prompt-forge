# セキュリティレビュー : 画面 02〜09

対象の issue：#70（履歴・一覧）・#71（入力フォーム）・#152（名前の書き方の案内）・#72（生成中）・#73（結果 3 案）・#74（評価メモ）・#75（プリセット）・#76（縮退・エラー）・#77（管理への導線）

## 1 回目（`main` の取り込み）

- 実施日時（JST）：2026-08-27
- 差分の指紋：`cab388d0f4163694`
- 対象：`main`（PR #170 のマージ）をこの枝へ取り込みます

### 変更の範囲

**この枝が持っていた履歴と、`main` の内容を合わせただけです。** 実装の変更を含みません。

食い違ったのは 4 ファイルで、いずれも PR #170 の側（`main`）が新しいものです。

| ファイル | 解いた方針 |
|---|---|
| `review-records/security-review/feature-issue-69-landing.md` | **`main` の内容を採ります。** 6 回目まで記録された側です |
| `test/pr170/landing.spec.ts` | **`main` の内容を採ります。** 読み込みの完了を待たない直しが入っています |
| `src/frontend/src/components/layout/AppBar.test.tsx` | **両方を残します。** この枝の `linkTo` への差し替えと、`main` の並びを合わせました |
| `src/frontend/src/strings/index.test.ts` | **両方を残します。** この枝の選択肢の除外と、`main` のモックの既定値の検査を合わせました。**同じ検査が二重に入らないことを確かめました** |

### 確認した観点

| 観点 | 該当 | 根拠 |
|---|---|---|
| 秘匿値の混入 | **ありません** | 取り込みだけです。値を書き足していません |
| 守りの後戻り | **ありません** | `main` が足した 3 つの検査（外への出口・モックの既定値・アカウント名）は、すべて残っています |
| 認証・権限 | 該当しません | |
| 外部への通信 | **増えません** | |

### 指摘

指摘はありません。

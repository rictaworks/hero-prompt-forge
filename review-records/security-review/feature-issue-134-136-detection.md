# セキュリティレビュー : feature-issue-134-136-detection

- 実施日時（JST）：2026-08-26 20:13
- 差分の指紋：3f94ed7aa9ba609d
- 対象：issue #134 ／ issue #136

## 判定

合格

## この変更の性質

**検出の網を広げます。広げ方を誤ると、権利の問題と関係のない利用者を締め出します。**

- 禁止入力の検出（#134）が行きすぎると、**ロゴ制作を業務とする利用者・商標の調査を業務とする士業**が生成をお申し込みになれません。
- アンチAIルック規則（#136）が行きすぎると、**利用者が指定したブランドカラーや撮影の指示が、黙って消えます。**

そのため、**両方向（誤検出と取りこぼし）で測ること**を最も重く見ました。

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査（password / secret / api key / token / 私有鍵） | 検出なし |
| **禁止入力：止めるべき言い回し** | 実測（13 件） | **取りこぼし 0 件。** issue #134 が挙げた 5 件（助詞が「は」・複合名詞・副助詞・修飾が長い）をすべて差し戻します |
| **禁止入力：通すべき言い回し** | 実測（15 件） | **誤検出 0 件。** 「他社のロゴの調査結果を掲載します」「商標を侵害しないか確認します」「ロゴ制作を承っております」などを通します |
| 主語と目的語の取り違え | 実測 | 「他社のロゴが入った看板を背景に使います。」は、取りこぼしのまま残します。助詞に「が」を加えていません |
| 直す前の実装で赤くなること | 実測 | 禁止入力のテストが 5 件失敗します |
| **アンチAIルック：通すべき素材 40 件** | 実測 | **誤検出 0 件（0.0%）。** 撮影の指示・照明・被写界深度・ブランドカラーが消えません |
| **アンチAIルック：止めるべき素材 33 件** | 実測 | 取りこぼし 11 件（33.3%）。直す前は 15 件（45.5%）でした |
| **短い語 11 語での巻き込み** | 実測 | 40 件中 5 件（12.5%）。**直す前と同じです。** 5 件とも、登録した語がその素材に語として本当に入っています |
| 語の切れ目で見る性質 | テスト | `teal` が `stealth` を巻き込みません。そろえるのは語ごとで、語の並びは変えません |
| **記号を含む語の巻き込み** | テスト | `art,` を登録しても `smart, clean layout` を巻き込みません。**直す前は丸ごと落ちていました** |
| 全角の英字 | テスト | `ＰＵＲＰＬＥ ＴＯ ＴＥＡＬ ＧＲＡＤＩＥＮＴ` を取り除きます。**直す前は素通りしていました** |
| 対応表の中身を検めること | テスト | 一覧でない・空・文字列でない・同じ語へ寄せる行を、いずれも `InvalidDefinitionError` で止めます |
| 複数形でない語を壊さないこと | テスト | `lens` ・ `glass` ・ `news` ・ `focus` ・ `analysis` ・ `canvas` を触りません |
| 正規表現の危険 | 目視 | 追加した正規表現に入れ子の量指定子がありません。窓は 20 文字の上限付きです |
| 例外に含める情報 | 目視 | 語と鍵のみです。利用者の入力・秘匿値を含みません |
| 書き方の検査 | RuboCop | 96 ファイル、指摘 0 件 |
| テスト | RSpec | 681 例、失敗 0 件 |
| ハードコードの検査 | 検査 | 日本語の文字列リテラルはありません |

## 判断したこと

**助詞に「が」を加えません。** 「他社のロゴが入った看板」は主語であり、目的語ではありません。加えると「他社のロゴが目印です」のような文まで巻き込みます。**取りこぼしを 1 件残す判断です。**

**`ロゴマーク` を `ロゴ` より先に置きます。** 先に `ロゴ` が当たると、利用者へお伝えする語が読みづらくなります。

**語順の違いと言い換えは、この変更では扱いません。** 語彙の問題ですので、規則辞書の側で語を足します（issue #65）。取りこぼし 11 件のうち 9 件がこれに当たります。残る 2 件は日本語の素材で、issue #41 の決定（素材は英語で作る）により発生しません。

**「決めること」として挙がっていた 3 点を、この変更で片付けました。**

| 論点 | 決めたこと |
|---|---|
| 記号を含む語 | **照合の側で落とします。** 規則辞書の編集画面（issue #65）で弾く形にすると、すでに入っている語を直せません |
| 全角の英字 | **照合の側でそろえます。** 日本語入力のままローマ字を打つと全角になります |
| 打ち消しの言い回し（`no ...`） | **扱いません。** issue #41 で「後段は `no …` の形の素材を作らない」と決め、注釈へ残してあります |

**計測の道具を `scripts/measure/` へ置きました（`src` の外です）。** 検出の仕組みを触るたびに、同じ素材で両方向を測り直せます。素材の一覧は、レビューで積み上げたものです。

## 指摘

指摘はありません。

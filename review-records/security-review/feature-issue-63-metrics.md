# セキュリティレビュー : 測定軸の記録

- 実施日時（JST）：2026-08-27
- 差分の指紋：0cb18f094c1ae3ca
- 対象：issue #63

## 変更の範囲

| 分類 | ファイル |
|---|---|
| 測定軸の記録（今回の中心） | `src/backend/app/services/metrics/recorder.rb` |
| 記録の入れ物 | `src/backend/app/models/metric_event.rb` ・ `db/migrate/20260827090000_create_metric_events.rb` |
| 記録の呼び出し | `src/backend/app/services/quota/reservation.rb` |
| 決着の引き当ての切り出し | `src/backend/app/services/quota/settlement.rb` |
| 文言 | `src/backend/config/locales/ja.yml` |
| テスト | `spec/services/metrics/recorder_spec.rb` ほか |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** 経路・認証の変更もありません。

## 確かめたこと

| 観点 | 方法 | 結果 |
|---|---|---|
| **個人を特定できる形で記録しないこと** | テスト | 残すのは**軸・日・件数**だけです。利用者の識別子を持つ列がありません |
| 仕様に無い軸を記録しないこと | テスト | `MetricEvent::AXES` の外は、記録も集計もできません |
| クォータ日の境界 | テスト | JST 03:00 を境界とします |
| 上限到達の数え方 | テスト | 巻き戻りません。枠を取る処理の外で記録します |
| クォータ返還の数え方 | テスト | 返還のときだけ数えます。確定では数えません |
| 並列の記録 | 実装 | **1 文で数え上げます。** 読んでから足して書きません |
| 秘匿値の混入 | 目視 | ありません |
| 外部への通信 | 目視 | ありません |
| 書き方の検査 | RuboCop | 152 ファイル、指摘 0 件 |
| テスト | RSpec | 1303 例、失敗 0 件 |
| ハードコードの検査 | 検査 | 検出 0 件 |

## 判断したこと

**個人を特定できる形で記録しません。** 残すのは、軸の名前・クォータ日・件数だけです。**利用者の識別子を渡す入口を持ちません。**

**出来事ごとに 1 行を作りません。** 行数が利用者数と生成回数に比例して増えます。**集計の対象は件数だけ**ですので、日と軸で 1 行にまとめます。

**仕様が定める軸だけを扱います。** 定義に無い指標を増やしません。知らない軸を渡されたら、その場で失敗させます。**増やす場合は requirements.md 7.1 を先に改めます。**

**数え上げは、データベースの側で 1 文で行います。** 読んでから足して書くと、同時に呼ばれたときに数が合いません。**行に錠をかける形も試しましたが、呼び出す側が例外の処理の途中にあるとき、入れ子のトランザクションが中断状態になり、記録そのものが落ちました。** 1 文なら、その心配がありません。検証を通しませんが、**軸の名前は先に検め、日と軸の重なりは一意索引が止めます。**

**上限到達は、枠を取る処理の外で記録します。** 中で記録すると、上限到達で巻き戻るときに記録まで一緒に巻き戻ります。

**決着の引き当てを切り出しました。** クォータの持ち場が長くなりすぎたためです。振る舞いは変えていません。

## 申し送り

**仕様が定める測定軸は 8 つあります**（requirements.md 7.1）。この PR で実装したのは、issue #63 の受け入れ条件が挙げる 2 つ（上限到達・クォータ返還）だけです。**残る 6 つは、生成リクエストの受け口（issue #55）と結果の画面（issue #73）が揃ってから足してください。**

**集計の画面（issue #68）は、この記録を読みます。**

## 指摘

指摘はありません。

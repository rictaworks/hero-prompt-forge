# セキュリティレビュー : 測定軸の記録

- 実施日時（JST）：2026-08-27（2 回目。レビューの指摘への対応を反映しました）
- 差分の指紋：fb6aec5703bcd7bf
- 対象：issue #63

## 変更の範囲

| 分類 | ファイル |
|---|---|
| 入れ物 | `src/backend/db/migrate/20260827090000_create_metric_events.rb`・`src/backend/db/schema.rb`・`src/backend/app/models/metric_event.rb` |
| 記録 | `src/backend/app/services/metrics/recorder.rb` |
| 本業から切り離す入口（**今回の追加**） | `src/backend/app/services/metrics/side_channel.rb` |
| 呼び出し | `src/backend/app/services/quota/reservation.rb` |
| 決着の引き当て | `src/backend/app/services/quota/settlement.rb` |
| テスト | `src/backend/spec/services/metrics/recorder_spec.rb`・`side_channel_spec.rb`・`spec/services/quota/reservation_spec.rb` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** 経路・認証の変更もありません。

## 残す情報

**軸の名前・クォータ日・件数の 3 つだけです。** 利用者の識別子を持ちません。渡す入口もありません。

**作成時刻・更新時刻を持ちません（`t.timestamps` を置きません）。** 作成時刻は「その日にはじめて起きた瞬間」をマイクロ秒で残します。`quota_consumptions`（利用者の識別子を持ちます）・`prompt_requests`・記録のいずれとも時刻で突き合わせられますので、**「その日の最初に上限へ達したのがどなたか」が絞り込めます。** 件数の少ない日ほど絞り込めます（PR #164 のレビューで指摘されました）。**集計に必要なのは日と件数だけです。**

**件数の列名を `occurrences` にします。** `count` だと `MetricEvent.count`（行数）と紛れ、集計の画面で静かに行数が返ります。

## 確かめたこと

| 観点 | 方法 | 結果 |
|---|---|---|
| 残す列 | テスト | **引き算をせず、列の全体**を検めます。`id` ・ `axis` ・ `occurred_on` ・ `occurrences` の 4 つだけです |
| 時刻の列 | テスト | 持ちません |
| 定義に無い軸 | テスト | その場で失敗させます。**`SideChannel` でも受け止めません** |
| 記録の失敗と上限到達のお知らせ | テスト | **お知らせが置き換わりません。** 次回のリセット時刻も届きます |
| 記録の失敗と返還の決着 | テスト | **返還は成功したままです。** やり直しの効かない状態を残しません |
| 記録の失敗の追跡 | テスト | `metrics.record_failed` として残します。**握りつぶしません** |
| 並列の数え上げ | テスト | 20 並列で 1 行・20 件です |
| 軸の名前の置き場 | 目視 | `MetricEvent` の定数だけです。書き写していません |
| SQL の組み立て | 目視 | `Arel.sql` へ渡すのは定数の文字列だけです。外から来た値を差し込みません |
| 秘匿値の混入 | 目視 | ありません |
| 書き方の検査 | RuboCop | 173 ファイル、指摘 0 件 |
| テスト | RSpec | 1525 例、失敗 0 件 |
| ハードコードの検査 | 検査 | 検出 0 件 |

## 判断したこと

**測定は本業の脇にあるものです。本業の失敗の種類を書き換えません。** `Metrics::SideChannel` を通し、記録の置き場の失敗だけを受け止めます。**握りつぶしません。** 失敗そのものを `metrics.record_failed` として残します。**定義に無い軸は受け止めません。** それは書き間違いであり、失敗させるべきものです。

**数え上げを、データベースの側で 1 文で行います。** 読んでから足して書くと、同時に呼ばれたときに数が合いません。行に錠をかける形は、呼び出す側が例外の処理の途中にあるときに入れ子のトランザクションが中断状態になり、記録そのものが落ちました。

**検証を通しません（`upsert`）。** 軸の名前は先に検め、日と軸の重なりは一意索引が止めます。

## 既知の制限（申し送り）

**呼び出す側がトランザクションで包むと、上限到達の記録が巻き戻ります。** `Metrics::Recorder` は自前のトランザクションを持ちません。**枠を取る処理の中で見れば巻き戻りませんが、外側で包まれると巻き込まれます。**

**`Quota::Reservation.reserve!` ・ `settle!` を、トランザクションで包んで呼ばないでください。** 実装のコメントにも書きました。**この制限を固定するテストを置いています**ので、直したときに赤くなり、申し送りを消し忘れずに済みます。

生成リクエスト API（issue #55）は、この申し送りに従って**包まずに**呼びます。

## 指摘

指摘はありません。

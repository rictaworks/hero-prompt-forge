# セキュリティレビュー : fix-issue-122-merge-gate-verdict

- 実施日時（JST）：2026-08-26 19:30
- 差分の指紋：40c17f483585f8d7
- 対象：issue #122 ／ issue #137

## 判定

合格

## この変更の性質

**検査の仕組みそのものを触ります。** 検査を緩める方向の変更は、以後のすべての PR に影響します。そのため、次の 2 点を最も重く見ました。

1. マージ前フックが、**通してはいけない記録を通さないこと**
2. 初期データの検査が、**中身の欠落を見逃さないこと**

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査（password / secret / api key / token / 私有鍵） | 検出なし |
| **フックが緩くなっていないこと** | テスト（9 件） | 判定が芳しくない記録・「## 判定」の節が無い記録・節が空の記録・reviewer の記録が無い場合・pr-checker の記録が無い場合・PR 番号が無い場合の 6 通りで、いずれも止めます |
| **直す前の実装で赤くなること** | 実測 | 変更前のフックに対して 9 件中 3 件が失敗します（判定の節の外の語・節が無い・節が空）。**直したことを、テストが実際に検知します** |
| フックの検査が外の状態に依存しないこと | 目視・テスト | 一時ディレクトリへ `git init` した最小のリポジトリを対象にします。このリポジトリの `review-records/` を読みません |
| 自己再帰の危険（TH1） | 目視 | 検査対象はフックのシェルスクリプトです。テストランナー自身を起動しません |
| 実行時間の上限（TH5） | 目視 | CI の実行を `timeout 120` で包みます。テスト側も 1 件ごとに 20 秒の上限を持ちます |
| **初期データの中身が欠けたら気づくこと** | 実測 | 初期データからクリシェ配色の語と 1 業種を消したところ、テストが 2 件失敗しました。**語の有無だけを見る形から、仕様が挙げる中身を名指しで確かめる形へ変わりました** |
| 初期データの検査がデータベースに依存しないこと | テスト | 規則辞書の記録を全件消したうえで、定義を読めることを確かめます |
| 外部からの入力の扱い | 目視 | フックが読むのは、標準入力のツール呼び出しの内容と、リポジトリ内の記録です。標準入力の内容を `eval` せず、`case` の一致と `grep -oE` の抽出だけに使います |
| PR 番号の取り出し | 目視 | `grep -oE '[0-9]+'` で数字だけを取り出し、そのままパスへ差し込みます。数字以外は含まれないため、パスの外へ出られません |
| 記録に残す情報 | 目視 | フックの出力は、ブランチ名・PR 番号・記録のパスのみです。利用者の情報を含みません |
| 書き方の検査 | RuboCop | 94 ファイル、指摘 0 件 |
| テスト | RSpec 633 例・node --test 9 例 | いずれも失敗 0 件 |
| ハードコードの検査 | 検査 | 日本語の文字列リテラルはありません |

## 判断したこと

**「## 判定」の節が無い記録は止めます。** 読めないものを通すと、検査が黙って無効になります。緩める方向へ倒しません。

**節の中身が空白だけの場合も止めます。** 見出しだけを置いて中身を書かない形で、検査を迂回できないようにするためです。

**判定の節の中では、これまでどおり語の一致で見ます。** 節の中に書かれる語は判定そのものであり、説明の文脈は入りません。

## 指摘

指摘はありません。

---

# セキュリティレビュー（2回目） : fix-issue-122-merge-gate-verdict

- 実施日時（JST）：2026-08-26 19:36
- 差分の指紋：9a581da793d7dc52
- 対象：レビュー用の一時的な作業ツリーの置き場を決めます

## 判定

合格

## 変更の内容

reviewer が PR の内容を実際に動かして確かめられるよう、`/.review/` を版管理から外します。dev コンテナはリポジトリの直下だけを見ますので、コンテナから見える位置に一時的な作業ツリーを作る必要があります。

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査 | 検出なし |
| **秘匿ファイルを取りこぼさないこと** | 目視 | 追加したのは `/.review/` の 1 行だけです。既存の除外（`.claude/*.md`・`CLIENT/`・`DEBUG/`・`ENV/`・`TASKS/`・`WORK/`）に手を入れていません |
| 除外の範囲が広すぎないこと | 目視 | 先頭の `/` を付け、リポジトリ直下の `.review/` だけを対象にします。配下のどこかにある同名のディレクトリまで巻き込みません |
| 検査の対象から外れないこと | 実測 | ハードコードの検査は `src/backend/app` ・ `src/backend/lib` ・ `src/frontend/src` を、与えられた起点からの相対で見ます。`/.review/` の下は起点に含まれないため、二重に数えることも、見落とすこともありません |
| 一時ツリーの後始末 | 目視 | 削除しません。`DELETE/` へ移す運用を `test/README.md` に書きました |

## 指摘

指摘はありません。

---

# セキュリティレビュー（3回目） : fix-issue-122-merge-gate-verdict

- 実施日時（JST）：2026-08-26 19:43
- 差分の指紋：6c600eb86a04b15c
- 対象：PR #140 のレビュー指摘への対応

## 判定

合格

## 直した内容

**コードレビューで、門番に穴が見つかりました。** 判定の節に「不合格」があるかだけを見ていたため、**「要修正」「マージ不可」「保留」と書いた記録が、いずれも通り抜けていました。**

本プロジェクトは、通らない項目を「要修正」「未達」と書く取り決めです。**いちばん書かれやすい語で、門番が黙って通る状態でした。**

| 直した内容 | 効果 |
|---|---|
| **除外制から許可制へ変えました。** 判定の節に「合格」があり、かつ「不合格」が無いときだけ通します | 書かれ方の数だけ穴が空く構造をやめました |
| コードの塀（```）の中を見ないようにしました | 記録の中でフックの書式を例示しても、それを判定として拾いません |
| 止めたときの案内に、判定の節の書き方と、読み取った判定の中身を添えました | 書き手が原因に辿り着けます |
| 初期データの検査を、排除する語と注入する語で分けました | 片方を空にしても気づけなかった状態を直しました |

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査 | 検出なし |
| **通してはいけない記録が通らないこと** | テスト（15 件） | 「要修正」「保留」「マージ不可」「不合格」、判定の節が無い・空・一行形式、コードの塀の中だけに見出しがある場合を、いずれも止めます |
| 通すべき記録が通ること | テスト | 「合格」「条件付き合格」、判定の節の外に語がある場合を、いずれも通します |
| **片方向だけの守りになっていないこと** | 実測 | 初期データの `forbidden_terms` を空にすると、テストが 2 件失敗します。直す前は 13 件すべてが緑のままでした |
| 読み取った判定を案内へ出すこと | 目視 | 出すのは reviewer の記録の判定の節だけです。利用者の情報を含みません |
| 書き方の検査 | RuboCop | 94 ファイル、指摘 0 件 |
| テスト | RSpec 639 例・node --test 15 例 | いずれも失敗 0 件 |

## 判断したこと

**「条件付き合格」は通します。** 判定の語に「合格」が含まれており、マージしてよいという reviewer の判断です。条件の履行は申し送りとして追います。

**一行形式の見出し（`## 判定 : 合格`）は止めます。** 書式を 1 つに絞ると、読み取りの分岐が増えません。止めたときの案内で書式を伝えます。

## 指摘

指摘はありません。

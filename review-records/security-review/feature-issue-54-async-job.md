# セキュリティレビュー : 非同期ジョブとリトライ

- 実施日時（JST）：2026-08-27（3 回目。2 回目のレビューの指摘への対応を反映しました）
- 差分の指紋
  - 1 回目：`3b9671f9302c37f5`
  - 2 回目：`bfffbc15d38c36d6`
  - **3 回目（今回）：`131746e6f74ab744`**
- 対象：issue #54

## 変更の範囲

| 分類 | ファイル |
|---|---|
| 非同期ジョブ（今回の中心） | `src/backend/app/jobs/generate_prompt_job.rb` |
| 成果物の収め方（**今回の切り出し**） | `src/backend/app/services/prompt_requests/delivery.rb` |
| 決着していない状態の定義・状態の名前の定数化 | `src/backend/app/models/prompt_request.rb` |
| 業種の既定値が欠けたときの専用の種別 | `src/backend/app/models/rule_dictionary.rb`・`src/backend/app/services/generation/input_normalizer.rb` |
| 1 案ぶんの出力の切り出し | `src/backend/app/services/generation/prompt_package.rb` |
| 組み立ての段 | `src/backend/app/services/generation/prompt_generation_service.rb` |
| いま使う版の取り出し | `src/backend/app/models/rule_dictionary.rb` |
| テスト | `src/backend/spec/jobs/generate_prompt_job_spec.rb`（40 例） |
| テストの置き方の決まり | `test/README.md` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** 経路・認証の変更もありません。

## レビューの指摘への対応

### 重大1（投入し直しが仕事をやり直しません）

**同じジョブの投入し直しは、`generating` から再開できるようにしました。** `executions` が 2 以上のときだけ再開を許します。**別の投入は 1 回目ですので、`generating` を見たら見送ります。**

**最後まで走らせるテストを置きました。** `perform_enqueued_jobs` で打ち切りまで通し、**組み立てを上限の回数だけ試みること・失敗として記録すること・クォータを返すこと**を固定しました。**この抜けが重大1 を見逃した原因でしたので、そこから直しました。**

### 重大2（クォータの確定・返還が一致しません）

**確定だけが残った場合を、投入し直しで拾い直します。** 「成果物は提供済みで、枠が予約のまま」を見つけたら、決着だけを行います。**案を作り直しません。**

### 重大3（二重投入で状態が巻き戻ります）

**行に錠をかけてから状態を見ます。** 手元の古い値ではなく、読み直した値で判定します。**錠は権利を取る間だけです。** 組み立ての間は外します。組み立ては外への問い合わせを含みますので、その間ずっと錠をかけると行が長く塞がります。

### 要修正1（繰り返しても通らない誤り）

**規則辞書の不備を `PERMANENT_FAILURES` へ加えました。** 公開済みの版が無い・渡し忘れ・内容が選択肢の外・業種の既定値が無い、のいずれも繰り返しません。

### 要修正2（失敗の記録が 2 度走ります）

**`discard_on` の塊を外しました。** 記録は `after_discard` が一度だけ行います。

### 要修正3（`retry_on StandardError` の網が広すぎます）

**繰り返す価値のある誤りだけを挙げました**（行き詰まり・錠の待ち時間切れ・問い合わせの打ち切り・接続の不成立・接続の待ち時間切れ・時間切れ・入出力の誤り）。

**想定していない誤りは、失敗として記録してから外へ出します。** 握りつぶしません。**そのうえで、生成リクエストを `generating` のまま取り残しません。**

### 要修正5（状態の文字列を手で書いています）

**`PromptRequest::UNSETTLED_STATUSES` を定義し、そこから引きます。**

### 要修正6（`test/pr165/` がありません）

**判断を `test/README.md` へ書きました。** 画面の操作を伴わない PR には置きません。**ブラウザで操作する対象がありませんので、自動化できる手順がありません。** できない確認を、できるように書きません。

### 提案への対応

- **案の数を、保存の前に検めます。** 足りなければその場で失敗させます。
- **保存した形を読み戻せることを、テストで固定しました。**

## 2 回目のレビューの指摘への対応

### 要修正1（記録が上限に達した経路で 2 度走ります）

**`retry_on` の塊から記録を外しました。** `retry_on` は塊を呼んだ直後に `after_discard` を呼びますので、**塊で記録すると 2 度走ります。** 塊を渡す目的は「投げ直させないこと」だけです。**一度だけ走ることを、テストで固定しました。**

### 要修正2（働き手が落ちて `generating` のまま残った行を誰も拾えません）

**時間で見分けます。** `executions` は投入時の値ですので、働き手の異常終了では見分けられません。**`STALE_AFTER`（15 分）を越えて動きのない `generating` の行は、新しい投入でも拾い直します。**

**短くしすぎません。** 組み立ての途中で別の働き手が横入りします。**走り出したばかりの行へ横入りしないことも、テストで固定しました。**

### 要修正3（PR 本文のユーザーテストが不正解になります）

**PR 本文を実測へ合わせました。**

### 要修正4（返還が落ちた場合だけ立て直しの道がありません）

**`awaiting_settlement?` の条件を広げました。** 「成果物は提供済みで枠が予約のまま」と、その鏡像である「失敗として記録済みで枠が予約のまま」の**両方**を拾います。**片方だけに立て直しを用意すると、もう片方が取り残されます。**

### 要修正5（`KeyError` の網が広すぎます）

**`RuleDictionary::MissingDefaultsError` を定義し、`KeyError` を外しました。** `KeyError` は Ruby のどこの `fetch` でも上がる広い種別です。**書き間違いによる取り違えまで、辞書の不備として静かに片付きます。**

### 提案への対応

- **順調に終わった足取りを残します。** `jobs.generate_prompt_stored`（案を収めました）と `jobs.generate_prompt_settled`（枠を決着させました）を足しました。
- **状態の名前を `PromptRequest` の定数へ寄せました。** 手書きの文字列を残しません。
- **ジョブの層では縮退しないこと**を、PR 本文へ明記しました。

## 確かめたこと

| 観点 | 方法 | 結果 |
|---|---|---|
| リトライの上限 | テスト（打ち切りまで実走） | 3 回試み、越えたら失敗として記録します |
| 繰り返しても変わらない誤り | テスト | その場で失敗として記録し、**投入し直しません** |
| 失敗の記録の回数 | テスト | **上限に達した経路でも、一度だけです** |
| 働き手が落ちた場合 | テスト | **15 分を越えて動きのない行を拾い直します** |
| 走り出したばかりの行 | テスト | **横入りしません** |
| 返還が外れた場合 | テスト | **投入し直しで返還します。案を作りません** |
| 想定していない誤り | テスト | 記録してから**外へ出します。生成中のまま取り残しません** |
| クォータとの一致 | テスト | 提供できたら確定、失敗したら返還します |
| 確定が外れた場合 | テスト | 投入し直しで確定します。**案を作り直しません** |
| 二重の投入（順に走る） | テスト | 見送ります |
| 二重の投入（古い値を持つ） | テスト | **状態を巻き戻しません。案を増やしません** |
| すでに決着している場合 | テスト | 触れません。**二度目の記録で状態を壊しません** |
| 記録に残す情報 | 目視 | 識別子と**例外の種別だけ**です。**利用者の入力を残しません** |
| 錠をかける時間 | 目視 | 権利を取る間だけです。組み立ての間は外します |
| 秘匿値の混入 | 目視 | ありません |
| 外部への通信 | テスト | **テストでは 1 件も行いません** |
| 書き方の検査 | RuboCop | 170 ファイル、指摘 0 件 |
| テスト | RSpec | 1545 例、失敗 0 件（ジョブは 49 例） |
| ハードコードの検査 | 検査 | 検出 0 件 |

## 判断したこと

**上限に達した場合は、記録して終えます。投げ直しません。** 時間をおけば通ることがある誤りを上限まで試したうえでの、想定された行き止まりです。**状態・理由・クォータの返還・記録がすべて残りますので、握りつぶしにはあたりません。** 想定していない誤りとは扱いを分けます。

**`queued` のままでも、いったん `generating` を通してから失敗させます。** ジョブが動き出している以上、生成は始まっています（requirements.md 12.1 は `queued` から直に `failed` へ進む道を持ちません）。

## 申し送り

**失敗した生成リクエストの再実行の導線**は、生成リクエスト API（issue #55）が持ちます。

**縮退の印**は、結果ポーリング API（issue #56）で返します。

**ジョブの層では縮退しません。** 縮退は段の中（`DegradedComposer`）で案ごとに行います。結果として issue #54 の目的は満たします。

## 指摘

指摘はありません。

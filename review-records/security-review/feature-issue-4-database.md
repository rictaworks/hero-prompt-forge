# セキュリティレビュー : feature-issue-4-database

- 実施日時（JST）：2026-08-25 10:28
- 差分の指紋：7d4614ddfcfb800e
- 対象：issue #4

## 判定

合格

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | gitleaks 8.30.1 `protect --staged` | 検出なし |
| 接続情報の直書き | `database.yml` の内容 | 利用者名・パスワード・ホスト名を書いていません。すべて環境変数から読みます |
| 既定値による握りつぶし | `ENV.fetch` の使用 | 既定値を持たせず、未設定なら起動時に失敗します。フォールバックしません |
| 未使用設定の残存 | 別データベース前提のスキーマ定義 | `DELETE/` へ退避しました |
| 本番起動 | 本番設定での読み込み・ジョブ・キャッシュの読み書き | 正常 |

## 指摘

指摘はありません。

Active Storage の変換機能が libvips を要求する警告が出ますが、画像の変換を行わないため実害はありません。Active Storage 自体の要否は、利用する機能が確定した時点で判断します。

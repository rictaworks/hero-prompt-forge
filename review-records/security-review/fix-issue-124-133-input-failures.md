# セキュリティレビュー : fix-issue-124-133-input-failures

- 実施日時（JST）：2026-08-26 19:45
- 差分の指紋：d5745ec7d5181a3e
- 対象：issue #124 ／ issue #133

## 判定

合格

## この変更の性質

**利用者の入力が最初に触れる層です。** issue #55 で入口を作ると、ここへ外部からの値がそのまま届きます。次の 3 点を重く見ました。

1. **想定外の形の入力で、理由の分からない失敗にならないこと**
2. **同時に呼ばれても、他人の入力の結果が混ざらないこと**
3. **記録に利用者の入力そのものを残さないこと**

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査（password / secret / api key / token / 私有鍵） | 検出なし |
| **入力そのものの型違い** | テスト | 配列・文字列・数値・構造体・真偽値・空のいずれでも `InvalidInputError` です。素の `TypeError` ・ `NameError` になりません。**`[]` を持つかではなく、`Hash` か `ActionController::Parameters` かで見ます** |
| 直す前の実装で赤くなること | 実測 | 直す前の実装に対して 6 件が失敗します（型違い 5 件＋途中の状態を持たないこと 1 件） |
| **同時に呼んだときの混ざり** | テスト（構造・実測の 2 通り） | 誤りの一覧をインスタンスへ持たない形へ変えました。呼び出しごとに `Attempt` を作ります。**構造の例（`instance_variables` が `@dictionary` だけであること）が、直す前の実装に対して確実に失敗します。** あわせて 40 スレッド × 20 回の同時呼び出しでも混ざらないことを確かめます |
| 同時呼び出しの例を守りとして数えないこと | 実測・注釈 | 8000 回まで増やしても、直す前の実装が通ってしまいました。**当たり外れのある例を守りとして数えず、その旨をテストの注釈へ残しました** |
| **記録に残す情報** | テスト | `Trace.step` に残すのは項目名と理由の記号だけです。**利用者の入力そのものが記録へ出ないことを、実際にログを覗いて確かめます**（読み取れない形の入力でも中身を残しません） |
| 例外に含める情報 | 目視 | 項目名と理由の記号のみです。文言をこの層で作りません |
| ブランドカラーの空の要素 | テスト | 未指定として落とします。**同じ色の重ねがけは、これまでどおり誤りとして返します。** 扱いを分ける理由をテストの注釈へ残しました |
| **検証の文言の欠落** | テスト | 使っている検証の種類から文言の鍵を求め、すべて定義されていることを確かめます。**新しい検証を足したときに、足し忘れをテストが知らせます** |
| 起きる例外の種類 | テスト | 利用者を欠いた保存・クォータ日を欠いた保存・書式違い・長さ超過の 4 通りが、いずれも `RecordInvalid` になります。文言の欠落として失敗しません |
| 文言の文体 | テスト | 追加した 3 件を含め、すべてですます調で終わります |
| 書き方の検査 | RuboCop | 94 ファイル、指摘 0 件 |
| テスト | RSpec | 647 例、失敗 0 件 |
| ハードコードの検査 | 検査 | 日本語の文字列リテラルはありません |

## 判断したこと

**ブランドカラーの空の要素は、未指定として落とします。** 同じ色の重ねがけとは扱いを分けます。空の欄は「指定していない」ことの素直な表れです。同じ色を 2 度書くのは「2 色指定したつもり」との食い違いですので、黙って減らさずに誤りとして返します。**揃えない理由をテストの注釈へ残しました。**

**呼び出しの途中の状態を `Attempt` へ移しました。** 呼び出しごとに作り直すため、使い方に依存しなくなります。選択肢の定義（`InputChoices`）は、これまでどおり `InputNormalizer` から引けます。

**追加した文言は、実際に使っている検証の分だけです。** 使っていない検証の文言を先回りして足しません。足りなくなればテストが知らせます。

## 指摘

指摘はありません。

---

# セキュリティレビュー（2回目） : fix-issue-124-133-input-failures

- 実施日時（JST）：2026-08-26 20:04
- 差分の指紋：4cb16724b18830ab
- 対象：PR #141 のレビュー指摘への対応

## 判定

合格

## 直した内容

| 指摘 | 対応 |
|---|---|
| **要修正 1：対応表に無い種類の検証を足すと、テストが緑のまま通ります** | 対応表の `else` で空を返すのをやめ、その場で失敗させます。`acceptance` の検証を実際に足して、テストが赤くなることを実測しました |
| **要修正 2：使っていない公開面と、事実と違う注釈があります** | `InputNormalizer` の `include InputChoices` を外しました。試験の参照も `Generation::InputChoices::` へそろえ、参照の経路を 1 本にしました |
| 提案 1：不正な UTF-8 で素の例外になります | `valid_encoding?` を確かめ、項目名を添えた誤りにします。サービス概要・業種・ブランドカラーの 3 経路でテストを固定しました |
| 提案 2：空の要素を落とす理由が、決まりの持ち主から離れています | `brand_colors.rb` のクラスの注釈へ、扱いを分ける理由を書きました |
| 提案 3：注釈の位置がずれています | 同時呼び出しに関する注釈を、それぞれの節の直前へ移しました |
| 提案 4：ですます調の確認が二重です | 新設側の例を外しました。`locales_spec.rb` が `ja` 配下すべてを同じ条件で確かめています |

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査 | 検出なし |
| **対応表の網羅が強制されること** | 実測 | `User` へ `acceptance` の検証を足すと、テストが 1 件失敗します。**足し忘れをテストが知らせます** |
| 対応表に無い種類を見つけたら失敗すること | テスト | `UnknownValidatorError` です。空を返して通しません |
| **文字として扱えない並び** | テスト | 3 経路とも項目名を添えた `invalid_type` です。素の `Encoding::CompatibilityError` になりません |
| 公開面が増えていないこと | 目視 | `InputNormalizer` から 15 個の定数が消えました。参照は `Generation::InputChoices::` の 1 本です |
| 選択肢の照合が変わっていないこと | テスト | 必須・任意の取り出しを 1 つの手続きへまとめました。欠けていた場合の扱いだけが違います。既存の例がすべて通ります |
| 書き方の検査 | RuboCop | 94 ファイル、指摘 0 件 |
| テスト | RSpec | 650 例、失敗 0 件 |

## 判断したこと

**`WithValidator`（`validates ... with:`）の分岐は置きません。** いま使っている箇所がなく、使い始めたときに対応表へ足す判断を、テストに強制させます。

**不正な UTF-8 は `invalid_type` として返します。** 新しい理由の記号を増やしません。画面（issue #71）が扱う理由を増やすと、文言の用意が漏れます。

## 指摘

指摘はありません。

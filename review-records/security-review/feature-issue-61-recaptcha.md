# セキュリティレビュー : reCAPTCHA の適用

- 実施日時（JST）：2026-08-27
- 差分の指紋：e3f432735539fba2
- 対象：issue #61

## 変更の範囲

| 分類 | ファイル |
|---|---|
| 人の操作の確かめ（今回の中心） | `src/backend/app/controllers/concerns/verifies_humans.rb` |
| 照合 | `src/backend/app/services/bot_protection/recaptcha_verifier.rb` |
| 設定の読み込み | `src/backend/app/services/bot_protection/settings.rb` |
| 設定 | `src/backend/config/recaptcha.yml` |
| 適用先 | `src/backend/app/controllers/api/v1/prompt_requests_controller.rb` |
| 文言 | `src/backend/config/locales/ja.yml` |
| 環境変数の見本 | `.env.example` |
| テスト | `src/backend/spec/services/bot_protection/recaptcha_verifier_spec.rb`（23 例）・`src/backend/spec/requests/api/v1/human_verification_spec.rb`（14 例） |
| 契約 | `SPEC/api/README.md` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。**

## 秘匿値の扱い

**秘密鍵を環境変数（`RECAPTCHA_SECRET_KEY`）から読みます。** ソースにも設定ファイルにも書きません。`.env.example` は空欄だけを置きます。

**秘密鍵を URL へ載せません。** 本文（form）で送ります。**URL は記録へ残ります。** テストで固定しました。

**鍵が無ければ、その場で失敗させます。** 既定へ寄せると、**照合したつもりで照合していない状態**になります。

**サイト鍵は秘匿値ではありません。** ブラウザから見える値ですので、`NEXT_PUBLIC_RECAPTCHA_SITE_KEY` として分けて書きました。

## 確かめたこと

| 観点 | 方法 | 結果 |
|---|---|---|
| 検証に失敗したら生成しない | テスト | 403 です。**記録も枠も作りません** |
| 得点の下限 | テスト | 0.5 未満は通しません。**0.5 ちょうどは通します** |
| 行動の名前 | テスト | 違えば通しません。**他の画面で取った得点を使い回せません** |
| 合図が無い場合 | テスト | 403 です。**照合へ行きません**（無駄な呼び出しをしません） |
| 断られた場合 | テスト | 403 です |
| 秘密鍵を環境変数から読む | テスト | 読みます。**URL へ載せません。本文で送ります** |
| 送る項目 | テスト | `secret` ・ `response` ・ `remoteip` の 3 つだけです。**利用者を指す値を送りません** |
| 秘密鍵が無い場合 | テスト | その場で失敗させます |
| **本番で鍵が無い場合** | テスト | **その場で失敗させます。** 黙って無効になりません |
| 開発での扱い | テスト | **鍵が設定されているときだけ照合します。** 無ければ飛ばします |
| 環境の判定 | テスト | **環境だけで決まります。** 要求側の値では切り替わりません |
| 通らなかった理由の露出 | テスト | **返しません。** `details` は空です |
| 照合そのものができない場合 | テスト | 503 です。**通信の失敗をすべて受け止めます** |
| 閲覧の経路 | テスト | **守りません。** 合図が無くても履歴を見られます |
| 外部への通信 | テスト | **テストでは 1 件も行いません**（すべて差し替えました） |
| 秘匿値の混入 | 目視 | ありません |
| 書き方の検査 | RuboCop | 187 ファイル、指摘 0 件 |
| テスト | RSpec | 1680 例、失敗 0 件（うち新規 37 例） |
| ハードコードの検査 | 検査 | 検出 0 件 |

## 判断したこと

**Bot 対策を自作しません**（CLAUDE.md）。判定は reCAPTCHA へ委ねます。**v3 を使います。** 利用者に絵を選ばせません。

**合図は見出しで受け取ります。** 本文へ混ぜると、入力の項目として記録へ流れます。**合図は認証の材料であって、生成の入力ではありません。**

**投入する経路だけを守ります。** 閲覧の経路は守りません。画面を開くたびに合図を求めると、**上限に達した方が履歴すら見られなくなります。**

**本番では必ず照合します。** 秘密鍵が設定されていなければ、その場で失敗させます。**鍵の入れ忘れで Bot 対策が黙って無効になる状態を作りません。**

**開発とテストでは、鍵が設定されているときだけ照合します。** 手元で画面を触るたびに Google の鍵を要求しないためです。**この分かれ方は環境だけで決まります。** 要求側の値（見出し・引数・クッキー）では切り替わりません。**本番の画面に、飛ばす導線を出しません。**

**通らなかった理由を利用者へ返しません。** Google が返す理由の符号は、Bot 対策の内側の情報です。**返すと、通り抜け方を探る手がかりになります。**

**照合そのものができない場合と、通らなかった場合を分けます。** 前者は 503（利用者の落ち度ではありません）、後者は 403 です。

**得点の下限も待ち時間も、設定ファイルへ置きます。** 運用で変わります。**厳しくすると本当の利用者を弾き、緩めると Bot を通します。**

**行動の名前を確かめます。** 確かめないと、別の画面で取った得点を使い回せます。

## 申し送り

**画面側（Next.js）の組み込みは、この PR に含みません。** サイト鍵を `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` から読み、行動の名前 `generate_prompt` で得点を取り、`X-Recaptcha-Token` の見出しで送ってください（issue #71）。

**本番の秘密鍵は Railway の変数へ入れてください。** 未設定のまま本番を動かすと、投入の経路がその場で失敗します。

## 指摘

指摘はありません。

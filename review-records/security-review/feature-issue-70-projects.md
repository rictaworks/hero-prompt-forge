# セキュリティレビュー : 履歴・一覧画面

- 実施日時（JST）：2026-08-27
- 差分の指紋
  - 1 回目（画面）：`eef4275e6ddc2ac6`
  - **2 回目（PR #170 のレビューへの対応）：`976201d84e9f14cc`**
- 対象：issue #70

## 変更の範囲

| 分類 | ファイル |
|---|---|
| 画面（今回の中心） | `src/frontend/src/components/projects/index.tsx`・`projects.module.css` |
| 画面の外枠 | `src/frontend/src/components/app/AppShell.tsx`・`app-shell.module.css` |
| 入口 | `src/frontend/src/app/projects/page.tsx` |
| バックエンドへの呼び出し | `src/frontend/src/lib/api.ts` |
| 資源の型 | `src/frontend/src/types/resources.ts` |
| 書式 | `src/frontend/src/lib/format.ts` |
| ログイン中の利用者 | `src/frontend/src/hooks/useSession.ts` |
| ログイン中の利用者（サーバー側） | `src/backend/app/controllers/api/v1/session_controller.rb`・`src/backend/config/routes.rb` |
| 文言・画面の一覧 | `src/frontend/src/strings/ja.ts`・`src/config/screens.ts` |
| テスト | `src/backend/spec/requests/api/v1/session_spec.rb`・`src/frontend/src/components/layout/AppBar.test.tsx` |

## 新しい経路

**`GET /api/v1/session` を足しました。** 上部バーが表示名とプラン値を出すために使います。

**返すのは表示名とプラン値だけです。** X のユーザー ID を返しません。**画面に出す必要が無く、他の利用者を辿る手がかりになります。** テストで固定しました。

**プラン値の判定を求めません。** `pending` の方も自分の状態を見られる必要があります。**見られないと、なぜ使えないのかが分かりません。** 認証だけを求めます。

## 確かめたこと

| 観点 | 方法 | 結果 |
|---|---|---|
| 未認証 | テスト | 401 です |
| X のユーザー ID の露出 | テスト | **返しません**（本文に含まれないことを確かめました） |
| プラン値が有効でない場合 | テスト | 自分の状態を見られます |
| 他人の資源 | 目視 | 画面は一覧の応答をそのまま並べます。**絞り込みはサーバー側が行います** |
| 上限到達中の閲覧 | 目視 | 閲覧の経路に枠の判定を置いていません（PR #167） |
| 縮退の印 | 目視 | 一覧の各行に出します |
| 失敗の握りつぶし | 目視 | `api.ts` は契約の形の失敗を `ApiError` として投げ直します。**既定値へ寄せません** |
| 契約の形でない失敗 | 目視 | `UnexpectedResponseError` として投げます。**黙って成功にしません** |
| 秘匿値の混入 | 目視 | ありません。**バックエンドの場所を画面へ書きません** |
| `alert()` / `confirm()` / `prompt()` | 目視 | 使っていません |
| 絵文字 | 目視 | ありません。アイコンは Font Awesome です |
| 型検査 | tsc | 指摘 0 件 |
| 書き方の検査 | ESLint | 指摘 0 件 |
| テスト | Jest | 59 例、失敗 0 件 |
| テスト | RSpec | 1667 例、失敗 0 件 |
| 書き方の検査 | RuboCop | 184 ファイル、指摘 0 件 |
| 本番の組み立て | next build | 成功します |
| ハードコードの検査 | 検査 | 検出 0 件 |

## 判断したこと

**上部バーの値を、その都度サーバーから引きます。** 画面へ持たせません。持たせると、他の端末で変わった値が反映されません。

**読み込みの間は、上部バーの値を空にします。** 仮の値を出しません。**仮の値は、実際の値と違っていても気づけません。**

**選択肢の値を、そのまま画面へ出しません。** 呼び名を設定から引きます。`saas` ではなく「SaaS・ソフトウェア」と出します。

**縮退の印を一覧に出します**（requirements.md 4.2）。**どの案が規則辞書だけで組み立てられたのかを、開かずに見分けられます。**

**上部バーの導線は、画面の一覧（`SCREENS`）が決めます。** 実装済みかどうかを、テストも一覧から引きます。**画面の名前をテストへ書き写しません。**

## 申し送り

**結果の画面（`/requests/:id`）と入力フォーム（`/requests/new`）は、この PR に含みません**（issue #71 ・ #72 ・ #73）。**一覧からの導線は先に置いています。** 移動先ができるまでは、開いても何もありません。

**頁分け（ページネーション）は、この PR に含みません。** 件数が増えたら足します。

## PR #170 のレビューへの対応（この枝に含みます）

**ランディング（PR #170）と同じ枝で作業していますので、その直しもここへ含みます。** 直しの本体は PR #170 へ移します。

| 指摘 | 対応 |
|---|---|
| **F1**（開いただけでログインの手続きが始まります） | **外への出口を、素の入口で描きます。** 画面の中の移動の仕組みは移動先を先に読み込みますので、**押していないのに `/auth/start` が呼ばれていました。** |
| **F2**（ブラウザ操作のテストが失敗します） | **見出しを完全一致で探します。** 「From Input to Package」が「Input」を含むためでした。 |
| **R2**（添え書きがモックと違います） | **改行とアカウント名の色を、モックへ合わせました。** |
| **R3**（公開画面の必須の設定） | **OGP と canonical を足しました。** 公開先は `NEXT_PUBLIC_BASE_URL` から引きます。**未設定なら、その場で失敗させます。** |
| **R4**（画像の出どころの記録） | **`src/frontend/public/images/README.md` へ書きました。** **利用条件が未確認であること**と、確認が取れない場合は AI 生成へ差し替えることも書きました。 |
| **R5**（文言の検査が変異試験で抜けます） | **語尾で見る形へ改めました。** 文は「です。」「ます。」等で終わること、`labels` は述語で終わらないことを検めます。 |
| **R6**（公開画面に他社の名称が出ます） | **`hero-prompt-forge` へ直しました。** モックの既定値をそのまま公開しません。**モックは書き換えていません。** |
| **P1**（読み込まれていない CSS） | **`DELETE/unused-2026-08-27/` へ移しました。** 削除ではなく移動です。 |
| **P2**（ログインの入口の置き場） | **`src/config/backend.ts` へ寄せました。** |
| **P3**（中継先が組み立て時に焼き込まれます） | **`ENV/DEVELOPMENT.md` と `ENV/PRODUCTION.md` へ書きました。** |
| **P7**（規約・ポリシーへの導線） | **issue #172 を立てました。** |

## 指摘

指摘はありません。

# セキュリティレビュー : fix-issue-129-input-type-check

- 実施日時（JST）：2026-08-26 13:50
- 差分の指紋：28e4ffdb1f65fb65
- 対象：issue #129

## 判定

合格

## 直した弱点

| 弱点 | 影響 | 対応 |
|---|---|---|
| 入力の型を確かめていませんでした | 型を変えて送るだけで、項目名の無い失敗を起こせました。利用者には理由の分からない失敗として返り、エラー表示の仕組みにも乗りませんでした | 受け取る項目を先に絞り、型を確かめます。型違いは項目名を添えた誤りとして集めます |
| 規則辞書の内容を選択肢に照らしていませんでした | 管理画面の編集で選べない値が入ると、この層を無言で通過しました | 引いた値も選択肢に照らします。外れていれば失敗させます |
| 一覧を二重に持っていました | 片方だけを増やすと、保存はできるのに生成では弾かれる食い違いが生まれました | 既存の定義をそのまま参照します |

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査（password / secret / api key / token / 私有鍵） | 検出なし |
| 想定外の型 | テスト | 数値・連想配列・読み取れない入力のいずれも、項目名を添えた誤りとして返します |
| 受け取る項目の絞り込み | 目視・テスト | 想定した8項目だけを取り出します。届いた鍵をすべて記号へ直しません |
| 例外に含める情報 | 目視 | 項目名と理由の記号のみです。利用者の入力そのものを含みません |
| 規則辞書の不備 | テスト | 定義が無い場合・選択肢の外の場合のいずれも失敗させます |
| 誤りの隠れ | テスト | 利用者の入力の誤りを、規則辞書の不備より先に返します |
| 組み立ての誤り | テスト | 規則辞書が渡されていなければ、入口で失敗させます |
| 上限の回避 | テスト | 色の重ねがけを1色に減らして通しません。誤りとして返します |
| 文言の混入 | 検査 | 画面へ出す文言をこの層に持ちません。ハードコード検出テストが成功します |

## あわせて行ったこと

レビューの再現作業で `src/backend/` に残っていた一時ファイル 2 件（`tmp_repro_race2.rb`・`tmp_repro_race3.rb`）を `DELETE/reviewer-tmp-2026-08-26/` へ移しました。削除はしていません。いずれも版管理に入っていない確認用のスクリプトです。

## 指摘

指摘はありません。

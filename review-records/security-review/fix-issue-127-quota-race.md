# セキュリティレビュー : fix-issue-127-quota-race

- 実施日時（JST）：2026-08-26 13:10
- 差分の指紋：d63f05499a03d0d2
- 対象：issue #127

## 判定

合格

## 直した弱点

| 弱点 | 影響 | 対応 |
|---|---|---|
| 返還済みの枠を同時に取り直せました | 1アカウント1日1回の上限を迂回できました。二重に押すだけで起こります | 行に錠をかけてから状態を見ます。取り直しは1件だけ成立します |
| 決着先を取り違えました | 日をまたぐ再実行で、当日の枠が予約のまま残り、戻す手立てがありませんでした | 予約中の記録だけを決着の対象にします。あわせて、予約中は生成リクエストごとに1件だけとする一意制約を置きます |

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | ステージ差分への正規表現検査（password / secret / api key / token / 私有鍵） | 検出なし |
| 上限の迂回 | テスト | 別々の接続から同時に4件取り直し、成立するのが1件だけであることを確認しています |
| 修正前の再現 | テスト | 修正前の実装では、追加した5例のうち**4例**が失敗することを確認しています（issue #132 の指摘を受け、2026-08-26 に実測して直しました。「翌日に取り直すと、その日の枠を使います」は修正前でも通ります） |
| 枠が戻らなくなる経路 | テスト | 日をまたぐ再実行で、決着が当日の予約に当たることを確認しています |
| 履歴の保全 | テスト | 取り直しのときに、返還のもとになった生成リクエストとの結び付きを消しません |
| 失敗の握りつぶし | テスト | 上限到達として読み替えるのは、その日の記録がすでにある場合だけです。それ以外の保存の失敗は隠さず投げ直します |
| 錠の範囲 | 目視 | 錠は1行に限り、取得の順序は利用者 × クォータ日で一意です。行き詰まりが起きる組み合わせを作りません |
| 例外に含める情報 | 目視 | クォータ日・状態・リクエストの識別子のみです。秘匿値・個人情報を含みません |
| テストデータの後片付け | 目視 | 接続をまたぐ例のみ、そのテストが作った記録を片付けます。対象は手元のテスト用データベースです |

## 指摘

指摘はありません。

# セキュリティレビュー : アートディレクションノート

- 実施日時（JST）：2026-08-26
- 差分の指紋：c7e1f28b33609f6e
- 対象：issue #51

## 変更の範囲

| 分類 | ファイル |
|---|---|
| ノートの組み立て（今回の中心） | `src/backend/app/services/generation/art_direction_note.rb` |
| 調整したことの組み立て | `src/backend/app/services/generation/adjustment_list.rb` |
| 文言 | `src/backend/config/locales/ja.yml` |
| テスト | `src/backend/spec/services/generation/art_direction_note_spec.rb` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** 経路・認証・保存の変更もありません。

## 確かめたこと

| 観点 | 方法 | 結果 |
|---|---|---|
| 利用者の入力を、そのまま画面へ出すか | 目視 | **出しません。** 出すのは、色の名前・色コード・余白の位置・構図の指示だけです。**サービス概要をそのまま含めません** |
| 文言の置き場 | 目視 | すべて `config/locales/ja.yml` です。実装の中へ書きません |
| 秘匿値の混入 | 目視 | ありません |
| 外部への通信 | 目視 | ありません |
| 仕様が定める 3 点 | テスト | 余白の可読性・ブランドカラーの再現度・クリシェ混入を必ず含みます |
| 弱めた指定の明記 | テスト | 弱めた色・アクセント・2 色目を分けて伝えます |
| 控えに無いことを書かないこと | テスト | 色の指定が無ければ、色の調整を書きません |
| 余白の指定が無い案 | テスト | 使わないようにお伝えします |
| ですます調 | テスト | `spec/config/locales_spec.rb` が確かめます |
| 書き方の検査 | RuboCop | 150 ファイル、指摘 0 件 |
| テスト | RSpec | 1303 例、失敗 0 件 |
| ハードコードの検査 | 検査 | 検出 0 件 |

## 判断したこと

**控えから組み立てます。推し量りません。** 各段は、何をどう扱ったかを控えへ残しています。**控えに無いことは書きません。** 推し量ると、実際の指示と食い違う説明が出ます。

**確かめることは、必ず 3 点を含みます。** 生成 AI の出力は指示どおりとは限りません。**「指示したから大丈夫」とは書きません。** 出来上がった絵を見て確かめていただく必要があります。

**余白の指定が無い案は、使わないようにお伝えします。** 4.2 は「コピースペースを持たない案を出力しない」と定めています。**ここへ届くのは組み立ての誤りですが、利用者が気づける形で残します。**

**弱めたのか、そのまま入れたのかを分けて伝えます。** 「弱める」は「見えなくする」ことではありません。**利用者が指定した色が、どう扱われたかを正確にお伝えします。**

**語を並べる区切りも、実装の中へ書きません。**

## 申し送り

**人物の見込みをサイトの設定で上書きした場合の一文は、まだ足していません。** 上書きの仕組み（issue #147）が別の PR で進んでいるためです。**文言だけを先に用意してあります**（`adjustments.person_safety_from_project`）。**issue #147 のマージ後に、控えの `people_source` を見る 1 行を足してください。**

**画像の大きさをすり替えた場合の一文も、文言だけを用意してあります**（`adjustments.size_substituted`）。**整形の段（issue #47）の控えは、下書きではなく `Formatted` に付きます。** 組み立ての段（issue #146）で、両方の控えを 1 つのノートへまとめてください。

## 指摘

指摘はありません。

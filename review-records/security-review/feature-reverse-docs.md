# セキュリティレビュー : README と SPEC を、実装から起こします

- 実施日時（JST）：2026-08-27
- 差分の指紋：4bc2cbad60cbe35a
- 対象：issue #78 ・ #79

## 変更の範囲

| 分類 | ファイル |
|---|---|
| ページ一覧・API 一覧・自動ログイン | `README.md` |
| リバースエンジニアリング図（新規） | `SPEC/er.md` ・ `class.md` ・ `state.md` ・ `sequence.md` ・ `dfd.md` ・ `usecase.md` |
| SPEC の索引（新規） | `SPEC/README.md` |
| 文書の検査（新規） | `test/docs/docs.test.mjs` ・ `package.json` |

**`src/` を 1 文字も変更していません。** 文書と、その検査だけです。

## 確かめたこと

| 観点 | 方法 | 結果 |
|---|---|---|
| **本番の住所の露出** | 目視 | **ありません。** 書いたのは手元の開発環境の住所（`localhost:3300`）だけです |
| **バックエンドの住所の隠蔽** | 目視 | 保たれています。API 一覧に**バックエンドのドメインを書きません**。「画面の中の中継を通ります」と明記しました |
| **管理画面の住所** | 目視 | **パスだけを書き、本番の住所を書きません。** 「本番の住所は公開しません」と明記しました |
| 秘匿値の混入 | 目視 | ありません。環境変数は**名前だけ**を挙げ、値を書きません |
| `.claude/` の内容 | 目視 | 1 文字も転記していません |
| 個人名 | 目視 | ありません。連絡先は `info@rictaworks.jp` です |
| 絵文字 | 目視 | ありません |
| 未実装のものの記載 | **テスト** | **検出 0 件。** 機械で検めます（後述） |
| 図の描画 | Mermaid CLI | **6 図すべてが実際に描けます** |
| 文書の検査 | node --test | **5 例、失敗 0 件** |
| 束ねた検査 | `npm run test:all` | **39 例、失敗 0 件。** 直書きの検出 0 件 |

## 「未実装のものを書かない」を、機械で守ります

**文書は、書いた時点では正しくても、実装が動いた日に黙って嘘になります。** 画面や経路が消えても、文書はそのまま残ります。**読んだ方が、無い画面を開きます。**

`test/docs/docs.test.mjs` が、次の 5 つを検めます。

| 検め | 内容 |
|---|---|
| README に書いた画面は、すべて実装されています | `src/frontend/src/app` の下の `page.tsx` と突き合わせます |
| 実装した画面は、すべて README に書かれています | **書き漏らしも捕まえます** |
| README に書いた経路は、すべて実装されています | `config/routes.rb` と控えの並びと突き合わせます |
| SPEC の図は、すべて Mermaid のコード塀で閉じています | 閉じ忘れると、図が本文として表示されます |
| SPEC の索引は、置いてある文書をすべて挙げます | 足した文書が索引から漏れません |

**実際に効くことを確かめました。** 法務のページ（`/terms` ・ `/privacy` ・ `/commerce`）が `main` に入る前は、**この検査が赤でした**。入った後に緑になりました。

## 判断したこと

**`requirements.md` との差を、`SPEC/README.md` へ書きました。** 仕様に届いていない点が 2 つあります（LangSmith を有効にしていないこと、測定軸のうち 2 つを記録していないこと）。**差を隠さず、追っている場所と併せて書きます。**

**開発者用の管理画面は、パスだけを書きます。** 本番の住所を書きません。**利用者の画面から辿れないことも明記しました。**

**自動ログインの節を設けました。** 効く条件（`APP_ENV` が `development` ・ `test` であること、その利用者が居ること）と、**本番では効かないこと**を書きました。**値そのものは書きません。**

連絡先は info@rictaworks.jp です。

# セキュリティレビュー : feature-issue-11-logging

- 実施日時（JST）：2026-08-25 11:30
- 差分の指紋：61c5b3eef2634239
- 対象：issue #11

## 判定

合格

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | gitleaks 8.30.1 `protect --staged` | 検出なし |
| ログへの秘匿値の出力 | 除外項目の確認 | `password` ・ `secret` ・ `token` ・ `api_key` ・ `authorization` ・ `code_verifier` を除外。Rails 既定の除外項目も維持しています |
| 例外の握りつぶし | 実装とテスト | 記録後に必ず投げ直します。テストで固定しています |
| ログの出力先 | 設定 | 標準の出力のみです。外部サービスへ送信していません |
| 記録する文脈の値 | 実装の引数 | 呼び出し側が明示的に渡した値のみを記録します。リクエスト全体を丸ごと記録しません |

## 指摘

指摘はありません。

# セキュリティレビュー : feature-issue-5-env

- 実施日時（JST）：2026-08-25 10:31
- 差分の指紋：0ee966c2d7bf544b
- 対象：issue #5

## 判定

合格

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | gitleaks 8.30.1 `protect --staged` | 検出なし |
| `.env` の追跡 | ステージ一覧の照合 | 追跡されていません。`.env.example` のみを追跡します |
| `.env.example` の中身 | 目視 | 変数名のみです。実際の値を含みません |
| 接続情報の直書き | `docker-compose.yml` | 直値が無くなり、すべて `.env` からの読み込みになりました |
| 既定値による握りつぶし | `${VAR:?...}` と `ENV.fetch` | 未設定なら起動時に失敗します。既定値へ寄せません |
| 本番での開発者向け分岐 | 本番設定での実行 | `developer_shortcuts_allowed?` が false を返すことを確認しました |
| ブラウザへ渡る値 | `NEXT_PUBLIC_*` | 環境名のみです。秘匿値を渡していません |

## 指摘

指摘はありません。

# セキュリティレビュー : feature-issue-12-ci

- 実施日時（JST）：2026-08-25 11:31
- 差分の指紋：a3c2a42eb6a4a366
- 対象：issue #12

## 判定

合格

## 実施した検査

| 検査 | 方法 | 結果 |
|---|---|---|
| 資格情報の混入 | gitleaks 8.30.1 `protect --staged` | 検出なし |
| 検査用の値の扱い | ワークフローの内容 | 使い捨ての値のみです。名称に `ci_only` ・ `not_a_real_secret` を含め、本番への流用を防いでいます。到達先は検査用のコンテナに限られます |
| 権限 | ワークフローの権限指定 | 既定のまま広げていません。書き込みの権限を要求していません |
| 外部の動作 | 使用している動作の出所 | `actions/checkout` ・ `actions/setup-node` ・ `ruby/setup-ruby` の公式のみです |
| 本番への到達 | 接続先 | すべて検査用のコンテナです。本番へ接続しません |
| 本番での近道 | production-boot の確認 | 開発向けの近道が有効なら失敗します |

## 指摘

指摘はありません。

# pr-checker 記録：PR #187（issue #182 レビューで見つかった、後戻りを止める押さえを足す）

- 対象 PR：#187
- ブランチ：`feature/issue-182-regression-guards`（`67e0c75`）
- 土台：`main`（`5459e18`）
- 実施日時：2026-08-29（JST）
- 作業場所：`~/github/rictaworks/hero-prompt-forge`（ブランチを直接検めました）

## 判定

合格

タイトルはそのまま（もとから日本語・ですます調・issue 番号参照で規約に沿っています）とし、本文を規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ組み直しました。

## 反映した内容

- タイトル：`[整備] レビューで見つかった、後戻りを止める押さえを足します (#182)`（**変更していません**）
- 本文：規定の型へ組み直しました。専門用語（「変異試験」「mutation testing」等）を、非エンジニア向けの言い回しへ言い換えました

## 画面での確認と、確認できなかったこと

**実際に開発サーバー（Docker コンテナ内）を起動し、ブラウザで確認を試みました。**

| 手順 | 結果 |
|---|---|
| ホーム画面（`http://localhost:3300/`）を開く | **成功。** タイトル・本文とも正しく表示されました |
| 履歴画面（`/projects`）へ移動する | **成功。** 画面タイトルが「Projects & Requests」に変わり、見出し・案内文が表示されました |
| 「同じ条件で作り直す」ボタンを押し、移動先の URL に条件が含まれることを確認する | **確認できませんでした。** 確認の途中で、共有の Docker 環境（`db` コンテナ）が I/O 待ちにより応答しなくなりました（`docker compose ps` で `unhealthy` を確認）。リポジトリの `CLAUDE.md` に記載されている、既知の環境要因（USB 接続の外付けディスク上での I/O 待ちの多発によりコンテナが不安定になる）です。**この PR の変更が原因ではありません**（この PR の変更を加える前から、同じ環境要因が繰り返し記録されています） |

**この 1 点は、確認できなかったことを正直に PR 本文へ明記しました。** 代わりに、次の 2 つの根拠で担保しています。

1. 追加した Jest テスト（`src/frontend/src/components/projects/index.test.tsx`）が、`href` の中身（`project_id` と `request_id` の両方）を直接検めており、成功しています（手元で実行・確認済み：2 例とも成功）
2. GitHub Actions の「ブラウザ操作の確認（Playwright）」ジョブが、この PR の内容を含めて実行され、**成功しています**（実行 `33229141515`、ジョブ ID `99038637222`、1m59s）

**エラー画面（`error.tsx`）のボタンの見た目変更は、通常の操作では到達できない画面のため、ブラウザからの手順としては「確認できていません」と本文に明記しました。手順を捏造していません。**

## 実測した数値（本文へ反映済み）

| 検査 | 実測 |
|---|---|
| RSpec | 1974 例・失敗 0 件（手元・環境が応答していた時点で実測） |
| Jest | 158 例・失敗 0 件（手元で実測） |
| RuboCop | 182 ファイル・指摘 0 件（手元で実測） |
| `npm run test:all` | 成功 |
| GitHub Actions（CI） | 6 ジョブすべて pass（`33229141515`） |

## 補足

- PR 本文には「なぜテストが緑のままなのに問題なのか」を非エンジニア向けに言い換えて書きました（「特定の大事な条件を確かめているかどうかの証にはならない」という趣旨）。
- コードレビュー（差分の良し悪し・設計・セキュリティ）は本記録の対象外です（`review-records/reviewer/pr187.md` を参照してください）。

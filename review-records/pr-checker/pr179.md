# pr-checker 記録：PR #179（issue #173 ブラウザ操作の確認を自動検査へ組み込む）

- 対象 PR：#179
- ブランチ：`feature/e2e-in-ci`（`32d4120`）
- 土台：`main`（`be10d2d`。現在の `main` は `6e97fd8` です）
- 実施日時：2026-08-27（JST）
- 作業ツリー：確認用の作業ツリー `DELETE/pr179-checker-20260827` を新しく作り、そこで確かめました。**実装側の作業ツリー（`DELETE/impl-173`）へ触れていません。`main` の作業ツリーでブランチを切り替えていません。**

## 判定

合格

タイトルはそのままとし、本文を規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ組み直しました。**ユーザーテストの手順が実際の GitHub の画面と食い違っていた箇所を、実測に合わせて直しました。**

## 反映した内容

- タイトル：`[整備] ブラウザ操作の確認を自動検査へ組み込みます (#173)`（**変更していません。** 日本語・ですます調で、規約に沿っています）
- 本文：**組み直しました。** 日本語化は不要でした（もとから日本語・ですます調です）。**ユーザーテストの手順は書き直しました。**

## この PR は画面を持ちません

**`src/` を 1 文字も変更していません。** 変更は 4 ファイル（+241 行 / -1 行）で、内訳は自動検査の手順（`.github/workflows/ci.yml`）・後戻りを止める押さえ（`test/harness/test-directories.test.mjs`）・説明書（`test/README.md`）・記録（`review-records/security-review/feature-e2e-in-ci.md`）です。

そのため、**ユーザーテストは「GitHub のこの PR の画面だけで完結する手順」としました。** アプリの画面を開く手順を捏造していません。本文にも「この PR は画面を持ちません」を独立した節として残しました。

## 画面と食い違っていた箇所（直しました）

**実際の GitHub Actions の実行（run `33065520678`、`e2e` ジョブ `98494501170`）を API とログで読み、1 つずつ突き合わせました。**

| 更新前の本文 | 実際の画面 | 対応 |
|---|---|---|
| 「この PR の画面の下のほう、「Checks」（検査）の一覧をご覧ください」 | どこから始めるかが曖昧です。会話のタブの下の箱は畳まれており、開く操作が要ります | **押せる URL（`https://github.com/rictaworks/hero-prompt-forge/pull/179/checks`）を書きました。** 一覧は画面の左側に出ます |
| 「一覧に増えていることを確かめてください」 | 比べる相手が無いため、増えたかどうかは判断できません | **検査は全部で 6 つです。** 6 つの名前をすべて並べ、〈ブラウザ操作の確認（Playwright）〉に印を付けました |
| 手順の一覧が「6 行」である前提（データベースの準備／自動ログインの利用者を作る／画面の組み立て／バックエンドを起動／画面を起動／ブラウザ操作の確認） | **実物は 15 行です。** 間に〈依存の導入（画面）〉〈依存の導入（確認の道具）〉〈ブラウザの導入〉が挟まり、先頭に `Set up job` など 5 行、末尾に〈起動の記録〉が付きます | **15 行すべてを、実物の順で表にしました。** 「上から順に並んでいること」を確かめる手順ですので、抜けがあると読み手が止まります |
| （記載なし） | **〈起動の記録〉は灰色（skipped）です。** 失敗したときだけ動く手順です | **「灰色で正解です」と書き足しました。** 書かないと「1 つだけ緑でない」と誤って報告されます |
| 「「Details」を押します」 | 準備の URL（チェックの画面）から入る場合は、**左側の検査の名前を押します。** 〈Details〉は会話のタブから入ったときの導線です | **入口を 1 本に絞り、押す場所を「左側の一覧の名前」にしました** |
| 「テストの名前が日本語で並び、すべて通っていることを確かめてください」 | **11 件です。** ログには `Running 11 tests using 2 workers` と `11 passed` が出ます | **件数と、11 件の項目名をすべて書きました。** 目印の文字列（`11 passed`）も書きました |
| （記載なし） | 11 件のうち〈ランディング › ログインの入口は画面と同じ場所を指します〉が、この PR の主眼（バックエンドの住所を画面へ書かない）を見ている項目です | **どれが主眼かを明示しました。** 何を見ているのか分からないまま眺める手順にしません |
| 「…であれば駄目です」という突き放した書き方 | 読み手は次に何をすればよいか分かりません | **末尾の〈うまくいかなかったとき〉へ誘導する書き方にしました** |
| （記載なし） | 表示言語の設定で〈チェック〉／〈Checks〉、〈詳細〉／〈Details〉が入れ替わります | **日本語表示と英語表示を併記しました** |

## 本文へ足した節

- **「なぜ変えたか」**（新設）。issue #173 と、「走らないテストは無いのと同じ」「バックエンドのドメインを隠蔽する決まりを見ているのはこの確認だけ」という困りごとを書きました。
- **「変更したファイル」**（新設）。4 件を、自動検査の手順・押さえ・説明書・記録に分けて並べ、**`src/` を含まないこと**を明記しました。
- **「ご用意いただくもの」**（準備へ追加）。ブラウザだけで足りること、インストールが不要なこと、所要およそ 5 分を書きました。
- **「うまくいかなかったとき」**（新設）。画面の写真・住所（URL）・時刻（JST）・ブラウザの名前を求め、連絡先を `info@rictaworks.jp` としました。
- **「自動での確認（実測値）」**へ、ブラウザ操作の確認の件数（11 例）と、束ねた検査 34 例の内訳を足しました。

もとの本文の「変えたこと」の表と「変更しなかったもの」は、内容が正確でしたので残しました。**「この PR は画面を持ちません」の節も残し、代わりに何を確かめるのかを書き足しました。**

## 実測した数値

**共有のデータベースへ負荷をかけないため、RSpec の全例は実行していません。** バックエンドの結果は CI の実行から読みました。

| 検査 | 実測 | 本文の記載（更新前） | 判断 |
|---|---|---|---|
| 置き場の検査（`npm run test:harness`） | **6 例・失敗 0 件**（手元で実測） | 6 例、失敗 0 件 | 一致 |
| 束ねた検査（`npm run test:all`） | **34 例・失敗 0 件**（手元で実測。フック 23 ＋ 直書き 5 ＋ 置き場 6） | 34 例、失敗 0 件 | 一致。**内訳を書き足しました** |
| 直書きの文言の検査（`npm run check:hardcode`） | 検出 0 件（手元で実測。「日本語を含む文字列リテラルはありません。」） | 検出 0 件 | 一致 |
| ブラウザ操作の確認（`npx playwright test`） | **11 例・失敗 0 件**（CI ログ。`Running 11 tests using 2 workers` ／ `11 passed (5.0s)`） | 記載なし | **書き足しました** |
| 変更ファイル数 | 4 件（+241 / -1 行） | 記載なし | 書き足しました |
| 自動検査の数 | **6 つ**（すべて緑） | 「ほかの 5 つ」とだけ記載 | **6 つの一覧として書き直しました** |

**自動検査は、この PR の先頭（`32d4120`）で 6 つとも緑です**（run `33065520678`。`e2e` ジョブは 11:01〜11:02 UTC ＝ 20:01〜20:02 JST、所要 1 分 45 秒）。

| 検査の名前（実物どおり） | 結果 |
|---|---|
| ブラウザ操作の確認（Playwright） | 緑（1m45s） |
| バックエンド（RuboCop / RSpec） | 緑（55s） |
| フロントエンド（ESLint / 型検査 / Jest / ビルド） | 緑（36s） |
| 本番と同じ設定での読み込み確認 | 緑（28s） |
| 開発フックの検査 | 緑（7s） |
| ハードコードの検査 | 緑（5s） |

## 手順の名前を実物と突き合わせた結果

`e2e` ジョブの手順を、GitHub の API（`actions/jobs/98494501170`）から読み、本文の表と 1 行ずつ照合しました。

| 順 | 手順の名前 | 結果 |
|---|---|---|
| 1 | Set up job | 緑 |
| 2 | Initialize containers | 緑 |
| 3 | Run actions/checkout@v4 | 緑 |
| 4 | Run ruby/setup-ruby@v1 | 緑 |
| 5 | Run actions/setup-node@v4 | 緑 |
| 6 | データベースの準備 | 緑 |
| 7 | 自動ログインの利用者を作る | 緑 |
| 8 | 依存の導入（画面） | 緑 |
| 9 | 画面の組み立て | 緑 |
| 10 | 依存の導入（確認の道具） | 緑 |
| 11 | ブラウザの導入 | 緑 |
| 12 | バックエンドを起動 | 緑 |
| 13 | 画面を起動 | 緑 |
| 14 | ブラウザ操作の確認 | 緑 |
| 15 | 起動の記録 | **灰色（skipped）。失敗したときだけ動きます** |

**〈バックエンドを起動〉〈画面を起動〉が〈ブラウザ操作の確認〉より前にあることを、実物で確かめました。**

## 押さえが効くことの確認

**本文の「壊し方」の表を、3 つとも実際に壊して確かめました。** データベースを使わない検査ですので、共有のデータベースへ負荷をかけていません。

| 壊し方 | 実測 | 本文の記載 | 判断 |
|---|---|---|---|
| `e2e` ジョブを丸ごと外します | `not ok 4 / 5 / 6`（6 例中 3 例が赤） | 赤（3 例） | 一致。**「6 例中」を書き足しました** |
| 接続先を本番の住所（`https://hero.rictaworks.jp`）へ変えます | `not ok 5`（6 例中 1 例が赤） | 赤（1 例） | 一致 |
| 起動より先に確認を走らせます（〈ブラウザ操作の確認〉を〈バックエンドを起動〉の前へ移します） | `not ok 6`（6 例中 1 例が赤） | 赤（1 例） | 一致 |

**壊したあとは、3 つとも元へ戻しました**（確認用の作業ツリーで `git checkout -- .github/workflows/ci.yml` により復元。`git status` は空です）。

## ブラウザで確かめられないこと

- **この PR の中身は自動検査の手順です。アプリの画面には何も現れません。** ユーザーテストは、GitHub の PR の画面で「新しい検査が動いていること」を確かめるものだと本文に明記しました。
- **`e2e` ジョブが実際にブラウザを動かしている様子**は、動画としては残りません。確かめられるのは、ログに並ぶ 11 件の項目名と `11 passed` の文字です。手順もそこまでに留めました。
- **`main` への取り込みでもこのジョブが走ること**は、取り込みが済むまで画面で確かめられません。本文では `ci.yml` の設定として書くに留め、確認の手順には入れていません。

## 制約の遵守

- **削除コマンドを一切生成していません。** 確認用の作業ツリー `DELETE/pr179-checker-20260827` は、そのまま `DELETE/`（ゴミ箱）に残しています。
- **実装側の作業ツリーへ触れていません。** ブランチも切り替えていません。
- **RSpec の全例を実行していません。** 共有のデータベースへ重い実行を重ねないためです。バックエンドの結果は CI の実行（run `33065520678`）から読みました。
- **開発用のデータベースへ書き込んでいません。** 実行したのは `node --test` による検査だけです。
- コマンドはすべて `docker compose exec -T dev bash -lc '...'` で実行しました。
- 本文は一時ファイル経由（`gh pr edit 179 --body-file`）で反映しました。反映後の本文を読み直し、文字化けが無いことを確かめました。
- コードレビューは行っていません（reviewer の担当です）。
- `.claude/` 直下の md の内容を、本文・記録へ転記していません。

## 申し送り

- **この PR の土台は `be10d2d` で、現在の `main`（`6e97fd8`）より 1 つ古いです。** 取り込みの前に土台を合わせるかどうかは、reviewer と担当の方のご判断でお願いします。**自動検査は `32d4120` の状態で 6 つとも緑です。**
- **確認 1 の「検査が 6 つ」は、この PR の時点の数です。** 今後ジョブが増えれば数が変わります。手順を使い回す場合は、数の読み替えが要ります。
- **確認 3 の 11 件は、`test/pr-setup/` と `test/pr170/` の合計です。** 画面を変える PR が増えるたびに件数が増えます。
- **`test/README.md` に `/.review/` の下へ作業ツリーを作る手順が書かれています。** 当方は従来どおり `DELETE/` の下に作りました。置き場の指定が 2 通りある状態ですので、どちらへ寄せるかを決めていただけますと助かります。

連絡先は info@rictaworks.jp です。

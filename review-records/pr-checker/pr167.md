# PR #167 pr-checker 記録

- 対象 PR：#167 「[実装] プロジェクト・プリセット・生成履歴・評価メモ API を実装します (#57, #58, #59, #60)」
- ブランチ：`feature/issue-57-60-resource-api`（先頭 `956fe9c`）
- 土台：`main`（`9ba7f9f`）
- 実施日時：2026-08-27（JST）
- 対象 issue：#57、#58、#59、#60

## 判定

合格

## タイトル

変更しませんでした。日本語・ですます調で、種別と対象と issue 番号がそろっています。

## 本文で直したこと

### 1. 「この PR の土台」を実態へ合わせました

修正前は「PR #166 の上に積んでいます。先に #166 をマージしてください。」でした。**#166 はマージ済みで、土台は `main` です。**

### 2. 「確かめたこと」の数値を測り直しました

土台が `main` へ変わったため、本文の数値がすべて古くなっていました。**PR 先頭と `main` の 2 つの作業ツリーを作って測りました。**

| 項目 | 本文の記載（修正前） | 実測 | 対応 |
|---|---|---|---|
| RSpec 例数 | 1643 例 | **1728 例、失敗 0 件** | 直しました |
| うち新規 | 59 例 | **66 例**（`main` は 1662 例） | 直しました |
| RuboCop | 182 ファイル | **189 ファイル、指摘 0 件** | 直しました |
| ハードコード検査 | 検出 0 件 | 検出 0 件 | 一致 |
| 変更ファイル数 | 記載なし | **18 ファイル** | 追記しました |
| GitHub の自動検査 | 記載なし | **5 件すべて成功** | 追記しました |

### 3. 非エンジニア向けのユーザーテスト手順を、実際に試してから書きました

**この PR は画面を持ちません。** 18 件に `.tsx` は 1 件も含まれません。`test/README.md` の「置かない場合」に当たるため、`test/pr167/` を置かない旨を明記しました。

そのうえで、**ブラウザだけで実際に確かめられる範囲**を探し、開発環境で自分で実行して確認してから書きました。

| URL | 実測 |
|---|---|
| `http://localhost:3301/health` | 200 `{"status":"ok"}` |
| `http://localhost:3301/api/v1/projects` | 401「ログインが必要です。」 |
| `http://localhost:3301/api/v1/presets` | 同上 |
| `http://localhost:3301/api/v1/prompt_requests` | 同上 |
| `http://localhost:3301/api/v1/prompt_outputs/1/evaluation_note` | 同上 |

**判定を「401 が返る」ではなく「画面に『ログインが必要です。』が表示される」と書きました。** 失敗の見分け方も添えました。**番号は 3301 であることを注意として置きました。**

### 4. X ログインは「いま確かめられません」と正直に書きました

**手順を捏造しないため、実際に試しました。結果は不可でした。**

- `docker-compose.yml` に `X_OAUTH_*` を渡す記述が**ありません**（この PR の時点では）。
- `env | grep -c X_OAUTH` は **0** を返します。
- `/auth/start` を開くと **500**（`KeyError: key not found: "X_OAUTH_CLIENT_ID"`）になります。
- `DEVELOPMENT_AUTO_LOGIN_X_USER_ID` も `.env` にありません。

**本文に「いま確かめられないこと」の節を設け、確かめられない項目を列挙し、手順を書いていないのは書けないためであることを書きました。**

### 5. 体裁を本文テンプレートへ寄せました

「何を変えたか」「なぜ変えたか」「変更したファイル」「うまくいかなかったとき」を整えました。絵文字は使っていません。

## 申し送り（PR 本文には書いていません）

- **`src/backend/spec/models/preset_spec.rb.append` が 0 バイトの新規ファイルとして差分に含まれています。** 作業中の中間ファイルが残ったように見えます。
- 開発環境で X ログインを完了できるようにするには、`docker-compose.yml` の `dev` サービスへ環境変数を渡す記述と、`.env` への実在の値の投入の両方が要ります。

## 後片付け

- 起動した Rails を停止しました。
- 作業ツリー 2 つを `git checkout --detach` でブランチから解放しました。
- 削除系のコマンドは一切実行していません。
- 実装側の作業ツリーは読み取りのみです。

## 呼び出し側の対応

- **中間ファイルを `DELETE/stray-2026-08-27/` へ移しました**（削除ではなく移動です）。
- **`docker-compose.yml` へ環境変数を並べる変更は、PR #170 に含めました**（同じファイルを 2 つの PR で触ると衝突するためです）。

連絡先は info@rictaworks.jp です。

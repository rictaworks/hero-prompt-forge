# PR 整備 : PR #145

- 実施日時（JST）：2026-08-26 20:39
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/145
- 対象ブランチ：`feature/issue-42-139-composition`（HEAD `8ca84fb`）
- 対象 issue：#42 ／ #139
- 担当：pr-checker

## 1. タイトル

`[実装] コピースペースの構図規定と、人物の見込みの判定を実装します` で確定しました。日本語で、ですます調です。絵文字・個人名・秘匿情報を含みません。

## 2. 数値・ファイル名・件数の実測

一時的な作業ツリー（HEAD `8ca84fb`）を作り、開発コンテナ内で実測しました。

| 本文の記載 | 実測値 | 判定 |
|---|---|---|
| 変更したファイル 10 件 | 10 件 | 一致します |
| `.tsx` を 1 件も含まない | 0 件です | 一致します |
| 余白のテスト 39 件 | 39 examples, 0 failures | 一致します |
| スタイルの指示のテスト 60 件 | 60 examples, 0 failures | 一致します |
| 設定の中身のテスト 22 件 | 22 examples, 0 failures | 一致します |
| バックエンドのテスト全体 715 件 | **718 examples, 0 failures** | **相違があるため 718 件へ直しました** |
| RuboCop 99 ファイル・指摘 0 件 | 99 files inspected, no offenses detected | 一致します |

**相違の原因の見立て**：セキュリティレビューの記録にも「715 例」とあります。記録の実施時点からテストが 3 件増えたものと見られます。実装の不具合ではありません。

変更したファイルの表に載る 10 件のファイル名・並び順・分類は、いずれも実物と一致しました。

## 3. 「実際の設定で動かしました」の出力の照合

指定のコマンドを一時作業ツリー内で実行しました。**本文に載せた 2 つの出力は、行の内容・順序ともに実物と一致します。** SaaS だけに `the subject seen from behind` が 1 行入り、EC には人物の構図が入らない点も、本文の説明のとおりです。

## 4. 本文のその他の記載の裏取り

| 記載 | 確認結果 |
|---|---|
| 業種ごとの人物の見込み | `industry_defaults` と一致します |
| 余白の指示は 4 つ | `reserved` / `subject` / `gaze` / `restraint` と一致します |
| 縦横比は 16:9 / 21:9 / 3:2 | `aspect_ratios` の 3 件と一致します |
| 規則の設定の版を `v2026.08.3` へ上げた | `origin/main` は `v2026.08.2`、本ブランチは `v2026.08.3` です |
| 人物の構図を 1 つだけ当てる／当てなかった事実も控えに残す | 実装されています |
| 余白を確保したことを控えに残す | 実装されています |
| CI の 5 項目の名称 | `gh pr checks 145` の 5 件と一致し、すべて成功しています |
| 参照する issue 番号 | #42・#139・#50・#55・#71・#73 はいずれも実在し、件名が一致します |

**表現の修正を 1 か所行いました。** 「お申し込みの画面（issue #71）と受け口（issue #55）」を、それぞれの件名に合わせて「入力フォームの画面（issue #71）と、生成の受け口（issue #55）」へ直しました。

## 5. 文体・表記の確認

| 観点 | 結果 |
|---|---|
| タイトル・本文が日本語であること | 満たします |
| ですます調であること | 満たします |
| 絵文字を使っていないこと | 満たします（0 件） |
| 個人名を書いていないこと | 満たします。連絡先は `info@rictaworks.jp` のみです |
| 秘匿情報を含まないこと | 満たします |
| 未実装のものを書いていないこと | 満たします |

## 6. 非エンジニア向けユーザーテスト手順の確認

ブラウザだけで完了でき、開始する画面を明示し、1 手順 1 操作で、期待結果が画面に見えるもので書かれています。失敗時の報告事項（画面の写真・URL・時刻（JST））も末尾にあります。

**手順を捏造していないことの裏取り**：`src/frontend` には入力フォームも結果画面もありません。`routes.rb` にも生成の受け口はありません。したがって「アプリの画面で確かめられない」という記載は事実です。

## 7. まとめ

- タイトル：修正なしで確定しました。
- 本文：3 か所（テスト件数 2 か所、issue の呼称 1 か所）を実物に合わせて直しました。
- 「実際の設定で動かしました」の 2 つの出力は、実物と完全に一致しました。
- 申し送り：セキュリティレビュー記録側の「715 例」は、現在の HEAD では 718 例です。

---

# PR 整備（2回目） : PR #145

- 実施日時（JST）：2026-08-26 20:57
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/145
- 対象ブランチ：`feature/issue-42-139-composition`（HEAD `dcedb14`）
- 追加されたコミット：`dcedb14`「[実装] コピースペースの確保を、素材で確かめます」
- 対象 issue：#42 ／ #139
- 担当：pr-checker（タイトル・本文の整備のみを行います。コードレビューは行いません）

## 1. 実測の環境

一時的な作業ツリー `.review/prcheck145b`（HEAD `dcedb14`）を作り、開発コンテナ内で実測しました。実装側の作業ツリーでは `git fetch origin` と読み取り系のコマンドのみを実行し、`git checkout` ・ `git switch` ・ `git stash` は実行していません。

## 2. 数値の実測（1回目からの変化）

| 本文の記載（1回目の反映後） | 今回の実測値 | 対応 |
|---|---|---|
| 変更したファイル 10 件 | 10 件 | 一致します。修正はありません |
| `.tsx` を 1 件も含まない | `.tsx` は 0 件です | 一致します |
| 余白のテスト 39 件 | **42 examples, 0 failures** | **42 件へ直しました** |
| スタイルの指示のテスト 60 件 | **65 examples, 0 failures** | **65 件へ直しました** |
| 設定の中身のテスト 22 件 | 22 examples, 0 failures | 一致します |
| バックエンドのテスト全体 718 件 | **726 examples, 0 failures** | **726 件へ直しました**（ご指摘のとおりの値です） |
| RuboCop 99 ファイル・指摘 0 件 | 99 files inspected, no offenses detected | 一致します |

変更したファイルの一覧（10 件）は、1回目と同じ構成です。`dcedb14` は既存の 8 ファイルを直しただけで、新しいファイルは増えていません。本文のファイル表の分類・並び順も、実物と一致します。

CI は `gh pr checks 145` で 5 項目すべて成功を確認しました。本文に並べた 5 項目の名称も、実物と一致します。

## 3. 「実際の設定で動かしました」の再実行と差し替え

指定のコマンドを一時作業ツリー内で再実行しました（`bin/rails db:seed`・`bin/rails runner` ともに正常終了）。**英文が変わっていたため、本文の 2 つの出力を実測へ差し替えました。**

**SaaS の実測出力（差し替え後の本文と一致します）**

```
35mm lens
soft key light from a north-facing window
bounced fill from a white wall
subtle rim light separating the subject
shallow depth of field on the subject plane
the subject seen from behind
clear copy space across the left third of the frame
the main subject placed at the upper right intersection of a rule of thirds grid
the subject contained within the right two thirds of the frame
the left third rendered as an even low contrast surface
composed for a 16:9 wide crop
```

**EC の実測出力（差し替え後の本文と一致します）**

```
35mm lens
soft key light from a north-facing window
bounced fill from a white wall
subtle rim light separating the subject
shallow depth of field on the subject plane
clear copy space across the left third of the frame
the main subject placed at the upper right intersection of a rule of thirds grid
the subject contained within the right two thirds of the frame
the left third rendered as an even low contrast surface
composed for a 16:9 wide crop
```

変わった行は 3 行です。いずれも `dcedb14` の英文の直しと対応しています。

| 変わる前 | 変わった後 |
|---|---|
| `the main subject positioned on the right third intersection of a rule of thirds grid` | `the main subject placed at the upper right intersection of a rule of thirds grid` |
| `the subject oriented toward the left side without extending into it` | `the subject contained within the right two thirds of the frame` |
| `the left third kept free of competing detail` | `the left third rendered as an even low contrast surface` |

SaaS だけに `the subject seen from behind` が 1 行入り、EC には人物の構図が入らない点は、今回も変わりません。

## 4. `dcedb14` の内容の本文への反映

次のとおり本文へ反映しました。

- 「どう直したか」の 4 つの指示の表を、実物の英文へ合わせて直しました。「視線の向き」という役割名を「被写体の収まり」へ改め、「余白へは入り込みません」という打ち消しの説明を「右 3 分の 2 の中に収めます」という肯定形へ直しました。交点に「右上」と高さを書き、余白側の説明を「むらのない、コントラストの低い面」へ直しました。
- 「確保したことを控えに残します」を、**「余白が残っているかは、控えではなく、出来上がった指示そのもので確かめます」**へ書き換えました。理由（控えは消えないが指示は消えるため、落ちたのに確保済みに見える状態を防ぐこと）も添えました。
- **余白の規定を 2 回当てない**ことを書き足しました。
- **業種の見込みを見るのは実写のときだけである**ことを書き足しました。
- **人物の構図を当てなかった理由を 2 つに分けた**ことを、表で書き足しました。分けない場合にどう困るか（イラストを選んだだけなのに業種のせいに読める控えが残ること）と、この控えを issue #51 で利用者へ見せることも書きました。
- 「レビューの指摘に対応しました」という節を新設し、`dcedb14` で直した 8 点を表にまとめました。
- **issue #146 と issue #147 を起票したこと**、**この PR では実装しないこと**を明記しました。

## 5. 記載内容の裏取り

| 記載 | 確認結果 |
|---|---|
| 余白の判定を実際の指示で行う | `copy_space.rb` の `reserved?` が `main_terms` の `copy space` を見る形になっています。一致します |
| 余白の規定を 2 回当てない | `AlreadyReservedError` と `ensure_not_reserved!` があります。一致します |
| 当てなかった理由が 2 つ | `style_spec.rb` の `people_unlikely` と `style_has_none` です。一致します |
| 実写だけが見込みを引く | `safety_decision` が、避ける構図の定義が空なら業種を見ずに戻ります。一致します |
| 打ち消しの言い回しが無い | `config/copy_space_rules.yml` の 3 位置ぶんを目視し、`without` などの打ち消しが無いことを確認しました |
| 下中央の視線が水平・遠景 | `the subject looking level toward the far distance` です。一致します |
| 交点に高さが入る | `upper right` ・ `upper left` ・ `along the upper third line` です。一致します |
| 業種ごとの割り当ての前提（飲食は料理単体、EC は物販） | `initial.yml` の注釈と一致します |
| issue #146 ・ #147 が実在すること | どちらも実在し、件名が本文の説明と一致します |
| 版は `v2026.08.3` のまま | `dcedb14` は `version` を変えていません。本文の版の記載は正しいままです |
| 業種ごとの人物の見込みの表 | `industry_defaults` と一致します |

## 6. 「デプロイのときの申し送り」の節の確認

**内容は正しいです。** 裏取りの結果です。

- `db/seeds.rb` は `find_or_initialize_by(version:)` で、その版が無いときだけ作って公開します。何度実行しても結果が変わりません。**新しい版は、この手順が走ったときにだけ環境へ届きます。** 記載のとおりです。
- 古い版（`v2026.08.2`）のままだと、業種ごとの人物の見込み（`people`）が無いため、`PeopleExpectation` がその場で失敗します。**実写を選んだ生成が失敗する**という記載は正しいです。
- 「止まるのは実写だけです」も正しいです。`initial.yml` で避ける構図（`person_safety`）を持つ絵柄は `photoreal` だけで、イラスト・3D・抽象は持ちません。`dcedb14` で「定義が無ければ業種を見ない」形になったため、これらの絵柄はこの設定を引きません。

**非エンジニアへの伝わりやすさの観点で、2 か所を書き直しました。**

- 「設定を投入する手順（`db:seed`）」から、コマンド名の `db:seed` を外し、**「リリースの流れの中で規則の設定をデータベースへ入れる作業」**という説明へ置き換えました。ブラウザしか使わない読み手にとって、コマンド名は手がかりになりません。
- 「無停止での入れ替えどころか、実写の生成が全面的に止まります」という言い回しを、**「古い版が入ったままの環境では、実写を選んだ生成がすべて失敗します」**へ改めました。意味は同じで、必要以上に強い表現を避けています。

節の位置は「確認した結果」の直後、ユーザーテスト手順の前に置いたままにしました。作業者向けの申し送りであり、ユーザーテストの手順と混ざらない位置が適切なためです。

## 7. 文体・表記の確認

| 観点 | 結果 |
|---|---|
| タイトル・本文が日本語であること | 満たします。タイトルは変更していません |
| ですます調であること | 満たします。追記した節も含めて確認しました |
| 絵文字を使っていないこと | 満たします（絵文字の文字範囲を機械的に検査し、0 件でした） |
| 個人名を書いていないこと | 満たします。連絡先は `info@rictaworks.jp` のみです |
| 秘匿情報を含まないこと | 満たします。`.claude/` 直下の md の内容は転記していません |
| 未実装のものを書いていないこと | 満たします。issue #146 ・ #147 は「この PR では実装しません」と明記しました |
| ファイルのエンコード | UTF-8 の一時ファイル経由で反映しました |

## 8. 非エンジニア向けユーザーテスト手順の確認

手順の構成は 1回目から変えていません。件数の記載だけを 718 件から 726 件へ直しました。

| 観点 | 結果 |
|---|---|
| ブラウザだけで完了できること | 満たします。ターミナル・コマンド・`curl`・環境変数・ログの確認を含みません |
| 開始する画面を最初に明示していること | 満たします |
| URL がクリックできる形であること | 満たします |
| 1 手順 1 操作であること | 満たします。手順 1 は 3 手順、手順 2 は 2 手順、手順 3 は 2 手順で、いずれも 1 手順に 1 操作です |
| 「どこを」「どうする」「何が見えれば正解か」の 3 点セットであること | 満たします |
| 期待結果が画面に見えるもので書かれていること | 満たします |
| 失敗時に何を報告するかを末尾に書いていること | 満たします |
| ブラウザで確認できない旨を明記していること | 満たします。入力フォーム（issue #71）と生成の受け口（issue #55）が未実装であることを書いています |

**手順を捏造していないことの再確認**：`dcedb14` でも `src/frontend` に画面は増えておらず、`src/backend/config/routes.rb` にも生成の受け口はありません。「アプリの画面で確かめられない」という記載は、今回も事実です。

## 9. 反映と後片付け

`gh pr edit 145 --title "..." --body-file <一時ファイル>` で反映しました。反映後の本文を再取得し、42 件 ・ 65 件 ・ 726 件への修正と、英文 3 行の差し替えが入っていることを確認しました。

実測に使った一時作業ツリーは、`mv` で `DELETE/review-worktrees/pr145b-20260826-prchecker` へ移しました。削除コマンドは使っていません。

## 10. まとめ

- タイトル：変更していません。
- 本文：テスト件数 4 か所、英文の出力 2 か所（各 3 行）、説明の節 5 か所を実測へ合わせて直し、「レビューの指摘に対応しました」の節を新設しました。
- 「デプロイのときの申し送り」の節は、内容が正しいことを確認しました。非エンジニアへの伝わりやすさのため、コマンド名を外す等の書き直しを 2 か所行いました。
- 1回目に申し送った「セキュリティレビュー記録の 715 例」は、`dcedb14` の追記で 726 例が併記されています。追記側の値は今回の実測と一致します。

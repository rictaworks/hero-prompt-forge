# PR 整備 : PR #125

- 実施日時（JST）：2026-08-26 11:47
- 対象 PR：#125
- 対象 Issue：#37

## 判定

合格

タイトルと本文はいずれも日本語のですます調で書かれていました。書き直しは不要でしたので、不足していた 2 点のみを補いました。

## 確認したこと

| 観点 | 判定 | 備考 |
|---|---|---|
| タイトルが日本語のですます調であること | 合格 | `[実装] クォータの予約・確定・返還を実装します` です。変更していません |
| 本文が日本語のですます調であること | 合格 | 全体がですます調です。だ・である調の混入はありません |
| 非エンジニアに伝わる書き方であること | 合格 | 「予約」「確定」「返却」を日常の言葉で説明し、表で場面ごとの扱いを示しています |
| ユーザーテスト手順があること | 補いました | もとは「手順はありません」で終わっていました。PR ページ上で実施できる確認 3 項目を追記しました |
| 変更したファイルの節があること | 補いました | 3 件を分類して追記しました |
| 「うまくいかなかったとき」が 3 点セットであること | 合格 | 画面の写真・URL・操作した時刻（JST）がそろっています。連絡先も `info@rictaworks.jp` です |
| 絵文字を使っていないこと | 合格 | 検出なし |
| 未実装のものを書いていないこと | 合格 | 画面の確認は「issue #71 以降に書けるようになります」と時期を明示しています |

## 補った内容

### 1. 「変更したファイル」の節

テンプレートで必須の節が欠けていました。3 件を次のとおり分類して追記しました。

| 分類 | ファイル |
|---|---|
| 枠の出し入れを行う仕組み | `src/backend/app/services/quota/reservation.rb` |
| その仕組みのテスト | `src/backend/spec/services/quota/reservation_spec.rb` |
| 作業の記録 | `review-records/security-review/feature-issue-37-quota-reservation.md` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** この PR がブラウザの見た目を変えないことを、ファイルの一覧からも確かめられます。

### 2. ユーザーテスト手順

もとの本文は「アプリの画面で確認できる手順はありません」とだけ書かれていました。事実としては正しいのですが、**PR のページを開くだけでできる確認**がありますので、次の 3 項目を追記しました。ターミナルもコマンドも使いません。

1. PR のページを開き、下の方にある自動チェックの一覧で 4 項目すべてに緑色のチェック印が付いていることを確かめます。
2. そのうち「バックエンド（RuboCop / RSpec）」に緑色のチェック印が付いていることを確かめます。これは、この PR で作った仕組みのテスト 21 件を含む 298 件すべてが通ったという意味です。
3. 上部の「Files changed」を開き、変更が 3 件で、`.tsx` で終わるファイル（画面を作るファイル）が 1 件も無いことを確かめます。

実際の CI の結果（4 項目すべて成功）と、変更ファイルの一覧を確認したうえで書いています。

アプリの画面での確認手順を書けるようになる時期（issue #71「[画面] 03 入力フォームを実装する」以降）も、もとの記載のまま残しています。そのときに確かめられることも、もとの本文に 3 点書かれています。

## 反映の方法

`gh pr edit 125 --body-file <一時ファイル>` で反映しました。引数へ直接本文を書くと文字化けの恐れがあるため、一時ファイルを経由しています。反映後に `gh pr view 125` で読み直し、文字化けが無いことと見出しの構成を確認しました。

## 指摘

指摘はありません。

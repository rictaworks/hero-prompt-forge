# pr-checker 記録：PR #178（issue #177 管理画面の守りに、後戻りを止める押さえを足す）

- 対象 PR：#178
- ブランチ：`feature/admin-guards`（`a35bc09`）
- 土台：`main`（`be10d2d`）
- 実施日時：2026-08-27（JST）
- 作業ツリー：確認用の作業ツリー `DELETE/pr178-checker-20260827` を新しく作り、そこで確かめました。**実装側の作業ツリー（`DELETE/impl-177`）へ触れていません。`main` の作業ツリーでブランチを切り替えていません。**

## 判定

合格

タイトルはそのままとし、本文を規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ組み直しました。**ユーザーテストの手順が実際の画面と食い違っていた箇所を、実測に合わせて直しました。**

## 反映した内容

- タイトル：`[整備] 管理画面の守りに、後戻りを止める押さえを足します (#177)`（**変更していません。** 日本語・ですます調で、規約に沿っています）
- 本文：**組み直しました。** 日本語化は不要でした（もとから日本語・ですます調です）。**ユーザーテストの手順は書き直しました。**

## 画面と食い違っていた箇所（直しました）

**実際に管理画面を立ち上げて、1 つずつ突き合わせました。**

| 更新前の本文 | 実際の画面 | 対応 |
|---|---|---|
| 〈本日の生成枠をリセットする〉を押します | ボタンの文字は〈**本日の枠をリセットする**〉です | 直しました |
| 一覧から利用者をひとり選びます | 一覧の各行の末尾にある〈**開く**〉を押します | 押す場所を書きました |
| 押せば記録が 1 行増える前提です | **本日の消費が無い利用者では記録は増えません。** 赤い帯で〈本日の消費がありませんので、リセットは要りません。〉と出ます | **3 つの分かれ道（緑・記録なし・生成中）を書き足しました** |
| 画面の中身が出ないことを確かめてください | でたらめな値では**同じ窓がもう一度出ます。** 取り消すと `HTTP Basic: Access denied.` の 1 行だけが出ます | **画面に見えるもので書き直しました** |
| 住所（URL）は担当者に聞いてください | 手元の開発環境では `http://localhost:3301/admin/` です | **住所を明記し、押せる形にしました** |
| 「利用者名と合言葉を尋ねる小さな窓が出ます」 | **ブラウザ自身が出す窓**です。画面の中の入力欄ではありません | **窓の正体と、押すボタンの呼び名（〈ログイン〉〈OK〉〈サインイン〉）を補いました** |

**BASIC 認証の画面ですので、「どこに何を入れるのか」が分からないと最初の 1 歩で止まります。** ここを厚く書きました。

## 本文へ足した節

- **「1. 管理画面が開きます」**（新設）。**最初の 1 歩を独立した確認にしました。** 正解の見え方を〈hero-prompt-forge 管理〉と 3 つの並び（〈規則辞書〉〈利用者とプラン値〉〈利用状況〉）で書きました。
- **「実施者」の列の確認**（確認 3 へ追加）。**issue #177 の M12 の核心を、ブラウザだけで確かめられる唯一の点です。** 「準備の窓に入れた利用者名と同じ文字が出ていること」を正解にしました。
- **「ブラウザでは確かめられないこと」**（新設）。**この PR の中身は検査（テスト）ですので、検査が動く様子は画面に出ません。** 手順は「検査が守っている表示が、これまでどおり出ていること」を確かめるものだと明記しました。**手順を捏造していません。**
- **「変更したファイル」**（新設）。11 件を、管理画面の実装・テスト（Rails）・直書きの検査・記録に分けて並べました。
- **「なぜ変えたか」**（新設）。押さえが無い決まりは黙って元へ戻ること、監査の記録は「誰が」「いつ」が欠けると意味が無いことを書きました。
- **「うまくいかなかったとき」**（新設）。画面の写真・住所（URL）・時刻（JST）・ブラウザの名前を求め、連絡先を `info@rictaworks.jp` としました。
- **開発環境だけで行う断り**（準備へ追加）。確認 3 は本当にリセットを 1 回行い、記録が 1 行増えます。**本番の管理画面では行わないことを書きました。**

もとの本文の「直したこと」の表と「変更しなかったもの」は、内容が正確でしたので残しました。

## 実測した数値

| 検査 | 実測 | 本文の記載（更新前） | 判断 |
|---|---|---|---|
| RSpec | **1865 例・失敗 0 件**（CI run `33063433610`） | 1866 例 | **直しました** |
| RuboCop | 205 ファイル・指摘 0 件（CI と手元） | 205 ファイル・指摘 0 件 | 一致 |
| 直書きの検査の仕組みのテスト | 8 例・失敗 0 件（手元で実測） | 8 例・失敗 0 件 | 一致 |
| 直書きの文言の検査 | 検出 0 件（手元で実測） | 検出 0 件 | 一致 |
| Jest（フロントエンド） | 10 ファイル・66 例・失敗 0 件（CI） | 記載なし | 追記しました |
| 本番と同じ設定での読み込み確認 | 成功（CI） | 記載なし | 追記しました |
| 開発フックの検査 | 成功（CI） | 記載なし | 追記しました |
| 変更ファイル数 | 11 件（新規 5・変更 6、+319 / -34 行） | 記載なし | 追記しました |

**自動検査は、この PR の先頭（`a35bc09`）で 5 つとも緑です**（run `33063433610`、10:31 UTC ＝ 19:31 JST）。

**手元での全例の実行は 1865 例中 3 例が落ちました。この PR の変更とは関係がありません。** 共有のテスト用データベースに、他の作業が残した規則辞書（`vprobe…` の版が 4 件）が残っており、`RuleDictionary.current` が公開済みの版を拾うためです。**掃除された CI の環境では 0 件です。**

## 画面で実際に確かめたこと

**隔離したデータベース（`hero_prompt_forge_prcheck178`）を作り、そこで管理画面を立ち上げて応答を読みました。開発用のデータへ書き込んでいません。**

| 見たもの | 実測 |
|---|---|
| 資格情報を渡さない | 401（中身は出ません） |
| でたらめな資格情報 | 401・本文は `HTTP Basic: Access denied.` の 1 行 |
| 正しい資格情報 | 200・〈hero-prompt-forge 管理〉と、〈規則辞書〉〈利用者とプラン値〉〈利用状況〉の並び |
| 利用者の一覧 | 〈X のユーザーID〉〈表示名〉〈プラン値〉〈最後に判定した日時〉と、各行の〈開く〉 |
| 利用者の画面 | 〈判定を取り直す〉〈**本日の枠をリセットする**〉〈管理の操作の記録〉 |
| 記録が無いとき | 〈この利用者に対する管理の操作は、まだありません。〉 |
| 記録の日時の書式 | `%Y年%-m月%-d日 %H:%M`（例：`2026年8月27日 10:30`）。時刻は JST です |
| 画面と文言の差分 | `src/backend/app/views` ・ `src/backend/config/locales` ・ `requirements.md` ・ `app-ui/` に変更はありません（実測） |

**「リセットを押して記録が 1 行増えるところ」までは、確かめられませんでした。** 共有の PostgreSQL が 19:59（JST）に落ち、復旧処理が続いているためです（申し送りに書きます）。**この 1 点は、PR に含まれる `spec/requests/admin/users_spec.rb` が、実施者・年・書式の 3 つを確かめており、CI で緑です。**

## 押さえが効くことの確認

**本文の「壊し方」の表のうち、データベースを使わない 2 つを、実際に壊して確かめました。**

| 壊し方 | 実測 |
|---|---|
| 検査の対象から `.erb` を外します | `not ok 4 / 5 / 7`（8 例中 3 例が赤） |
| 雛形の属性の読み取りを止めます | `not ok 5 / 7`（8 例中 2 例が赤） |

**壊したあとは元へ戻しました**（確認用の作業ツリーで `git checkout --` により復元。差分は残っていません）。

残る 2 つ（断り書きを画面から消す・日時の書式から年を落とす）は RSpec が要るため、データベースの復旧待ちで未実測です。**対応するテストが本文の記載と一致していることは、差分で確かめました。**

## 制約の遵守

- **削除コマンドを一切生成していません。** 確認用の作業ツリー `DELETE/pr178-checker-20260827` と、確認に使った `seed_check.rb` は、そのまま `DELETE/`（ゴミ箱）に残しています。
- **実装側の作業ツリーへ触れていません。** ブランチも切り替えていません。
- **開発用のデータベースへ書き込んでいません。** 隔離したデータベースを作って使いました。
- コマンドはすべて `docker compose exec -T dev bash -lc '...'` で実行しました。
- コードレビューは行っていません（reviewer の担当です）。
- `.claude/` 直下の md の内容を、本文・記録へ転記していません。

## 申し送り

- **共有の PostgreSQL が 19:59（JST）に落ちました。** 複数の作業（PR #176 ・ PR #178 のレビューと、当方の全例の実行）が同時に走り、負荷が重なったものと見られます。**当方の全例の実行は止めました。** 復旧（データ領域の同期）は **20:29 の時点でも続いています**（落ちてから 30 分です）。**復旧が終わるまで、テストは通りません。**
- **共有のテスト用データベースに、他の作業の残りものがあります。** `rule_dictionaries` に `vprobe…` の版が 4 件残っており、`RuleDictionary.current` を見る 3 例が落ちます。**消さずに残しています。** 整理は担当の方のご判断でお願いします。
- **〈判定を取り直す〉は、ユーザーテストの手順に入れていません。** 押すと判定サービスへ実際に問い合わせます。判定サービスの設定が無い環境では、開発者向けのエラー画面になります。
- 〈操作〉の列は `reset_quota` のまま出ます。開発者用の画面ですので、本文にもそのまま書きました。

連絡先は info@rictaworks.jp です。

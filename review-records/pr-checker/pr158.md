# PR 整備 : PR #158（2 回分をまとめた記録です）

- 対象：PR #158「[実装] 人物の見込みを、プロジェクト単位で上書きできるようにします」 / issue #147

---

# PR 整備 : PR #158

- 実施日時（JST）：2026-08-26 23:43
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/158
- ブランチ：`feature/issue-147-people-override`
- 先端コミット：`8c66669`
- 関連 issue：#147
- 担当：pr-checker（コードレビューは行いません）

## 1. 作業のしかた

実装側の作業ツリー（`feature/issue-51-art-direction-note` を出したまま）を触らないため、
`git worktree add DELETE/work-pr158 origin/feature/issue-147-people-override --detach` で
一時的な作業ツリーを作り、そこで実測しました。作業ツリーは最初からゴミ箱（`DELETE/`）の
下に作りましたので、後片付けの移動は不要です。**リポジトリ直下に一時ファイルを作っていません。
`.env` も `config/master.key` も複製していません**（開発コンテナが環境変数を持っているため、
複製の必要がありませんでした）。

実行環境は稼働中の開発コンテナ `hero-prompt-forge-dev-1`（Ruby 3.3.12）です。

## 2. 数値の実測

| 確かめたこと | 本文の記載 | 実測 | 判定 |
|---|---|---|---|
| 変更したファイルの件数 | 6 件 | 6 件 | 一致 |
| RSpec 全体の件数 | 1,251 件すべて成功 | 1251 examples, 0 failures | 一致 |
| RuboCop のファイル数 | 141 ファイル・指摘 0 件 | 141 files inspected, no offenses detected | 一致 |
| `.tsx` の変更 | 1 件も含まない | 0 件 | 一致 |
| データベースの構造 | 変えていない | `db/migrate/` の追加なし。`brand_settings` は既存列 | 一致 |

実行したコマンドです。

```
git diff --name-only origin/main...origin/feature/issue-147-people-override | wc -l   -> 6
bundle exec rspec            -> 1251 examples, 0 failures （13.81 秒）
bundle exec rubocop --format simple -> 141 files inspected, no offenses detected
```

GitHub 側の申告（`gh pr view 158 --json changedFiles`）も 6 件でした。
CI（run 32980216511）の記録も `1251 examples, 0 failures` ・ `141 files inspected` で、
手元の実測と一致します。

変更したファイルの一覧です。

```
A  review-records/security-review/feature-issue-147-people-override.md
M  src/backend/app/models/project.rb
M  src/backend/app/services/generation/people_expectation.rb
M  src/backend/app/services/generation/style_spec.rb
M  src/backend/spec/models/project_spec.rb
M  src/backend/spec/services/generation/style_spec_spec.rb
```

## 3. 4 つのふるまいの実測

`DELETE/work-pr158/src/backend/spec/pr158_probe_spec.rb` に確認用のテストを置き、
**実データ（`config/rule_dictionary/initial.yml`）を読み込んだ規則辞書**で動かしました。
差し替えの辞書ではなく、本番に載る初期データで確かめています。

業種ごとの標準値（実測）です。

```
saas=expected  restaurant=unlikely  medical=expected  education=expected
real_estate=unlikely  manufacturing=expected  professional_services=expected
ecommerce=unlikely  beauty=expected  other=unlikely
```

本文の表にある 3 つの例と突き合わせました。EC（`ecommerce`）は `unlikely`、
飲食（`restaurant`）は `unlikely`、製造（`manufacturing`）は `expected` です。
**本文の「これまで」の欄は 3 例とも実データと合っています。**

### (1) 写らない見込みの業種を「写る」へ寄せる

入力：`industry: ecommerce`（標準は `unlikely`）／ `people: expected`

```
main_terms に "the subject seen from behind" が入りました
notes = {kind: :person_safety_applied,
         compositions: ["the subject seen from behind"],
         people_source: :project}
```

**そのとおり動きます。** 人物を避ける構図が入り、出どころが `project` として残ります。

### (2) 写る見込みの業種を「写らない」へ寄せる

入力：`industry: saas`（標準は `expected`）／ `people: unlikely`

```
main_terms に "the subject seen from behind" は入りません
notes = {kind: :person_safety_skipped, reason: :people_unlikely,
         industry: "saas", people_source: :project}
```

**そのとおり動きます。** 構図が入らず、当てなかった理由と出どころが残ります。

### (3) 指定が無ければ業種の標準値を使う

```
ecommerce（指定なし）: 構図なし / notes people_source: :industry, reason: :people_unlikely
saas   （指定なし）: 構図あり / notes people_source: :industry, kind: :person_safety_applied
```

**そのとおり動きます。** 出どころが `industry` になり、これまでの動きが変わりません。

### (4) 選べない値は保存でも生成でも止まる

```
保存 : Project.new(brand_settings: {"people" => "maybe"}).valid? -> false
       errors = ["Brand settings に選べない値が指定されています。"]
生成 : Generation::PeopleExpectation::InvalidOverrideError
       "人物の見込みの上書きが選べない値です: \"maybe\""
```

**そのとおり動きます。** 黙って標準値へ寄せる動きはありません。

確認用のテストは 5 件すべて成功しました（`5 examples, 0 failures`）。
このテストは一時的な作業ツリーの中だけに置いてあり、PR の中身には含めていません。

## 4. ユーザーテスト手順の検証

非エンジニアが手順どおりに操作して、書いてある「正解です」が本当に出るかを確かめました。

| 手順 | 書いてあった内容 | 実際 | 対応 |
|---|---|---|---|
| 準備 | PR のページを開く | URL は正しい。タイトルも本文の記載と一致 | 表示位置（画面のいちばん上ではなく、番号 `#158` の上）を書き足しました |
| 1 | 〈Show all checks〉を押し、5 項目に緑の印 | 検査は 5 件・すべて成功。名前も 5 つとも一致 | 押す前に見える〈All checks have passed〉と〈5 successful checks〉を先に確かめる形へ直しました。押す場所も「いちばん下」ではなく「コメント入力欄のすぐ上の箱」に直しました。日本語表示のときの表記も併記しました |
| 2 | `.tsx` が 1 件も無いこと | 0 件で正しい | タブの横に出る件数の印〈6〉も確かめる形にして、件数の確認を兼ねられるようにしました。日本語表示の表記を併記しました |
| 3 | `CHOICES` の行に 2 つだけ | 差分の中に文脈行として実際に出ます | 探し方（下へスクロールしてファイル名の箱を探す・閉じていたら開く）と、見えるはずの行そのもの（`CHOICES = [EXPECTED, UNLIKELY].freeze`）を書きました |

そのほか確かめたことです。

- PR の状態は `MERGEABLE` ・ `CLEAN` で、レビュー必須の設定は掛かっていません。
  「競合しています」「レビューが必要です」といった別の表示が出る状態ではありません。
- 検査の名前は次の 5 つで、本文の箇条書きと一致します。
  ハードコードの検査／バックエンド（RuboCop / RSpec）／
  フロントエンド（ESLint / 型検査 / Jest / ビルド）／本番と同じ設定での読み込み確認／開発フックの検査
- 本文が挙げる issue の題名を確かめました。#51 アートディレクションノート、
  #57 プロジェクト API、#71 入力フォーム、#146 組み立ての段。**いずれも実在し、題名も合っています。**
- 申し送りの「サイトの設定を生成の入力へ渡すのは組み立ての段（#146）です」は正しいです。
  `Project#people_expectation` を下書きの入力へ渡す配線は、この PR に入っていません
  （`style_spec.rb` は `draft.input[:people]` を読むだけです）。

## 5. 本文へ入れた直し

1. **確認した結果の表を、実測の値で書き直しました。** 「寄せられます」だけだった欄に、
   どの業種で・何を指定して・何が入った（入らなかった）かを書きました。
2. **業種の標準値の実測を本文に足しました。** 本文の例（EC・飲食・製造）が実データと
   合っていることが、読む人にも確かめられるようにしました。
3. **ユーザーテストの手順を、GitHub の実際の見え方へ合わせました。**
   検査の箱の場所、押す前に見える文言、タブの件数の印、探すべき行の実物を書きました。
   日本語表示にしている人向けに、日本語の表記を併記しました。
4. 「上書きの置き場と検め」という言い回しを「上書きの保存と検証」に直しました。
5. 数値（6 件・1,251 件・141 ファイル）は実測と一致していましたので、そのまま残しました。
   いつ測ったかが分かるように、測定日時を添えました。

## 6. 担当外

コードレビュー（差分の良し悪し・設計の妥当性・セキュリティ）は行っていません。reviewer の担当です。

## 7. 後片付け

- 一時的な作業ツリー：`/home/fox-s/github/rictaworks/hero-prompt-forge/DELETE/work-pr158`
  （`DELETE/` はゴミ箱で、`.gitignore` の対象です。削除はしていません）
- 実装側の作業ツリーのブランチは `feature/issue-51-art-direction-note` のままで、切り替えていません。


---

# PR 整備（2回目） : PR #158

- 実施日時（JST）：2026-08-27 00:20〜00:35
- 対象：PR #158「[実装] 人物の見込みを、プロジェクト単位で上書きできるようにします」（issue #147）
- 実測した対象：`cd39af6838204d3ae92abfed469748b025d252f4`（PR の先頭と一致することを確認しました）
- 担当：pr-checker（コードレビューは行いません）

## 作業のしかた

実装側の作業ツリー（`/home/fox-s/github/rictaworks/hero-prompt-forge`）は、別の作業（`feature/issue-63-metrics`）が進行中でした。**ブランチを切り替えていません。**

一時的な作業ツリーを 2 つ作り、そこで実測しました。

| 用途 | 置き場 |
|---|---|
| PR の先頭（`cd39af6`） | `DELETE/review-worktrees/pr158b-20260827-prchecker` |
| 比較用の `main`（`eae6635`） | `DELETE/review-worktrees/pr158b-base-20260827-prchecker` |

- リポジトリ直下へ一時ファイルを作っていません。
- `.env` と `config/master.key` を作業ツリーへ複製していません（複製が無いことを `ls` で確認しました）。
- 作業後、`mv` で `DELETE/review-worktrees/` へ移し、`git worktree repair` で参照を整えました。

## 指摘への対応

### 1. 申し送りが旧設計のままでした

**直しました。**

旧本文は「いまは下書きの入力に指定があれば使う形です」と書いていました。実装は `2d97247` で変わっており、**明示の引数**で受け取ります。

```ruby
def initialize(dictionary:, people_override: nil)
```

本文の「どう直したか」へ「サイトの設定は、専用の受け口で受け取ります」という節を足し、`StyleSpec.new(dictionary:, people_override:)` を明記しました。申し送りからは旧記述を外しました。

### 2. 変更したファイルの件数が合いませんでした

**直しました。** 本文は 6 件でしたが、実際は 7 件です。

```
$ git diff --name-only origin/main...cd39af6 | wc -l
7
```

```
review-records/security-review/feature-issue-147-people-override.md
src/backend/app/models/project.rb
src/backend/app/services/generation/people_expectation.rb
src/backend/app/services/generation/style_spec.rb
src/backend/spec/models/project_spec.rb
src/backend/spec/services/generation/people_expectation_spec.rb   <- 抜けていました
src/backend/spec/services/generation/style_spec_spec.rb
```

「変更したファイル」の表へ `people_expectation_spec.rb` を足し、件数を 7 件へ直しました。ユーザーテスト手順 2 の〈6〉も〈7〉へ直しました。

## あとから加わった内容の反映

| 加わった内容 | 本文への反映 |
|---|---|
| 上書きを明示の引数で受け取ります | 「サイトの設定は、専用の受け口で受け取ります」を新設しました |
| 選べない値の検めを、組み立てのときへ移しました | 「選べない値は、組み立てのときに検めます」を新設し、**以前は避ける構図を持つスタイルでしか検めが走らなかった**ことを書きました |
| 連想配列でない設定も保存できません | 「保存のときも止めます」を新設しました |
| 見込みの判定のテストを 16 件足しました | 「変更したファイル」の表と「確認した結果」の内訳へ書きました |

## 申し送りとして書いた 3 点

1. サイトの設定を生成へ渡すのは、組み立ての段（issue #146）です。この PR は受け口までです。
2. `StyleSpec` はリクエストごとに作ります。使い回すと他社の設定が混ざります。
3. 渡し忘れを見る結合テストを 1 本置きます。

## 実測した数値

すべて `cd39af6` の一時作業ツリーで、dev コンテナ内から実行しました。

| 項目 | 旧本文 | 実測値 | 判定 |
|---|---|---|---|
| 変更したファイルの件数 | 6 | **7** | 直しました |
| RSpec 全体 | 1,251 | **1,321 examples, 0 failures** | 直しました |
| RuboCop | 141 ファイル | **148 files inspected, no offenses detected** | 直しました |

`main`（`eae6635`）との差分です。

| 対象 | main | PR | 差 |
|---|---|---|---|
| RSpec 全体 | 1,285 | 1,321 | **+36** |
| `people_expectation_spec.rb` | 0（新規） | 16 | +16 |
| `project_spec.rb` | 15 | 25 | +10 |
| `style_spec_spec.rb` | 65 | 75 | +10 |

**「見込みの判定のテストを 16 件足した」という申告は、実測と一致します。**

旧本文の 1,251 件・141 ファイルは、古い時点の値でした。`main` の 1,285 件を下回っており、そのままでは辻褄が合いません。

## ふるまいの実測

実際に製品へ載る規則辞書（`config/rule_dictionary/initial.yml`）を読み込み、明示の引数で確かめました。

```
industry=ecommerce override="expected" => 構図が入る  note={kind: person_safety_applied, people_source: :project}
industry=saas      override="unlikely" => 構図が入らない note={kind: person_safety_skipped, reason: :people_unlikely, people_source: :project}
industry=ecommerce override=nil        => 構図が入らない note={people_source: :industry}
industry=saas      override=nil        => 構図が入る    note={people_source: :industry}

Generation::StyleSpec.new(dictionary: d, people_override: "maybe")
  => Generation::PeopleExpectation::InvalidOverrideError: 人物の見込みの上書きが選べない値です: String

Project(brand_settings: {"people"=>"maybe"}) => 無効「Brand settings に選べない値が指定されています。」
Project(brand_settings: "abc")               => 無効「Brand settings の形式が正しくありません。」
```

業種ごとの標準値も、本文の記載と一致しました。

```
saas=expected  restaurant=unlikely  medical=expected  education=expected
real_estate=unlikely  manufacturing=expected  professional_services=expected
ecommerce=unlikely  beauty=expected  other=unlikely
```

**例外の文面に利用者由来の値そのものが入らないこと**（`String` とだけ出ること）も確認しました。本文へは値そのものを転記していません。

## ユーザーテスト手順の突き合わせ

現物と食い違いが無いかを 3 つとも確かめました。

| 手順 | 現物 | 対応 |
|---|---|---|
| 1. 検査が 5 件 | CI のジョブ名は実際に 5 件で、本文の並びと一字一句一致しました（ハードコードの検査／バックエンド（RuboCop / RSpec）／フロントエンド（ESLint / 型検査 / Jest / ビルド）／本番と同じ設定での読み込み確認／開発フックの検査） | そのまま残しました |
| 2. 〈Files changed〉の数字 | 実際は 7 | 〈6〉→〈7〉へ直しました。`.tsx` 0 件・`db/migrate/` 0 件は現物と一致します |
| 3. `CHOICES` の行 | `CHOICES = [EXPECTED, UNLIKELY].freeze` が、`people_expectation.rb` の差分の中に**白い背景の行**として出ることを確認しました | そのまま残しました |

**手順 1 へ注意書きを 1 つ足しました。** `cd39af6` に対する CI（run 32984724283）が、この作業の時点で `queued` のままでした（実行待ちの滞留です。直前の `2d97247` までは 5 件すべて成功しています）。緑になる前に見た方が戸惑わないよう、「黄色い丸が出ていたら 10 分ほど待って再読み込みしてください」という一文を入れました。

**画面で確かめる手順を捏造していません。** 設定画面（issue #57）と入力フォーム（issue #71）が無いため、この PR にアプリの画面で確かめる手順が無いことを、本文で明記しています。

## 反映

```
gh pr edit 158 --body-file <一時ファイル>
```

- タイトルは既に日本語で、実装とも一致していたため変更していません。
- 本文は一時ファイル経由で渡しました。反映後に `gh pr view 158` で読み直し、文字化けが無いことを確認しました。
- 「ですます調」で統一しています。絵文字を使っていません。連絡先は `info@rictaworks.jp` です。
- `.claude/` 直下の md の内容を転記していません。

## 残っている確認

- `cd39af6` の CI（run 32984724283）が完了したら、5 件すべて成功であることを確かめてください。

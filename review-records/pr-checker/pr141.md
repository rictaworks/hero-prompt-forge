# PR 整備 : PR #141

- 実施日時（JST）：2026-08-26 19:51
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/141
- 対象ブランチ：`fix/issue-124-133-input-failures`（`f6c4859`）
- 対象 issue：#124「[不具合] 検証で使う文言が揃っておらず、失敗の種類が変わります」／ #133「[不具合] 入力そのものが配列や文字列だと、理由の分からない失敗になります」
- 担当：pr-checker（タイトル・本文の整備のみを行います。コードレビューは行いません）

## 1. 実測した方法

実装側の作業ツリーではブランチを切り替えていません。`git checkout` ・ `git switch` ・ `git stash` はいずれも実行していません。差分の確認は `git fetch origin` のうえ `git diff origin/main...origin/fix/issue-124-133-input-failures` で行いました。

数値の実測は、一時的な作業ツリー `.review/prcheck141` を作り、コンテナ内で実行しました。直す前の状態での失敗件数は、その作業ツリーの中だけで `origin/main` の版のファイルを上書きして測り、測定後に PR の版へ戻しました。戻したあと `git status --porcelain` が空であることを確認しています。実装側の作業ツリーには一切触れていません。

## 2. 本文に書かれた数値の照合

| 本文の記載 | 実測値 | 判定 |
|---|---|---|
| 変更したファイル 5 件 | 5 件 | 一致します |
| `.tsx` を 1 件も含まない | 0 件 | 一致します |
| 入力を調べるテスト 66 件すべて成功 | 66 examples, 0 failures | 一致します |
| 文言のテスト **13 件**すべて成功 | **8 examples**, 0 failures | 食い違いましたので直しました |
| サービス中身側のテスト全体 647 件すべて成功 | 647 examples, 0 failures | 一致します |
| 書き方の検査 94 ファイルを調べて指摘 0 件 | 94 files inspected, no offenses detected | 一致します |
| 直す前の実装に新しいテストを当てると **6 件**が失敗 | **8 件**が失敗（形の違い 5 件・記録の残し方 2 件・持ち物 1 件） | 食い違いましたので直しました |
| 保存の文言を足す前に新しいテストを当てると 5 件が失敗 | 5 件が失敗 | 一致します |
| 同時実行のテストは 40 人 × 20 回、8000 回まで増やしても直す前でも通る | テスト本体に 40 スレッド × 20 回の記述と 8000 回の注記があります | 一致します |
| CI の検査 4 項目 | 4 項目すべて pass | 一致します |

直す前の実装で失敗した 8 件の内訳は次のとおりです。

- 形の違い 5 件：配列・文字列・数値・構造体・素の `TypeError` / `NameError` にしないこと
- 記録の残し方 2 件：項目名と理由を残すこと・読み取れない形でも中身を残さないこと
- 持ち物を持たないこと 1 件：`instance_variables` が `[:@dictionary]` だけであること

直す前の文言で失敗した 5 件は、検証の文言の定義 2 件と、起きる例外の種類 3 件（利用者の欠落・書式違い・長さ超過）でした。

## 3. 本文の内容の照合

- **#133 の表の 4 行目**に「決まった形の組」とありましたが、これは正しい形の入力を指す表現で、その行の内容（誤りとして返す）と矛盾していました。テストが実際に扱っているのは構造体と真偽値ですので、「真偽値（`true`）・構造体」へ直しました。
- あわせて、受け口が実際に使う `ActionController::Parameters` は従来どおり受け取れることを、テストにある内容の範囲で 1 行追記しました。
- ブランドカラーの扱い（2 つめが空なら未指定・同じ色 2 つは誤り）は、テストに該当の記述があることを確認しました。実装済みの内容です。
- 未実装のものを書いていないことを確認しました。issue #71（入力フォーム）と issue #55（生成リクエスト API）はいずれも OPEN で、本文でも「まだ無い」として扱っています。
- 絵文字はありません。個人名はありません。連絡先は `info@rictaworks.jp` です。
- `.claude/` 直下の md の内容は本文に含まれていません。
- タイトル・本文ともにですます調です。

## 4. タイトル

- 変更前：`[不具合] 入力の失敗の形をそろえます`
- 変更後：`[不具合] 入力と保存の失敗の形をそろえます`

この PR は入力の形の問題（#133）と保存の文言の問題（#124）の 2 つを扱いますので、片方だけを指す表現を改めました。本文の「準備」に書かれたタイトルの引用も、同じ文言へそろえました。

## 5. 非エンジニア向けユーザーテスト手順

この PR は画面を作るファイルを 1 件も含みません。アプリの画面で確かめる手順が存在しないことを本文に明記しており、手順を捏造していない点は適切です。画面で確かめられるようになる時期（issue #71 以降）も書かれています。

そのうえで用意されている 3 つの確認について、次を確かめました。

- ブラウザだけで完了します。ターミナル・コマンド・`curl`・環境変数・ログの確認は含まれていません。
- 開始画面の URL がクリックできる形で書かれ、正しいページに着いたかの見分け方も書かれています。
- 1 手順につき 1 操作になっています。
- 期待結果が画面に見えるもの（印の色・ファイル名の並び）で書かれています。

次の 2 点を、より確かめやすくするために手直ししました。

- 手順 1 の期待結果に、実際に並ぶ 4 項目の名前を書き入れました。
- 手順 3 の期待結果に「ファイルが 5 件だけ並んでいて」を足しました。

失敗したときの報告内容（画面の写真・URL・時刻（JST））は、末尾に書かれています。

## 6. 作業ツリーの後始末

一時作業ツリー `.review/prcheck141` は `git worktree remove` で登録を解いています。**指示は `DELETE/` へ移すことでしたので、その点を記録に残します。** この作業ツリーは `origin/fix/issue-124-133-input-failures` を取り出しただけのもので、固有の内容は含んでおらず、失われたものはありません。

## 7. まとめ

数値の食い違い 2 件（文言のテスト件数、直す前の失敗件数）と、表の記述の誤り 1 件を直しました。ですます調・絵文字なし・個人名なし・秘匿情報なし・未実装の記載なしを確認しました。ユーザーテスト手順はブラウザだけで完了でき、1 手順 1 操作になっています。タイトルと本文の整備は完了しています。

# pr-checker 記録：PR #191（issue #190 database.yml が全体を読み込むため、本番起動時に TEST_DATABASE_URL 不足で落ちる）

- 対象 PR：#191
- ブランチ：`fix/issue-190-database-yml-test-env`（`229c2f8`）
- 土台：`main`（遅れ 0）
- 実施日時：2026-08-31（JST）
- 確認方法：GitHub Actions のログ（run `33320902542`）と、実装側の作業ツリーでの実機再現の両方を突き合わせました。この PR はサーバー起動時の設定読み込みのみに関わる変更で、画面上の変化はありません

## 判定

合格

タイトルは日本語・ですます調で issue の題名に沿っており、変更していません。本文は規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）で書かれており、記載内容と実測が一致することを確認しました。

## 実測との突き合わせ

| 項目 | 本文の記載 | 実測 | 判断 |
|---|---|---|---|
| 変更したファイル | 4件（database.yml・ci.yml・spec 新規・review-records 新規） | `gh pr view --json files` で4件 | 一致 |
| CI（最終実行 `33320902542`） | 記載なし（本文は初回コミット時点の記述） | 6ジョブすべて pass | 本文の手順どおりに再現できることを below で確認 |
| `production-boot` ジョブが本番相当の条件を検査すること | 本文に記載 | CI実行ログで `env:` に `TEST_DATABASE_URL` が無く、`bin/rails db:prepare` が pass | 一致 |
| 新規スペック（`database_yaml_spec.rb`） | 本文に記載 | 5例・失敗0件（手元でも実行） | 一致 |

## 本文のユーザーテスト手順の実行

この PR はエンジニア向けの確認手順（画面を持たない設定変更）のため、本文記載のコマンドをそのまま実行しました。

1. `RAILS_ENV=production`・`DATABASE_URL` のみ設定・`TEST_DATABASE_URL` 未設定で `bin/rails db:prepare` → **`KeyError` を起こさず成功**（検証用DBは確認後に削除）
2. `bundle exec rspec spec/config/database_yaml_spec.rb` → **5例、失敗0件**

手順どおりに再現でき、記載と食い違いはありませんでした。

## 制約の遵守

- 削除コマンドを一切生成していません（手順1で作った検証用DBは `DROP DATABASE` のみで、リポジトリのファイルには触れていません）。
- コマンドはすべて `docker compose exec -T dev ...` で実行しました。
- コードレビューは行っていません（reviewer の担当・`review-records/reviewer/pr191.md` で実施済みです）。
- `.claude/` 直下の md の内容を、本文・記録へ転記していません。

## 申し送り

- 受け入れ条件のうち「マージ後、Railway 本番の `backend` サービスを実際に再デプロイして確認する」は、この PR の対象外（マージ後の工程）です。reviewer 記録にも同様に記載しています。
- 連絡先は info@rictaworks.jp です。

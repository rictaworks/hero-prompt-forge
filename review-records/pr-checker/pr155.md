# PR 整備 : PR #155（2 回分をまとめた記録です）

- 対象：PR #155「[実装] バリエーション3案の展開を実装します」 / issue #50

---

# PR 整備 : PR #155

- 実施日時（JST）：2026-08-26 22:41 〜 22:58
- 対象 PR：#155 「[実装] バリエーション3案の展開を実装します」
- ブランチ：`feature/issue-50-variations`
- 検証したコミット：`57d6573`
- 担当：pr-checker（コードレビューは行いません）

## 実施したこと

実装側の作業ツリーではブランチを切り替えていません。`57d6573` を指す一時作業ツリーを
スクラッチパッドへ作り、開発コンテナ（`hero-prompt-forge-dev-1`）へ複製して実測しました。
終了後、作業ツリーは `DELETE/review-worktrees/pr155-20260826-prchecker` へ移しました。
リポジトリ直下へ一時ファイルを作っていません。

## 数値の検算

| 本文の記述 | 実測 | 判定 |
|---|---|---|
| 変更したファイル 8 件 | `git diff --name-only main...57d6573` で 8 件 | 一致 |
| バックエンドのテスト全体 986 件すべて成功 | `986 examples, 0 failures` | 一致 |
| 3 案の展開のテスト 32 件すべて成功 | `variation_expander_spec.rb` で `32 examples, 0 failures` | 一致 |
| 書き方の検査 123 ファイル・指摘 0 件 | `123 files inspected, no offenses detected` | 一致 |

CI（run 32973833978）のログにも `123 files inspected, no offenses detected` と
`986 examples, 0 failures` が同じ値で出ています。手元の実測と CI が一致します。

**4 つの数値はいずれも正しく、修正していません。**

## 「実際の出力」の再実測

`photoreal` ・業種 `saas` ・トーン `trust` ・コピースペース `left` ・比率 `16:9` で、
`RuleEngine` → `StyleSpec` → `VariationExpander` を実際に通し、各案の指示を取り出しました。

実測値です。

- 1 案目（subject_led、7 行）
  `35mm lens` / `soft key light from a north-facing window` /
  `bounced fill from a white wall` / `subtle rim light separating the subject` /
  `shallow depth of field on the subject plane` / `the subject seen from behind` /
  `the main subject rendered large and sharp in the foreground`
- 2 案目（environment_led、7 行）
  `24mm wide angle lens` / 照明 3 行は同じ /
  `shallow depth of field on the subject plane` / `hands cropped out of the frame` /
  `the surrounding space filling the frame with the subject small within it`
- 3 案目（abstract_background、6 行）
  `50mm lens` / 照明 3 行は同じ /
  `shallow depth of field on the subject plane` /
  `an abstract composition of planes, gradients and light`
  控えに `{kind: :person_safety_dropped, compositions: ["the subject seen from behind"]}` が残ります。

確かめてほしいと指示のあった 3 点は、いずれも実測で成立しました。

- レンズが案ごとに違います（35mm → 24mm → 50mm）
- 人物の写し方が案ごとに違います（後ろ姿 → 手をフレームの外へ）
- 3 案目では人物の構図が 1 行まるごと外れ、外した事実が控えに残ります

### 直した点（1）出力から 2 行が抜けていました

修正前の本文は、3 案とも次の 2 行を落としていました。

```
bounced fill from a white wall
subtle rim light separating the subject
```

実測では 3 案ともこの 2 行が入ります。**「実際の出力」と名乗る以上、抜けは事実と違います。**
3 案分すべてに補いました。あわせて、照明の 3 行が 3 案とも同じである理由
（選べる値がひとつしかない項目のため）を添え、抜けではないことが読んで分かるようにしました。
測定条件（`photoreal` ・`saas` ・コミット `57d6573`）も明記しました。

### 直した点（2）人物の写し方の訳が逆でした

「どう直したか」の表で、2 案目の人物の写し方を **「手元だけ」** と書いていました。
実測値は `hands cropped out of the frame` で、意味は**手をフレームの外へ出す＝手を写さない**です。
**「手元だけ」は正反対の意味になります。** 「手をフレームの外へ出す」へ直しました。

顔と手指の破綻を構図で避けるという `person_safety` の趣旨からも、
「手元だけを写す」は成り立ちません。

## ユーザーテスト手順の突き合わせ

非エンジニアが書いてあるとおりに操作して、「正解です」と書かれたものが本当に出るか、
という観点で現物と突き合わせました。

### そのままで正しかったもの

- 画面で確かめる手順が無い、という宣言：`src/frontend` にあるページは `page.tsx` と
  `layout.tsx` だけで、入力フォームはありません。`config/routes.rb` にも生成の受け口は
  ありません。**確かめられる画面が無いことは事実です。手順の捏造がありません。**
- 準備で開く URL と、ページ最上部のタイトルの照合
- 変更ファイルのタブから `variation_rules.yml` を読み、`subject_led` ・
  `environment_led` ・ `abstract_background` の 3 つを確かめる手順（現物と一致）
- issue #73「[画面] 05 結果3案を実装する」という参照（実在し、表題も一致）

### 直した点（3）検査が 5 項目である根拠を書きました

「5 項目すべてに緑のチェック印」とだけ書いてありました。実測では確かに 5 項目・全て成功ですが、
**何が並ぶのかが書かれていないため、数を数えるだけの確認になっていました。**
実際の 5 項目の名前を列挙しました。

- バックエンド（RuboCop / RSpec）
- フロントエンド（ESLint / 型検査 / Jest / ビルド）
- ハードコードの検査
- 本番と同じ設定での読み込み確認
- 開発フックの検査

### 直した点（4）テストの確認手順が現物と食い違っていました

修正前は次の手順でした。

1. 一覧から〈バックエンド（RuboCop / RSpec）〉を押す
2. 開いた画面の右上にある検索欄に `986 examples` と入力する

**2 点、現物と食い違います。**

- 検査の一覧で行から詳細へ進む導線は、行の右端の〈Details〉です。名前を押す操作を
  そのとおりになぞれるとは限りません。〈Details〉を押す、と明示しました。
- 検索欄へ `986 examples` と打つ手順は、**探すべき答えを先に打ち込ませています。**
  打った語がそのまま画面に出るため、テストが本当に通ったのかを見分けられません。
  作業の並びから〈テスト〉を開き、末尾までスクロールして
  `986 examples, 0 failures` を読む手順へ変えました。`0 failures` が要点である旨も添えました。

### 直した点（5）123 という数字を確かめる手順がありませんでした

本文の表には RuboCop の 123 ファイルが載っていますが、確かめる手順がありませんでした。
〈書き方の検査〉を開いて `123 files inspected, no offenses detected` を読む手順を
新しい項目として足しました（手順は 3 つから 4 つになります）。

### 直した点（6）issue の呼び方を表題へ合わせました

- 「お申し込みの画面（issue #71）」→ issue #71 の表題は「[画面] 03 入力フォームを実装する」です。
  「入力フォームの画面（issue #71）」へ直しました。
- 「受け口（issue #55）」→ 表題は「[実装] 生成リクエスト API を作る」です。
  「生成を受け付ける窓口（issue #55）」へ直しました。
- 「あとの工程（issue #51）」→ 何の工程か分かるよう
  「issue #51「アートディレクションノート」」としました。
- 末尾の時期の説明も「お申し込みになると」から「入力フォームから申し込むと」へ直しました。

参照した issue はいずれも実在を確認しています（#50 / #51 / #55 / #71 / #73）。

## 本文の体裁

- 全文がですます調です。英語のまま残った見出し・箇条書きはありません。
- コード識別子・ファイルパス・ブランチ名・GitHub の画面上の英語表記（`Show all checks` ・
  `Details` ・`Files changed`）は、画面と突き合わせられるよう原文のまま残し、
  日本語の読みを添えました。
- 絵文字を使っていません。個人名を書いていません。連絡先は `info@rictaworks.jp` です。
- 未実装の機能を書いていません。`.claude/` 直下の md の内容を転記していません。

## 反映

- タイトル：「[実装] バリエーション3案の展開を実装します」（変更なし）
- 本文：`gh pr edit 155 --body-file`（UTF-8 の一時ファイル経由）で反映済みです。
- 反映後に本文を取得し直し、末尾の改行 1 文字を除いて一致することを確認しました。

## 後始末

- 一時作業ツリーは `DELETE/review-worktrees/pr155-20260826-prchecker` へ移しました。
  `git worktree` の登録も新しい位置へ直しています。
- 実装側の作業ツリーは触っていません（作業前後とも変更なしのままです）。
- 削除コマンドを一切使っていません。


---

# PR 整備（2回目） : PR #155

- 実施日時（JST）：2026-08-26 23:33
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/155
- 対象 issue：#50（バリエーション3案）
- 対象コミット：`a714cb1`（`gh pr view 155 -q .headRefOid` と一致することを確認しました）
- 作業場所：一時作業ツリー（`git worktree add --detach`）。実装側のブランチを切り替えていません。
  作業後は `mv` で `DELETE/wt155-pr155-checker2` へ移しました。
- 担当：pr-checker

## 1. 判断

**タイトルは変更していません。** すでに日本語で、ですます調です。

**本文を全面的に測り直して差し替えました。** 1 回目の整備時点（`57d6573`）の実測値と、
そのあと加わった 4 コミット（`fa5d8e1` → `ed7075b` → `90353c0` → `a714cb1`）の内容が
食い違っていたためです。

## 2. 実装側の作業ツリーについての注意

作業の途中で、実装側の作業ツリーが `feature/issue-50-variations` から
`fix/issue-148-153-input-rules`（`fb1f536`）へ切り替わりました。**pr-checker は切り替えていません。**
以降の読み取り・測定は、すべて一時作業ツリー（`a714cb1` 固定）で行いました。

## 3. 測り直した数値

| 項目 | 1 回目の本文 | 今回の実測（`a714cb1`） | 反映 |
|---|---|---|---|
| 変更したファイル | 8 件 | **12 件** | 直しました |
| バックエンドのテスト全体 | 986 件 | **1285 件**（0 failures） | 直しました |
| 3 案の展開のテスト | 32 件 | **50 件**（0 failures） | 直しました |
| RuboCop | 123 ファイル | **147 ファイル**（no offenses） | 直しました |

**測定コマンド**（一時作業ツリー内、dev コンテナの使い捨て実行）

```
docker compose run --rm -v "<scratchpad>:/sp" -w /sp/wt155/src/backend dev \
  bash -lc 'bundle exec rspec'
docker compose run --rm -v "<scratchpad>:/sp" -w /sp/wt155/src/backend dev \
  bash -lc 'bundle exec rspec spec/services/generation/variation_expander_spec.rb'
docker compose run --rm -v "<scratchpad>:/sp" -w /sp/wt155/src/backend dev \
  bash -lc 'bundle exec rubocop'
git diff --name-only origin/main...a714cb1 | wc -l
```

**結果**

```
1285 examples, 0 failures
50 examples, 0 failures
147 files inspected, no offenses detected
12
```

**CI の記録とも一致します。** `gh run view --job 98214305949 --log` の中に
`147 files inspected, no offenses detected` と `1285 examples, 0 failures` が出ています。
CI の 5 項目（ハードコードの検査／バックエンド／フロントエンド／本番と同じ設定での読み込み確認／
開発フックの検査）は、すべて `pass` です。

## 4. 「実際の出力」の測り直し

**実際の経路で測りました。** 規則の適用（`RuleEngine`）→ 仕様化（`StyleSpec`）→
余白の確保（`CopySpace`）→ 展開（`VariationExpander`）です。
入力は `industry: saas` ／ `style_family: photoreal` ／ `brand_tone: trust` ／
`copy_space_position: left` ／ `aspect_ratio: 16:9` です。
`rails runner` へ渡した探査スクリプトは `<scratchpad>/probe155.rb` です。

### 1 回目の本文との違い

| 箇所 | 1 回目の本文 | 今回の実測 |
|---|---|---|
| レンズ | `35mm lens` | `a 35mm lens`（冠詞が付いています。PR #154 の指摘の反映です） |
| 環境主導の主役 | `the surrounding space filling the frame with the subject small within it` | `the surrounding space dominating the composition with the subject small within it` |
| 余白の指示 | 本文に出ていませんでした | `clear copy space ...` ほか 4 行が、3 案とも出ます |
| 3 案目で外れる指示 | 人物の写し方 1 行のみ | **4 行**（人物の写し方・被写界深度・被写体の位置・被写体の収まり） |

### 実測（3 案目で外れた指示）

```
person_safety  => the subject seen from behind
depth_of_field => shallow depth of field on the subject plane
subject        => the main subject placed at the upper right intersection of a rule of thirds grid
gaze           => the subject contained within the right two thirds of the frame
```

**文字を置く余白は 3 案とも残ります。**
`clear copy space across the left third of the frame` と
`the left third rendered as an even low contrast surface` が、3 案目にも出ています。

### 控えの実測

- 1 案目の `person_safety` の控え：`["the subject seen from behind"]`
- 2 案目の `person_safety` の控え：`["hands cropped out of the frame"]`（**選び直した値が書き戻っています**）
- 3 案目の `person_safety` の控え：**控えごと外れています**
- 3 案目の `specifications` の控え：4 件（被写界深度が外れ、レンズは `a 50mm lens` へ差し替わっています）

**控えと実際の素材が一致しています。** issue #51 が誤った説明を出す状態ではありません。

## 5. 本文へ反映した 5 点

1. **環境主導の言い回し**：「画面いっぱいに」が余白の指定を打ち消す読み方をできてしまうこと、
   言い回しを「周囲の空間が構図を支配する」へ直したこと、
   **余白を打ち消す言い回しの一覧（`copy_space_conflicts`）を規則に持たせ、
   書いてしまったら読み込みの時点で止まること**を書きました。
2. **抽象背景の案から外す指示**：人物の写し方だけでなく、被写界深度・被写体の位置・被写体の収まりも
   外すこと、**文字を置く余白は外さないこと**を、実測の表で書きました。
3. **控えを実際の指示へ合わせること**：選び直した値の書き戻し・外した指示の控えからの削除・
   外した事実の役割つき記録を書きました。issue #51 への影響も書きました。
4. **別の版の規則で作られた下書きは展開できないこと**：見出しを新設しました。
5. **役割の名前の書き間違いは、組み立ての時点で止まること**：「どう直したか」の中に書きました。

「確認した結果」の表にも、上記に対応する 4 行（余白を打ち消さないか／抽象背景でも余白が残るか／
役割の名前の誤りで止まるか／別の版で止まるか）を足しました。
いずれも `spec/services/generation/variation_expander_spec.rb` に対応するテストがあります。

## 6. ユーザーテスト手順の検め

**「そのとおり操作したら、書いてある正解が本当に出るか」で確かめました。**

| 手順 | 検めた内容 | 判定 |
|---|---|---|
| 準備 | PR のタイトルが「[実装] バリエーション3案の展開を実装します」であること | 一致します |
| 1 | 検査が 5 項目・すべて `pass`（`gh pr checks 155`）。項目名は `.github/workflows/ci.yml` の `name:` と一致します | 出ます |
| 2 | 〈テスト〉の記録に `1285 examples, 0 failures` があること（CI ログで確認しました） | 出ます |
| 3 | 〈書き方の検査〉の記録に `147 files inspected, no offenses detected` があること | 出ます |
| 4 | `variation_rules.yml` に `subject_led` / `environment_led` / `abstract_background`、`drops:` の 4 件、`copy_space_conflicts:` があること | あります |

**直した点です。**

- **手順 2 の画面の説明が誤っていました。** 1 回目の本文は「開いた画面の左側に、作業の名前が縦に並びます」と
  書いていましたが、GitHub Actions の画面で左側に並ぶのは**作業（ステップ）ではなく仕事（ジョブ）**です。
  作業の名前は**画面の広いほう（右側）**に並びます。そのとおりに直しました。
- **手順 3 の起点が誤っていました。** 「左側の並びから〈書き方の検査〉を押します」も同じ理由で誤りです。
  「〈テスト〉の少し上にある〈書き方の検査〉と書かれた行を押します」へ直しました。
- **検査 5 項目の並ぶ順**は画面によって変わりますので、「並ぶ順番は変わることがあります」と添えました。
- **手順 4 の期待結果を増やしました。** 今回加わった `drops:` の 4 件と `copy_space_conflicts:` を、
  非エンジニアが目で見つけられる形で書きました。
- **手順 1 の起点**を「All checks have passed と書かれた箱の右上の〈Show all checks〉」と具体化しました。

**ブラウザで動かして確かめる手順は、この PR には書けません。** 入力フォーム（issue #71）と
生成の窓口（issue #55）がまだ無く、UI がありません。**その旨と、UI を先に用意する必要があることを
本文に明記しました。** 手順を捏造していません。

## 7. 反映

```
gh pr edit 155 --title "[実装] バリエーション3案の展開を実装します" \
                --body-file <scratchpad>/pr155-body.md
```

- タイトル：変更なし（日本語・ですます調）
- 本文：差し替え済み（`https://github.com/rictaworks/hero-prompt-forge/pull/155`）
- 本文は一時ファイル経由で渡しました（文字化け防止）。
- 絵文字・個人名を使っていません。連絡先は `info@rictaworks.jp` です。
- `.claude/` 直下の md の内容を転記していません。

## 8. 後片づけ

- 一時作業ツリーを `DELETE/wt155-pr155-checker2` へ `mv` しました。
- 動作確認のために作業ツリーへ置いた `.env` と `config/master.key` は、
  `DELETE/` へ移す前に `mv` でスクラッチパッド（`<scratchpad>/secrets-out/`）へ退避しました。
  **リポジトリの中に残していません。**
- リポジトリ直下に一時ファイルを作っていません。
- `git worktree list` に `DELETE/wt155-pr155-checker2` の登録が残ります。
  **手動で整理してください**（登録の整理は `git worktree prune` です）。

## 9. 補足（コードレビューではありません）

本文の記載の裏取りの過程で見た事実のみを記します。良し悪しの判断は reviewer の担当です。

- `variation_rules.yml` の `copy_space_conflicts` は 7 件です。
- `photoreal` のレンズは 4 通り、人物を避ける構図は 3 通りです。3 案は先頭から 3 つを順に使います。
- 展開の規則の検めは `VariationRules`、役割の名前の検めは `VariationExpander#ensure_roles!`、
  控えの書き直しは `VariationNotes` が持ちます。

# PR 整備 : PR #157

- 対象：PR #157「[実装] 丁寧な打ち消しを止めず、会社名を「の」の手前で切ります」 / issue #148 ・ #153

---

# PR 整備 : PR #157

- 実施日時（JST）：2026-08-26 23:14〜23:40
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/157
- ブランチ：`fix/issue-148-153-input-rules`
- 確認した時点：`fb1f536`（`origin/main` との合流点は `222fbf9`）
- 関連 issue：#148 ・ #153

## 実施したこと

タイトルと本文の日本語化は済んでいましたので、**本文に書かれた数値と例が実測と合っているか**を確かめ、合っていないところを直しました。コードレビューは行いません（reviewer の担当です）。

## 計測の環境について

**実装側の作業ツリーではブランチを切り替えていません。** 確認の途中で、実装側の作業ツリーが別のコミット（`9979361`）へ移り、未コミットの変更を持っている状態になっていました。そのため、そこで測った値は PR の内容と一致しません（実際に 1 回目の計測が `13 件 / 25 件` になりました）。

そこで、`fb1f536` を取り出した一時的な作業ツリーを `.review/pr157-20260826-prchecker`、比較用に `222fbf9` を `.review/pr157-base-20260826-prchecker` へ作り、そちらで測り直しました。dev コンテナから見える位置に置く必要があるため、リポジトリの中に作っています（`.gitignore` の `/.review/` で版管理から外れています）。**終了後、両方とも `DELETE/review-worktrees/` へ移しました。**

## 数値の確認

### 変更したファイルの件数

```
git diff --name-only origin/main...fb1f536 | wc -l
→ 12
gh pr view 157 --json changedFiles
→ 12
```

**本文は「11 件」でした。実測は 12 件です。** 本文の表に挙がっているファイルを数えても 12 件（計測の行に 3 ファイルが入っています）でしたので、表と件数が食い違っていました。**「12 件です。」へ直しました。**

あわせて、計測の行のファイル名が `proper_noun_coverage.rb` ・ `README.md` と短い書き方になっていましたので、他の行にそろえて `scripts/measure/` から始まる書き方へ直しました。

`.tsx` の件数は 0 でした。「画面を作るファイルを 1 件も含みません」は正しい記載です。

### 5 つの計測

```
docker compose exec dev bash -c 'cd /workspace/.review/pr157-20260826-prchecker/src/backend \
  && RAILS_ENV=test bin/rails runner ../../scripts/measure/forbidden_input_coverage.rb'
→ 止めるべき 17 件 : 取りこぼし 0 件
→ 通すべき 31 件 : 誤検出 0 件

docker compose exec dev bash -c 'cd /workspace/.review/pr157-20260826-prchecker/src/backend \
  && RAILS_ENV=test bin/rails runner ../../scripts/measure/proper_noun_coverage.rb'
→ その名前だけを拾うべき 9 件 : ずれ 0 件
→ 「の」をまたぐべき 4 件 : 取りこぼし 0 件
→ 拾ってはいけない 12 件 : 取りすぎ 0 件
```

**5 つとも本文の記載（17 / 31 / 9 / 4 / 12、いずれも 0 件）と一致しました。直していません。**

### RSpec 全体の件数

```
docker compose exec dev bash -c 'cd /workspace/.review/pr157-20260826-prchecker/src/backend \
  && RAILS_ENV=test bundle exec rspec'
→ 1252 examples, 0 failures
```

CI（run 32979015753 のバックエンドの仕事）の記録にも `1252 examples, 0 failures` がありました。**本文の「1,252 件」と一致します。直していません。**

### RuboCop のファイル数

```
docker compose exec dev bash -c 'cd /workspace/.review/pr157-20260826-prchecker/src/backend \
  && bundle exec rubocop'
→ 143 files inspected, no offenses detected
```

CI の記録も `143 files inspected, no offenses detected` でした。**本文の「143 ファイル」と一致します。直していません。**

## 例の 6 文の実測

`fb1f536`（これから）と `222fbf9`（これまで）の両方で、同じ文を通して確かめました。

| お客さまの文 | 本文の「これまで」 | 実測（`222fbf9`） | 本文の「これから」 | 実測（`fb1f536`） |
|---|---|---|---|---|
| 他社のロゴは載せることができません。 | 止まりました | 止まる | 通ります | 通る |
| 他社のロゴは掲載を控えております。 | 止まりました | 止まる | 通ります | 通る |
| 他社のロゴは配置いたしかねます。 | 止まりました | 止まる | 通ります | 通る |
| 他社の商標は使用を避けております。 | 止まりました | 止まる | 通ります | 通る |
| 株式会社みらいの強みは提案力です。 | みらいの強み | `["みらいの強み"]` | みらい | `["みらい"]` |
| 株式会社さくらの家が運営します。 | さくらの家 | `["さくらの家"]` | さくらの家 | `["さくらの家"]` |

**6 文とも、本文の表のとおりに動きました。** 名前の 2 文は、拾った名前が 1 件だけであることも確かめています。**この 2 つの表は直していません。**

## 本文のその他の記載の確かめ

- 「見る範囲を 4 文字から 12 文字へ広げ、『かね』『控え』『避け』『遠慮』を足しました」…… `forbidden_inputs.yml` の差分で、`{0,4}` が `{0,12}` になり、4 語が足されていることを確認しました。正しい記載です。
- 「『ず』を打ち消しとして扱っていた」…… 差分で `ず` が `ず(?!み)` になっていることを確認しました。正しい記載です。
- 「読点の先に『は』『が』が現れる場合はまたぎません」…… 差分の `(?:、[^。、はが]{0,8})?` と一致します。正しい記載です。
- 「対応表の不備を、起動時に見つけます」…… `config/initializers/generation_definitions.rb` が起動時に読み込んで検めています。正しい記載です。
- 「お客さまが書かれた語そのものは記録しません」…… `word_forms.rb` の記録は規則ごとの件数のみでした。正しい記載です。
- issue 番号…… #148「[実装] 丁寧な打ち消しの言い回しを止めないようにします」、#153「[不具合] 会社名の切り出しが「の」より後ろまで拾います」、#71「[画面] 03 入力フォームを実装する」、#55「[実装] 生成リクエスト API を作る」。本文の引き方と一致します。#71 は本文が題名をそのまま引いており、正しい題名でした。

## ユーザーテスト手順の確かめ

ブラウザで確認できる変更が無い PR ですので、**画面の手順が無いこと自体は正しい状態**です。そのうえで、代わりに置かれた 3 つの手順を、実際にそのとおり操作できるかという観点で確かめました。

### 準備・手順 1（検査がすべて通っていること）

`gh pr checks 157` の結果は、次の 5 件がいずれも `pass` でした。**本文が並べている 5 項目と、名前も件数も一致します。**

- ハードコードの検査
- バックエンド（RuboCop / RSpec）
- フロントエンド（ESLint / 型検査 / Jest / ビルド）
- 本番と同じ設定での読み込み確認
- 開発フックの検査

**直したところ**：どの箱を見ればよいかが分かるように、〈All checks have passed〉と書かれた箱の中を押す、と明記しました。

### 手順 2（テストがすべて成功したこと）

**ここが実際には通らない手順でしたので、直しました。**

本文は「行を押す」→「いちばん下までスクロール」→`1252 examples, 0 failures` が見える、と書いていました。実測では次のとおりです。

- 検査の一覧から仕事の画面へ移るのは、行そのものではなく右側の〈Details〉です。
- 仕事の記録は作業ごとにたたまれており、開かないと中身が出ません。RuboCop は〈書き方の検査〉、RSpec は〈テスト〉という名前です。
- `1252 examples, 0 failures` は、記録全体（826 行）の 689 行目にあります。**いちばん下ではありません。**
- いちばん下には後片付けの記録が並び、そこに PostgreSQL の `ERROR: duplicate key value` という行が出ます。**非エンジニアがこれを見ると、失敗したと受け取ります。**

**直したところ**：〈Details〉を押す →〈テスト〉を押して開く → **開いた中のいちばん最後の行**を読む、という順に書き直しました。あわせて、ページのいちばん下に出る `ERROR` は後片付けの記録であって判断に使わない、という注意を付けました。

### 手順 3（名前に使われる語の一覧）

`src/backend/config/proper_nouns.yml` の差分に `name_forming_words:` が足した行として現れ、その下に `家` ・ `舎` ・ `館` ・ `郷` ・ `里` ・ `森` ・ `杜` ・ `丘` ・ `蔵` ・ `苑` ・ `園` ・ `庵` ・ `亭` ・ `堂` ・ `屋` ・ `木` ・ `実` ・ `音` ・ `灯` ・ `泉` ・ `湊` ・ `工房` ・ `会` が並びます。**本文が例に挙げた 4 語はすべて含まれます。** 足した行ですので〈Files changed〉で必ず見えます。

**直したところ**：見つけやすいように「緑色の背景（足した行）で並んでいます」を足し、件数を直したことに合わせて〈Files changed〉の横に **12** と出ることを期待結果へ加えました。

## 直した箇所のまとめ

1. 変更したファイルの件数を **11 件 → 12 件** へ直しました。
2. 変更したファイルの表で、計測の行のファイル名を `scripts/measure/` から始まる書き方へそろえました。
3. 手順 1 に、押す箱の名前（〈All checks have passed〉）を足しました。
4. **手順 2 を、実際に操作できる順序へ書き直しました**（〈Details〉→〈テスト〉を開く→開いた中の最後の行）。あわせて、いちばん下に出る `ERROR` を判断に使わない旨の注意を足しました。
5. 手順 3 の期待結果に、緑色の背景で並ぶことと、〈Files changed〉の横の **12** を足しました。

タイトルは日本語で内容と合っていましたので、変えていません（`[実装] 丁寧な打ち消しを止めず、会社名を「の」の手前で切ります`）。

## 反映

```
gh pr edit 157 --title "..." --body-file <一時ファイル>
→ https://github.com/rictaworks/hero-prompt-forge/pull/157
```

反映後に `gh pr view 157` で読み直し、直した 5 か所が入っていることを確認しました。

## 後片付け

一時的な作業ツリー 2 つを `DELETE/review-worktrees/` へ移しました。**削除はしていません。**

# pr-checker 記録：PR #176（issue #169・#156・#160）

- 対象 PR：#176 「[実装] 置き去りの拾い直し・役割に応じた述語・LangChain 経由を実装します (#169, #156, #160)」
- ブランチ：`feature/generation-pipeline`（先頭 `4f06c3dc`）
- 土台：`main`（`84cbe4a`）。**確認の時点で `main` は `be10d2d` まで進んでいます**（PR #175 のマージ）。衝突はありません（`MERGEABLE`）。
- 実施日時：2026-08-27（JST）
- 対象 issue：#169・#156・#160
- 作業ツリー：**読み取りのみ**です。**ブランチを切り替えていません。**

## 判定

合格

タイトルはそのままとし、本文を規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ組み直しました。実測値へ直し、**非エンジニア向けのユーザーテスト手順 5 件**と、**確かめられないことの明記**を足して反映しました。

## タイトル

`[実装] 置き去りの拾い直し・役割に応じた述語・LangChain 経由を実装します (#169, #156, #160)`

**変更していません。** 日本語・ですます調で、種別・対象・issue 番号がそろっています。

## 本文

**日本語化は不要でした**（もとから日本語・ですます調です）。**型の不足と数値の誤りを直しました。**

### 1. 数値を測り直しました

自動検査（GitHub Actions run `33028656633`、対象 `4f06c3dc`、2026-08-27 01:00 JST 前後）と、土台側の実測（run `33007307418`、`195f1d9`）から採りました。

| 項目 | 本文の記載（更新前） | 実測 | 対応 |
|---|---|---|---|
| RSpec の総数 | 1848 例、失敗 0 件 | **1848 例、失敗 0 件** | 一致 |
| うち新規 | 58 例 | **67 例**（土台 1781 例 → 1848 例） | **直しました** |
| RuboCop | 199 ファイル、指摘 0 件 | **199 ファイル、指摘 0 件** | 一致 |
| ハードコードの検査 | 検出 0 件 | 検出 0 件（pass） | 一致 |
| 本番と同じ設定での読み込み | 成功 | pass | 一致 |
| 自動検査の件数 | 記載なし | **5 件すべて pass** | 追記しました |
| 変更ファイル数 | 記載なし | **24 件（追加 1,139 行・削除 63 行）** | 追記しました |
| `.tsx` の変更 | 0 件 | **0 件** | 一致 |

**土台の 1781 例は、`84cbe4a` の 1 つ前の成功実行（`195f1d9`）から採りました。** `84cbe4a`（PR #170）は `src/backend/` を 1 件も触っていませんので、土台の値として使えます。新規の `it` は 50 個ですが、`%w[dalle nano_banana].each` などの繰り返しの中にあるため、**実行される例は 67 例になります。**

### 2. 本文に書かれた事実を裏取りしました

| 本文の記載 | 確認のしかた | 結果 |
|---|---|---|
| `langchainrb` のライセンスは MIT | GitHub API | **一致（MIT）** |
| 最終の push は 2026-08-26 | GitHub API（`2026-08-26T15:18:50Z`） | **一致** |
| archived ではありません | GitHub API（`archived: false`） | **一致** |
| 星の数 1,991 | GitHub API | **一致** |
| 置き去りと見なすまで 2 分 | `generate_prompt_job.rb` の `STALE_AFTER = 2.minutes` | **一致** |
| 定時の拾い直しは 5 分ごと・開発でも走ります | `config/recurring.yml`（`shared` を `development` と `production` の両方が読みます） | **一致** |
| API キーを見出しで送り、URL から落とします | `langchain_gemini.rb`（`x-goog-api-key` ・ `without_key`） | **一致** |
| 待ち時間の上限をこの持ち場で設けます | 同上（`open` ・ `read` ・ `write`） | **一致** |

**内容の良し悪しは見ていません**（reviewer の担当です）。**本文の記述が実物と合っているかだけを確かめました。**

### 3. 型へ組み直しました

- **「何を変えたか」**：3 点を 1 行ずつの箇条書きにしました。専門用語を使っていません。
- **「なぜ変えたか」**（新設）：issue 番号ごとに、解決したい困りごとを表で書きました。
- **「変更の中身」**：もとの説明（表・英文の例・LangChain の調査）は正確でしたので、見出しを整えて残しました。
- **「変更したファイル」**（新設）：24 件を、拾い直し（#169）・役割に応じた述語（#156）・LangChain 経由（#160）・テスト・設定と記録に分けて並べました。
- **「確かめたこと」**：自動検査の run へのリンクを付け、6 つの検査と実際の組み立てを表にしました。
- **「この PR は画面を持ちません」**：`test/README.md` の「置かない場合」に当たることと、**`test/pr176/` を置かない**ことを明記しました。**そのうえで、ユーザーテストが「GitHub の PR 画面で自動検査が緑であることを確かめる」形になる理由を書きました。**
- **「ユーザーテスト手順」**（新設）：下記のとおりです。
- **「いま確かめられないこと」**（新設）：3 点を挙げ、**手順を書いていないのは書けないためであること**を明記しました。
- **「うまくいかなかったとき」**（新設）：画面の写真・URL・時刻（JST）・ブラウザ名を求め、連絡先を `info@rictaworks.jp` としました。
- **「申し送り」**：もとの 3 点を残し、**土台の `main` が進んだこと**を足しました。
- **末尾の `🤖 Generated with ...` の行を外しました。** 規約が絵文字を禁じています。

## ユーザーテスト手順

**この PR は画面を持ちません。** `.tsx` の変更は 0 件です。**ブラウザで操作する対象がありませんので、手順は GitHub の PR 画面（公開リポジトリですのでログイン不要です）で完結する形にしました。** ターミナル・コマンド・環境変数・ログの確認を、手順に入れていません。

開始点として `https://github.com/rictaworks/hero-prompt-forge/pull/176` を明示しました。

| 手順 | 見るもの | こうなれば正解です |
|---|---|---|
| 1 | 〈Checks〉タブの検査の並び | 5 件すべてに緑のチェック印がつき、赤い×が 1 つもありません |
| 2 | 〈バックエンド（RuboCop / RSpec）〉→〈テスト〉の記録の末尾 | `1848 examples, 0 failures` と表示されます |
| 3 | 〈Files changed〉タブと絞り込みの欄 | 24 件と表示され、`.tsx` で絞ると 1 件も出ません |
| 4 | `src/backend/config/recurring.yml` の緑の行 | `schedule: every 5 minutes` と読めます |
| 5 | `src/backend/Gemfile` の緑の行 | `ライセンス   : MIT` ・ `保管の状態   : 現役です` と読めます |

**手順に書いた表示名は、実物から採りました。**

- 検査の 5 件の名前（ハードコードの検査／バックエンド（RuboCop / RSpec）／フロントエンド（ESLint / 型検査 / Jest / ビルド）／本番と同じ設定での読み込み確認／開発フックの検査）は、run `33028656633` の実際の名前です。
- 手順 2 の〈テスト〉は、バックエンドのジョブの実際の段の名前です（`書き方の検査` ・ `データベースの準備` ・ `テスト`）。`1848 examples, 0 failures` の行が、その記録に実在することを確かめました。
- 手順 4・5 で読ませる行が、**差分の追加行（緑）として実在すること**を確かめました。

## 確かめられないと書いたこと

**手順を捏造していません。** 次の 3 点は「いま確かめられないこと」として本文に明記しました。

- **取り残しの拾い直し**：生成が途中で止まる状況を、利用者の操作では作れません。
- **英文の書き方の変化**：生成の画面がまだありませんので、できあがった英文を画面で読めません。
- **LangChain 経由の呼び出し**：外への呼び出しはテストで差し替えており、画面に出るものが変わりません。

**利用者の操作で確かめるには、生成の画面が先に必要であること**を本文へ書きました。

## 制約の遵守

- 削除コマンドを一切生成していません。
- 実装側の作業ツリーでブランチを切り替えていません（読み取りのみです）。
- コードレビューを行っていません（reviewer の担当です）。
- `.claude/` 直下の md の内容を、PR 本文へ一切転記していません。
- 本文は一時ファイル（UTF-8）経由で `gh pr edit 176 --body-file` により反映しました。**反映後の本文が、渡したファイルと一致することを確かめました。**

## 申し送り

- **`main` は `be10d2d`（PR #175）まで進んでいます。** マージすると RSpec の件数は上記から変わります。**衝突はありません。**
- **自動検査は、この PR の先頭（`4f06c3dc`）で 5 件とも緑です**（run `33028656633`）。`main` を取り込み直した場合は、数え直しが要ります。
- 本文の「67 例」は土台 `84cbe4a` を基準にした値です。**`main` を取り込むと基準が変わります。**

連絡先は info@rictaworks.jp です。

# PR 整備 : PR #128

- 実施日時（JST）：2026-08-26 13:03
- 対象 PR：#128
- 対象 Issue：#127

## 判定

合格

タイトルと本文はいずれも日本語のですます調で書かれていました。書き直しは不要でしたので、不足していた 2 点を補い、あわせて数値の誤りを 1 点直しました。

## 確認したこと

| 観点 | 判定 | 備考 |
|---|---|---|
| タイトルが日本語のですます調であること | 合格 | `[不具合] クォータの作り直しで上限を迂回できる問題を修正します` です。変更していません |
| 本文が日本語のですます調であること | 合格 | 全体がですます調です。だ・である調の混入はありません |
| 英語のまま残った見出し・自動生成文が無いこと | 合格 | 見出しはすべて日本語です。`Closes #127` は GitHub が解釈する記述のため、そのまま残しています。手順中の `Show all checks` `Files changed` は画面にその文字で出るボタン名のため、原文のままにしています |
| 非エンジニアに伝わる書き方であること | 合格 | 「枠」「お返し」「錠をかける」といった言い換えで、専門用語を避けています |
| ユーザーテスト手順があること | 補いました | もとは PR ページでできる確認 3 点が箇条書きで書かれているだけでした。準備の URL を明示し、3 点セットの形に書き直しました |
| 変更したファイルの節があること | 補いました | 6 件を分類して追記しました |
| 「うまくいかなかったとき」が 3 点セットであること | 合格 | 画面の写真・URL・操作した時刻（JST）がそろっています。連絡先も `info@rictaworks.jp` です |
| 絵文字を使っていないこと | 合格 | 検出なし |
| 個人名を書いていないこと | 合格 | 検出なし |
| 未実装のものを書いていないこと | 合格 | 画面での確認は「issue #71 以降になります」と時期を明示しています |
| `.claude/` 直下の秘匿情報の転記が無いこと | 合格 | 検出なし |

## 本文の記載と実物の突き合わせ

本文に書かれた数値と一覧を、PR の中身と照らして確かめました。**1 件だけ食い違いがありましたので直しています。**

| 本文の記載 | 実物 | 判定 |
|---|---|---|
| 変更したファイル 6 件 | `reservation.rb` ／ `20260826130000_add_reserved_uniqueness_to_quota_consumptions.rb` ／ `schema.rb` ／ `reservation_spec.rb` ／ `reservation_race_spec.rb` ／ `fix-issue-127-quota-race.md` の 6 件 | 一致 |
| このファイルのテスト 30 件 → **28 件** | `reservation_spec.rb` は 26 件、`reservation_race_spec.rb` は 2 件で、合わせて 28 件です | **食い違い。直しました** |
| サービス中身側のテスト全体 334 件 | 自動チェックの記録に `334 examples, 0 failures` と出ています | 一致 |
| 直す前に、追加した 5 件のテストが失敗したこと | 差分で足したテストは `reservation_spec.rb` の 5 件です（日をまたぐ再実行 3 件・結び付きの保全 1 件・上限到達の誤読 1 件） | 一致 |
| 同時に 4 件送っても通るのは 1 件だけであること | `reservation_race_spec.rb` は 4 件を同時に投げ、成立 1 件・遮断 3 件を確かめています | 一致 |
| データベース側に「使用中の枠は 1 つの申し込みにつき 1 件だけ」の決まりを置いたこと | 追加された索引は、状態が使用中で申し込みが結び付いている場合だけに絞った一意の索引です | 一致 |
| お返し済み・使用済みの記録は複数残ること | 上の索引は使用中の記録だけを対象にしています | 一致 |
| 画面を作るファイルを含まないこと | `.tsx` で終わるファイルは 0 件 | 一致 |

自動チェックの状態も確かめました。**4 項目すべて成功しています**（ハードコードの検査／バックエンド（RuboCop / RSpec）／フロントエンド（ESLint / 型検査 / Jest / ビルド）／本番と同じ設定での読み込み確認）。本文に書いた期待結果と、実際の画面の見え方が一致します。

## 補った内容

### 1. 「変更したファイル」の節

テンプレートで必須の節が欠けていました。6 件を次のとおり分類して追記しました。

| 分類 | ファイル |
|---|---|
| 枠の出し入れの仕組み（今回の修正の本体） | `src/backend/app/services/quota/reservation.rb` |
| データベース側の決まりの追加 | `src/backend/db/migrate/20260826130000_add_reserved_uniqueness_to_quota_consumptions.rb` |
| データベースの構成の記録 | `src/backend/db/schema.rb` |
| 日をまたぐ再実行などのテスト | `src/backend/spec/services/quota/reservation_spec.rb` |
| 同時に押した場合のテスト（新設） | `src/backend/spec/services/quota/reservation_race_spec.rb` |
| 作業の記録 | `review-records/security-review/fix-issue-127-quota-race.md` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** この PR がブラウザの見た目を変えないことを、ファイルの一覧からも確かめられる旨を添えています。

### 2. ユーザーテスト手順

もとの本文は、画面の変更が無いことと、PR のページでできる確認 3 点が箇条書きで書かれていました。事実としては正しいのですが、**「どこを」「どうする」「何が見えれば正解か」の 3 点セット**になっていませんでしたので、次のとおり書き直しました。ターミナルもコマンドも使いません。

まず節の冒頭で、**この PR にはアプリの画面で確かめる手順が無いこと、そして手順が無いことが正しい状態であること**を先に明言しました。手順を捏造していません。

そのうえで **準備** として、開くべき URL を PR #128 のページと明示しました。どの画面から始めるかを最初に示すためです。

確認は 3 つの見出しに分けました。

1. **自動チェックがすべて通っていることを確かめます** — 画面を下までスクロールし、すべての検査の一覧を開きます。正解は「4 項目すべての左に緑色のチェック印が付いていること」です。4 項目の名前も本文に書き出しました。あわせて、**黄色い丸が回っている場合は検査の途中であること**と、3 分ほど待って再読み込みする対処を添えました。検査が終わる前に開いた方が、誤って不合格と判断しないためです。
2. **テストがすべて成功したことを確かめます** — 一覧から「バックエンド（RuboCop / RSpec）」を押し、見出しの印の色を見ます。正解は「緑色のチェック印が付いていること」で、これが 334 件すべての成功を意味する旨を添えました。赤いバツ印なら不正解であることも明記しています。
3. **画面を変えていないことを確かめます** — 変更されたファイルの一覧を開き、ファイル名を見ます。正解は「6 件で、`.tsx` で終わるファイルが 1 件も無いこと」です。`.tsx` が画面を作るファイルであることも添えました。

期待結果はすべて**画面に見えるもの**で書いています。「テストが通る」ではなく「緑色のチェック印が付いている」と書いています。

画面で確かめられるようになる時期（issue #71「[画面] 03 入力フォームを実装する」以降）と、そのときに確かめられること 2 点は、もとの記載を活かして節の末尾に小見出しとして残しています。

### 3. テスト件数の訂正

もとの本文には「このファイルのテスト 30 件すべて成功」とありました。**「このファイル」がどれを指すかが読み取れず、また件数も実物と合いませんでした。**

実物は `reservation_spec.rb` が 26 件、`reservation_race_spec.rb` が 2 件で、合わせて 28 件です。今回は競合のテストを別ファイルへ新設していますので、次のとおりファイル名と内訳を書く形へ改めました。

- 枠の出し入れのテスト：28 件すべて成功（`reservation_spec.rb` 26 件、`reservation_race_spec.rb` 2 件）

サービス中身側のテスト全体 334 件は、自動チェックの記録と一致しますので、そのままにしています。

## 反映の方法

`gh pr edit 128 --body-file <一時ファイル>` で反映しました。引数へ直接本文を書くと文字化けの恐れがあるため、一時ファイル（UTF-8）を経由しています。反映後に `gh pr view 128` で読み直し、文字化けが無いことと見出しの構成を確認しました。タイトルは変更していません。

## 指摘

指摘はありません。

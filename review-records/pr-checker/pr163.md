# PR 整備 : PR #163（2 回分をまとめた記録です）

- 対象：PR #163「[実装] プロンプトパッケージの組み立ての段を作ります」 / issue #146 ・ #161

---

# PR 整備 : PR #163

- 実施日時（JST）：2026-08-27 00:20 〜 00:52
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/163
- タイトル：[実装] プロンプトパッケージの組み立ての段を作ります
- ブランチ：`feature/issue-146-assembly`
- 取り込み先：`feature/issue-51-art-direction-note`（PR #159 の上に積んでいます）
- 対象コミット：`bb94db320fbe9348129b36921312279c108490b7`
- 関連 issue：#146

## 実測の方法

実装側の作業ツリーではブランチを切り替えていません。
検証用に切り離した作業ツリーを scratchpad へ作り、`bb94db3` を取り出して測りました。

- `.env` および `config/master.key` を作業ツリーへ複製していません。
- 接続情報は、稼働中の dev コンテナの環境変数を実行時にのみ渡しました。
- リポジトリ直下へ一時ファイルを作っていません。
- 作業ツリーは終了時に `DELETE/` へ `mv` しました。

計測に使った比較の基点は、指示どおり `feature/issue-51-art-direction-note` です。

```
git diff feature/issue-51-art-direction-note...feature/issue-146-assembly --stat
```

## 数値の突き合わせ

| 確かめた項目 | 本文の記載（修正前） | 実測 | 判定 |
|---|---|---|---|
| この PR で変更したファイルの件数 | 3 件 | 3 件 | 一致 |
| RSpec 全体の件数 | 1,322 件 | **1,333 件** | **不一致・修正** |
| 組み立ての段のテストの件数 | 19 件 | 19 件 | 一致 |
| RuboCop のファイル数 | 152 ファイル | **153 ファイル** | **不一致・修正** |

### 実測の出力

```
 .../security-review/feature-issue-146-assembly.md  |  55 +++++++
 .../generation/prompt_generation_service.rb        | 163 +++++++++++++++++++++
 .../generation/prompt_generation_service_spec.rb   | 144 ++++++++++++++++++
 3 files changed, 362 insertions(+)
```

```
1333 examples, 0 failures
19 examples, 0 failures
153 files inspected, no offenses detected
```

RSpec 全体は 2 回測り、いずれも 1333 件でした。
1 回目の途中に別作業と試験用データベースが競合して失敗が出ましたが、
測り直して 0 failures を確認しています。件数 1333 は両回で同じです。

## 本文の記載のうち、実測で裏が取れたもの

- 変更 3 件・追加 362 行・**削除 0 行**（既存ファイルを 1 行も書き換えていない、は正しいです）
- 変更ファイルに `.tsx` は 0 件です。
- `ensure_reserved!` は `prompt_generation_service.rb` の 130 行目と 148 行目にあります。
  近くに「出力の直前に、コピースペースの確保を必ず確かめます」の記述があります。
- 「4 系統」は `midjourney` / `dalle` / `stable_diffusion` / `nano_banana` の 4 つで、
  `PromptRequest::TARGET_MODELS` と一致します。本文へ 4 つの名前を明記しました。

## ユーザーテスト手順の検証と修正

非エンジニアが実際にそのとおり操作したときに、「正解です」と書いてある状態が
本当に画面へ出るか、という観点で確かめました。

### 修正 1 : 自動の検査の手順が成立しませんでした（重要）

修正前の手順 1 は、〈Show all checks〉を押して
「5 項目すべての左に緑色のチェック印」が付くことを正解としていました。

**このブランチでは CI が 1 度も動いていません。** 実測は次のとおりです。

```
gh pr checks 163           -> no checks reported on the 'feature/issue-146-assembly' branch
check-runs total_count     -> 0
commit status              -> pending / statuses 0 件
gh run list --branch feature/issue-146-assembly -> 0 件
```

他ブランチには実行履歴がありますので、CI の設定ではなくこの PR で動いていないだけです。
そのまま残すと、非エンジニアが検査の箱そのものを見つけられず、
「正解が出ない」と報告することになります。

手順から外し、〈自動の検査（CI）について〉という節へ置き換えました。
検査の結果が見当たらなくても不具合ではないことと、
本文の数値は開発側で実際に動かして数えた結果であることを明記しています。

### 修正 2 : 積んだ PR であることを手順へ書き添えました（依頼事項）

〈Files changed〉の見え方は、そのときの取り込み先によって変わります。実測しました。

- **いまの取り込み先（`feature/issue-51-art-direction-note`）**
  GitHub が返すファイル一覧は **3 件だけ**です。PR #159 の変更は並びません。
- **PR #159 が先に取り込まれたあと**
  GitHub が取り込み先を `main` へ自動で付け替えます。
  `git diff main...bb94db3` は **10 件**（#159 の 7 件を含む）です。

依頼にあった「#159 の変更も並びます」は、**#159 が先に取り込まれたあとに起きます。**
いまの時点では並びません。**両方の場合を本文へ書き分けました。**

件数が 3 件から 10 件へ増えても不具合ではないことを明記し、
判定の基準は件数ではなく「`.tsx` が 1 件も無いこと」に置きました。
`git diff main...bb94db3` でも `.tsx` は 0 件ですので、
どちらの状態でも手順は成立します。

### 修正 3 : 手順 1 を、いま画面で確かめられる内容へ入れ替えました

取り除いた検査の手順のかわりに、
タイトル直下の一行から取り込み先のブランチ名を読む手順を置きました。
取り込み先が `main` ではないことが画面で確かめられ、
積んである PR であることが手順 2 の前提として伝わります。

### そのまま残した手順

- `.tsx` が 1 件も無いことの確認（実測で 0 件）
- `ensure_reserved!` と近くの説明文の確認（実測で存在）

## そのほか

- タイトルは日本語で、ですます調です。変更していません。
- 本文に英語のまま残った見出し・自動生成文はありませんでした。
- 未実装の機能を本文に書いていません。
  画面が無いため手順が無いこと、確かめられる時期（issue #71 以降）を明記しています。
- 連絡先は `info@rictaworks.jp` です。個人名を書いていません。
- 絵文字を使っていません。
- `.claude/` 直下の md の内容を転記していません。
- 〈変更したファイル〉の節に、追加 362 行・削除 0 行を補いました。

## 反映

```
gh pr edit 163 --title "..." --body-file <一時ファイル>
```

本文は UTF-8 の一時ファイル経由で渡しました。反映後に取得し直し、
1,333 件・153 ファイル・19 件・3 件・10 件がいずれも本文へ入っていることを確認しました。


---

# PR 整備（2回目） : PR #163

- 実施日時（JST）：2026-08-27 00:56 〜 01:12
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/163
- タイトル：[実装] プロンプトパッケージの組み立ての段を作ります
- ブランチ：`feature/issue-146-assembly`
- 取り込み先：`feature/issue-51-art-direction-note`（PR #159 の上に積んでいます。#159 は OPEN です）
- 対象コミット：`af052283635bb09007b96d0fe5d3722fde4a5b4e`
- 前回の対象コミット：`bb94db320fbe9348129b36921312279c108490b7`
- 関連 issue：#146 / #161

## 実施の理由

レビュー後の修正が 2 回入り、本文が実装と食い違ったままになっていました。
また `Closes #161` が本文にありませんでした。最新の `af05228` の実測へ直しました。

## 実測の方法

実装側の作業ツリーではブランチを切り替えていません。
実装側は `feature/issue-146-assembly` を掴んだままでしたので、
検証用に切り離した作業ツリーを scratchpad へ作り、`af05228` を取り出して測りました。

- `.env` および `config/master.key` を作業ツリーへ複製していません。
- 接続情報は、稼働中の dev コンテナの環境変数を実行時にのみ渡しました。
- リポジトリ直下へ一時ファイルを作っていません。
- 作業ツリーは終了時に `DELETE/` へ `mv` し、`git -C <移した先> checkout --detach` で
  ブランチを解放しました。**実装側でブランチを切り替えられる状態です。**

比較の基点は、指示どおり `feature/issue-51-art-direction-note` です。

```
git diff feature/issue-51-art-direction-note...feature/issue-146-assembly --stat
```

## 数値の突き合わせ

| 確かめた項目 | 本文の記載（修正前・1回目の値） | 実測（`af05228`） | 判定 |
|---|---|---|---|
| この PR で変更したファイルの件数 | 3 件 | 3 件 | 一致 |
| 追加行数 | 362 行 | **607 行** | **不一致・修正** |
| 削除行数 | 0 行 | 0 行 | 一致 |
| RSpec 全体の件数 | 1,333 件 | **1,352 件** | **不一致・修正** |
| 組み立ての段のテストの件数 | 19 件 | **38 件** | **不一致・修正** |
| RuboCop のファイル数 | 153 ファイル | 153 ファイル | 一致 |

### 実測の出力

```
 .../security-review/feature-issue-146-assembly.md  | 151 +++++++++++++
 .../generation/prompt_generation_service.rb        | 216 +++++++++++++++++++
 .../generation/prompt_generation_service_spec.rb   | 240 +++++++++++++++++++++
 3 files changed, 607 insertions(+)
```

```
1352 examples, 0 failures
38 examples, 0 failures
153 files inspected, no offenses detected
```

変更ファイルに `.tsx` は 0 件です。`git diff main...af05228` でも 10 件中 `.tsx` は 0 件です。

## 加わった内容の裏取り

いずれも `af05228` の現物で確かめました。

### 1. 入力の点検を、権利に触れる入力の検出より先に置きました

`prompt_generation_service.rb` の `normalized` が `InputNormalizer` を先に呼び、
そのあと `ensure_allowed!` から `ForbiddenDetector` を呼びます。順序は現物どおりです。

**400 万字の所要時間を自分で測り直しました。** 一時の spec を作って通しました。

```
>>> raised=Generation::InputNormalizer::InvalidInputError elapsed=0.0610 sec
```

**0.061 秒で、入力の点検の側から返っています。** 検出まで到達していません。
修正前の 2.28 秒（400 万字）は、ソースの注記に実測として残っています。

本文には「0.1 秒もかからずに『入力が正しくありません』として返ります」と書きました。
**指示にあった「0.002 秒」は、自分の環境で裏が取れませんでしたので使っていません。**
自分で測った 0.061 秒を丸めた表現にしています。

### 2. 工程の並びを見直しました（issue #161）

```
STEPS = %i[proper_nouns style_spec copy_space anti_ai_rules].freeze
```

AI っぽさを避ける規則が最後です。指示がそろってから当たります。
ソースにも「4.1 の並びのまま最初に当てると、当てる相手が 1 件もありません（issue #161）」
と注記があります。**本文の工程一覧を、新しい 10 段の並びへ差し替えました。**

**末尾へ `Closes #161` を足しました。** `Closes #146` と 2 行並びます。

### 3. 工程を呼び忘れたら、テストが落ちます

spec に「段を外したら、テストが落ちます」の節があり、
`described_class::STEPS` が `%i[proper_nouns style_spec copy_space anti_ai_rules]` と
一致することを確かめています。加えて、名前の保持と打ち消しの語の注入を素材で確かめています。

### 4. 撮影の指示が失われた案を出力しません

`MissingSpecificationsError` と `ensure_specifications!`（199 行目）があります。
`packaged` が `ensure_reserved!` と `ensure_specifications!` を続けて呼びます。

### 5. 申し送りから「AI っぽさを避ける規則が働かない」を外しました

この PR で直りましたので、申し送りから削りました。反映後の本文で 0 件を確認しています。

## 「4 系統すべて日本語を含まない」の訂正

**修正前の記載は正しくありませんでした。** spec の現物は次のとおりです。

```ruby
# **日本語固有名詞は、そのまま残ることがあります**（requirements.md 4.1 の 6）。
# 読みが決まらない名前を訳すと、別のお名前になります。
it "#{model}：名前が無ければ日本語を含みません" do
```

確かめているのは「**お名前を渡さなかったときに**日本語が混ざらないこと」です。
本文の表からこの行を外し、〈整形について〉という節を新たに設けました。
日本語の名前がそのまま残ることがある理由（訳すと別のお名前になる）を添え、
**書き方が正しくなかったことを明記して訂正しています。**

## ユーザーテスト手順の検証

現物に当てて、3 手順とも成立することを確かめました。

| 手順 | 確かめたこと | 結果 |
|---|---|---|
| 1. 積んである PR であることの確認 | 取り込み先が `feature/issue-51-art-direction-note` | 成立します |
| 2. `.tsx` が無いことの確認 | 現状 3 件・#159 マージ後 10 件、いずれも `.tsx` は 0 件 | 成立します |
| 3. 出す直前の 2 つの確かめ | `ensure_reserved!` と `ensure_specifications!` が両方ある | 成立します |

手順 3 は、`ensure_specifications!` が加わりましたので、**2 つを探す形へ広げました。**
近くの説明文（「出力の直前に、コピースペースの確保を必ず確かめます」
「撮影の指示を欠く案は出力しません」）も現物で確認しています。

### 自動の検査（CI）について

**最新の `af05228` では、CI がまだ動いていません。**

```
check-runs (af05228) -> total_count 0
```

このブランチの実行履歴は 1 件だけで、**それは 1 つ前の `5b9297c` に対するもので、結果は failure です。**

```
32985987757 5b9297c completed failure
```

**最新のコミットに対する結果ではありませんので、本文で「緑になっている」とは書けません。**
1 回目の整備で外した「5 項目すべて緑」の手順は、今回も復活させていません。

節の書き方を一段強めました。結果の箱が見当たらなくても不具合ではないこと、
出ていた場合は**赤い印が無いかだけを見て、赤があれば知らせてほしいこと**、
**「緑になっているはずだ」という前提では見ないでほしいこと**を明記しています。

## そのほか

- タイトルは日本語で、ですます調です。変更していません。
- 本文に英語のまま残った見出し・自動生成文はありません。
- 未実装の機能を書いていません。画面が無いため手順が無いことを明記しています。
- 連絡先は `info@rictaworks.jp` です。個人名を書いていません。
- 絵文字を使っていません。
- `.claude/` 直下の md の内容を転記していません。

## 反映

```
gh pr edit 163 --title "..." --body-file <一時ファイル>
```

本文は UTF-8 の一時ファイル経由で渡しました。反映後に取得し直し、
607 行・1,352 件・38 件・153 ファイル・3 件・10 件・`Closes #146`・`Closes #161` が
いずれも本文へ入っていること、申し送りから当該項目が消えていることを確認しました。

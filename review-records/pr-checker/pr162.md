# PR 整備 : PR #162

- 実施日時（JST）：2026-08-27
- 対象：PR #162 `[実装] LLM による精緻化と縮退合成を実装します`
- ブランチ：`feature/issue-52-53-llm-refiner`
- 検証したコミット：`90cc68a`
- 実施したこと：タイトル・本文の点検と修正（コードレビューは行いません）

## 検証のやり方

**実装側の作業ツリーでブランチを切り替えていません。** 作業中に実装側が別ブランチ（`bb94db3`）へ移ったことを確認したため、一時的な作業ツリー（`90cc68a` を切り出したもの）を使いました。

テストは、既存の dev コンテナではなく**使い捨てのコンテナ**で動かしました。**テスト用データベースも専用のもの（`hero_prompt_forge_pr162_test`）を作り、実装側の作業と混ざらないようにしました。**

**`.env` と `config/master.key` を作業ツリーへ複製していません。** 接続先は環境変数でコンテナへ直接渡しました。**リポジトリ直下へ一時ファイルを作っていません。**

**`GEMINI_API_KEY` を設定していません。実際に Gemini へ通信していません。** 対象のテストは、いずれも呼び出しを差し替えています。

## 数値の照合

| 項目 | 修正前の記載 | 実測 | 判定 |
|---|---|---|---|
| 変更したファイルの件数 | 8 件 | 8 件 | 一致 |
| 既存ファイルの書き換え | 1 行もなし | 削除 0 行・すべて新規追加 | 一致 |
| 精緻化のテスト | 13 件 | **11 件** | **不一致。修正しました** |
| 縮退合成のテスト | 10 件 | **12 件** | **不一致。修正しました** |
| RSpec 全体 | 1,308 件 | 1,308 件、失敗 0 | 一致 |
| RuboCop | 153 ファイル・指摘 0 件 | 153 ファイル・指摘 0 件 | 一致 |

実測に使った指示です。

- `git diff --name-only origin/main...90cc68a | wc -l` → `8`
- `git diff --numstat origin/main...90cc68a` → 全 8 件が「追加のみ・削除 0」
- `bundle exec rspec spec/services/generation/llm_refiner_spec.rb` → `11 examples, 0 failures`
- `bundle exec rspec spec/services/generation/degraded_composer_spec.rb` → `12 examples, 0 failures`
- `bundle exec rspec` → `1308 examples, 0 failures`
- `bundle exec rubocop` → `153 files inspected, no offenses detected`

**件数の合計（23 件）は合っていました。2 つのテストの内訳が入れ替わったうえでずれていた形です。**

## 費用の見積もりの検算

単価は 100 万トークンあたり 入力 0.10 ドル・出力 0.40 ドルです。

- 入力：1,500 ÷ 1,000,000 × 0.10 = 0.00015 ドル
- 出力：1,500 ÷ 1,000,000 × 0.40 = 0.00060 ドル
- 合計：**0.00075 ドル**

**本文の「約 0.00075 ドル」は正しいです。** 1,000 回あたりの「約 0.75 ドル」も正しいです。

円の換算も確かめました。1 ドル 150 円として 0.00075 ドル = 0.1125 円、0.75 ドル = 112.5 円です。**修正前の「0.1 円ほど」「110 円ほど」は誤りではありませんが、換算のレートが書かれていませんでした。** 読み手が検算できるよう、「1 ドル 150 円で」を足し、値を 0.11 円・113 円へそろえました。

**計算の途中を表に出しました。** 修正前は結果だけが書いてあり、読み手が確かめられませんでした。

### 単価の比較に誤りがありました

修正前の本文は「**次に安いものと比べて出力の単価が 6 分の 1 以下**」としていました。**本文自身が載せている表と合いません。**

表の 4 モデルを安い順に並べると次のとおりです。

| モデル | 入力 | 出力 |
|---|---|---|
| gemini-2.5-flash-lite（採用） | 0.10 ドル | 0.40 ドル |
| gemini-3.1-flash-lite | 0.25 ドル | 1.50 ドル |
| gemini-2.5-flash | 0.30 ドル | 2.50 ドル |
| gemini-3.5-flash-lite | 0.30 ドル | 2.50 ドル |

**次に安いのは `gemini-2.5-flash`（2.50 ドル）ではなく `gemini-3.1-flash-lite`（1.50 ドル）です。** 入力・出力のどちらで見ても `gemini-3.1-flash-lite` が 2 番目です。

0.40 ÷ 1.50 = 0.267 ですので、**「6 分の 1 以下」は成り立ちません。**「3 分の 1 以下」が正しい表現です（入力は 0.10 ÷ 0.25 = 0.4 で「2 分の 1 以下」）。

修正後は、表を安い順に並べ替えたうえで「**次に安い `gemini-3.1-flash-lite` と比べても、入力で 2 分の 1 以下・出力で 3 分の 1 以下**」へ直しました。

**単価そのもの（0.10 ／ 0.40 ほか）は本文の記載を前提として扱いました。** 料金ページへの接続で確かめていません。

### 再試行の分を書き足しました

`DegradedComposer::DEFAULT_ATTEMPTS = 2` です。**失敗すると最大 2 回まで試します。** 費用がその分増えることが書かれていませんでしたので、足しました。

### 前提が実測でないことを明記しました

「1 案あたり 500 トークン」は見込みです。**この PR には組み立ての段が入っていないため、実測できません。** 見込みである旨と、実測で置き換える申し送りを足しました。

なお「1 回のお申し込みで 3 案」は `VariationRules::REQUIRED_COUNT = 3` と合っています。

## ユーザーテスト手順の点検

**非エンジニアがそのとおりに操作して、書いてある「正解です」が本当に出るか**という観点で見ました。

### 手順 2（修正前）は、そのとおりに操作しても確認できません

修正前の手順 2 は、〈Files changed〉タブの「画面の右上にある検索欄」に `GEMINI_API_KEY` と入力させるものでした。

**GitHub の〈Files changed〉タブにあるのは、ファイル名を絞り込む欄です。** ファイルの中身を検索する欄ではありません。しかも位置は画面の左側（ファイル一覧の上）で、「右上」ではありません。

`GEMINI_API_KEY` という名前のファイルは存在しませんので、**そのとおりに入力すると「該当なし」になります。** 期待結果として書かれていた「見つかるのは環境変数の名前だけ」は出ません。

**この手順は成り立ちませんので、差し替えました。**

差し替え後は、行番号まで指した固定リンクを開いて読むだけにしました。ブラウザで開けることを確認済みです（いずれも HTTP 200）。

- `.../blob/feature/issue-52-53-llm-refiner/src/backend/app/services/generation/gemini_client.rb#L24`
- `.../blob/feature/issue-52-53-llm-refiner/src/backend/config/llm.yml#L13`

行の中身も実物と一致することを確かめました。

- `gemini_client.rb` の 24 行目：`API_KEY_VARIABLE = 'GEMINI_API_KEY'`
- `llm.yml` の 13 行目：`model: gemini-2.5-flash-lite`

**リンクは行番号付きですので、その行に色が付いて表示されます。** 非エンジニアが探し回らずに済みます。

差分全体を調べ、**鍵そのものの文字列（`AIza` で始まる並びなど）が 1 件も無いこと**も確かめました。

### 手順 1（修正前）は、期待結果が出ません

修正前の手順 1 は「〈Show all checks〉を押す」「5 項目すべてに緑色のチェック印が付いています」というものでした。

**この PR には、自動の検査の結果が 1 件もありません。**

- `gh pr checks 162` → `no checks reported on the 'feature/issue-52-53-llm-refiner' branch`
- `gh api repos/.../commits/90cc68a/check-runs` → `total_count` は `0`
- `gh run list --branch feature/issue-52-53-llm-refiner` → 該当なし

**検査の箱そのものが出ませんので、〈Show all checks〉を押すことができません。** 緑のチェック印も出ません。

`.github/workflows/ci.yml` は `pull_request` で動く設定で、ジョブは 5 つです。**「5 項目」という数はワークフローと合っています。** 動いていないだけです。

手順を消さず、**正解の形（5 項目すべて緑）を書いたうえで、現時点では箱が出ないことと、その場合に何を報告すればよいかを明記**しました。手順の順番も入れ替え、**いま確実に確認できる 2 つを先頭**へ持ってきました。

### そのほか

- 「画面で確かめる手順がありません」と明記している点は、**この PR の実態と合っています。** 手順の捏造はありませんでした。
- 「うまくいかなかったとき」に、写真・URL・時刻（JST）の 3 点がそろっています。
- 連絡先は `info@rictaworks.jp` です。個人名はありません。

## 文章の点検

- タイトルはすでに日本語で、内容と合っています。**変更していません。**
- 本文に英語のまま残った見出し・箇条書き・自動生成文はありませんでした。
- 「ですます調」で統一されています。
- 絵文字はありません。
- **未実装の機能は書かれていません。** issue #71 ／ #55 ／ #73 は「まだ無いもの」として書かれています。
- `.claude/` 直下の md の内容は転記していません。

## 実装側への申し送り

**`src/backend/config/llm.yml` のコメントにも、同じ単価の誤りが残っています。**

> 次に安いのは `gemini-2.5-flash`（入力 0.30 ドル・出力 2.50 ドル）で、**出力の単価が 6 倍以上**になります。

**PR 本文は直しましたが、設定ファイルのコメントは直していません**（コードの修正は担当外です）。同じ表に `gemini-3.1-flash-lite`（0.25 ／ 1.50）が載っており、そちらが 2 番目です。**実装側で直してください。**

**マージの前に CI を動かしてください。** 現状はこの PR に検査の結果が 1 件もありません。

## 反映

`gh pr edit 162 --title "..." --body-file <一時ファイル>` で反映しました。**本文は UTF-8 の一時ファイル経由で渡しました。**

反映後に `gh pr view 162` で読み直し、修正した数値（11 件・12 件・1,308 件・153 ファイル・3 分の 1・0.00075 ドル）が入っていることを確認しました。

## 後片付け

一時的な作業ツリーは `mv` で `DELETE/` へ移しました。**削除していません。**

移動先は `DELETE/review-worktrees/pr162-20260827-prchecker` です。`git worktree repair` で登録も直しました。

**検証用に作ったテスト用データベース `hero_prompt_forge_pr162_test` は残してあります。** 削除コマンドを使わない方針のため、**不要になった時点で手動で削除してください。**

## この作業の範囲

**コードレビューは行っていません。** 差分の良し悪し・設計の妥当性・セキュリティの判断は `reviewer` の担当です。テストと RuboCop を動かしたのは、**本文に書かれた数値を確かめるため**であり、品質の判定のためではありません。


---

# PR 整備（2回目） : PR #162

- 実施日時（JST）：2026-08-27
- 対象：PR #162 `[実装] LLM による精緻化と縮退合成を実装します`
- ブランチ：`feature/issue-52-53-llm-refiner`
- 検証したコミット：`b90964e`（`origin/main` = `a5c2f1f` へリベース済み）
- 前回の記録：`pr162-checker.md`
- 実施したこと：タイトル・本文の点検と修正（コードレビューは行いません）

## リベースの確認

`git merge-base --is-ancestor origin/main b90964e` が真です。**`origin/main` が祖先ですので、まっすぐ乗っています。** `gh pr view` の `mergeable` も `MERGEABLE` です。

ブランチには 2 つのコミットが乗っています。

- `6329fc6` [実装] LLM による精緻化と縮退合成を実装します (#52 #53)
- `b90964e` [実装] 日本語の素材を送らず、通信の失敗をすべて受け止めます

## 検証のやり方

**実装側の作業ツリーでブランチを切り替えていません。** 一時的な作業ツリー（`b90964e` を切り出した detached HEAD）で確かめました。

テストは**使い捨てのコンテナ**で動かし、**テスト用データベースも専用のもの（`hero_prompt_forge_pr162_test`）**を使いました。実装側の作業と混ざりません。

**`.env` と `config/master.key` を作業ツリーへ複製していません。** 接続先は環境変数でコンテナへ直接渡しました。**リポジトリ直下へ一時ファイルを作っていません。**

**`GEMINI_API_KEY` を設定していません。実際に Gemini へ通信していません。** 対象のテストは、いずれも呼び出しを差し替えています。

## 数値の照合

| 項目 | 修正前の記載 | 実測 | 判定 |
|---|---|---|---|
| 変更したファイルの件数 | 8 件 | **11 件** | **不一致。修正しました** |
| 削除した行 | （記載なし） | **0 行** | 追記しました |
| 精緻化のテスト | 11 件 | 11 件 | 一致 |
| 縮退合成のテスト | 12 件 | 12 件 | 一致 |
| 呼び出しのテスト | （記載なし） | **19 件** | 追記しました |
| 設定のテスト | （記載なし） | **13 件** | 追記しました |
| RSpec 全体 | 1,308 件 | **1,484 件** | **不一致。修正しました** |
| RuboCop | 153 ファイル | **166 ファイル** | **不一致。修正しました** |
| CI | 未実行 | **5 項目すべて成功** | **修正しました** |

実測に使った指示です。

- `git diff --name-only origin/main...b90964e | wc -l` → `11`
- `git diff --numstat origin/main...b90964e` → 削除行の合計は `0`
- `bundle exec rspec spec/services/generation/llm_refiner_spec.rb` → `11 examples, 0 failures`
- `bundle exec rspec spec/services/generation/degraded_composer_spec.rb` → `12 examples, 0 failures`
- `bundle exec rspec spec/services/generation/gemini_client_spec.rb` → `19 examples, 0 failures`
- `bundle exec rspec spec/services/generation/llm_settings_spec.rb` → `13 examples, 0 failures`
- `bundle exec rspec` → `1484 examples, 0 failures`
- `bundle exec rubocop` → `166 files inspected, no offenses detected`

### 足したテストは 31 件ではなく 32 件です

**ご連絡では「31 件足しました」でしたが、実測は 32 件です。**

新しく増えたテストのファイルは 2 つで、どちらも新規追加です。

- `gemini_client_spec.rb`：**19 件**
- `llm_settings_spec.rb`：**13 件**

合計 **32 件**です。`llm_refiner_spec.rb`（11 件）と `degraded_composer_spec.rb`（12 件）は前回と同じ件数で、増えていません。

**本文には 32 件と書きました。** 内訳も併記し、あとから数え直せるようにしました。

### 変更したファイルは 11 件です

`.env.example` が加わり、テストのファイルが 2 つ増えたため、8 件から 11 件になりました。

**`.env.example` だけが「既存ファイルの変更」です。** ただし 7 行の追加のみで、削除は 0 行です。修正前の本文にあった「既存のファイルを 1 行も書き換えていません」は、**そのままでは正しくありません。**「削除した行は 0 行」「既存への変更は `.env.example` への 7 行の追加だけ」という書き方へ直しました。

### 全体の件数が増えた理由

`origin/main` が進んだ（PR #163 ／ #157 ／ #158 が入った）ため、**この PR の 32 件だけでは説明がつきません。** 1,308 → 1,484 の差 176 件には、`main` 側で増えた分が含まれます。本文には**測った時点の数（1,484 件）**だけを書き、差分の内訳は書きませんでした。

## 加わった内容の確認

ご連絡の 8 点を、実物で確かめました。**すべて実装されています。**

| 加わった内容 | 確かめた場所 | 結果 |
|---|---|---|
| 日本語を含む素材を送らない | `llm_refiner.rb` の `refinable`（`terms.grep_v(JAPANESE)`） | あります |
| 磨いたあとにアンチAIルック規則を再適用 | `llm_refiner.rb` の `ensure_allowed!` | あります |
| 通信の失敗をすべて受け止める | `gemini_client.rb` の `FAILURES`（`OpenSSL::SSL::SSLError` ・ `SystemCallError` ・ `Net::WriteTimeout` を含む 7 種） | あります |
| 同じ文が 2 つ返ったときに数が減る不具合 | `llm_refiner.rb` の `applied` が `add` ではなく `replace` を使う | 直っています |
| 呼び出しと設定のテスト | 2 ファイル | **32 件**（31 件ではありません） |
| `.env.example` へ `GEMINI_API_KEY` と無償枠の注意 | `.env.example` の 51〜57 行目 | あります |
| 書き込みの待ち時間 | `llm.yml` の `write_timeout_seconds: 10` | あります |
| 単価の比較の修正 | `llm.yml` の 6〜7 行目 | 直っています |

### 単価の比較を検算しました

`llm.yml` のコメントは「次に安いのは `gemini-3.1-flash-lite`（入力 0.25 ドル・出力 1.50 ドル）で、**入力が 2.5 倍、出力が 3.75 倍**」となりました。

- 入力：0.25 ÷ 0.10 = **2.5 倍** → 正しいです
- 出力：1.50 ÷ 0.40 = **3.75 倍** → 正しいです

**前回の申し送り（設定ファイルのコメントの誤り）は解消しています。** 本文の表現も、同じ「2.5 倍・3.75 倍」へそろえました。**設定ファイルと本文が同じことを言う状態になりました。**

### 費用の検算（変更なし）

単価は 100 万トークンあたり 入力 0.10 ドル・出力 0.40 ドルです。

- 入力：1,500 ÷ 1,000,000 × 0.10 = 0.00015 ドル
- 出力：1,500 ÷ 1,000,000 × 0.40 = 0.00060 ドル
- 合計：**0.00075 ドル**

**本文の「約 0.00075 ドル」「1,000 回で約 0.75 ドル」は正しいままです。** 換算レート（1 ドル 150 円）と計算過程も残しました。

**単価そのもの（0.10 ／ 0.40 ほか）は本文の記載を前提として扱いました。** 料金ページへ接続して確かめていません。

## ユーザーテスト手順の点検

### 手順 1 を通常の形へ戻しました

CI が通りましたので、前回入れた「箱がまだ出ません」という但し書きを**外しました。**

実測で確かめた結果です。

- `gh pr checks 162` → 5 項目すべて `pass`
- `gh pr view 162 --json statusCheckRollup` → 5 項目すべて `SUCCESS`

**5 項目の名前を、画面に出るとおりに本文へ並べました。**

- バックエンド（RuboCop / RSpec）
- フロントエンド（ESLint / 型検査 / Jest / ビルド）
- 本番と同じ設定での読み込み確認
- 開発フックの検査
- ハードコードの検査

押すところも、〈All checks have passed〉の行の右にある〈Show all checks〉と具体的に書きました。

### 行番号がずれていましたので直しました

**リベースと修正でファイルの中身が変わり、前回のリンクの行番号がずれていました。**

| 見せたい行 | 前回 | 今回 |
|---|---|---|
| `model: gemini-2.5-flash-lite` | `llm.yml#L13` | **`llm.yml#L16`** |
| `API_KEY_VARIABLE = 'GEMINI_API_KEY'` | `gemini_client.rb#L24` | `gemini_client.rb#L24`（変わらず） |

**`llm.yml` は無償枠の注意が 2 行増えたため、`model:` が 13 行目から 16 行目へ下がっていました。** 直さないと、非エンジニアには関係のない行が光ります。

### 手順 4 を足しました

無償枠を使わない注意書きを確かめる手順を足しました。**有償・無償の別はお客さまの情報の扱いに関わりますので、目で見て確かめられるようにしました。**

リンク先は `.env.example#L57` です。57 行目が `GEMINI_API_KEY=` で、**イコールのうしろに値が無いこと**も併せて確かめてもらいます。

### リンクが開けることを確かめました

本文に載せた 3 つの blob リンクは、いずれも HTTP 200 です。

- `.../gemini_client.rb#L24`
- `.../llm.yml#L16`
- `.../.env.example#L57`

行の中身も実物と一致します。

- `gemini_client.rb` 24 行目：`API_KEY_VARIABLE = 'GEMINI_API_KEY'`
- `llm.yml` 16 行目：`model: gemini-2.5-flash-lite`
- `.env.example` 57 行目：`GEMINI_API_KEY=`

差分全体を調べ、**鍵そのものの文字列（`AIza` で始まる並び）が 1 件も無いこと**も確かめました。

### そのほか

- 「画面で確かめる手順がありません」と明記している点は、**この PR の実態と合っています。** 手順の捏造はありません。
- 「うまくいかなかったとき」に、写真・URL・時刻（JST）の 3 点がそろっています。
- 連絡先は `info@rictaworks.jp` です。個人名はありません。

## そのほかに直したこと

### 組み立ての段の番号が違っていました

前回の本文は「**組み立ての段（issue #146）へつなぐのは、この PR の外です**」でした。**issue #146 はすでに閉じており、PR #163 として `main` へ入っています。**

「**すでに `main` へ入っています（PR #163）。そこへつなぎ込むのは、この PR の外です**」へ直しました。**未着手のように読める書き方をやめました。**

### レビューで直した内容を、本文へ書き足しました

修正前の本文には、レビュー後に加わった 5 点（日本語を送らない・規則の再適用・失敗の受け止め・不具合の修正・書き込みの待ち時間）が**1 つも書かれていませんでした。** 「レビューを受けて直したこと」という節を新しく作り、**専門用語を使わずに**まとめました。

待ち時間の記述も「接続 5 秒・応答 20 秒」から「**接続 5 秒・送信 10 秒・応答 20 秒**」へ直しました。

「送らないもの」の表と「検めること」の表にも、**日本語を含む素材**と**避けたい言い回しが戻っていないか**を足しました。

### 無償枠の注意を本文へ書きました

**入力が学習に使われる場合がある**という点は、お客さまの情報の扱いに関わります。本文の「使うモデルと、その根拠」と「申し送り」の両方に書きました。

## 文章の点検

- タイトルはすでに日本語で、内容と合っています。**変更していません。**
- 本文に英語のまま残った見出し・箇条書き・自動生成文はありません。
- 「ですます調」で統一されています。
- 絵文字はありません。
- **未実装の機能は書かれていません。** issue #71 ／ #55 ／ #73 は「まだ無いもの」として書かれています。
- `.claude/` 直下の md の内容は転記していません。

## 実装側への申し送り

**前回の申し送り（`llm.yml` のコメントの単価の誤り）は解消しました。**

**新しい指摘はありません。** ただし 1 点だけお伝えします。**指示文から `Keep every fragment in English.` の行が消えています。** 日本語を送らなくなったための整理と読み取れますし、`ensure_english!` による検めは残っていますので、**動きの問題はありません。** 意図どおりかだけご確認ください。

## 反映

`gh pr edit 162 --title "..." --body-file <一時ファイル>` で反映しました。**本文は UTF-8 の一時ファイル経由で渡しました。**

反映後に `gh pr view 162` で読み直し、修正した数値（11 件・11/12/19/13 件・32 件・1,484 件・166 ファイル・2.5 倍／3.75 倍）と手順 1 の記述が入っていることを確認しました。

## 後片付け

**作業ツリーの後始末で、別のエージェントと衝突しました。ありのまま記録します。**

作業ツリーを作ろうとしたところ、`scratchpad/wt162b` が**すでに存在していました**（`reviewer` が同じ名前で先に作っていました）。`b90964e` を指していたため、そのまま使いました。**名前を分けなかったことが誤りでした。**

後始末で `mv` を実行した時点では、**`reviewer` が同じディレクトリを `DELETE/review-worktrees/pr162b-20260827-reviewer` へ先に移し終えていました。** そのため私の `mv` は移す先を作れず、`DELETE/review-worktrees/pr162b-20260827-prchecker` は存在しません。

**結果として、求められた状態は満たされています。**

- 中身は `DELETE/review-worktrees/pr162b-20260827-reviewer` にあります（`DELETE/` の下です）。
- `git -C <そこ> status -sb` は `## HEAD (no branch)` です。**ブランチは解放されています。**
- `git status --porcelain` に追跡ファイルの欠けはありません。**失われたファイルはありません。**

**削除はしていません。**

**`git worktree list` に、消えた `scratchpad/wt162b` を指す登録が `prunable` として残っています。** `git worktree prune` は git の管理情報を消す操作ですので、**実行していません。** 不要になった時点で手動で整理してください。

**次回は、作業ツリーの名前へ役割を必ず入れます**（例：`wt162c-prchecker`）。

**検証用に作ったテスト用データベース `hero_prompt_forge_pr162_test` は残してあります。** 削除コマンドを使わない方針のため、**不要になった時点で手動で削除してください。**

## この作業の範囲

**コードレビューは行っていません。** 差分の良し悪し・設計の妥当性・セキュリティの判断は `reviewer` の担当です。テストと RuboCop を動かしたのは、**本文に書かれた数値を確かめるため**であり、品質の判定のためではありません。

## 数値の鮮度について

**確かめたのは `b90964e` です。** 記録を書いている時点で、実装側の作業ツリーに `llm_refiner.rb` ・ `degraded_composer.rb` と、その 2 つのテストへの**未コミットの変更が積まれています。**

**さらに手が入る場合は、本文の件数を測り直してください。** とくに精緻化（11 件）と縮退合成（12 件）、全体（1,484 件）、RuboCop（166 ファイル）が動きます。

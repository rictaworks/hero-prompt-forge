# pr-checker 記録：PR #185（issue #78 README を実装から起こします ／ #79 SPEC を実装から起こします）

- 対象 PR：#185
- ブランチ：`feature/reverse-docs`（`2bc7219`）
- 土台：`main`（`bf4cb46`。**現在の `main` と同じです。** 土台のずれはありません）
- 実施日時：2026-08-27（JST）
- 作業ツリー：確認用の作業ツリー `DELETE/pr185-checker-20260827`（PR の先頭）と `DELETE/pr185-checker-base-20260827`（`bf4cb46^`）を新しく作り、そこで確かめました。**実装側の作業ツリー（`DELETE/impl-78`）へ触れていません。`main` の作業ツリーでブランチを切り替えていません。**

## 判定

合格

タイトルはそのままとし、本文を規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ組み直しました。**ユーザーテストの手順が実際の GitHub の画面と食い違っていた箇所を、実測に合わせて直しました。**

## 反映した内容

- タイトル：`[文書] README と SPEC を、実装から起こします (#78, #79)`（**変更していません。** 日本語・ですます調で、規約に沿っています）
- 本文：**組み直しました。** 日本語化は不要でした（もとから日本語・ですます調です）。**ユーザーテストの手順（確認 1 〜 5）は書き直しました。**

## この PR は画面を持ちません

**`src/` を 1 文字も変更していません。** 変更は 12 ファイル（+1073 行 / -10 行）で、内訳は説明書（`README.md`）・実装から起こした図 6 件（`SPEC/er.md` ほか）・図の索引（`SPEC/README.md`）・文書の検査（`test/docs/docs.test.mjs`）・検査の呼び出し口（`package.json`）・自動検査の手順（`.github/workflows/ci.yml`）・記録（`review-records/security-review/feature-reverse-docs.md`）です。

そのため、**ユーザーテストは「GitHub のこの PR の画面だけで完結する手順」としました。** アプリの画面を開く手順を捏造していません。本文にも「この PR は画面を持ちません」を独立した節として残しました。

## 〈Display the rich diff〉を実物で確かめた結果

**このリポジトリは公開ですので、GitHub の画面の HTML を実際に取得して確かめました。**

- 取得した画面：`https://github.com/rictaworks/hero-prompt-forge/pull/185/files` ・ `.../pull/185/checks` ・ 各ファイルのリッチ表示（`/rictaworks/hero-prompt-forge/diffs/<番号>?...`）

| 確かめたこと | 実測 |
|---|---|
| 〈Display the rich diff〉という押しどころが実在するか | **実在します。** `aria-label="Display the rich diff"` のボタンが、変更した markdown のファイルごとに 1 つずつ付きます |
| アイコンの形 | **紙の形**です（`octicon-file`）。**その左に、山かっこの形（`octicon-code`、`aria-label="Display the source diff"`）が並び、既定ではそちらが押された状態です。** 2 つで 1 組（`BtnGroup`）です |
| 押した先で Mermaid の図が絵になるか | **なります。** リッチ表示の HTML に `<section class="js-render-needs-enrichment ..." data-type="mermaid" aria-label="mermaid rendered output container" data-src="https://viewscreen.githubusercontent.com/markdown/mermaid...">` が入ります。**新しく足した `SPEC/er.md`（`diffs/5`）でも `SPEC/class.md`（`diffs/3`）でも確認しました。** `class.md` は 5 か所、`er.md` は 1 か所です |
| 図の枚数 | `er.md` 1 ／ `class.md` 5 ／ `state.md` 4 ／ `sequence.md` 6 ／ `dfd.md` 2 ／ `usecase.md` 1 の**合計 19 枚**です |

## 画面と食い違っていた箇所（直しました）

| 更新前の本文 | 実際の画面 | 対応 |
|---|---|---|
| 「左上の〈Filter changed files〉の欄に `er.md` と入力します。**出てきた** `SPEC/er.md` の右上にある…を押します」 | **〈Filter changed files〉は、左側のファイル一覧（file tree）を絞り込む欄です**（`id="file-tree-filter-field"` ／ `data-target="diff-layout.fileTreePathFilter"`）。**絞り込んでも右側の差分は動きません。左の一覧の名前を押して初めて、そのファイルへ移ります**（一覧の項目は `#diff-<ハッシュ>` へのリンクです） | **絞り込みの手順をやめ、「左側の一覧で `SPEC` の下の `er.md` を押します」に直しました。** 一覧に 12 件しかありませんので、絞り込みは不要です |
| （記載なし） | **左側の一覧は、ブラウザの窓が狭いと出ません**（トグルが `d-none d-md-inline-block`）。〈Show file tree〉で出し入れします | **「見当たらないときは〈Show file tree〉を押します」を書き足しました** |
| 「`SPEC/er.md` の**右上**にある、紙のようなアイコン」 | **ファイル名の帯の右のほう**です。**アイコンは 2 つ並んでおり、左（山かっこ）が既定で選ばれています。** 押すのは右です。ログインしている方には、さらに右へ〈Viewed〉が付きますので「右上」だけでは指せません | **「帯の右のほうにアイコンが 2 つ並んだ組があります。左は山かっこ、右は紙の形。右を押します」と直しました** |
| 「〈Filter changed files〉の文字を消し、`README` と入力します。`README.md` の右上の…」 | **`README` で絞ると `README.md` と `SPEC/README.md` の 2 件が残ります。** どちらを押すのか分かりません | **「`SPEC` のフォルダより上にある `README.md`（フォルダに入っていない方）」と書き分けました** |
| **「「（なし）」という行が残っていれば不合格です。」** | **リッチ表示は「変更の前と後」を並べます。古い「（なし）」1 行だけの表は `<del>`（赤い取り消し線）として必ず表示されます。** README のリッチ表示（`diffs/1`）の HTML で、ページ一覧・API 一覧の両方について確認しました | **重大な食い違いです。このままでは、正常なのに必ず「不合格」と報告されます。** 「赤い取り消し線付きで出るのは消した跡です。それで正解です。見るのはそのすぐ下の緑の表です」と書き直しました |
| 「**6 図**すべて成功（Mermaid CLI で実測）」 | **6 つは「文書の数」です。図は 19 枚あります**（Mermaid CLI が出力した SVG を数えました） | **「6 つの文書・19 枚の図がすべて成功」に直しました。** 「置いたもの」の表にも、文書ごとの図の枚数を書き足しました |
| 「〈Checks〉のタブを押します。左側に並ぶ検査の名前を、上から下まで見ます。**6 つ**の検査すべてに緑の印」 | 数は合っています。**ただし比べる一覧が無く、順番も書かれていません。** 実物は〈CI on: pull_request〉という見出しの下に 6 つ並びます。**タブの名前の右に「6」の印も出ます** | **6 つの名前を、実物の並び順のまま表にしました。** 見出しの文言とタブの「6」も書き足しました |
| （記載なし） | **確認の途中は黄色い丸が回ります**（実測時、〈ブラウザ操作の確認（Playwright）〉が `in_progress` でした） | **「黄色い丸が回っているときは待ってから読み込み直してください」を書き足しました** |
| 「日本語のテストの名前が 5 つ並び、すべて `ok` になっています」 | 数は合っています。**ただし名前が書かれていないため、読み手は何と照合すればよいか分かりません** | **5 つの名前を CI の記録どおりに書き出し、目印（`# pass 5` ・ `# fail 0`）も書き足しました** |
| 「この PR の画面を開いてください。→ `<https://…/pull/185>`」 | **山かっこ囲みの URL は、GitHub 上では「#185」というリンクに縮まります。** 読み手はすでにその画面に居ますので、何をすればよいか分かりません | **`.../pull/185/files` を、そのまま押せる形で書きました。** 手順 1 の入口と一致させました |
| 「変更したファイル」の節が無い | 規定の型に必要です | **12 件を分類ごとに並べた節を新設し、`src/` を含まないことを明記しました** |
| 「なぜ変えたか」に Issue 番号が無い（本文冒頭の `Closes` のみ） | 規定の型に必要です | **#78 ・ #79 の題を添えて書き足しました** |
| 「法務のページが `main` に入る前は、この検査が赤でした」 | 主張どおりですが、**赤くなる中身（どのテストが、何と言って落ちるか）が書かれていません** | **実際に再現し、「1 つ目が赤になり『README に、実装が無い画面が書かれています: /terms, /privacy, /commerce』と出ます」と具体で書きました** |

## 実測した数値

**共有のデータベースへ負荷をかけないため、RSpec の全例は実行していません。** この PR は `src/` を変えていませんので、走らせる理由もありません。バックエンドの結果は CI の実行から読みました。

| 項目 | 実測 | 本文の記載（更新前） | 判断 |
|---|---|---|---|
| 文書の検査（`npm run test:docs`） | **5 例・失敗 0 件**（手元で実測） | 5 例、失敗 0 件 | 一致 |
| 束ねた検査（`npm run test:all`） | **42 例・失敗 0 件**（手元で実測。フック 23 ＋ 直書き 8 ＋ 置き場 6 ＋ 文書 5） | 42 例、失敗 0 件 | 一致。**内訳を書き足しました** |
| 直書きの文言の検査（`npm run check:hardcode`） | 検出 0 件（「日本語を含む文字列リテラルはありません。」） | 検出 0 件 | 一致 |
| Mermaid の描画 | **6 文書・19 枚すべて成功**（`mmdc` で SVG を出力。er 1 ／ class 5 ／ state 4 ／ sequence 6 ／ dfd 2 ／ usecase 1） | 「6 図すべて成功」 | **食い違い。19 枚へ直しました** |
| README ページ一覧の行数 | **10 行**（`/` ・ `/projects` ・ `/requests/new` ・ `/requests/:id` ・ `/requests/:id/result` ・ `/requests/:id/notes` ・ `/presets` ・ `/terms` ・ `/privacy` ・ `/commerce`） | 10 行 | 一致 |
| README API 一覧の行数 | **14 行**。先頭は「ログイン中の利用者」（`GET /api/v1/session`）。14 行すべてに `SPEC/api` への案内が付いています | 14 行 | 一致 |
| ER 図の表の数 | **11**（users ・ sessions ・ projects ・ presets ・ prompt_requests ・ prompt_outputs ・ evaluation_notes ・ quota_consumptions ・ admin_actions ・ rule_dictionaries ・ metric_events） | 11 | 一致 |
| ユースケースの数 | **利用者 13（UC1〜UC13）・開発者 5（UC20〜UC24）** | 利用者 13 ・ 開発者 5 | 一致 |
| 状態遷移図の数 | **4**（生成リクエスト・クォータ・規則辞書・プラン値） | 4 相当の記載 | 一致 |
| シーケンス図の数 | **6** | 6 本 | 一致 |
| 変更ファイル数 | **12 件（+1073 / -10 行）** | 記載なし | **書き足しました** |
| 自動検査の数 | **6 つ（すべて緑）** | 6 つ | 一致。**名前と並び順を書き足しました** |

**自動検査は、この PR の先頭（`2bc7219`）で 6 つとも緑です。**

| 検査の名前（実物どおり・実物の並び順） | 結果 |
|---|---|
| バックエンド（RuboCop / RSpec） | 緑 |
| フロントエンド（ESLint / 型検査 / Jest / ビルド） | 緑 |
| 本番と同じ設定での読み込み確認 | 緑 |
| 開発フックの検査 | 緑 |
| ハードコードの検査 | 緑 |
| ブラウザ操作の確認（Playwright） | 緑 |

## 検査の名前・手順の名前を実物と突き合わせた結果

〈ハードコードの検査〉のジョブ（`98531892032`）の手順を、GitHub の API から読み、本文の記載と照合しました。

| 順 | 手順の名前 | 結果 |
|---|---|---|
| 1 | Set up job | 緑 |
| 2 | Run actions/checkout@v4 | 緑 |
| 3 | Run actions/setup-node@v4 | 緑 |
| 4 | 検査の仕組み自体のテスト | 緑 |
| 5 | 直書きの文言が無いことの確認 | 緑 |
| 6 | **文書の検査** | 緑 |
| 11 | Post Run actions/setup-node@v4 | 緑 |
| 12 | Post Run actions/checkout@v4 | 緑 |
| 13 | Complete job | 緑 |

**〈文書の検査〉が〈ハードコードの検査〉のジョブの中にあることを、実物で確かめました。** 本文の確認 5 の押しどころ（〈ハードコードの検査〉→〈文書の検査〉）は正しいです。

CI の記録に出る 5 行も、そのまま照合しました。

```
ok 1 - README に書いた画面は、すべて実装されています
ok 2 - 実装した画面は、すべて README に書かれています
ok 3 - README に書いた経路は、すべて実装されています
ok 4 - SPEC の図は、すべて Mermaid のコード塀で閉じています
ok 5 - SPEC の索引は、置いてある文書をすべて挙げます
```

## 押さえが効くことの確認

**本文の「実際に効くことを確かめました」を、実際に再現しました。** 法務のページが入る前の `main`（`bf4cb46^` ＝ `737a2bf`）の作業ツリーへ、この PR の `README.md` ・ `SPEC/*.md` ・ `test/docs/docs.test.mjs` を写し取り、文書の検査を走らせました。

| 実測 | 判断 |
|---|---|
| `not ok 1 - README に書いた画面は、すべて実装されています` ／ `README に、実装が無い画面が書かれています: /terms, /privacy, /commerce。` ／ `# pass 4` ・ `# fail 1` | **主張どおりです。** 押さえは実際に噛みます |

**データベースを使わない検査ですので、共有のデータベースへ負荷をかけていません。** 写し取りは確認用の作業ツリーの中だけで行い、PR のブランチと `main` の作業ツリーへ触れていません。

## ブラウザで確かめられないこと

- **この PR の中身は文書と検査です。アプリの画面には何も現れません。** ユーザーテストは、GitHub の PR の画面で「図が絵として出ること」「README の表が埋まっていること」「新しい検査が緑であること」を確かめるものだと本文に明記しました。
- **`main` へ取り込んだ後にこの検査が走ることは、取り込みが済むまで画面で確かめられません。** 本文では `ci.yml` の設定として書くに留め、確認の手順に入れていません。
- **Mermaid の絵は `viewscreen.githubusercontent.com` の中で描かれます。** HTML には図の元の文字しか入りません。当方は Mermaid CLI で 19 枚とも SVG になることを別途実測し、その数値を本文へ書きました。

## 制約の遵守

- **削除コマンドを一切生成していません。** 確認用の作業ツリー `DELETE/pr185-checker-20260827` ・ `DELETE/pr185-checker-base-20260827` は、そのまま `DELETE/`（ゴミ箱）に残しています。
- **実装側の作業ツリー（`DELETE/impl-78`）へ触れていません。** ブランチも切り替えていません。
- **RSpec の全例を実行していません。** 共有のデータベースへ重い実行を重ねないためです。`src/` の変更がありませんので、走らせる理由もありません。
- **開発用のデータベースへ書き込んでいません。** 実行したのは `node --test` と Mermaid CLI だけです。
- コマンドはすべて `docker compose exec -T dev bash -lc '...'` で実行しました。
- **Mermaid CLI を動かすため、開発コンテナのキャッシュ（`~/.cache/puppeteer`）へ `chrome-headless-shell` を導入しました。** リポジトリのファイルは変えていません。
- 本文は一時ファイル経由（`gh pr edit 185 --body-file`）で反映しました。反映後の本文を読み直し、文字化けが無いこと（差分は末尾の改行 1 つのみ）を確かめました。
- コードレビューは行っていません（reviewer の担当です）。
- `.claude/` 直下の md の内容を、本文・記録へ転記していません。

## 申し送り

- **確認 2 ・ 3 の「（なし）」の扱いは、必ず直す必要がありました。** リッチ表示は消した跡を赤い取り消し線で残しますので、更新前の書き方（「残っていれば不合格です」）では、正常な画面を見た方が必ず「不合格」と報告します。**同じ書き方を、ほかの文書系 PR で使い回さないようご注意ください。**
- **左側のファイル一覧は、ブラウザの窓が狭いと隠れます。** 非エンジニアの方に依頼するときは、窓を広げていただくか、〈Show file tree〉の場所をあらかじめお伝えいただけますと確実です。
- **図の枚数（19 枚）は今後増えます。** 手順を使い回す場合は、枚数の読み替えが要ります。
- **`SPEC/development-flow.md` は、この PR より前からある文書です。** 索引の検査（テスト 5）はこの文書も対象にしており、`SPEC/README.md` に載っていますので緑です。
- **土台は現在の `main`（`bf4cb46`）と同じです。** 取り込み前に土台を合わせる作業は要りません。

連絡先は info@rictaworks.jp です。

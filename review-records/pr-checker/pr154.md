# PR 整備 : PR #154（3 回分をまとめた記録です）

- 対象：PR #154「[実装] モデルアダプタと 4 系統の整形を実装します」 / issue #45 ・ #46 ・ #47 ・ #48 ・ #49
- 回数：3 回（1 回目 初稿 / 2 回目 修正後の実測 / 3 回目 最新の実測）

---

# PR 整備 : PR #154

- 実施日時（JST）：2026-08-26 22:30
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/154
- 対象 issue：#45 ／ #46 ／ #47 ／ #48 ／ #49
- 対象ブランチ：`feature/issue-45-49-adapters`（`origin/main...` との差分で確認しました）
- 担当：pr-checker（コードレビューは行いません）

## 作業の前提

実装側の作業ツリー（`/home/fox-s/github/rictaworks/hero-prompt-forge`）でブランチを切り替えていません。`git checkout` ・ `git switch` ・ `git stash` を一度も実行していません。差分の確認は `git fetch origin` のうえ `git diff origin/main...origin/feature/issue-45-49-adapters` で行いました。

数値の実測には一時的な作業ツリー `.review/prcheck154` を作り、確認後に `DELETE/review-worktrees/pr154-20260826-prchecker` へ `mv` で移しました。リポジトリ直下へ一時ファイルを残していません。

## タイトル

変更していません。

```
[実装] モデルアダプタと 4 系統の整形を実装します
```

ですます調です。絵文字・個人名・秘匿情報を含みません。

## 実測して確かめたこと

| 確認したこと | 本文の記載 | 実測値 | 判定 |
|---|---|---|---|
| 変更したファイルの件数 | 7 件 | 7 件 | 一致します |
| `.tsx` を含まないこと | 含みません | 含みません | 一致します |
| アダプタのテスト | 69 件すべて成功 | 69 examples, 0 failures | 一致します |
| バックエンドのテスト全体 | 877 件すべて成功 | 877 examples, 0 failures | 一致します |
| 書き方の検査（RuboCop） | 113 ファイル・指摘 0 件 | 113 files inspected, no offenses detected | 一致します |
| 自動の検査（GitHub Actions） | 記載がありませんでした | 5 項目すべて成功 | 追記しました |

参照している issue 番号の題名も照合し、すべて実在して内容が合っていることを確かめました（#40 アンチAIルック規則、#55 生成リクエスト API、#71 入力フォーム、#73 結果3案）。

## 「実際の出力」4 件の照合

一時作業ツリー内で `Adapters::ModelAdapter` を実行し、4 系統の出力を再現しました。本文に載せた 4 件は、いずれも実物と同じ内容でした。

| 系統 | 本文（`main_prompt`） | 出さないでほしいもの（`negative_prompt`） | 設定（`parameters`） |
|---|---|---|---|
| midjourney | `a calm office, 35mm lens, clear copy space across the left third of the frame` | `deformed hands, watermark` | `--ar 16:9` ／ `--no deformed hands, watermark` |
| dalle | `A hero image for a website, composed for a 16:9 frame. A calm office. 35mm lens. Clear copy space across the left third of the frame.` | `nil`（欄がありません） | `size 16:9` |
| stable_diffusion | `a calm office, 35mm lens, (clear copy space across the left third of the frame:1.2)` | `deformed hands, watermark` | `aspect_ratio 16:9` |
| nano_banana | `A website hero image in a 16:9 frame. A calm office. 35mm lens. Clear copy space across the left third of the frame.` | `deformed hands, watermark` | `aspect_ratio 16:9` |

## 直した箇所

1. **「実際の出力」を実物どおりに書き直しました。** 元の本文は DALL-E 系と nano banana 系の出力を 2 行に折り返して載せていました。実物は 1 行です。折り返しを外して 1 行にしました。
2. **「実際の出力」に、抜けていた項目を足しました。** 元の本文は Midjourney 系だけ「設定」を載せ、他の 3 系統は設定を載せていませんでした。4 系統とも「本文」「出さないでほしいもの」「設定」の 3 項目をそろえました。DALL-E 系は欄が無いことを明記しました。
3. **入力にした指示を明記しました。** どの指示から出た出力なのかが本文からは分かりませんでした。内容・出さないでほしいもの・画面の比率を先に書きました。
4. **「変更したファイル」に行数を足しました。** 実測した追加行数（126 ／ 44 ／ 47 ／ 48 ／ 44 ／ 222 ／ 54）を載せ、既存のファイルを書き換えていないことを追記しました。
5. **「確認した結果」に自動の検査の行を足しました。** 5 項目すべて成功していることを実測して載せました。
6. **節の並びをテンプレートの順に整えました。** 「変更したファイル」を「なぜ変えたか」の直後へ移しました。
7. **ユーザーテスト手順を 3 つから 4 つへ増やし、期待結果を画面に見える文字にしました。**
   - 元の手順 2 は「緑色のチェック印が付いています」だけで、本文が主張する 877 件・113 ファイルを画面で確かめられませんでした。GitHub Actions の記録の画面に、実際に `877 examples, 0 failures` と `113 files inspected, no offenses detected` が出ることを確かめたうえで、それぞれ手順 2 と手順 3 に分けました。
   - 手順の中で押す〈テスト〉〈書き方の検査〉という項目名が、実際の画面の表示と同じであることを確かめました。
   - 元の手順 3 は「次の 4 つのファイルが並んでいます」でしたが、実際は 7 件が並びます。「全部で 7 件のファイルが並び、その中に次の 4 つがあります」に直しました。
   - 手順 1 の期待結果を「5 項目すべての左に緑色のチェック印」から「検査の名前が 5 行並び、そのすべての左に緑色のチェック印」に直し、数えるものをはっきりさせました。
   - 手順 4 の冒頭に「ブラウザの戻るボタンで PR のページへ戻ります」を足し、どの画面から始めるかが切れないようにしました。
8. **ターミナルもコマンドも使わないことを明記しました。**

## 確かめた観点

- **ですます調です。** タイトル・本文とも、だ・である調の文がありません。
- **絵文字がありません。** 本文全体を走査して確かめました。
- **個人名がありません。** 連絡先は `info@rictaworks.jp` のみです。
- **秘匿情報がありません。** `.claude/` 直下の md（CC.md ／ OWASP10.md ／ QC10.md）の内容を転記していません。ファイル名にも触れていません。
- **未実装のものを書いていません。** 画面での確認ができないことを明記し、確認できるようになる時期を issue #73 として示しています。手順を捏造していません。
- **ユーザーテストはブラウザだけで完了します。** 4 つの手順のいずれも、URL を開く・押す・読むだけです。`curl` ・環境変数・ログファイルの確認を含みません。
- **1 手順 1 操作です。** 各手順は「どこを」「どうする」「何が見えれば正解か」の 3 点セットで書いています。
- **失敗時の報告事項を末尾に書いています。** 画面の写真・URL・時刻（JST）の 3 点です。

## 反映

`gh pr edit 154 --title "..." --body-file <一時ファイル>` で反映しました。本文は UTF-8 の一時ファイル経由で渡し、文字化けが無いことを `gh pr view 154` で確かめました。

## コードレビューについて

コードレビューは行っていません。差分の良し悪し・設計の妥当性・セキュリティの指摘は reviewer の担当です。


---

# PR 整備（2回目） : PR #154

- 実施日時（JST）：2026-08-26 22:21
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/154
- タイトル：`[実装] モデルアダプタと 4 系統の整形を実装します`（変更しません。日本語で、内容と合っています）
- ブランチ：`feature/issue-45-49-adapters`
- 検証したコミット：`affe007`（`[実装] 実モデルの記法へ合わせ、貼り付けられる最終形を返します`）
- 担当：pr-checker（コードレビューは行いません。タイトル・本文の日本語化とユーザーテスト記載のみです）

## 作業の進め方

実装側の作業ツリーではブランチを切り替えません。`.review/pr154-2`（`.gitignore` 済み・dev コンテナから見える位置）へ一時的な作業ツリーを作り、`origin/feature/issue-45-49-adapters`（`affe007`）を detached で取り出して検証しました。終了後に `mv` で `DELETE/pr154-2-worktree` へ移しました。リポジトリ直下へ一時ファイルを作っていません。

## 実測した数値

すべて `affe007` の作業ツリーを dev コンテナで動かして測りました。GitHub Actions の記録とも一致します。

| 項目 | 前回の本文 | 実測 | 反映 |
|---|---|---|---|
| 変更したファイルの件数 | 7 件 | **11 件** | 直しました |
| RSpec 全体 | 877 件 | **1082 examples, 0 failures** | 直しました |
| アダプタのテスト | 69 件 | **128 examples, 0 failures** | 直しました |
| RuboCop | 113 ファイル | **129 files inspected, no offenses detected** | 直しました |
| GitHub Actions | 5 項目成功 | **5 項目成功（head_sha = affe007）** | 変わりません |

CI の記録（`https://github.com/rictaworks/hero-prompt-forge/actions/runs/32973195644`）でも `129 files inspected, no offenses detected` ／ `1082 examples, 0 failures` を確認しました。

## 変更したファイル（11 件・すべて新規）

| 分類 | ファイル | 行数 |
|---|---|---|
| 共通の約束 | `src/backend/app/services/adapters/model_adapter.rb` | 179 |
| 共通の約束 | `src/backend/app/services/adapters/formatted.rb` | 33 |
| 共通の約束 | `src/backend/app/services/adapters/adapter_rules.rb` | 80 |
| 自然文の共通部分 | `src/backend/app/services/adapters/narrative_adapter.rb` | 65 |
| Midjourney 系 | `src/backend/app/services/adapters/midjourney_adapter.rb` | 64 |
| DALL-E 系 | `src/backend/app/services/adapters/dalle_adapter.rb` | 60 |
| Stable Diffusion 系 | `src/backend/app/services/adapters/stable_diffusion_adapter.rb` | 74 |
| nano banana 系 | `src/backend/app/services/adapters/nano_banana_adapter.rb` | 37 |
| 記法の設定ファイル | `src/backend/config/model_adapters.yml` | 73 |
| テスト | `src/backend/spec/services/adapters/model_adapter_spec.rb` | 414 |
| 作業の記録 | `review-records/security-review/feature-issue-45-49-adapters.md` | 105 |

`.tsx` は 1 件も含みません。既存ファイルの書き換えはありません（すべて ADDED）。

## 「実際の出力」の実測（4 系統すべて測り直しました）

入力は次のとおりです。

- 素材：`a calm office` ／ `35mm lens` ／ `clear copy space across the left third of the frame`
- 打ち消し：`deformed hands` ／ `watermark`
- アスペクト比：`16:9`

### Midjourney 系

```
prompt          : a calm office, 35mm lens, clear copy space across the left third of the frame --ar 16:9 --no deformed hands, watermark
main_prompt     : a calm office, 35mm lens, clear copy space across the left third of the frame
negative_prompt : "deformed hands, watermark"
parameters      : {"aspect_ratio"=>"16:9"}
negative_prompt?: true
```

### DALL-E 系

```
prompt          : This is a hero image for a website, composed for a 16:9 frame. The image includes a calm office, 35mm lens, and clear copy space across the left third of the frame.
negative_prompt : nil
parameters      : {"aspect_ratio"=>"16:9", "size"=>"1792x1024"}
negative_prompt?: false
```

### Stable Diffusion 系

```
prompt          : a calm office, 35mm lens, (clear copy space across the left third of the frame:1.2)
negative_prompt : "deformed hands, watermark"
parameters      : {"aspect_ratio"=>"16:9"}
negative_prompt?: true
```

### nano banana 系

```
prompt          : This is a hero image for a website, composed for a 16:9 frame. The image includes a calm office, 35mm lens, and clear copy space across the left third of the frame.
negative_prompt : nil
parameters      : {"aspect_ratio"=>"16:9"}
negative_prompt?: false
```

### 変わった場面の実測

| 場面 | 実測した出力 |
|---|---|
| 打ち消しが 0 件（Midjourney 系） | `a calm office, clear copy space across the left third of the frame --ar 16:9`（`--no` が付きません） |
| 素材が既に `16:9` を述べている（DALL-E 系） | `This is a hero image for a website. The image includes a calm office, composed for a 16:9 frame, and clear copy space across the left third of the frame.`（比を二度言いません） |
| 強調対象に括弧・コロン（Stable Diffusion 系） | `UnsafeTermError: 重みを付ける素材に、記法で意味を持つ文字が含まれます: clear copy space (left third): 40%` |
| 余白の指定が無い（4 系統すべて） | `InvalidDraftError: コピースペースの指定がありません。` |
| 対応表に無い比 `9:16`（DALL-E 系） | `InvalidDraftError: 画像の大きさの対応がありません: 9:16` |

## 本文へ反映した 9 点

依頼どおり、非エンジニアにも分かる言い方で「直したこと（コミット `affe007`）」の節を新設し、次の 9 点を書きました。

1. そのまま貼り付けられる形で返すようにしたこと（Midjourney 系は設定を本文の最後に置かないと効かないこと）
2. 設定の名前を 4 系統で `aspect_ratio` にそろえたこと
3. DALL-E 系の画像の大きさを画素（`1792x1024`）へ直したこと。対応表に無い比は止めること
4. 打ち消しが 0 件のとき、空の指定を出さないこと
5. 文章で書く系統（DALL-E 系・nano banana 系）を述語のある文にしたこと。縦横比を二度言わないこと
6. nano banana 系は打ち消しの欄を持たない扱いに直したこと
7. 強調の指定が壊れる場合はその場で止めること
8. 余白の指定が無い場合は、どの系統でも整形しないこと
9. プロンプトへ入る英文を `src/backend/config/model_adapters.yml` へ出したこと

あわせて、冒頭の表の nano banana 系の「出さないでほしいもの」を「別の欄です」から「**欄がありません**」へ直しました。前回の本文は実装と食い違っていました。

## ユーザーテスト手順

**アプリの画面で確かめる手順はありません。** お申し込みの画面（issue #71）と受け口（issue #55）がまだ無いためです。手順を捏造せず、その旨を本文へ明記しました。

そのかわり、PR のページをブラウザで見るだけでできる確認を 5 つ書きました（前回は 4 つでした）。

1. 自動の検査 5 項目がすべて緑であること
2. テストが `1082 examples, 0 failures` であること
3. 書き方の検査が `129 files inspected, no offenses detected` であること
4. 11 件のファイルの中に 4 系統のアダプタがあること
5. **（今回追加）** `src/backend/config/model_adapters.yml` に英文がまとまっていること

いずれもターミナル・コマンド・`curl`・環境変数・ログを使いません。GitHub の画面上の日本語の項目名（〈Show all checks〉〈Details〉〈テスト〉〈書き方の検査〉〈Files changed〉）が実在することを確認済みです。

## 確認した規約

- ですます調で統一しています
- 絵文字を使っていません
- 未実装の機能を書いていません（画面がまだ無いことを明記しています）
- 個人名を書かず、連絡先を `info@rictaworks.jp` にしています
- `.claude/` 直下の md の内容を転記していません
- 本文は一時ファイル（`pr154-body.md`）経由で `gh pr edit 154 --body-file` により反映しました

## 反映

```
gh pr edit 154 --body-file <scratchpad>/pr154-body.md
=> https://github.com/rictaworks/hero-prompt-forge/pull/154
```

タイトルは変更していません。

## 後始末

一時的な作業ツリー `.review/pr154-2` を `DELETE/pr154-2-worktree` へ `mv` しました。削除コマンドは使っていません。`git worktree` の管理情報が残りますので、必要であれば手動で `git worktree prune` を実行してください。


---

# PR 整備（3回目） : PR #154

- 実施日時（JST）：2026-08-26 23:07
- 対象：https://github.com/rictaworks/hero-prompt-forge/pull/154
- 対象コミット：`0fe634a`（`[実装] 打ち消しの素材も検め、大きさの対応表を検証します`）
- 担当：pr-checker
- 結果：**タイトルと本文を更新しました。**

## 経緯

2 回目の整備は `affe007` を前提に書かれていました。その後 `bd5b632` ・ `0fe634a` の 2 コミットが積まれ、本文の記述が事実と合わなくなっていました。

レビューで挙がった指摘は次の 2 点です。

1. 本文が「いずれも新しく足したファイルで、既存のファイルを 1 行も書き換えていません」「11 件です」と書いていましたが、実際は **16 件**で、うち **3 件は既存ファイルの書き換え**でした。
2. 出力例・例外名・件数・規則辞書の版上げが未反映でした。

## 作業の環境

**実装側の作業ツリーではブランチを切り替えていません。** 一時的な作業ツリーを作って測りました。

| 項目 | 値 |
|---|---|
| 一時作業ツリー | `DELETE/review-worktrees/pr154c-20260826-prchecker`（`0fe634a` を detached で展開） |
| 実行環境 | `docker compose exec dev`（Ruby 3.3.12 / PostgreSQL 16） |
| 補助の作業ツリー | `<scratchpad>/wt154e`（ソース閲覧のみ。測定には使っていません） |

**リポジトリ直下へ一時ファイルを作っていません。** 測定用のスクリプト（`probe.rb` ／ `probe2.rb`）は一時作業ツリーの中に置きました。作業ツリーは `DELETE/review-worktrees/` 配下です。

なお、作業の途中で実装側の作業ツリーが別の作業により `feature/issue-50-variations` へ切り替わりました。**こちらからは一切触っていません。**

## 測り直した数値

**すべて `0fe634a` で実測しました。**

| 項目 | 旧本文 | 実測 | 出典 |
|---|---|---|---|
| 変更したファイル | 11 件 | **16 件**（新規 13 ／ 書き換え 3） | `git diff --name-status origin/main...0fe634a` ／ `gh pr view 154 --json changedFiles` |
| 増減 | 記載なし | **+1654 / -11** | `gh pr view 154 --json additions,deletions` |
| バックエンドのテスト全体 | 1082 件 | **1106 件、0 失敗** | `bundle exec rspec` ／ CI ログ |
| アダプタのテスト | 128 件 | **152 件、0 失敗** | `bundle exec rspec spec/services/adapters/model_adapter_spec.rb` |
| RuboCop | 129 ファイル | **131 ファイル、指摘 0 件** | `bundle exec rubocop` ／ CI ログ |
| GitHub Actions | 5 項目成功 | **5 項目成功**（変更なし） | `gh pr checks 154` |
| 規則辞書の版 | 記載なし | **`v2026.08.4`** | `config/rule_dictionary/initial.yml` |

CI（run `32977484019` / job `98205659083`）のログにも `1106 examples, 0 failures` ・ `131 files inspected, no offenses detected` が出ています。**ユーザーテストの手順で読ませる数値と、CI の実表示が一致しています。**

### 書き換えた 3 件の内訳

| ファイル | 増減 |
|---|---|
| `src/backend/app/services/generation/copy_space.rb` | +8 / -3 |
| `src/backend/config/rule_dictionary/initial.yml` | +10 / -7 |
| `src/backend/spec/services/generation/style_spec_spec.rb` | +1 / -1 |

### 新規 13 件の行数（本文の表と一致させました）

`model_adapter.rb` 195 ／ `formatted.rb` 34 ／ `adapter_rules.rb` 80 ／ `draft_guard.rb` 42 ／ `term_guard.rb` 39 ／ `narrative_adapter.rb` 90 ／ `midjourney_adapter.rb` 71 ／ `dalle_adapter.rb` 118 ／ `stable_diffusion_adapter.rb` 97 ／ `nano_banana_adapter.rb` 37 ／ `model_adapters.yml` 92 ／ `model_adapter_spec.rb` 542 ／ 作業の記録 198

## 実際の出力の測り直し

**4 系統すべてを実測し直しました。** 旧本文の出力例は、コピースペースの段を通していない素材で作られており、**実際の経路より短い**ものでした。今回は `Generation::CopySpace#apply` を通した下書きで測っています。

### 測定の条件

- 素材：`a calm office` ／ `a 35mm lens`（**規則辞書の版上げで冠詞が付いた形**です）
- 打ち消し：`deformed hands` ／ `watermark`
- 画面の比：`16:9`、余白の位置：左

コピースペースの段が足す 5 件を含め、**整形へ届く素材は 7 件**です。

```
a calm office
a 35mm lens
clear copy space across the left third of the frame
the main subject placed at the upper right intersection of a rule of thirds grid
the subject contained within the right two thirds of the frame
the left third rendered as an even low contrast surface
composed for a 16:9 wide crop
```

控えには `{:kind=>:copy_space_reserved, :position=>"left", :aspect_ratio=>"16:9", :aspect_ratio_term=>"composed for a 16:9 wide crop"}` が残ります。**指摘 4 の「比を述べる素材そのものを控えへ残す」が、実際に効いていることを確認しました。**

### 4 系統の最終形（すべて実測）

Midjourney 系。

```
a calm office, a 35mm lens, clear copy space across the left third of the frame, the main subject placed at the upper right intersection of a rule of thirds grid, the subject contained within the right two thirds of the frame, the left third rendered as an even low contrast surface, composed for a 16:9 wide crop --ar 16:9 --no deformed hands, watermark
```

DALL-E 系 ／ nano banana 系（同一の本文です）。

```
This is a hero image for a website. The image shows a calm office, a 35mm lens, clear copy space across the left third of the frame, the main subject placed at the upper right intersection of a rule of thirds grid, the subject contained within the right two thirds of the frame, and the left third rendered as an even low contrast surface. The whole frame is composed for a 16:9 wide crop.
```

**比を述べる素材が、独立した最後の 1 文になっています。** 素材の並び（`The image shows ...`）からは外れています。

Stable Diffusion 系。

```
a calm office, a 35mm lens, (clear copy space across the left third of the frame:1.2), the main subject placed at the upper right intersection of a rule of thirds grid, the subject contained within the right two thirds of the frame, the left third rendered as an even low contrast surface, composed for a 16:9 wide crop
```

**強調は 1 件だけです**（控え `emphasis_applied` の `emphasized` が 1）。

### 設定・控え

| 系統 | `parameters` | `negative_prompt` | `notes` |
|---|---|---|---|
| Midjourney | `aspect_ratio` = `16:9` | `deformed hands, watermark` | なし |
| DALL-E | `aspect_ratio` = `16:9` ／ `size` = `1792x1024` | **なし** | `size_substituted`（`16:9` ／ `1792x1024` ／ `dall-e-3`） |
| Stable Diffusion | `aspect_ratio` = `16:9` | `deformed hands, watermark` | `emphasis_applied`（1 件 ／ 重み `1.2`） |
| nano banana | `aspect_ratio` = `16:9` | **なし** | なし |

`21:9` でも測りました。本文は `The whole frame is composed for a 21:9 ultra wide crop.` に変わり、`size` は `1792x1024` のまま、控えに `size_substituted`（`21:9` ／ `1792x1024` ／ `dall-e-3`）が残ります。

### 例外の実測（旧本文の記載を差し替えました）

| 場面 | 実測した例外と文言 |
|---|---|
| 打ち消しに `--` が混ざる（Midjourney） | `UnsafeTermError : 素材に、この記法で意味を持つ文字が含まれます: 0 件目 / --` |
| 本文の素材に `--` が混ざる（Midjourney） | `UnsafeTermError : 素材に、この記法で意味を持つ文字が含まれます: 2 件目 / --` |
| 素材に丸括弧が混ざる（SD） | `UnsafeTermError : 素材に、この記法で意味を持つ文字が含まれます: 2 件目 / ( )` |
| 強調する素材にコロンが混ざる（SD） | `UnsafeTermError : 重みを付ける素材に、重みの区切りとなる文字が含まれます。` |
| 余白の指定が無い（4 系統） | `InvalidDraftError : コピースペースの指定がありません。` |
| 素材が 1 件も無い（4 系統） | `InvalidDraftError : 素材がありません。` |
| 知らない生成 AI | `UnknownModelError : 対応していない生成モデルです: String / 対応しているのは midjourney, dalle, stable_diffusion, nano_banana です` |
| 大きさの表が画素でない（DALL-E） | `InvalidDefinitionError : 画像の大きさの対応表が読めません: dalle.sizes` |
| 版の記載が無い（DALL-E） | `InvalidDefinitionError : 記法が英文ではありません: dalle.api_version` |
| 表に比が無い（DALL-E） | `InvalidDefinitionError : 画像の大きさの対応がありません: 16:9` |

**旧本文は、強調の失敗を `UnsafeTermError : 重みを付ける素材に、記法で意味を持つ文字が含まれます` と書いていました。実在しない文言です。** 丸括弧は素材の検めで先に止まり、コロンだけが重み付けの段で止まります。**2 つの別の例外を 1 つに混ぜていました。**

**旧本文は、大きさの対応が無い場合を `InvalidDraftError` と書いていました。実際は `InvalidDefinitionError` です。** また例として `9:16` を挙げていましたが、**`9:16` は入力の選択肢に無く、コピースペースの段で先に弾かれます。実際の経路では到達しません。** 到達し得る `16:9`（対応表側が壊れた場合）へ差し替えました。

## 本文へ反映した 4 点

| # | 指摘 | 反映先 |
|---|---|---|
| 1 | 打ち消しの素材も検める。Midjourney は打ち消しを本文の末尾へ付けるため、`--` が混ざると先に付けた比を上書きする | 「3 回目（`0fe634a`）」の節。`--ar 16:9 --no watermark --ar 1:1` という並びを示し、**あとから出た指定が効く**ことを書きました |
| 2 | DALL-E の大きさの表を読み込み時に検める。画素でない値・版の記載が無い場合は止まる | 同節。`dall-e-3` を明記した理由（`gpt-image-1` は `1536x1024` を受け、`1792x1024` を弾く）も書きました |
| 3 | 規則辞書を `v2026.08.4` へ上げ、レンズなどへ冠詞を足した | 「2 回目（`bd5b632`）」の節と、変更ファイルの表 |
| 4 | 余白の段が、比を述べる素材そのものを控えへ残す | 「2 回目（`bd5b632`）」の節。整形の段が独立した 1 文にすることも書きました |

## 申し送り

**issue #156「[実装] 素材の役割に応じた述語で、自然文を組み立てます」（OPEN）を本文へ書きました。**

`The image shows a 35mm lens` は「レンズが画に写っている」という含意を持ちます。レンズは撮影の手段であり、画に写るものではありません。**この PR では画面の比だけを控えから受け取って独立した 1 文にしましたが、同じ考え方を撮影の手段・被写体の置き方へも広げる必要があります。** この PR の範囲外であることを明記しました。

## ユーザーテスト手順の検め

**「非エンジニアがそのとおりに操作して、書いてある〈正解です〉が本当に出るか」という観点で、1 手順ずつ確かめました。**

| 手順 | 書いてある期待結果 | 実在の確認 |
|---|---|---|
| 1. 検査がすべて通っている | 5 行、すべて緑 | `gh pr checks 154` で 5 件すべて `pass`。**行の名前を 5 つとも本文へ列挙しました**（旧本文は数だけでした） |
| 2. テストが 1106 件成功 | `1106 examples, 0 failures` | CI ログに同一文字列を確認。ステップ名も実在（〈テスト〉） |
| 3. 指摘 0 件 | `131 files inspected, no offenses detected` | CI ログに同一文字列を確認。ステップ名も実在（〈書き方の検査〉） |
| 4. 4 系統に対応 | `16 changed files` と 4 つのファイル名 | `gh pr view 154 --json files` で 16 件。4 つのアダプタが含まれることを確認 |
| 5. 英文が設定ファイルへ出ている | `This is a hero image for a website` ／ `1792x1024` ／ `dall-e-3` | `config/model_adapters.yml` に 3 つとも実在 |
| 6. 規則辞書の版が上がっている | `- version: v2026.08.3` ／ `+ version: v2026.08.4` ／ `- 35mm lens` ／ `+ a 35mm lens` | `git diff` で 4 行とも実在。**この PR で新設した手順です** |

### 直した点

- 手順 4 の「11 件のファイルが並び」を **`16 changed files`** へ直しました。**旧本文のとおりに数えると合いません。**
- 手順 2 ・ 3 の件数を実測値へ直しました。
- 手順 1 に、緑の印が付く 5 行の名前を書き足しました。**「5 行並ぶ」だけでは、どれが欠けても気づけません。**
- 手順 5 の言い方を「1 つのファイルの中にまとまって並んでいます」から「**緑色の背景で並んでいます**」へ直しました。**〈Files changed〉の画面で実際に見える状態**に合わせました。
- 手順 6 を新設しました。**指摘 3（規則辞書の版上げ）を、ブラウザだけで確かめられるようにするためです。**
- 「画面がまだ無いので、画面での確認はできません」という一文を足しました。**手順を捏造していないことを明示するためです。**

**ターミナル・コマンド・`curl`・環境変数・ログの読み方を、手順に一切含めていません。** すべて GitHub の画面上のクリックとスクロールで完結します。

## 反映

```
gh pr edit 154 --title "[実装] モデルアダプタと 4 系統の整形を実装します" --body-file <一時ファイル>
```

- タイトルは変更なし（すでに日本語・ですます調です）。
- 本文は全面的に書き直しました。**英語のまま残っている見出し・箇条書き・自動生成文はありません。**
- コード識別子・ファイルパス・コマンド・例外名・生成 AI の名前は原文のままにしています。
- `Closes #45` 〜 `Closes #49` を維持しました。
- **`.claude/` 直下の md の内容を転記していません。**
- 連絡先は `info@rictaworks.jp` です。個人名を書いていません。
- 絵文字を使っていません。

## 後始末

一時作業ツリーを `DELETE/` へ移しました。**削除していません。**

- `DELETE/review-worktrees/pr154c-20260826-prchecker`（`DELETE/` 配下のため、そのまま残します）
- `<scratchpad>/wt154e` → `DELETE/review-worktrees/pr154e-20260826-prchecker-moved`


---


# PR 整備 : PR #150（2 回分をまとめた記録です）

- 対象：PR #150「[実装] 生存確認の窓口と管理画面の入口を実装します」 / issue #62 ・ #64
- 回数：2 回（1 回目 初稿 / 2 回目 レビュー後の修正を実測して更新）

---

# PR 整備 : PR #150

- 実施日時（JST）：2026-08-26 22:10
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/150
- 対象 issue：#62 / #64
- 枝：`feature/issue-62-64-ops` → `main`
- 担当：pr-checker（コードレビューは行いません）

## 確かめ方

実装側の作業ツリーでは枝を切り替えていません。`git fetch origin` のうえ、
`git diff origin/main...origin/feature/issue-62-64-ops` と `git show <ref>:<path>` で中身を読みました。
テストの件数は、この PR の CI（GitHub Actions）の記録を読んで確かめました。
一時的な作業ツリーは作っていませんので、`DELETE/` への移動もありません。

## 1. 数値・ファイル名・件数の照合

| 本文の記述 | 実物 | 判定 | 対応 |
|---|---|---|---|
| 変更したファイル 12 件 | 13 件（`git diff --stat` で 13 files changed） | 不一致 | 13 件へ直しました |
| 生存確認のテスト 9 件 | `spec/requests/health_spec.rb` の `it` は 9 件 | 一致 | そのままです |
| 管理画面のテスト 11 件 | `spec/requests/admin_spec.rb` の `it` は 11 件 | 一致 | そのままです |
| バックエンドのテスト全体 773 件 | CI の記録は `830 examples, 0 failures` | 不一致 | 830 件へ直しました |
| RuboCop 109 ファイル | CI の記録は `113 files inspected, no offenses detected` | 不一致 | 113 ファイルへ直しました |
| 自動の検査 5 項目 | `gh pr checks 150` は 5 項目すべて pass | 一致 | そのままです |

### 773 件・109 ファイルがずれた理由

この枝の分岐後に `main` へ PR #145（68dd5a7）が入り、テストが増えています。
773 件・109 ファイルは**枝を単独で調べた時点の数**で、CI が調べる「`main` へ合わせた状態」の数は 830 件・113 ファイルです。
本文には直した数を書いたうえで、**ずれた理由と元の数も併記**しました。数を黙って差し替えると、書いた側の記録と食い違って見えるためです。

### ファイルの一覧の直し

本文は `管理画面の見た目（2 件）` の行に片方だけを書き、もう片方を「ほか」で省いていました。
省くと照合ができませんので、2 行に分けて両方を書きました。

- `src/backend/app/views/layouts/admin.html.erb`（外枠）
- `src/backend/app/views/admin/dashboard/show.html.erb`（中身）

13 件の内訳は `git diff --stat` の 13 行と 1 対 1 で対応します。

## 2. 表現・秘匿情報・未実装の記述

| 見たこと | 結果 |
|---|---|
| タイトルが日本語で、ですます調であること | 満たしています（変更なし） |
| 本文がすべて日本語で、ですます調であること | 満たしています |
| 絵文字が無いこと | 有りません（機械で走査して確かめました） |
| 個人名が無いこと | 有りません。連絡先は `info@rictaworks.jp` です |
| `.claude/` 直下の md の内容が転記されていないこと | 転記は有りません |
| 鍵の値そのものが本文に書かれていないこと | 書かれていません（名前だけです） |
| 未実装のものを実装済みとして書いていないこと | 書いていません。#65〜#68 は「これから足す画面」の表に分けてあります |

### 「まだ無い画面への案内を出していない」の事実確認

実物のビューを読んで確かめました。

- `src/backend/app/views/layouts/admin.html.erb`：`<header>` に見出しが 1 行あるだけで、メニューもリンクも有りません。
- `src/backend/app/views/admin/dashboard/show.html.erb`：見出しと説明の文が 1 行ずつで、`link_to` も `<a>` も有りません。
- `src/backend/config/routes.rb`：`namespace :admin` の下は `root to: 'dashboard#show'` の 1 本だけです。

**押せる入口は 1 つも有りません**ので、記述はおおむね事実です。
ただし `ja.yml` の `admin.dashboard.description` に
「規則辞書の編集・利用者の管理・クォータのリセット・利用状況の集計は、順次ここへ足します。」
という**説明の文**が有ります。これは押せませんが、言葉としては案内にあたります。

そこで見出しを「まだ無い画面への**リンク**を置きません」へ改め、
「画面にもメニューにもリンクを置いていない」「説明の文が 1 行あるだけで押せる先は無い」と、
実物どおりに書き分けました。**「案内を出しません」のままでは、説明の文と食い違って見えるためです。**

### 参照している issue 番号の確認

#62 / #64 / #65 / #66 / #67 / #68 / #69 をすべて照会し、本文の説明と表題が合うことを確かめました。#69 の表題も本文の引用どおりです。

## 3. 非エンジニア向けユーザーテスト手順

### 前提の確認

この PR は `.tsx` を 1 件も含まず、追加したのは監視用の窓口と開発者用の管理画面です。
**お客さまの画面で確かめられる変更は入っていません。** 本文がその旨を明記して手順を捏造していない点は、正しい書き方です。
そのうえで、PR のページだけで済む確認を 3 つ置く構成を残しました。

### 手順 3（`.env.example` を読む確認）の見直し

**元の書き方には無理が有りました。**

- 「`.env.example` という名前のファイルを探し、その中身を読みます」の 1 手順に、探す・開く・読む の 3 操作が入っていました。
- 「`ADMIN_BASIC_AUTH_USER=` の右側に何も書かれていない」を確かめるには、
  設定ファイルの書式（名前と値を `=` で区切る決まり）を知っている必要が有ります。
  ブラウザしか使わない方にとっては、**読み取りではなく解釈**を求める手順です。

そこで、**解釈を求めない見比べ**に直しました。

1. 〈Files changed〉タブを押します。
2. 左の一覧から `.env.example` を押します。
3. 緑色の背景が付いた行のうち `ADMIN_` で始まる 2 行を探します。
4. その 2 行を、本文に載せた見本と見比べます。

見本を本文に直接載せましたので、**同じかどうかを見るだけ**で判定できます。
あわせて「`=` の右側に文字が並んでいたら、その場で止めて知らせてください」という
失敗時の分かれ道も、具体例つきで書きました。**何が異常かを本人が判断せずに済むようにするためです。**

### 手順全体の見直し

- 1 手順 1 操作になっているかを、手順 1〜3 のすべてで数え直しました。
- 「準備」に **「こうなれば正解です」** を足しました。元は正しいページかどうかの判定が本文に埋もれていました。
- 手順 2 の「手順 1 で開いた一覧」を「手順 1 で出した一覧」へ直しました。手順 1 で押すのは〈Show all checks〉で、別の画面を開くわけではないためです。
- 「ターミナルもコマンドも使いません」と冒頭に明記しました。
- 期待結果はすべて画面に見えるもの（緑色のチェック印・見本と同じ行）で書かれています。ステータスコードを期待結果にしている箇所は有りません。
- 末尾の報告事項（画面の写真・URL・時刻（JST））は元から有り、そのまま残しました。

## 反映

```
gh pr edit 150 --title "[実装] ヘルスチェックと、管理画面の BASIC 認証を実装します" \
  --body-file <一時ファイル>
```

本文は一時ファイル（UTF-8）で渡しました。タイトルは元のままで問題が無いため、内容を変えていません。
反映後に本文を取り直し、「13 件」「830 件」「113 ファイル」が入っていることと、絵文字が無いことを機械で確かめました。

## 残る申し送り

- 830 件・113 ファイルは、この PR の CI が出した数です。`main` がさらに進むと数が変わります。**マージ直前に数を書き換える運用にはしません。** 本文にその旨（`main` へ合わせた状態での数であること）を書いてあります。
- 手順 3 は、リポジトリの中身を見る確認です。**画面の確認では有りません。** お客さまの画面での確認は issue #69 以降になる旨を本文に残しました。


---

# PR 整備（2回目） : PR #150

- 実施日時（JST）：2026-08-26 22:35
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/150
- 対象 issue：#62 / #64
- 枝：`feature/issue-62-64-ops` → `main`
- 追加のコミット：`40e3179`（管理画面の CSRF 対策）／`910af09`（`assume_ssl` の無効化と TLS への転送）
- 担当：pr-checker（コードレビューは行いません）

## 作業のしかた

実装側の作業ツリーでは枝を切り替えていません。`git fetch origin` のうえ、
`git diff origin/main...origin/feature/issue-62-64-ops` と `git show <ref>:<path>` で中身を読みました。
**一時的な作業ツリーは作っていません。** 数値は、この PR の CI（GitHub Actions）の記録を読んで確かめました。
リポジトリ直下に一時ファイルを作っていません。作業前に `git status --short` を見て、作業ツリーが空であることを確かめています。

なお、前回に指摘のあった `review-pr150-tmp/` は当方の作業では作っていませんが、
今回は一時ファイルをすべてスクラッチパッド（`/tmp/claude-1000/.../scratchpad`）の中だけに置きました。
`DELETE/agent-temp-2026-08-26/` の存在を確認しています。

## 1. 数値の実測と直し

前回の本文（1回目の整備後）と、今回の実物を並べます。

| 本文の記述 | 前回の値 | 実測 | 対応 |
|---|---|---|---|
| 変更したファイル | 13 件 | 17 件（`git diff --stat` が 17 files changed） | 17 件へ直しました |
| 生存確認のテスト（`health_spec.rb`） | 9 件 | 14 件 | 14 件へ直しました |
| 管理画面のテスト（`admin_spec.rb`） | 11 件 | 16 件 | 16 件へ直しました |
| 文言のテスト（`locales_spec.rb`） | 記載なし | 7 件 | 行を足しました |
| バックエンドのテスト全体 | 830 件 | CI の記録は `840 examples, 0 failures` | 840 件へ直しました |
| RuboCop | 113 ファイル | CI の記録は `114 files inspected, no offenses detected` | 114 ファイルへ直しました |
| 自動の検査の項目 | 5 項目 | `gh pr checks 150` は 5 項目すべて pass | そのままです |

### 数の整合

- テストの増分は `health_spec.rb` が +5、`admin_spec.rb` が +5 の合わせて **+10** です。
  全体の件数も 830 件 → 840 件で **+10** ですので、増分が一致します。
- RuboCop のファイル数は +1 です。追加されたコードのファイルは
  `src/backend/app/controllers/admin/spec_protected_controller.rb` の 1 件で、
  ほかの追加は `docker-compose.yml`（YAML）と設定ファイルの書き換えですので、+1 と合います。
- 各テストファイルの件数は `it` の並びを数えて確かめました。
  `health_spec.rb` は 4 + 4 + 4 + 2 = 14、`admin_spec.rb` は 2 + 3 + 3 + 2 + 5 + 1 = 16 です。
  共有の例（`shared_examples`）や 1 行形式の `it { }` は使われていませんので、数え上げと実行件数が一致します。

### 変更したファイル 17 件の内訳

前回の 13 件に、次の 4 件が加わりました。

| 増えたファイル | 何のためか |
|---|---|
| `docker-compose.yml` | 開発環境へ管理画面の資格情報を渡す口を足します |
| `src/backend/app/controllers/admin/spec_protected_controller.rb` | 書き込みの守りを確かめるための入口です |
| `src/backend/config/application.rb` | 守りが働くための土台（セッション）を組み込みます |
| `src/backend/config/environments/production.rb` | 本番の通信を暗号化へ転送します |

本文の一覧を 17 行にし、`git diff --stat` の 17 行と 1 対 1 で照合できるようにしました。

## 2. 追加分の内容を本文へ反映

「レビューで見つかり、この PR の中で直したこと」という節を新しく作り、5 点を書きました。
専門用語を避け、**何が起きるとまずいのか**が分かる書き方にしました。

1. **なりすまし書き込みへの守りが働いていなかったこと。** 「書いてはあるのに効かない書き方だった」「印の無い書き込みが通る状態だった」「いまは書き込みの入口が無いので実害は出ていない」「設定の編集画面（#65）が書き込みを足した瞬間に、守りが無いまま本番へ出る」ところまで書きました。**実害の有無をぼかしません。**
2. **本番で、暗号化されていない通信を暗号化へ転送すること。** 「合言葉が読み取れる形で流れる」という理由と、**生存確認だけは平文でも答える**例外、そしてその理由（監視が追従できないと、稼働しているのに落ちていると判定される）を書きました。
3. **転送が一度も起きない設定になっていたこと**を直した旨。
4. `docker-compose.yml` への受け渡しの口。**値そのものは書き込まない**旨を添えました。
5. 待ち受ける番号の確認を、**書きぶりの確認から実測へ**改めた旨。

## 3. 未実装のもの・事実と違う記述の確認

### 検証用の入口を「本番にある入口」として書かないこと

`src/backend/app/controllers/admin/spec_protected_controller.rb` は、
`config/routes.rb` に登録されていません。`admin_spec.rb` の中で
`Rails.application.routes.draw` により**テストの間だけ**割り当てられ、
`after` で `reload_routes!` により元へ戻ります。

本文では **「テストのときだけ現れる検証用の入口」「この入口は本番の一覧に現れません」** と書き、
変更したファイルの一覧でも同じ但し書きを付けました。
**外から触れる入口が増えたと読めてはいけないためです。**

### 生存確認の例外の範囲

`production.rb` の除外は `['/up', '/health']` の 2 つです。
本文は「生存確認の窓口だけは、暗号化なしでも答えます」と書いています。
`/up`（アプリの起動確認）も `/health`（保管庫まで含む確認）も、どちらも生存確認の窓口ですので、記述と実物は合っています。
**管理画面が除外に入っていないこと**も確かめました（`admin_spec` と同じ趣旨のテストが `health_spec` にあります）。

### そのほか

| 見たこと | 結果 |
|---|---|
| タイトルが日本語で、ですます調であること | 満たしています（変更なし） |
| 本文がすべて日本語で、ですます調であること | 満たしています |
| 絵文字が無いこと | 有りません（機械で走査して確かめました） |
| 個人名が無いこと | 有りません。連絡先は `info@rictaworks.jp` です |
| `.claude/` 直下の md の内容が転記されていないこと | 転記は有りません |
| 鍵の値そのものが本文に書かれていないこと | 書かれていません（名前だけです） |
| 未実装のものを実装済みとして書いていないこと | 書いていません |
| まだ無い画面へのリンクが無いこと | ビュー・レイアウト・ルーティングを読み直し、リンクが 1 つも無いことを再確認しました |

## 4. 非エンジニア向けユーザーテスト手順

### 手順の構成は変えていません

追加された 2 コミットは、**設定と守りの変更だけ**です。`.tsx` は 1 件も含まれず、
お客さまの画面に見えるものは増えていません。**画面で確かめられる手順を新しく作れませんので、作りませんでした。**
本文の「お客さまの画面で確かめる手順がありません。手順が無いことが、この PR の正しい状態です」は、いまも事実です。

### 足したこと

- 手順 1 に、5 項目の中に〈本番と同じ設定での読み込み確認〉が含まれること、
  **本番と同じ設定でアプリが立ち上がることを機械が確かめている**ことを 1 行足しました。
- 手順 2 に、**「印の無い書き込みが止まること」と「本番では暗号化されていない通信を暗号化へ転送すること」のテストがこの中に入っている**ことを足しました。
  **どちらも画面には出ない守りですので、機械の検査で確かめる**という理由も添えました。
  新しく直したことが、どの緑の印で担保されているのかを結び付けるためです。

### 前回の見直しを維持していること

- 1 手順 1 操作であることを、手順 1〜3 のすべてで数え直しました。
- 手順 3（`.env.example`）は、前回の「見本と見比べる」形のまま残しました。
  設定ファイルの書式を解釈させず、**同じかどうかを見るだけ**で判定できます。
- 期待結果はすべて画面に見えるもの（緑色のチェック印・見本と同じ行）です。
- 準備に「こうなれば正解です」があり、末尾に報告事項（画面の写真・URL・時刻（JST））があります。

## 反映

```
gh pr edit 150 --body-file <スクラッチパッドの一時ファイル>
```

タイトルは前回のままで問題が無いため、変えていません（`--title` を渡していません）。
反映後に本文を取り直し、「17 件です」「840 件」「114 ファイル」「14 件すべて」「16 件すべて」「7 件すべて」が入っていること、
古い数（773 / 109 / 830 / 113 / 13 件）が残っていないこと、絵文字が無いことを機械で確かめました。

## 残る申し送り

- 840 件・114 ファイルは、この PR の CI が出した数です。`main` がさらに進むと数が変わります。**マージ直前に数を書き換える運用にはしません。** 本文にその旨を書いてあります。
- `SpecProtectedController` は本番の経路表に現れませんが、**アプリの中には置かれます。** 経路が増えていないことは、今後の PR でも確かめる対象になります。

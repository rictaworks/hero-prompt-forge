# PR 整備 : PR #159（2 回分をまとめた記録です）

- 対象：PR #159「[実装] アートディレクションノートを実装します」 / issue #51

---

# PR 整備 : PR #159

- 実施日時（JST）：2026-08-27 00:20
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/159
- ブランチ：`feature/issue-51-art-direction-note`
- 対象コミット：`fcd1924163ab63f0421ab9772b5bcb26ec0c91c9`
- 比較の基点：`origin/main` = `eae6635cc040dc9a32ab8197133979d83e322d03`
- 作業ツリー：`.review/pr159-20260827-prchecker` に作り、完了後に `DELETE/review-worktrees/pr159-20260827-prchecker` へ移しました。実装側の作業ツリー（`feature/issue-146-assembly`）はブランチを切り替えていません。

## 1. 数値の実測

| 項目 | 本文の記載（整備前） | 実測 | 判定 |
|---|---|---|---|
| 変更したファイルの数 | 5 件 | 5 件（`git diff --name-only origin/main...fcd1924`） | 一致 |
| 追加・削除の行数 | 記載なし | 455 追加・0 削除 | 追記しました |
| アートディレクションノートのテスト | 18 件 | 18 examples, 0 failures | 一致 |
| RSpec 全体（バックエンド） | 1,303 件 | 1303 examples, 0 failures | 一致 |
| RuboCop | 150 ファイル・指摘 0 件 | 150 files inspected, no offenses detected | 一致 |

実行した経路です。dev コンテナ（`hero-prompt-forge-dev-1`）の中で、作業ツリーの `src/backend` を作業場所にして実行しました。

- `bin/rails db:test:prepare`
- `bundle exec rspec spec/services/generation/art_direction_note_spec.rb`
- `bundle exec rspec`
- `bundle exec rubocop`

**数値はすべて元の記載と一致しました。** ただし RuboCop はバックエンドだけを調べたものですので、本文へ「バックエンド」と明記しました。

## 2. 「実際に出る内容」の実測

実際の経路（`RuleEngine` → `StyleSpec` → `CopySpace` → `ConflictResolver` → `ArtDirectionNote`）で下書きを組み立て、出力をそのまま採りました。共通の入力は 業種 `saas`・スタイル `photoreal`・トーン `trust`・余白 `left`・縦横比 `16:9` です。

### 見つかった食い違い（最も重い直し）

**整備前の本文は、既定の状態の見本として「deep teal（#0E7C7B）は、AI っぽさを避ける規則に当たったため、ほのかに感じる程度まで弱めて残しました。」を載せていました。これは既定では出ません。**

- 現在の規則辞書 `config/rule_dictionary/initial.yml`（`v2026.08.4`）の `forbidden_terms` は `purple to teal gradient` などの句だけで、`teal` のような単語を持ちません。
- 定義ファイルのコメント自身が「短い語を足さないでください」と禁じています。
- したがって `BrandColorIntegration#build` の `forbidden_match` は当たらず、強さは `ACCENT` のままです。

実測（既定の辞書・`#0E7C7B` 1 色）の「この案で調整したこと」です。

```
deep teal（#0E7C7B）は、主役に添えるアクセントとして入れました。
全体の雰囲気の指定は、文字を置く余白を飾らないように控えめにしました。
```

**弱めた場合の見本は、条件を明記したうえで別立てにしました。** 規則辞書の `forbidden_terms` に `teal` を持たせた辞書で実測した結果です。

```
deep teal（#0E7C7B）は、AI っぽさを避ける規則に当たったため、ほのかに感じる程度まで弱めて残しました。
全体の雰囲気の指定は、文字を置く余白を飾らないように控えめにしました。
```

### そのほかの直し

- **「確かめること」は常に 4 件です。** 整備前の本文は 4 件を並べていましたが、件数を書いていませんでした。`checkpoints_for` は `copy_space` ・ `brand_color` ・ `cliche` ・ `person_safety` の 4 件を返し、`person_safety` は入っていない場合も「入れていません」の文で必ず出ます。本文へ「常に 4 件出ます」と書きました。
- **整備前の本文は、文を任意の位置で改行して載せていました。** 実物は 1 件が 1 段落です。実測のとおりに直しました。
- **`the subject seen from behindという指示で` は、実測がそのままこの形です。** 直さず、そのまま貼りました（本文は実測を写す場所ですので、こちらでは整えません）。
- スタイル系統の配色を弱めた一文（`style_palette_weakened`）は、`photoreal` では出ませんでした。整備前の本文にも書かれていませんでしたので、そのままにしました。
- 見本を 5 つへ増やしました。1: 色 1 つ（既定）／ 2: 弱めた場合／ 3: 色 2 つ（`orange`・`deep purple`）／ 4: 色の指定なし（対象外）／ 5: 人物の指示が入らない業種（`ecommerce`）。

## 3. ユーザーテスト手順の直し

**非エンジニアがそのとおりに操作して、書いてある「正解です」が本当に出るかを見ました。**

| 手順 | 整備前 | 実測・判定 | 直した内容 |
|---|---|---|---|
| 1. 検査が通っていること | 「5 項目すべてに緑のチェック印」 | `gh pr checks 159` で 5 件・すべて pass。GitHub の枠は〈All checks have passed〉／〈Show all checks〉 | **正しい。** 見つけやすいよう、5 件の検査名を本文へ並べました |
| 2. 確かめる観点が 3 つ以上 | 「`checkpoints:` の下に `copy_space` ・ `brand_color` ・ `cliche` の 3 つが並んでいます」 | **出ません。** `ja.yml` の `checkpoints:` の下には 7 個の鍵（`copy_space` ・ `copy_space_missing` ・ `brand_color` ・ `brand_color_absent` ・ `cliche` ・ `person_safety` ・ `person_safety_skipped`）が並びます。数えた人は 7 と数えます。加えて `headings:` の下にも `checkpoints:` があり、どちらを見ればよいか決まりません | **差し替えました。** ブラウザの検索（`Ctrl` と `F`）で日本語の見出し 3 つ（`文字を置く余白の読みやすさ` ・ `ブランドカラーの再現度` ・ `AI っぽさの混入`）を探す手順にしました |
| 3. 文言が実装の中に無いこと | 「`art_direction_note.rb` に日本語の文が 1 つも書かれていません（コメントを除きます）」 | **出ません。** 同ファイルは日本語のコメントで埋まっており、非エンジニアにコメントとコードの区別を求めます。さらに `raise InvalidDraftError, "下書きを渡してください: ..."` という日本語の文字列があり、字面のとおりでは不正解になります | **差し替えました。** 「弱めたことをお伝えする文言が入っていること」を、`弱めて残しました` の検索で確かめる手順にしました（issue #51 の受け入れ条件そのものです） |
| （新設）変えたファイルの数 | なし | 〈Files changed〉の見出しは「Showing 5 changed files with 455 additions and 0 deletions.」 | 手順 2 として足しました。`.tsx` が無いことも同じ画面で確かめられます |

**「画面で確かめられません」という説明の位置も直しました。** 整備前は手順のあとに置かれていましたので、手順の前へ移し、理由（issue #73・#74 の画面がまだ無いこと）を先に読めるようにしました。

## 4. そのほかの確認

- 引用している issue 番号と題名を照合しました。#51 / #73 / #74 / #146 / #147 は一致しました。
- **#47 は「[実装] DALL-E 系アダプタを作る」でした。** 整備前の本文は「整形の段（issue #47）」と書いていましたので、「DALL-E 系アダプタ（issue #47）」へ直しました。`app/services/adapters/dalle_adapter.rb` に `SIZE_SUBSTITUTED_NOTE_KIND` があることを確かめています。
- タイトルは「[実装] アートディレクションノートを実装します」で、すでに日本語・ですます調です。**変えていません。**
- 本文は最後まで日本語で、英語のまま残った見出し・自動生成文はありませんでした。プロンプトへ渡す英文（`the subject seen from behind` 等）と GitHub の画面に出る英語（`Show all checks` 等）だけを残しています。
- 連絡先は `info@rictaworks.jp` です。個人名を書いていません。
- `.claude/` 直下の md の内容は転記していません。
- 未実装の機能を書いていません。まだ足していない一文（`person_safety_from_project` ・ `size_substituted`）は「申し送り」に、文言だけを用意してある旨として残しました。

## 5. 反映

```
gh pr edit 159 --title "[実装] アートディレクションノートを実装します" \
  --body-file <一時ファイル>
```

反映後に `gh pr view 159` で読み直し、数値（455・1,303・150・18・5）が本文へ入っていることを確かめました。

## 6. 後始末

- 一時の作業ツリーを `DELETE/review-worktrees/pr159-20260827-prchecker` へ移しました。**削除していません。**
- 実測に使った台本は、その作業ツリーの中（`src/backend/tmp/prchecker/note_dump.rb`）に置いたまま一緒に移しました。
- リポジトリ直下へ一時ファイルを作っていません。`.env` ・ `config/master.key` を複製していません。
- 実装側の作業ツリーは `feature/issue-146-assembly` のままで、`git status` は空です。


---

# PR 整備（2回目） : PR #159

- 実施日時（JST）：2026-08-27 00:31
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/159
- ブランチ：`feature/issue-51-art-direction-note`
- 対象コミット：`93b0f4995a74950ad6e630465cc4bef12838418f`（1 回目は `fcd1924`）
- 比較の基点：`origin/main` = `eae6635cc040dc9a32ab8197133979d83e322d03`
- 追加されたコミット：`93b0f49` [実装] 弱めた色と人物の理由を分けて伝え、既定へ寄せません
- 作業ツリー：`.review/pr159b-20260827-prchecker` に作り、完了後に `DELETE/review-worktrees/pr159b-20260827-prchecker` へ移しました。実装側の作業ツリー（`feature/issue-146-assembly`）はブランチを切り替えていません。

## 1. 数値の実測

| 項目 | 1 回目（`fcd1924`） | 今回（`93b0f49`） | 本文へ反映 |
|---|---|---|---|
| 変更したファイルの数 | 5 件 | **7 件** | しました |
| 追加・削除の行数 | 455 追加・0 削除 | **695 追加・10 削除** | しました |
| アートディレクションノートのテスト | 18 件 | **29 件**（0 failures） | しました |
| RSpec 全体（バックエンド） | 1,303 件 | **1,314 件**（0 failures） | しました |
| RuboCop | 150 ファイル・指摘 0 件 | **151 ファイル・指摘 0 件** | しました |

実行した経路です。dev コンテナ（`hero-prompt-forge-dev-1`）の中で、作業ツリーの `src/backend` を作業場所にして実行しました。

- `bin/rails db:test:prepare`
- `bundle exec rspec spec/services/generation/art_direction_note_spec.rb` → `29 examples, 0 failures`
- `bundle exec rspec` → `1314 examples, 0 failures`
- `bundle exec rubocop` → `151 files inspected, no offenses detected`

**増えたファイルは 2 件です。** `src/backend/app/services/generation/note_checkpoints.rb`（新規）と `src/backend/spec/config/locales_spec.rb`（変更）です。本文の「変更したファイル」の表へ 2 行足しました。

**テストは 18 件から 29 件へ、11 件増えました。** 依頼の文面は「3 案へ展開した案を通すテストを 6 件足しました」でしたが、実測では 3 案の節が 6 件、それ以外（弱めた色の尋ね方 2 件・人物の理由 2 件・節の見出し 1 件）が 5 件で、合わせて 11 件でした。**本文には合計の 29 件だけを書き、内訳は書いていません。**

## 2. 「実際に出る内容」の実測

実際の経路（`RuleEngine` → `StyleSpec` → `CopySpace` → `ConflictResolver` → `ArtDirectionNote`）で下書きを作り、3 案の場合は `VariationExpander` を挟んで通しました。共通の入力は 業種 `saas`・スタイル `photoreal`・トーン `trust`・余白 `left`・縦横比 `16:9` です。

**本文の見本を 6 つへ組み直しました。**

| 見本 | 内容 | 1 回目からの扱い |
|---|---|---|
| 1 | 色 1 つ（既定の辞書） | そのまま（実測一致） |
| 2 | **弱めた場合** | **文が変わったため差し替え** |
| 3 | **弱めた色 + そのまま入れた色が混ざる場合** | **新設** |
| 4 | **3 案へ展開した場合（3 案目＝抽象背景を含む）** | **新設** |
| 5 | **人物を入れなかった理由の 3 通り** | **新設**（1 回目は 1 通りだけ） |
| 6 | 色の指定なし（対象外） | そのまま（実測一致） |

### 弱めた場合（見本 2）

**「確かめること」の文が入れ替わりました。** 1 回目の本文は、弱めた色にも「アクセントとして現れていますか」と尋ねる文が出ていました。今回の実測です。

```
deep teal は、AI っぽさを避ける規則に当たったため弱めています。ほのかに感じる程度に留まっていますか。強く出ている場合はお知らせください。
```

「調整したこと」の側は変わっていません（`deep teal（#0E7C7B）は、…ほのかに感じる程度まで弱めて残しました。`）。

**弱まるのは、規則辞書の `forbidden_terms` に色の語が入っている場合だけです。** 現在の `config/rule_dictionary/initial.yml`（`v2026.08.4`）は句だけを持ちますので、既定では弱まりません。**本文には、この条件を明記したうえで載せました。**

### 混ざる場合（見本 3）

`labels.sentence_separator`（半角空白）でつながり、1 つの文として出ます。実測です。

```
指定いただいた orange が、絵の中にアクセントとして現れていますか。画面全体を塗る色になっていませんか。 deep teal は、AI っぽさを避ける規則に当たったため弱めています。ほのかに感じる程度に留まっていますか。強く出ている場合はお知らせください。
```

### 3 案へ展開した場合（見本 4）

3 案とも「確かめること」は 4 件でした。**違うのは人物の写り方だけです。**

- 1 案目：`the subject seen from behind`
- 2 案目：`hands cropped out of the frame`
- 3 案目（抽象背景）：`この案は具体物を置かないため、人物の指示を外しています。人物が写っている場合は、この案を使わないでください。`

3 案目の「調整したこと」は 6 件でした。**日本語の呼び名で出ることを実測しました。**

```
この案は具体物を置かないため、人物の写り方の指示を外しました。
この案は具体物を置かないため、被写界深度の指示を外しました。
この案は具体物を置かないため、被写体の位置の指示を外しました。
この案は具体物を置かないため、被写体の収まりの指示を外しました。
deep teal（#0E7C7B）は、主役に添えるアクセントとして入れました。
全体の雰囲気の指定は、文字を置く余白を飾らないように控えめにしました。
```

`depth_of_field` などの開発者向けの名前は出ませんでした。

### 人物の理由（見本 5）

- 業種の見込み（`ecommerce`）：`この業種では人物が写らない見込みのため、…`
- スタイルに定めが無い（`illustration`）：`この絵のスタイルには、人物の崩れを避ける指示の定めがありません。…`
- 具体物を置かない（3 案目）：`この案は具体物を置かないため、人物の指示を外しています。…`

### 件数についての直し

**1 回目の本文は「常に 4 件出ます」と書いていました。今回の実装では、常にではありません。** `NoteCheckpoints#person_safety` は、当てた控え・外した控え・見送った控えのいずれも無い場合に `nil` を返します。余白の指定が無い素の下書きで実測したところ、**3 件**でした。本文を「通常は 4 件出ます」へ直しました。

### その他

- `the subject seen from behindという指示で` は、実測がそのままこの形です。**直さず、そのまま貼りました**（本文は実測を写す場所ですので、こちらでは整えません）。
- 節の見出し（`headings`）が結果に含まれるようになりましたので、本文の冒頭へ「節の見出しも一緒にお返しします」と書きました。

## 3. 本文の記述で、現物と食い違っていたところ

**1 回目の本文の「申し送り」が、現物と合わなくなっていました。**

- 1 回目は「人物の見込みをサイトの設定で上書きした場合の一文（`adjustments.person_safety_from_project`）と、画像の大きさをすり替えた場合の一文（`adjustments.size_substituted`）を、文言だけ先に用意してあります」と書いていました。
- **`93b0f49` で、この 2 つの文言は `ja.yml` から外されました**（依頼の 6 点目「使われていない文言を外し」）。`grep` で確認しました。`ja.yml` に残っていません。
- したがって「文言だけ用意してある」は事実でなくなりましたので、**この 2 項目を本文から外しました。** 未実装のものを書かないためです。
- 依頼のあった 3 点（issue #161・英文の素材・固有名詞の控え）へ差し替えました。issue #161 は「[不具合] 規則の適用の時点で、まだ素材がありません」で、実在を確かめました。

**なお `app/services/generation/adjustment_list.rb` のクラスコメントには、まだ `adjustments.person_safety_from_project` を「用意してあります」と書いた行が残っています**（14 行目）。**これはコードの中の記述ですので、こちらでは触っていません。** reviewer へお渡しします。

## 4. ユーザーテスト手順の直し

**非エンジニアがそのとおりに操作して、書いてある「正解です」が本当に出るかを見ました。**

| 手順 | 前回 | 実測・判定 | 直した内容 |
|---|---|---|---|
| 1. 検査が通っていること | 5 件の検査名を並べて「すべて緑のチェック印」 | **`93b0f49` に対する検査が、まだ 1 件も走っていません。** `gh api .../commits/93b0f49/check-runs` は空、`gh pr checks 159` は「no checks reported」、`statusCheckRollup` も空です。約 5 分間、20〜30 秒ごとに 16 回問い合わせましたが変わりませんでした。この状態では、枠そのものが画面に出ません | **枠が見当たらない場合の逃げ道を足しました。**「まだ検査が終わっていません。5 分ほど待って再読み込みしてください。それでも出ない場合は『うまくいかなかったとき』へ」と書きました。検査名 5 件はそのまま残しました（`ci.yml` のジョブ名と一致することを確認済みです） |
| 2. 変えたファイルの数 | 「Showing 5 changed files with 455 additions and 0 deletions.」 | **数が変わりました**（7 / 695 / 10） | 差し替えました |
| 3. 確かめる観点 3 点 | 日本語の見出し 3 つをブラウザの検索で探す | **そのまま出ます。** `headings` の下に `文字を置く余白の読みやすさ` ・ `ブランドカラーの再現度` ・ `AI っぽさの混入` が残っていることを確認しました | そのまま |
| 4. 弱めたことの文言 | `弱めて残しました` を探す | 出ますが、**今回の直しの中心は「逆のことを尋ねない」ことです** | **探す語を `ほのかに感じる程度に留まっていますか` へ差し替えました。** 期待する行を `%{colors} は、…` の形で書きました（`ja.yml` の実物と一字一句同じです） |
| 5.（新設）人物の理由が 3 通り | なし | `この業種では人物が写らない見込み` ・ `この絵のスタイルには、人物の崩れを避ける指示の定めがありません` ・ `この案は具体物を置かないため、人物の指示を外しています` の 3 行が `ja.yml` にあります | 手順 5 として足しました |

**すべてブラウザの操作だけで終わります。** ターミナル・コマンド・ログの確認は 1 つも含みません。

**画面で確かめられない旨は、手順の前に置いたままにしました。** 理由（issue #73・#74 の画面がまだ無いこと）を先に読めます。

## 5. そのほかの確認

- 引用している issue 番号と題名を照合しました。#50（CLOSED・バリエーション3案の展開）・#51・#73・#74・#161 は一致しました。
- タイトルは「[実装] アートディレクションノートを実装します」のままです。**変えていません。** PR 全体の内容を表しており、日本語・ですます調です。
- 本文は最後まで日本語です。英語のまま残しているのは、プロンプトへ渡す英文（`the subject seen from behind` 等）と GitHub の画面に出る英語（`Show all checks` ・ `Files changed` 等）だけです。
- 連絡先は `info@rictaworks.jp` です。個人名を書いていません。
- `.claude/` 直下の md の内容は転記していません。
- 未実装のものを書いていません（3 章のとおり、書けなくなった 2 項目を外しました）。

## 6. 反映

```
gh pr edit 159 --title "[実装] アートディレクションノートを実装します" \
  --body-file <一時ファイル>
```

反映後に `gh pr view 159` で読み直し、数値（695・1,314・151・29・7）と、弱めた場合の文が本文へ入っていることを確かめました。

## 7. 後始末

- 一時の作業ツリーを `DELETE/review-worktrees/pr159b-20260827-prchecker` へ移しました。**削除していません。**
- 実測に使った台本は、その作業ツリーの中（`src/backend/tmp/prchecker/note_dump.rb`）に置いたまま一緒に移しました。
- リポジトリ直下へ一時ファイルを作っていません。`.env` ・ `config/master.key` を複製していません。
- 実装側の作業ツリーは `feature/issue-146-assembly` のままで、`git status` は空です。

## 8. 申し送り（reviewer・coordinator へ）

1. **`93b0f49` の CI が走っていません。** マージ前に、検査が 5 件とも通ることを確かめてください。ユーザーテストの手順 1 は、検査が出るまで判定できません。
2. **`adjustment_list.rb` の 14 行目のコメントが、外した文言（`adjustments.person_safety_from_project`）を「用意してあります」と書いたままです。** 本文からは外しましたが、コードの中は直っていません。
3. **「確かめること」は 3 件になる場合があります。** 余白の控えも人物の控えも無い下書きで実測しました（本文には「通常は 4 件」と書きました）。

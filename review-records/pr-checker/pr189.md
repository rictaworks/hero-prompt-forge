# pr-checker 記録：PR #189（issue #184 本番へ出す前に片づけること）

- 対象 PR：#189
- ブランチ：`feature/production-launch-cleanup`（`6a6dc36`）
- 土台：`main`（`cad5079`、遅れ 0）
- 実施日時：2026-08-29（JST）
- 確認方法：実装側の作業ツリー（`docker compose exec dev`、開発サーバー）で、実際に画面を操作して確かめました

## 判定

合格

タイトルはもとから日本語・ですます調で規約に沿っていたため変更していません。本文は規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ整え、`gh pr edit 189 --body-file` で反映しました。反映後の本文を読み直し、文字化けが無いことを確認しています。

## 反映した内容

- タイトル：`[整備] 本番へ出す前に片づけること (#184)`（変更していません）
- 本文：型へ組み直し、**ユーザーテスト手順は実際の画面で操作して裏を取ったうえで書きました**（下記）

## 画面で実際に確かめたこと

開発用コンテナ内で `bin/rails server -p 3001` と `npm run dev -- -p 3000` を起動し、ホストから `http://localhost:3300/requests/new`（コンテナの 3000 番へマッピング）をブラウザで開いて確認しました。

| 確認 | 手順 | 実測 |
|---|---|---|
| 1. イラスト × ブランドカラーの禁止 | 業種「SaaS・ソフトウェア」・スタイル系統「イラスト」・生成モデル「Midjourney」・1 色目 `#0E7C7B` を入力し、〈3 案を生成〉を押す | 送信されず、〈ブランドカラー〉欄の下に「イラスト・抽象のスタイル系統では、ブランドカラーを指定すると生成が必ず失敗します。ブランドカラーを空にするか、スタイル系統を変更してください。」が赤字で表示されました（`alert` ロールの要素として確認） |
| 2. 実写 × ブランドカラーは通ること | スタイル系統だけ「実写」へ変更し、同じブランドカラーのまま〈3 案を生成〉を押す | 上記のメッセージは出ず、`location.href` は `/requests/new`（クエリ文字列なし）のままで、画面には「DAILY QUOTA・本日の生成枠を使い切りました」という別の案内が出ました。**これは検証用アカウントの本日の生成枠が別の確認作業ですでに消費されていたためで、正常な挙動です。** 検証の要点である「禁止メッセージが出ないこと」は満たしています |

**この 2 手順は、PR 本文のユーザーテスト手順とそのまま対応しています。** 本文には「開発環境では本日の生成枠をすでに使い切っている場合があり、その場合は『DAILY QUOTA』という別の案内が出ますが、これは正常です」と明記しました。実際に踏んだ状態をそのまま案内に反映しています。

## 組み上がる前の送信保護（手順としては含めていません）

**この項目はブラウザの操作だけでは再現・確認できないため、ユーザーテスト手順に含めていません。** 組み上がる前の一瞬を意図的に作るには、ネットワークを遅らせる等の開発者向けの操作が要り、実際にブラウザ操作の確認（Playwright、`test/pr189/hydration-guard.spec.ts`）で自動化して確認しています（CI 実行 `33240330556` で pass）。手順を捏造していません。

## 実測した数値

| 検査 | 実測 |
|---|---|
| CI（最終実行 `33240330556`） | 6 ジョブすべて pass |
| Jest（フロントエンド） | 18 スイート・175 例・失敗 0 件（手元） |
| ESLint / `tsc --noEmit` | 指摘 0 件（手元） |
| Playwright（`test/pr189`） | 3 例・失敗 0 件（開発サーバー・本番相当の組み立ての両方、手元） |
| 変更ファイル数 | 8 件（`src/frontend` 3・`test` 1・`test/README.md`・`.gitignore`・`review-records` 2） |

## 制約の遵守

- **削除コマンドを一切生成していません。** 確認に使った一時スクリプト・出力（`DELETE/scratch-e2e/`）は `DELETE/`（ゴミ箱、`.gitignore` 対象）に置いたままです。
- **開発用のデータベースへの書き込みは、通常の画面操作（フォーム送信）によるものだけです。** 直接の SQL 操作等は行っていません。
- コマンドはすべて `docker compose exec -T dev bash ...` で実行しました。
- 本文は一時ファイル経由（`gh pr edit 189 --body-file`）で反映しました。反映後の本文を読み直し、文字化けが無いことを確かめました。
- コードレビューは行っていません（reviewer の担当・`review-records/reviewer/pr189.md` で実施済みです）。
- `.claude/` 直下の md の内容を、本文・記録へ転記していません。

## 申し送り

- **検証用アカウント（開発環境の自動ログイン利用者）の本日の生成枠は、この確認作業を含む複数回の検証で使い切っています。** 次回リセットは JST 03:00 です。実写でのブランドカラー確認をもう一度「受け付けの成功」まで見たい場合は、リセット後に行ってください（今回は「禁止メッセージが出ないこと」までで検証の目的を満たしています）。
- 連絡先は info@rictaworks.jp です。

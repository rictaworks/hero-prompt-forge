# PR 整備 : PR #130

- 実施日時（JST）：2026-08-26 14:02
- 対象 PR：#130
- 対象 Issue：#129

## 判定

合格

タイトルと本文はいずれも日本語のですます調で書かれていました。書き直しは不要でしたので、不足していた 2 点を補い、あわせて記載を 1 点細かくしました。

## 確認したこと

| 観点 | 判定 | 備考 |
|---|---|---|
| タイトルが日本語のですます調であること | 合格 | `[不具合] 入力の型違いが理由の分からない失敗になる問題を修正します` です。変更していません |
| 本文が日本語のですます調であること | 合格 | 全体がですます調です。だ・である調の混入はありません |
| 英語のまま残った見出し・自動生成文が無いこと | 合格 | 見出しはすべて日本語です。`Closes #129` は GitHub が解釈する記述のため、そのまま残しています。手順中の `All checks have passed` `Show all checks` `Files changed` は画面にその文字で出るボタン名・枠の名前のため、原文のままにしています |
| 非エンジニアに伝わる書き方であること | 合格 | 「文章を書く欄」「選べるもの」「まとまり」といった言い換えで、`Integer` `Array` `NoMethodError` のような語を避けています |
| ユーザーテスト手順があること | 補いました | もとは画面の変更が無いことと、PR ページでできる確認 3 点が箇条書きで書かれているだけでした。準備の URL を明示し、3 点セットの形に書き直しました |
| 変更したファイルの節があること | 補いました | 5 件を分類して追記しました |
| 「うまくいかなかったとき」が 3 点セットであること | 合格 | 画面の写真・URL・操作した時刻（JST）がそろっています。連絡先も `info@rictaworks.jp` です |
| 絵文字を使っていないこと | 合格 | 検出なし |
| 個人名を書いていないこと | 合格 | 検出なし |
| 未実装のものを書いていないこと | 合格 | 画面での確認は「issue #71 以降になります」と時期を明示しています |
| `.claude/` 直下の秘匿情報の転記が無いこと | 合格 | 検出なし |

## 本文の記載と実物の突き合わせ

本文に書かれた数値と一覧を、PR の中身と照らして確かめました。**食い違いはありませんでした。**

| 本文の記載 | 実物 | 判定 |
|---|---|---|
| 変更したファイル 5 件 | `input_normalizer.rb` ／ `input_choices.rb` ／ `brand_colors.rb` ／ `input_normalizer_spec.rb` ／ `fix-issue-129-input-type-check.md` の 5 件 | 一致 |
| 受け取る項目を 8 項目に決めたこと | `input_choices.rb` の `KNOWN_FIELDS` は `industry` `style_family` `target_model` `brand_tone` `service_summary` `brand_colors` `copy_space_position` `aspect_ratio` の 8 件です | 一致 |
| このファイルのテスト 47 件 | `input_normalizer_spec.rb` の `it` は 47 件です。総当たりの 4 件は `it` の中で繰り返す形のため、件数は 47 件のままです | 一致 |
| サービス中身側のテスト全体 352 件 | 自動チェックの記録に `352 examples, 0 failures` と出ています | 一致 |
| 書き方の検査 指摘 0 件 | 自動チェックの記録に `82 files inspected, no offenses detected` と出ています | 一致（**ファイル数を本文へ書き足しました**） |
| 一覧を 1 か所で持つようにしたこと | `InputChoices` が `INDUSTRIES = Project::INDUSTRIES` ／ `STYLE_FAMILIES = Project::STYLE_FAMILIES` ／ `TARGET_MODELS = PromptRequest::TARGET_MODELS` と参照しています。書き写しはありません | 一致 |
| 一覧が 1 か所であることをテストで固定したこと | テストは `equal`（同じものであること）で確かめています。中身の一致ではなく同一を見ているため、書き写しに戻れば失敗します | 一致 |
| 設定が渡されていないとき最初に止まること | `initialize` の先頭で `MissingDictionaryError` を投げています | 一致 |
| 入力の誤りが、設定の不備より先に返ること | `call` は入力の誤りをまとめて `InvalidInputError` で投げたあとに、標準トーンを引いています | 一致 |
| 設定から引いた標準トーンも選択肢に照らすこと | `default_tone` が選択肢の外なら `InvalidDictionaryError` を投げています | 一致 |
| 同じ色の重ねがけを誤りとして返すこと | `BrandColors` が大文字へ揃えたうえで重なりを見て `:duplicated` を返します。大文字小文字が違うだけの重ねがけも捕まえます | 一致 |
| 画面を作るファイルを含まないこと | `.tsx` で終わるファイルは 0 件 | 一致 |

自動チェックの状態も確かめました。**4 項目すべて成功しています**（ハードコードの検査／バックエンド（RuboCop / RSpec）／フロントエンド（ESLint / 型検査 / Jest / ビルド）／本番と同じ設定での読み込み確認）。本文に書いた期待結果と、実際の画面の見え方が一致します。

なお、確認を始めた時点では 4 項目のうち 3 項目が実行中でした。そのため**手順の中に、黄色い丸が回っている場合の待ち方を書き添えています**。検査が終わる前に開いた方が、誤って不合格と判断しないためです。

## 補った内容

### 1. 「変更したファイル」の節

テンプレートで必須の節が欠けていました。5 件を次のとおり分類して追記しました。

| 分類 | ファイル |
|---|---|
| 入力の確認と整えの本体（今回の修正の中心） | `src/backend/app/services/generation/input_normalizer.rb` |
| 選べる値の一覧の置き場所（新設） | `src/backend/app/services/generation/input_choices.rb` |
| ブランドカラーの確認の置き場所（新設） | `src/backend/app/services/generation/brand_colors.rb` |
| 入力の確認のテスト | `src/backend/spec/services/generation/input_normalizer_spec.rb` |
| 作業の記録 | `review-records/security-review/fix-issue-129-input-type-check.md` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** この PR がブラウザの見た目を変えないことを、ファイルの一覧からも確かめられる旨を添えています。

### 2. ユーザーテスト手順

もとの本文は、画面の変更が無いことと、PR のページでできる確認 3 点が箇条書きで書かれていました。事実としては正しいのですが、**「どこを」「どうする」「何が見えれば正解か」の 3 点セット**になっていませんでしたので、次のとおり書き直しました。ターミナルもコマンドも使いません。

まず節の冒頭で、**この PR にはアプリの画面で確かめる手順が無いこと、そして手順が無いことが正しい状態であること**を先に明言しました。手順を捏造していません。

そのうえで **準備** として、開くべき URL を PR #130 のページと明示しました。どの画面から始めるかを最初に示すためです。

確認は 3 つの見出しに分けました。

1. **自動の検査がすべて通っていることを確かめます** — 画面を下までスクロールし、すべての検査の一覧を開きます。正解は「4 項目すべての左に緑色のチェック印が付いていること」です。4 項目の名前も本文に書き出しました。あわせて、**黄色い丸が回っている場合は検査の途中であること**と、3 分ほど待って再読み込みする対処を添えました。
2. **テストがすべて成功したことを確かめます** — 一覧から「バックエンド（RuboCop / RSpec）」を押し、見出しの印の色を見ます。正解は「緑色のチェック印が付いていること」で、これが 352 件すべての成功を意味する旨を添えました。赤いバツ印なら不正解であることも明記しています。
3. **画面を変えていないことを確かめます** — 変更されたファイルの一覧を開き、ファイル名を見ます。正解は「5 件で、`.tsx` で終わるファイルが 1 件も無いこと」です。`.tsx` が画面を作るファイルであることも添えました。

期待結果はすべて**画面に見えるもの**で書いています。「テストが通る」ではなく「緑色のチェック印が付いている」と書いています。

画面で確かめられるようになる時期（issue #71「[画面] 03 入力フォームを実装する」以降）と、そのときに確かめられること 2 点は、もとの記載を活かして節の末尾に小見出しとして残しています。

### 3. テストの件数の書き方

もとの本文には「このファイルのテスト 47 件すべて成功」とありました。**件数は実物と一致していましたが、「このファイル」がどれを指すかが読み取れませんでした。**

この PR は入力の確認を 3 つのファイルへ分けていますので、どのファイルのテストなのかが分かるよう、次のとおりファイル名を添える形へ改めました。

- 入力の確認のテスト（`input_normalizer_spec.rb`）：47 件すべて成功

あわせて「書き方の検査 指摘 0 件」に、実際に検査したファイル数（82 ファイル）を書き足しました。何件を調べたうえでの 0 件なのかが分かるためです。

サービス中身側のテスト全体 352 件は、自動チェックの記録と一致しますので、そのままにしています。

## 反映の方法

`gh pr edit 130 --body-file <一時ファイル>` で反映しました。引数へ直接本文を書くと文字化けの恐れがあるため、一時ファイル（UTF-8）を経由しています。反映後に `gh pr view 130` で読み直し、文字化けが無いことと見出しの構成を確認しました。タイトルは変更していません。

## 指摘

指摘はありません。

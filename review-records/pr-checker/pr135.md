# PR 整備 : PR #135

- 実施日時（JST）：2026-08-26 18:35
- 対象 PR：#135
- 対象 Issue：#40

## 判定

合格

タイトルと本文はいずれも日本語のですます調で書かれていました。タイトルは変更していません。本文は、不足していた 2 点を補い、記載を 2 点細かくしました。**本文に書かれた数値はすべて実物と一致していました。**

## 確認したこと

| 観点 | 判定 | 備考 |
|---|---|---|
| タイトルが日本語のですます調であること | 合格 | `[実装] アンチAIルック規則の適用を実装します` です。変更していません |
| 本文が日本語のですます調であること | 合格 | 全体がですます調です。だ・である調の混入はありません |
| 英語のまま残った見出し・自動生成文が無いこと | 合格 | 見出しはすべて日本語です。`Closes #40` は GitHub が解釈する記述のため、そのまま残しています。手順中の `All checks have passed` `Show all checks` `Files changed` は画面にその文字で出るボタン名・タブ名のため、原文のままにしています |
| 非エンジニアに伝わる書き方であること | 合格 | 「避けたい表現」「打ち消しの指示」「下書き」「控え」といった言い換えで、`Struct` `freeze` `uniq` `raise` のような語を避けています |
| ユーザーテスト手順があること | 補いました | もとは画面の変更が無いことと、PR ページでできる確認 3 点が箇条書きで書かれているだけでした。準備の URL を明示し、3 点セットの形に書き直しました |
| 変更したファイルの節があること | 補いました | 5 件を分類して追記しました |
| 「うまくいかなかったとき」が 3 点セットであること | 合格 | 画面の写真・URL・操作した時刻（JST）がそろっています。連絡先も `info@rictaworks.jp` です |
| 絵文字を使っていないこと | 合格 | 検出なし |
| 個人名を書いていないこと | 合格 | 検出なし |
| 未実装のものを書いていないこと | 直しました | 規則辞書について「管理画面から更新できる予定のデータ」と書かれていました。編集画面は未実装ですので、issue #65 で作る旨へ書き換えました（後述） |
| `.claude/` 直下の秘匿情報の転記が無いこと | 合格 | 検出なし |

## 本文の記載と実物の突き合わせ

本文に書かれた数値と一覧を、PR の中身と自動チェックの記録に照らして確かめました。**食い違いはありませんでした。**

| 本文の記載 | 実物 | 判定 |
|---|---|---|
| 変更したファイル 5 件 | `draft.rb` ／ `rule_engine.rb` ／ `draft_spec.rb` ／ `rule_engine_spec.rb` ／ `feature-issue-40-rule-engine.md` の 5 件 | 一致 |
| 下書きのテスト 23 件 | `draft_spec.rb` の `it` は 23 件です | 一致（**ファイル名を本文へ書き足しました**） |
| 規則の適用のテスト 19 件 | `rule_engine_spec.rb` の `it` は 19 件です | 一致（**ファイル名を本文へ書き足しました**） |
| サービス中身側のテスト全体 516 件 | 自動チェックの記録に `516 examples, 0 failures` と出ています | 一致 |
| 書き方の検査 88 ファイルを調べて指摘 0 件 | 自動チェックの記録に `88 files inspected, no offenses detected` と出ています | 一致 |
| 避けたい表現をプログラムへ直書きしていないこと | 語はすべて規則辞書側から読み込む形です。実装が持つのは読み出しの鍵の名前だけです | 一致 |
| 語を含む言い回しも取り除くこと | 語そのものだけでなく、語を含む素材を対象にしています。テストでも固定しています | 一致 |
| 打ち消しの指示が重複しないこと | 重ねずにまとめる形です。テストでも固定しています | 一致 |
| 取り除いた語を控えに残すこと | 取り除いた語を控えへ積む形です。控えに残すのは記号と語だけで、利用者へ見せる文言を持ちません | 一致 |
| 規則辞書の版を記録すること | 下書きへ版を記録します。テストでも固定しています | 一致 |
| 規則辞書が無いとき、その場で止まること | `MissingDictionaryError` を投げます。素通りする分岐はありません | 一致 |
| 規則辞書の定義が足りないとき、素通りしないこと | `InvalidDictionaryError` を投げます。空の定義で進む分岐はありません | 一致 |
| 下書きを書き換えられないこと | 下書きは作った時点で凍結され、各段階は新しい下書きを返す形です。テストでも固定しています | 一致 |
| 初期の規則辞書で、仕様が挙げるクリシェを排除できること | 初期データを読み込んだうえでの確認が `rule_engine_spec.rb` にあります | 一致 |
| 画面を作るファイルを含まないこと | `.tsx` で終わるファイルは 0 件 | 一致 |
| 参照している issue 番号 | #40（本件）、#65「[管理] 規則辞書の編集画面を作る」、#73「[画面] 05 結果3案を実装する」。いずれも実在し、題名も一致します | 一致 |

自動チェックの状態も確かめました。**4 項目すべて成功しています**（ハードコードの検査／バックエンド（RuboCop / RSpec）／フロントエンド（ESLint / 型検査 / Jest / ビルド）／本番と同じ設定での読み込み確認）。本文に書いた期待結果と、実際の画面の見え方が一致します。

なお、確認を始めた時点では 4 項目のうち 3 項目が実行中でした。完了を待って結果を確かめたうえで、**手順の中に、黄色い丸が回っている場合の待ち方を書き添えています**。検査が終わる前に開いた方が、誤って不合格と判断しないためです。

### 全体件数の増え方について

本文の「サービス中身側のテスト全体 516 件」は、`main` の直近の記録が `474 examples` であることと、この PR が 23 件と 19 件の計 42 件を足すことから、`474 + 42 = 516` として説明が付きます。書き方の検査の 88 ファイルも、`main` の 84 ファイルにこの PR の 4 ファイル（記録の md を除く）を足した数と一致します。**増分の辻褄も合っています。**

## 補った内容

### 1. 「変更したファイル」の節

テンプレートで必須の節が欠けていました。5 件を次のとおり分類して追記しました。

| 分類 | ファイル |
|---|---|
| アンチAIルック規則を当てる本体（今回の中心） | `src/backend/app/services/generation/rule_engine.rb` |
| 段階どうしで受け渡す下書き（新設） | `src/backend/app/services/generation/draft.rb` |
| 規則を当てる仕組みのテスト | `src/backend/spec/services/generation/rule_engine_spec.rb` |
| 下書きのテスト | `src/backend/spec/services/generation/draft_spec.rb` |
| 作業の記録 | `review-records/security-review/feature-issue-40-rule-engine.md` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** この PR がブラウザの見た目を変えないことを、ファイルの一覧からも確かめられる旨を添えています。

### 2. ユーザーテスト手順

もとの本文は、画面の変更が無いことと、PR のページでできる確認 3 点が箇条書きで書かれていました。事実としては正しいのですが、**「どこを」「どうする」「何が見えれば正解か」の 3 点セット**になっていませんでしたので、次のとおり書き直しました。ターミナルもコマンドも使いません。

まず節の見出しを `## ユーザーテスト手順（ブラウザだけで確認できます）` へ改め、冒頭で **この PR にはアプリの画面で確かめる手順が無いこと、そして手順が無いことが正しい状態であること**を先に明言しました。手順を捏造していません。

そのうえで **準備** として、開くべき URL を PR #135 のページと明示しました。どの画面から始めるかを最初に示すためです。

確認は 3 つの見出しに分けました。

1. **自動の検査がすべて通っていることを確かめます** — 画面を下までスクロールし、すべての検査の一覧を開きます。正解は「4 項目すべての左に緑色のチェック印が付いていること」です。4 項目の名前も本文に書き出しました。あわせて、**黄色い丸が回っている場合は検査の途中であること**と、3 分ほど待って再読み込みする対処を添えました。
2. **テストがすべて成功したことを確かめます** — 一覧から「バックエンド（RuboCop / RSpec）」を押し、見出しの印の色を見ます。正解は「緑色のチェック印が付いていること」で、これが 516 件すべての成功を意味する旨を添えました。赤いバツ印なら不正解であることも明記しています。
3. **画面を変えていないことを確かめます** — 変更されたファイルの一覧を開き、ファイル名を見ます。正解は「5 件で、`.tsx` で終わるファイルが 1 件も無いこと」です。`.tsx` が画面を作るファイルであることも添えました。

期待結果はすべて**画面に見えるもの**で書いています。「テストが通る」ではなく「緑色のチェック印が付いている」と書いています。

画面で確かめられるようになる時期（issue #73「[画面] 05 結果3案を実装する」以降）と、そのときに確かめられること 2 点は、もとの記載を活かして節の末尾に小見出しとして残しています。もとの本文には issue の番号だけが書かれていましたので、**題名を添えて、どの作業を指すのかが分かるようにしました**。

### 3. 未実装の機能の書き方

もとの本文に、規則辞書を **「管理画面から更新できる予定のデータ」** という説明がありました。**規則辞書の編集画面はまだありません。** 「予定」と断ってはいるものの、PR 本文へ未実装の機能を書かない決まりに照らして、次のとおり書き換えました。

- 変更前：避けたい表現の一覧は、**規則辞書**（管理画面から更新できる予定のデータ）に持っています。プログラムの中に書き込んでいません。
- 変更後：避けたい表現の一覧は、**規則辞書**というデータに版ごとに保存しています。**プログラムの中に書き込んでいません。** 規則辞書を画面から編集できるようにするのは issue #65 で、この PR には含みません。

**この PR で作ったものと、これから作るものの境目**をはっきりさせています。編集画面が issue #65「[管理] 規則辞書の編集画面を作る」として実在することも確かめたうえで、番号を添えています。

### 4. テストの件数の書き方

もとの本文には「下書きのテスト 23 件すべて成功」「規則の適用のテスト 19 件すべて成功」とありました。**件数は実物と一致していましたが、それぞれがどのファイルのテストを指すかが読み取れませんでした。**

本文には全体の 516 件も並んで書かれており、23 件と 19 件がその内訳のどこにあたるかが分かりにくい状態でしたので、次のとおりファイル名を添える形へ改めました。

- 下書きのテスト（`draft_spec.rb`）：23 件すべて成功
- 規則の適用のテスト（`rule_engine_spec.rb`）：19 件すべて成功

サービス中身側のテスト全体 516 件と、書き方の検査 88 ファイルで指摘 0 件は、自動チェックの記録と一致しますので、そのままにしています。

## そのまま残した内容

もとの本文には、次の 4 つの節が独自の見出しで書かれていました。テンプレートに無い節ですが、**この PR の要点を非エンジニアに伝えるうえで有効ですので、すべて残しています。**

| 節 | 残した理由 |
|---|---|
| どう避けるか | 「入れない」「打ち消す」の二方向を表にまとめており、仕組みの全体像が 1 目で分かります |
| 取り除いた語は記録します | 「黙って消しません」という利用者への約束が書かれており、大切な点です |
| どの版の規則で作ったかを残します | あとから追える理由が書かれており、運用上の意味が伝わります |
| 途中経過の受け渡し方 | 下書きを書き換えない理由が、専門用語を使わずに説明されています |

「判断したこと」の節も、なぜその作りにしたかが書かれていますので、そのままにしています。

## 反映の方法

`gh pr edit 135 --body-file <一時ファイル>` で反映しました。引数へ直接本文を書くと文字化けの恐れがあるため、一時ファイル（UTF-8）を経由しています。反映後に `gh pr view 135` で読み直し、文字化けが無いことと見出しの構成（`## 何を変えたか` から `Closes #40` まで）を確認しました。タイトルは変更していません。

## 指摘

指摘はありません。

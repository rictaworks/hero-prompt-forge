# PR 整備 : PR #143

- 実施日時（JST）：2026-08-26 20:09
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/143
- 対象 issue：#132
- ブランチ：`fix/issue-132-quota-race-regression` → `main`
- 担当：pr-checker（タイトルと本文の整備のみを行います。コードレビューは行いません）

## 実施したこと

`gh pr edit 143 --body-file` で本文を差し替えました。タイトルは修正の必要がありませんでしたので、そのままにしました。

実装側の作業ツリーではブランチを切り替えていません。計測には一時的な作業ツリー `.review/prcheck143` を作り、作業後に `mv` で `DELETE/review-worktrees/prcheck143-20260826-prchecker` へ移しました。削除コマンドは使っていません。

## 1. 数値・ファイル名・件数の突き合わせ

| 本文の記載 | 実測した結果 | 判定 |
|---|---|---|
| 枠のテスト **37 件** | `reservation_spec.rb` は **36 examples, 0 failures** | **食い違い。36 件へ直しました** |
| バックエンドのテスト全体 **651 件** | 全体で **651 examples, 0 failures** | 一致 |
| 書き方の検査 **96 ファイル** | **96 files inspected, no offenses detected** | 一致 |
| 変更したファイル **7 件** | 7 件。`git diff --stat` も `7 files changed` | 一致 |
| 変更したファイルの一覧（7 行の表） | 7 件すべてパスも分類も実物と一致します | 一致 |
| `.tsx` を 1 件も含まないこと | 変更 7 件に `.tsx` はありません | 一致 |
| 枠のテストを直す前の実装に当てると **5 件が失敗** | `origin/main` の実装で **36 examples, 5 failures** | 一致 |
| 見張りを外すと新しい同時申し込みテストが失敗すること | `with_lock` を外して 5 回動かし、**5 回とも失敗**しました | 一致。実測した回数へ書き換えました |
| 見張りがある状態で新しいテストが成功すること | **12 回連続で成功**しました | 一致。実測した回数へ書き換えました |
| 従来のテストは見張りを外しても **5 回とも成功してしまう** | 15 回動かして **14 回成功**しました | **食い違い。「15 回中 14 回」へ直しました** |
| 自動の検査 5 項目の名前 | `gh pr checks 143` の 5 項目と完全に一致し、すべて pass です | 一致 |
| issue #71 ・ #55 が未着手であること | 両方とも OPEN です | 一致 |
| PR #128 の記録の数値を直したこと | 差分に、5 件のうち実際に失敗するのは 4 件である旨の修正が入っています | 一致 |

### 直した箇所

1. **「枠のテスト 37 件」を「36 件」へ直しました。** どのファイルのテストかが分かるよう `reservation_spec.rb` を添えました。
2. **「5 回とも成功してしまいました」を「15 回中 14 回は成功してしまいました」へ直しました。** 冒頭の要約表と本文の数値もそろえました。従来のテストが回帰を捕まえられていないという趣旨は変わりません。
3. 確認結果の表の回数を、実測した回数（12 回・5 回）へ書き換えました。
4. 「サービス中身側のテスト全体」を「バックエンドのテスト全体」へ改めました。
5. 確認結果の表に「2026-08-26（JST）に実測しました」の一行を足しました。
6. 「書き方の検査」に `RuboCop` を添えました。

## 2. 表記の確認

| 観点 | 結果 |
|---|---|
| タイトルがですます調で日本語であること | 問題ありません |
| 本文がすべてですます調であること | だ・である調の文はありません |
| 絵文字が無いこと | タイトル・本文とも 0 件です |
| 英語のまま残った見出し・自動生成文が無いこと | ありません |
| 個人名が無いこと | ありません |
| 連絡先 | `info@rictaworks.jp` を使っています |
| 秘匿情報の転記が無いこと | ありません |
| 未実装のものを書いていないこと | 画面での確認手順を書かず、issue #71 ・ #55 が未着手であることを明記しています |
| issue との結び付き | 冒頭で issue #132 に触れ、末尾に `Closes #132` があります |

## 3. 非エンジニア向けユーザーテスト手順の確認

| 観点 | 結果 |
|---|---|
| ブラウザだけで完了できること | 3 つの確認はすべて GitHub の PR ページ上の操作です |
| 開始地点が明示されていること | URL とページの見分け方を書いています |
| 1 手順 1 操作であること | いずれも 1 行 1 操作です |
| 期待結果が画面に見えるもので書かれていること | 「緑色のチェック印」「ファイルが 7 件だけ並んでいる」など、目で見える状態のみです |
| 画面で確かめられない PR である旨の明記 | 明記しています。手順の捏造はありません |
| 失敗時の報告事項 | 画面の写真・URL・時刻（JST）を書いています |

〈Show all checks〉と〈Files changed〉は画面上の英語表記のため、押すボタンの名前としてはそのまま残し、**意味を日本語で括弧書きに足しました。**

## 4. 参考として申し送ります

数値の実測中に、**同時申し込みのテストが、まれに `let!(:user)` の作成で失敗する現象**を 2 度観測しました。前の実行で後片付けが済まず、同じ `x_user_id` の利用者が残っていることが原因と見られます。このテストはテストごとのトランザクションを使わず `after` で後片付けをするため、途中で失敗すると次の実行へ持ち越されます。

その後、素の状態で 12 回連続・さらに 10 回連続で成功しており、CI も pass しています。本文の数値の正しさには影響しません。

## 5. 結論

**タイトルは修正不要でした。本文は 2 か所の数値の食い違いを直し、あわせて回数の表記と用語を実測値へそろえました。** 整備後の本文は、数値・ファイル名・件数がすべて実物と一致します。

---

# PR 整備（2回目） : PR #143

- 実施日時（JST）：2026-08-26 20:31
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/143
- 対象 issue：#132
- 整備時点の先頭コミット：`ff00981`（実装の先頭は `894300c`）
- 担当：pr-checker

## 経緯

1 回目の整備のあとに追加コミット `894300c`（取り直しの経路にも保存点を作る変更）が入り、reviewer から本文の件数が実測とずれているという申し送りを受けました。**本文の数値をすべて測り直し、追加コミットの内容を本文へ反映しました。**

## 1. reviewer の申し送りの検証

**3 点とも reviewer の実測どおりでした。**

| 本文の記載（整備前） | 自分の実測 | 判定 |
|---|---|---|
| 枠のテスト 36 件 | `reservation_spec.rb` は **41 examples, 0 failures** | **41 件へ直しました** |
| 直す前の実装に当てると 5 件が失敗 | **41 examples, 9 failures** | **9 件へ直しました** |
| バックエンドのテスト全体 651 件 | **656 examples, 0 failures** | **656 件へ直しました** |

### 申し送りに含まれていなかった、もう 1 件のずれ

| 本文の記載（整備前） | 自分の実測 | 判定 |
|---|---|---|
| 書き方の検査 96 ファイル | **100 files inspected, no offenses detected** | **100 ファイルへ直しました** |

例外クラスを 4 件切り出したぶん、検査対象が増えていました。

## 2. 変更したファイルの数え直し

| 時点 | 件数 |
|---|---|
| 1 回目の整備時（`187246b`） | 7 件 |
| `894300c` 時点 | 12 件 |
| `ff00981` 時点 | 14 件 |

本文の「変更したファイル」の表を作り直し、実物のパスと `diff` で突き合わせて**完全一致**を確認しました。`.tsx` は 1 件もありません。ユーザーテスト手順の件数も直しました。

## 3. 追加コミットの内容の本文への反映

| 反映するもの | 本文のどこへ書いたか |
|---|---|
| `reclaim!` にも保存点を作ったこと | 冒頭の要約表へ 4 行目を足し、「### 4. 同じ壊れ方が、別の入口にも残っていました」を新設しました |
| 例外クラスを `reservation/` へそろえたこと | 「### 5. あわせて整えたこと」に書き、「動きは変わりません」と明記しました |
| 並列のテストが前の実行の残りで落ちないようにしたこと | 同上。1 回目の整備での申し送りへの対応である旨を書いています |
| 記録の「5例」を「4例」へ直したこと | 「### 6. 記録の数値を実測し直しました」を表に作り替えました |

## 4. 追加で実測したこと

| 実測したこと | 結果 |
|---|---|
| `reclaim!` の保存点だけを外して `reservation_spec.rb` を動かす | **41 examples, 4 failures** |
| `with_lock` を外して並列のテストを 5 回動かす | 5 回とも失敗 |
| `with_lock` がある状態で 12 回動かす | 12 回とも成功 |

**1 回目の整備で申し送った、前の実行の残りによる散発的な失敗は、再現しませんでした。** 追加コミットの `clear_leftovers` が効いています。

## 5. 表記とユーザーテスト手順の確認

ですます調・絵文字なし・個人名なし・秘匿情報なし・未実装の記載なしを確認しました。古い数値（36 件・651・96 ファイル・12 件）は残っていません。ユーザーテスト手順は、ブラウザだけで完了でき、1 手順 1 操作です。自動の検査 5 項目の名前は `gh pr checks 143` と完全に一致し、すべて成功しています。

## 6. 結論

**reviewer の申し送りの 3 点は、いずれも実測どおりでした。本文をすべて実測値へ直しました。** あわせて、申し送りに含まれていなかった書き方の検査のファイル数（96 → 100）も直しました。

**申し送りが 1 件あります。** この PR は本文へ書いた数値が実装の変化に追随しにくい作りです。**マージ直前にもう一度だけ件数を数え直してください。**

---

# 追記（マージ直前の数え直し）

- 実施日時（JST）：2026-08-26 20:44
- 実施：実装担当

pr-checker の申し送りに従い、この記録（`review-records/pr-checker/pr143.md`）を足したあとの件数を数え直しました。**15 件です。** 本文の「変更したファイル」の件数と、ユーザーテスト手順 3 の件数を 15 件へ直しました。

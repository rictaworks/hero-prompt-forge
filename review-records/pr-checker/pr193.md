# pr-checker 記録：PR #193（issue #192 本番のセキュリティヘッダ欠落・GitHub Actions可変タグ参照）

- 対象PR：#193
- ブランチ：`feature/issue-192-security-headers-actions-pin`（`d44ed79`）
- 土台：`main`（遅れ0）
- 実施日時：2026-08-31（JST）
- 確認方法：GitHub Actionsのログ（run `33333395807`）と、本番相当ビルドの実機起動の両方を突き合わせました。この PR は応答ヘッダとCI定義のみの変更で、画面上の変化はありません

## 判定

合格

タイトルは日本語・ですます調でissueの題名に沿っており、変更していません。本文は規定の型で書かれており、記載内容と実測が一致することを確認しました。

## 実測との突き合わせ

| 項目 | 本文の記載 | 実測 | 判断 |
|---|---|---|---|
| 変更したファイル | 5件（next.config.ts・security-headers.ts新規・ci.yml・release-deploy.yml・review-records新規） | `gh pr view --json files` で5件 | 一致 |
| CI（最終実行 `33333395807`） | 記載なし（初回コミット時点の記述） | 6ジョブすべてpass | 手順どおりに再現できることを確認 |
| ヘッダの実測 | 本文の手順どおり | `curl -sSI` で3ヘッダとも確認 | 一致 |

## 本文のユーザーテスト手順の実行

1. `npm run build` → `npm run start` で起動 → ブラウザでコンソールを確認 → **CSP違反のエラーなし**
2. `curl -sSI` でヘッダ確認 → **`Content-Security-Policy`・`X-Content-Type-Options: nosniff`・`X-Frame-Options: DENY` の3つを確認**

手順どおりに再現でき、記載と食い違いはありませんでした。

## 制約の遵守

- 削除コマンドを一切生成していません。
- コマンドはすべて `docker compose exec -T dev ...` で実行しました。
- コードレビューは行っていません（reviewerの担当・`review-records/reviewer/pr193.md`で実施済みです）。
- `.claude/`直下のmdの内容を、本文・記録へ転記していません。

## 申し送り

- 本文の「申し送り」節に記載のとおり、実装中にWindowsのクラッシュでWSL側のgitオブジェクトが破損する事象がありました。再クローン後の内容で問題なくCIが通ることを確認済みです。
- 連絡先はinfo@rictaworks.jpです。

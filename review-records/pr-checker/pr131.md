# PR 整備 : PR #131

- 実施日時（JST）：2026-08-26 14:49
- 対象 PR：#131
- 対象 Issue：#39

## 判定

合格

タイトルと本文はいずれも日本語のですます調で書かれていました。書き直しは不要でしたので、不足していた 2 点を補い、あわせて記載を 1 点細かくしました。

## 確認したこと

| 観点 | 判定 | 備考 |
|---|---|---|
| タイトルが日本語のですます調であること | 合格 | `[実装] 禁止入力の検出を実装します` です。変更していません |
| 本文が日本語のですます調であること | 合格 | 全体がですます調です。だ・である調の混入はありません |
| 英語のまま残った見出し・自動生成文が無いこと | 合格 | 見出しはすべて日本語です。`Closes #39` は GitHub が解釈する記述のため、そのまま残しています。手順中の `All checks have passed` `Show all checks` `Files changed` は画面にその文字で出るボタン名・タブ名のため、原文のままにしています |
| 非エンジニアに伝わる書き方であること | 合格 | 「そのままでは作れない内容」「見つける語」「素通し」といった言い換えで、`Regexp` `YAML` `Struct` のような語を避けています |
| ユーザーテスト手順があること | 補いました | もとは画面の変更が無いことと、PR ページでできる確認 3 点が箇条書きで書かれているだけでした。準備の URL を明示し、3 点セットの形に書き直しました |
| 変更したファイルの節があること | 補いました | 4 件を分類して追記しました |
| 「うまくいかなかったとき」が 3 点セットであること | 合格 | 画面の写真・URL・操作した時刻（JST）がそろっています。連絡先も `info@rictaworks.jp` です |
| 絵文字を使っていないこと | 合格 | 検出なし |
| 個人名を書いていないこと | 合格 | 検出なし。設定ファイル `config/forbidden_inputs.yml` には検出対象として著名な方のお名前が並びますが、**本文へは 1 件も転記していません**。「著名な方のお名前」という言い方にとどめています |
| 未実装のものを書いていないこと | 合格 | 画面での確認は「issue #71 以降になります」と時期を明示しています |
| `.claude/` 直下の秘匿情報の転記が無いこと | 合格 | 検出なし |

## 本文の記載と実物の突き合わせ

本文に書かれた数値と一覧を、PR の中身と照らして確かめました。**食い違いはありませんでした。**

| 本文の記載 | 実物 | 判定 |
|---|---|---|
| 変更したファイル 4 件 | `forbidden_detector.rb` ／ `forbidden_inputs.yml` ／ `forbidden_detector_spec.rb` ／ `feature-issue-39-forbidden-detector.md` の 4 件 | 一致 |
| この仕組みのテスト 19 件 | `forbidden_detector_spec.rb` の `it` は 19 件です | 一致（**ファイル名を本文へ書き足しました**） |
| サービス中身側のテスト全体 371 件 | 自動チェックの記録に `371 examples, 0 failures` と出ています | 一致 |
| 書き方の検査 84 ファイルを調べて指摘 0 件 | 自動チェックの記録に `84 files inspected, no offenses detected` と出ています | 一致 |
| 見つける語をコードへ直書きしていないこと | `RULES_PATH = 'config/forbidden_inputs.yml'` を読み込む形です。語はすべて設定ファイル側にあります | 一致 |
| 種別が 3 つ（実在の人物／企業のロゴ・商標／第三者の著作物）であること | 設定ファイルの `kind` は `real_person` ／ `brand_logo` ／ `third_party_work` の 3 件です | 一致 |
| 「お客さん」のような普通の言い回しを止めないこと | 敬称の規則が漢字・カタカナ始まりに限られており、ひらがな始まりを拾いません。テストでも固定しています | 一致 |
| 利用者へ見せる文言をこの仕組みが持たないこと | 返す `Reason` は `kind` と `matched` の 2 つだけです。文言を持ちません。テストでも固定しています | 一致 |
| 複数の理由をまとめて返せること | すべての規則を通して集める形です。最初の 1 件で打ち切りません | 一致 |
| 設定ファイルが読めないとき、その場で止まること | 規則の定義が無ければ `InvalidRuleError` を投げます。空の規則で素通しする分岐はありません | 一致 |
| すべてを網羅するものではないと明記していること | 実装の先頭と設定ファイルの先頭の両方に、同じ趣旨の注意書きが書かれています | 一致 |
| 画面を作るファイルを含まないこと | `.tsx` で終わるファイルは 0 件 | 一致 |

自動チェックの状態も確かめました。**4 項目すべて成功しています**（ハードコードの検査／バックエンド（RuboCop / RSpec）／フロントエンド（ESLint / 型検査 / Jest / ビルド）／本番と同じ設定での読み込み確認）。本文に書いた期待結果と、実際の画面の見え方が一致します。

なお、確認を始めた時点では 4 項目のうち 3 項目が実行中でした。完了を待って結果を確かめたうえで、**手順の中に、黄色い丸が回っている場合の待ち方を書き添えています**。検査が終わる前に開いた方が、誤って不合格と判断しないためです。

## 補った内容

### 1. 「変更したファイル」の節

テンプレートで必須の節が欠けていました。4 件を次のとおり分類して追記しました。

| 分類 | ファイル |
|---|---|
| 禁止入力を見つける本体（今回の中心） | `src/backend/app/services/generation/forbidden_detector.rb` |
| 見つける語の置き場所（新設） | `src/backend/config/forbidden_inputs.yml` |
| 禁止入力を見つける仕組みのテスト | `src/backend/spec/services/generation/forbidden_detector_spec.rb` |
| 作業の記録 | `review-records/security-review/feature-issue-39-forbidden-detector.md` |

**画面を作るファイル（`.tsx`）を 1 件も含みません。** この PR がブラウザの見た目を変えないことを、ファイルの一覧からも確かめられる旨を添えています。

### 2. ユーザーテスト手順

もとの本文は、画面の変更が無いことと、PR のページでできる確認 3 点が箇条書きで書かれていました。事実としては正しいのですが、**「どこを」「どうする」「何が見えれば正解か」の 3 点セット**になっていませんでしたので、次のとおり書き直しました。ターミナルもコマンドも使いません。

まず節の冒頭で、**この PR にはアプリの画面で確かめる手順が無いこと、そして手順が無いことが正しい状態であること**を先に明言しました。手順を捏造していません。

そのうえで **準備** として、開くべき URL を PR #131 のページと明示しました。どの画面から始めるかを最初に示すためです。

確認は 3 つの見出しに分けました。

1. **自動の検査がすべて通っていることを確かめます** — 画面を下までスクロールし、すべての検査の一覧を開きます。正解は「4 項目すべての左に緑色のチェック印が付いていること」です。4 項目の名前も本文に書き出しました。あわせて、**黄色い丸が回っている場合は検査の途中であること**と、3 分ほど待って再読み込みする対処を添えました。
2. **テストがすべて成功したことを確かめます** — 一覧から「バックエンド（RuboCop / RSpec）」を押し、見出しの印の色を見ます。正解は「緑色のチェック印が付いていること」で、これが 371 件すべての成功を意味する旨を添えました。赤いバツ印なら不正解であることも明記しています。
3. **画面を変えていないことを確かめます** — 変更されたファイルの一覧を開き、ファイル名を見ます。正解は「4 件で、`.tsx` で終わるファイルが 1 件も無いこと」です。`.tsx` が画面を作るファイルであることも添えました。

期待結果はすべて**画面に見えるもの**で書いています。「テストが通る」ではなく「緑色のチェック印が付いている」と書いています。

画面で確かめられるようになる時期（issue #71「[画面] 03 入力フォームを実装する」以降）と、そのときに確かめられること 2 点は、もとの記載を活かして節の末尾に小見出しとして残しています。

### 3. テストの件数の書き方

もとの本文には「この仕組みのテスト 19 件すべて成功」とありました。**件数は実物と一致していましたが、「この仕組み」がどのファイルのテストを指すかが読み取れませんでした。**

本文には全体の 371 件も並んで書かれており、19 件がその内訳のどこにあたるかが分かりにくい状態でしたので、次のとおりファイル名を添える形へ改めました。

- この仕組みのテスト（`forbidden_detector_spec.rb`）：19 件すべて成功

サービス中身側のテスト全体 371 件と、書き方の検査 84 ファイルで指摘 0 件は、自動チェックの記録と一致しますので、そのままにしています。

## 個人名の扱いについて

この PR は性質上、設定ファイル `config/forbidden_inputs.yml` に**実在の方のお名前や企業名・作品名が検出対象として並びます**。これらは仕組みが機能するために必要な記述ですので、実装側はそのままにしています。

一方で、**PR 本文へは 1 件も転記していません。** もとの本文がすでに「著名な方のお名前」「よく知られた企業名」「作品名、作家名」という言い方にとどめていましたので、その方針を維持しました。PR のページは公開の場であり、特定の方や企業の名前を並べて掲げる必要が無いためです。

## 反映の方法

`gh pr edit 131 --body-file <一時ファイル>` で反映しました。引数へ直接本文を書くと文字化けの恐れがあるため、一時ファイル（UTF-8）を経由しています。反映後に `gh pr view 131` で読み直し、文字化けが無いことと見出しの構成（`## 何を変えたか` から `Closes #39` まで）を確認しました。タイトルは変更していません。

## 指摘

指摘はありません。

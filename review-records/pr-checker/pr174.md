# pr-checker 記録：PR #174（issue #70 ・ #71 ・ #152 ・ #72 ・ #73 ・ #74 ・ #75 ・ #76 ・ #77「画面 02〜09 の実装」）

- 対象 PR：#174
- ブランチ：`feature/screens-02-09`（`bcbd499`）
- 土台：`main`（`84cbe4a`）
- 実施日時：2026-08-27（JST）
- 作業ツリー：`DELETE/pr174-checker-20260827` を `--detach` で新しく作りました。**実装側の作業ツリーではブランチを切り替えていません。**

## 判定

合格

タイトルはそのままとし、本文を規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ組み直しました。**非エンジニア向けのユーザーテスト手順 22 件を追記し、`test/pr174/screens.spec.ts` の 24 例と 1 対 1 で対応させました。**

**ただし、マージの前に直す必要のある点を 1 つ見つけました。下記の「申し送り」の 1 件目です。**

## 反映した内容

- タイトル：**変更していません。** 日本語・ですます調で、規約に沿っています。
- 本文：**全面的に書き直しました。** ユーザーテスト手順は**新規に追加**しました。

## 実測した数値

自動検査（GitHub Actions・run `33020985158`）のログから採りました。**5 つの検査すべてが通っています。**

| 検査 | 実測 | 本文の記載（更新前） | 判断 |
|---|---|---|---|
| RSpec | 1790 例、失敗 0 件 | 1790 例、失敗 0 件 | 一致 |
| RuboCop | 196 ファイル、指摘 0 件 | 196 ファイル、指摘 0 件 | 一致 |
| Jest | 12 ファイル・88 例、失敗 0 件 | 「88 例」 | **ファイル数を足しました** |
| ESLint | 指摘 0 件 | 指摘 0 件 | 一致 |
| TypeScript の型検査 | 指摘 0 件 | 指摘 0 件 | 一致 |
| `next build` | 成功（8 つの場所） | 成功（8 つの場所） | 一致 |
| ハードコードの検査 | 検出 0 件 | 検出 0 件 | 一致 |
| 本番と同じ設定での読み込み確認 | pass | 記載なし | **追記しました** |
| 開発フックの検査 | pass | 記載なし | **追記しました** |
| ブラウザ操作の確認 | 24 例、失敗 0 件（`--workers=1`） | 「PR の番号が決まってから足します」 | **実測へ直しました** |
| 変更ファイル数 | 46 件 | 記載なし | 追記 |

## 本文へ足した節

- **「何を変えたか」「なぜ変えたか」**（新設）。専門用語を使わずに書き、`closes` の 9 件をここへまとめました。
- **「変更したファイル」**（新設）。46 件を分類して並べ、**「削除したファイルはありません」と明記**しました。
- **「確かめたこと」**（差し替え）。run 番号を添え、ブラウザ操作の実測を足しました。
- **「ユーザーテスト手順」**（新設）。下記のとおりです。
- **「この PR では確かめられないこと」**（新設）。4 件を挙げました。
- **「うまくいかなかったとき」**（新設）。連絡先は `info@rictaworks.jp` です。
- **「申し送り」**（差し替え）。

## ユーザーテスト手順

**ブラウザだけで確認できる手順を 22 件書きました。** ターミナル・コマンド・`curl`・環境変数・ログの確認を、手順に一切入れていません。

**`test/pr174/screens.spec.ts` と 1 対 1 で対応させました。** 手順の番号は spec のコメント（手順 1〜22）と同じです。手順 22 が自動テスト 3 例に当たりますので、**22 手順 = 24 例**です。

| 手順 | 見るもの |
|---|---|
| 1 | 大きな見出し〈Projects & Requests〉 |
| 2 | 上部の帯に〈Admin〉〈管理〉が出ないこと |
| 3 | 〈新規生成〉の行き先が `/requests/new` であること |
| 4 | 過去の生成の行に日付・モデル・案の数と〈開く〉が出ること |
| 5 | 〈完了（縮退）〉と〈縮退〉の印 |
| 6 | 必須の 3 項目に〈必須〉の印 |
| 7 | 未選択で送ると 3 つの注意が出て、画面が切り替わらないこと |
| 8 | 〈名前を確実に反映させる書き方〉と例 3 つ |
| 9 | ブラウザの標準の警告窓が出ないこと |
| 10 | 見出し〈Generating〉と素性の 4 項目 |
| 11 | 〈DEGRADED MODE〉の帯と〈NOT REFINED〉 |
| 12 | 〈3 案を見る〉で結果へ移ること |
| 13 | 〈被写体主導〉〈環境主導〉〈抽象背景〉の 3 案 |
| 14 | 〈コピー〉ボタン |
| 15 | 〈NOT REFINED〉が 3 つ |
| 16 | 〈出来上がった絵で確かめること〉 |
| 17 | 〈所感〉に書いて保存すると〈保存済み〉に変わること |
| 18 | 再読み込みしても〈所感〉が残っていること |
| 19 | 大きな見出し〈Saved Conditions〉 |
| 20 | 〈プリセットを保存しました。〉 |
| 21 | 〈この条件で作る〉で条件が選ばれた状態になること |
| 22 | 横幅 375 で、3 画面とも横向きのスクロールバーが出ないこと |

**手順 20 に「そのつど違う名前にしてください」と書きました。** プリセット名は利用者ごとに一意です。

**確かめられないことは、確かめられないと書きました。**

- ログインの操作そのもの（この PR に含まれません）
- 管理画面（入口を分けています。**「出ないこと」を手順 2 で確かめます**）
- 同一の生成をやり直す操作（API に経路がありません）
- プリセットを消す操作（API が持っていません）

## ブラウザで実際に確かめた結果

| 実行のしかた | 結果 |
|---|---|
| `--workers=1` | **24 例すべて成功（1.3 分）** |
| 既定の並列 | 21 例成功・3 例失敗 |

**並列で落ちる 3 例の原因は順番の依存です。** 手順 18 は手順 17 が保存した内容を読みます。**画面の中身の問題ではありません。**

**手順 14 のコピーの動きも、別途ブラウザで確かめました。** 〈コピー〉を押すと〈コピー済み〉に変わりました。

**手順 3 の行き先も実測しました。** `/projects` の上部の帯にあるリンクは 4 本だけで、**管理へ向かうものは 1 本もありません**（#77）。

## 制約の遵守

- **削除コマンドを一切生成していません。**
- **実装側の作業ツリーでブランチを切り替えていません。** 読み取りのみです。
- コマンドはすべて `docker compose exec -T dev bash -lc '...'` で、**リポジトリ直下から**実行しました。
- **動いている開発サーバーを止めていません。**
- コードレビューは行っていません。
- 外部への通信をしていません。

## 申し送り

- **`.env.example` に、`main` を取り込んだときの衝突の跡が残っています。** `bcbd499` の時点で跡の 3 行がそのまま入っており、`E2E_BASE_URL` が 2 つ並んでいます。**マージの前に直す必要があります。**
- **`test/pr174/screens.spec.ts` は `--workers=1` で実行してください。** 並列だと手順 17・18 の順番の依存で落ちます。
- **開発サーバーの番号が、ホストのブラウザから届きません。** いま動いている `3020` ・ `3021` はコンテナの中の番号で、公開されているのは `3300 -> 3000` と `3301 -> 3001` です。**非エンジニアの方に実際にお願いする前に、`3000` で起動し直す必要があります。**
- 自動検査は `bcbd499` で 5 つとも緑です（run `33020985158`）。**`main` が進んだ場合は数え直しが要ります。**
- 本文に載せた開発環境の実測値は、**手元の開発データに依存します。**

## 追記（実装側の対応）

**申し送りの 1 件目（`.env.example` の衝突の跡）と 2 件目（並列で落ちること）は、`3158f55` で直しました。**

- 跡の 3 行を取り除き、`E2E_BASE_URL` を 1 つ（`http://localhost:3000`）にしました。**リポジトリ全体を調べ、他に跡が残っていないことを確かめました**（検出 0 件）。
- 手順 18 の例を、その例だけで完結する形へ改めました。**自分で書いてから読み直します。** 並べて動かして **24 例すべてが緑**になることを確かめました（21.4 秒）。

連絡先は info@rictaworks.jp です。

---

## 2 回目

- 対象 PR：#174
- ブランチ：`feature/screens-02-09`（`68fc03c`）
- 土台：`main`（`84cbe4a`）
- 実施日時：2026-08-27（JST）
- 見た差分：`3158f55` と `68fc03c` の 2 つのコミットです
- 作業ツリー：1 回目に作った `DELETE/pr174-checker-20260827` を `68fc03c` へ進めました。**実装側の作業ツリーでブランチを切り替えていません。**

### 判定

合格

**本文のユーザーテスト手順が、いまの画面と食い違っていました。** 見つかった食い違いをすべて直し、**手順を 22 件から 30 件へ増やしました**（自動の例は 24 例から 36 例へ増えています）。タイトルは変えていません。

### 何を確かめたか

`68fc03c` は、レビューの指摘へ答えるために**画面の見せ方そのものを変えています。** 変わったところを 1 つずつ本文と突き合わせました。

| 変わったところ | 1 回目の本文 | いまの画面 | 直したか |
|---|---|---|---|
| **生成の画面の大きな見出し** | 〈Generating〉 | **〈Package Ready〉**（受け取り済みのため）。上の小さな文字も〈PACKAGE〉です | **直しました**（手順 10） |
| **終わった状態で〈Generating〉を出さないこと** | 記載なし | 出しません | **手順 10b を足しました** |
| **縮退の見せ方** | 帯と〈NOT REFINED〉だけ | 帯に加えて**〈REQUEST #1 / VARIATION 01〉・構図の名前・生成モデル・辞書の版・組み立てた本文の引用**が出ます | **直しました**（手順 11） |
| **管理の入口** | `/projects` の 1 画面だけを見る | **4 画面で、`/admin` で始まる入口が 1 つも無いことを見ます** | **直しました**（手順 2） |
| **プリセットの呼び出し** | 「一覧の一番上」を押し、`SaaS・ソフトウェア` ・ `Midjourney` を期待 | **自分で保存した名前の行**を押し、`美容` ・ `イラスト` ・ `DALL·E` の**3 つとも**戻ることを見ます | **直しました**（手順 21） |
| **禁止入力の差し戻し** | 記載なし | 〈REJECTED〉・〈REASON 01〉・〈SUGGESTION〉・**送った文章の写しと語の印**・〈入力を修正する〉 | **手順 23 〜 26 を足しました** |
| **上限到達** | 記載なし | 〈DAILY QUOTA〉・〈NEXT RESET〉・**大きな `03:00`**・`JST / 2026/08/28 03:00`・**〈消費の帰属〉**・〈返還される場合〉 | **手順 27 〜 29 を足しました** |
| **一覧の小見出し** | 記載なし | 〈プロジェクト〉〈生成履歴〉が**見える形**で出ます | **手順 1 ・ 4 へ足しました** |
| **一覧の印** | 〈縮退〉のみ | 〈縮退〉と**〈受け取り済み〉** | **直しました**（手順 5） |
| **読み込みの逃がし先**（`error.tsx`） | 記載なし | 〈もう一度読み込む〉が出ます | **「確かめられないこと」へ足しました** |

### いちばん大きな食い違い

**開発サーバーの番号が、ホストのブラウザから届きませんでした。**

1 回目の本文は `http://localhost:3020/...` を案内していました。**これはコンテナの中の番号です。** `ENV/DEVELOPMENT.md` と `.env`（`NEXT_PUBLIC_BASE_URL`）のとおり、ホスト側へ公開されているのは **`3300`（画面）と `3301`（API）** です。**非エンジニアの方がブラウザに `3020` と打っても、何も開きません。**

**本文の URL を、すべて `3300` へ直しました**（`3020` の残りは 0 件です）。1 回目の申し送りに書いていた内容を、本文そのものへ反映した形です。

### 実測した数値（`68fc03c`）

自動検査（GitHub Actions・run `33028031709`）のログから採りました。**5 つの検査すべてが通っています。**

| 検査 | 実測 | 本文の記載（更新前） | 判断 |
|---|---|---|---|
| RSpec | 1799 例、失敗 0 件 | 1790 例 | **直しました** |
| RuboCop | 197 ファイル、指摘 0 件 | 196 ファイル | **直しました** |
| Jest | 15 ファイル・132 例、失敗 0 件 | 12 ファイル・88 例 | **直しました** |
| ESLint | 指摘 0 件 | 同じ | 一致 |
| TypeScript の型検査 | 指摘 0 件 | 同じ | 一致 |
| `next build` | 成功（8 つの場所） | 同じ | 一致 |
| ハードコードの検査 | 検出 0 件 | 同じ | 一致 |
| 本番と同じ設定での読み込み確認 | pass | 同じ | 一致 |
| 開発フックの検査 | pass | 同じ | 一致 |
| ブラウザ操作の確認 | **36 例**（`test/pr174/screens.spec.ts`） | 24 例 | **直しました** |
| 変更ファイル数 | **51 件** | 46 件 | **直しました** |

**`.env.example` は、もう差分に含まれません。** `3158f55` で取り込みの跡を始末したためです。**1 回目の申し送りの 1 件目は解消しています。**

**`playwright.config.ts` が `fullyParallel: false` ・ `workers: 1` になりました。** 1 回目の申し送りの 2 件目（`--workers=1` を付けてください）は不要になりましたので、**「設定どおりに流してください」へ書き換えました。**

### ブラウザで実際に確かめた結果

**確認の相手が混み合っていましたので、他の確認を邪魔しない形で行いました。**

- すでに動いていた開発サーバー（別の確認が使用中）を**止めていません。**
- **自分用に、番号を分けた開発サーバーを別に立てました**（API は `3005`、画面は `3010`）。実装側の作業ツリーのファイルは読むだけで、**ブランチを切り替えていません。**
- 確認が済んだあと、**自分で立てた 2 つだけを止めました。**

**次の 18 例が緑になることを、この目で確かめました。**

| 手順 | 例 | 結果 |
|---|---|---|
| 1 ・ 19 | 見出しが出ます（履歴・一覧／プリセット） | **緑** |
| 2 | 管理へ向かう入口がありません（4 画面） | **緑（4 例）** |
| 4 | 過去の生成が並びます | **緑** |
| 5 | 縮退の印が出ます | **緑** |
| 10 | 状態が出ます（**〈Package Ready〉**） | **緑** |
| 10b | 終わった状態で、生成中の見出しを出しません | **緑** |
| 11 | 縮退の帯が出ます | **緑** |
| 21 | 保存した条件を入力フォームへ呼び出せます | **緑** |
| 23 | 禁止入力の差し戻しが画面に出ます | **緑** |
| 24 | 差し戻しの理由と直し方が出ます | **緑** |
| 25 | 送った文章に印が付きます | **緑** |
| 26 | 入力へ戻れます | **緑** |
| 27 | 上限到達に、次回のリセット時刻が出ます | **緑** |

**手順 28 ・ 29 は、この場では確かめ切れませんでした。** 理由は下の「環境で起きたこと」に書きます。**画面の作りの問題ではありません。** 実装側の記録（`review-records/security-review/feature-screens-02-09.md` の 5 回目）では、**36 例が 2 回続けて緑**と実測されています。

**残りの手順（3 ・ 6 〜 9 ・ 12 〜 18 ・ 20 ・ 22）は、1 回目に緑を確かめており、`68fc03c` で画面が変わっていません。**

### 期待結果の裏取り

画面の言葉を、置き場と突き合わせて確かめました。**推し量って書いていません。**

- 〈Package Ready〉〈PACKAGE〉〈Limits & Fallbacks〉：`src/frontend/src/strings/ja.ts` の `requestStatus.labels`
- 〈消費の帰属〉〈返還される場合〉〈縮退も失敗したとき〉〈JST〉：同じく `exceptions.labels`
- 〈入力を修正する〉：`exceptions.labels.rejectedFix`（**1 回目の〈入力の修正へ戻る〉から変わっています**）
- `03:00` と `2026/08/28 03:00`：リセットは **JST 3:00** です（`src/backend/app/services/quota/quota_day.rb`）。**枠の区切りは午前 0 時ではありません。**
- 〈消費の帰属〉に出る日付：**次回のリセットの前日**です。開発のデータベースで、いまの帰属が `2026-08-27` であることを確かめました
- `美容` ・ `イラスト` ・ `DALL·E`：`choices` の呼び名です
- 〈有名人〉が差し戻される語であること：`src/backend/config/forbidden_inputs.yml`
- **差し戻しは枠を使いません**：`PromptRequests::Acceptance` が、禁止入力の判定を枠の予約より先に置いています

**手順の並び順を、この事実に合わせました。** 枠を使わない手順（1 〜 26）を先に、**枠を使い切る手順（27 〜 29）を最後に**置き、そのことを本文の冒頭で先にお伝えしています。

### 本文へ足した節・直した節

- **「何を変えたか」**：うまくいかなかったときの画面（上限到達・差し戻し・縮退）を足しました。
- **「何を作ったか」**：08 の行を「縮退・上限到達・差し戻し」に改めました。
- **「判断したこと」**：状態に合わせた見出し・次回のリセット時刻を既定へ寄せないこと・〈同じ条件で作る〉が条件をそのまま引き継ぐこと・差し戻しで文章に印を付けることを足しました。
- **「変更したファイル」**：51 件へ直し、`error.tsx` ・ `exceptions.test.tsx` ・ `input_choices_contract_spec.rb` ・ `lib/recaptcha.test.ts` ・ `types/resources.test.ts` ・ `playwright.config.ts` を足しました。
- **「確かめたこと」**：run 番号と数値を `68fc03c` の実測へ差し替え、1 つずつ流す理由を添えました。
- **「ユーザーテスト手順」**：URL をすべて `3300` へ直し、**30 手順**へ組み直しました。
- **「この PR では確かめられないこと」**：読み込みの逃がし先を足し、〈同じ条件で作る〉の書き方を実態へ合わせました。
- **「申し送り」**：解消した 2 件を書き換え、枠を使い切る手順への注意を足しました。

### 環境で起きたこと（PR の内容とは別です）

**確認の途中で、開発のデータベースがクラッシュ復旧に入りました。** 復旧に約 300 秒かかり、その間、動いていたバックエンドは `ActiveRecord::ConnectionNotEstablished` を返し続けました。**このとき観測した失敗は、すべてこれが原因です。** データベースが戻ったあと、番号を分けて立て直した環境では、同じ例がすべて緑になりました。

**そのあと、生成を受け付けた裏の処理が外部の AI の API を待ち続け、バックエンドの応答が遅くなりました**（`/api/v1/projects` で 44 秒）。**手順 28 ・ 29 を確かめ切れなかったのは、これが理由です。**

**どちらも、この PR が持ち込んだものではありません。** ただし、**非エンジニアの方に実際にお願いする前には、開発サーバーを起動し直す必要があります。**

### 制約の遵守

- **削除コマンドを一切生成していません。** 止めたのは、自分で立てた開発サーバーの処理だけです。
- **実装側の作業ツリーでブランチを切り替えていません。**
- **他の確認が使っている開発サーバーを止めていません。**
- コマンドはすべて `docker compose exec -T dev bash -lc '...'` で実行しました。
- コードレビューは行っていません（reviewer が別途実施しています）。
- 本文はすべて一時ファイル経由（`--body-file`）で渡しました。**文字化けは起きていません**（反映後に取り直して突き合わせ済みです）。

### 申し送り

- **手順 27 〜 29 は、その日の生成の枠を使い切ります。** 本文の冒頭と手順の見出しで、二重にお伝えしています。
- **〈同じ条件で作る〉が条件をそのまま引き継ぐこと**には、**ブラウザ操作の例がありません。** 画面の単体テストのみです。手順との 1 対 1 を崩さないため、本文には手順を足していません。**例を足すかどうかは、実装側でご判断ください。**
- **開発のデータが増え続けています。** 確認のたびにプロジェクトが増え、いまは 140 件を超えています。**入力フォームの読み込みが目に見えて遅くなっています。**
- **開発サーバーを起動し直してから、非エンジニアの方へお願いしてください。** ホスト側の番号は `3300`（画面）と `3301`（API）です。
- 本文に載せた画面の値（案の中身・日付・番号）は、**手元の開発データに依存します。**

連絡先は info@rictaworks.jp です。

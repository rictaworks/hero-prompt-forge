# PR 整備 : PR #149（2 回分をまとめた記録です）

- 対象：PR #149「[実装] 固有名詞の取り扱いを実装します」 / issue #44
- 回数：2 回（1 回目 初稿 / 2 回目 修正後の実測へ更新）

---

# PR 整備 : PR #149

- 実施日時（JST）：2026-08-26 21:15
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/149
- 対象 issue：#44 「[実装] 日本語固有名詞の保持を実装する」
- ブランチ：`feature/issue-44-proper-noun`（`origin/main` との差分で確認しました）
- 実施者：pr-checker

## 実施したこと

タイトルと本文の整備です。コードレビューは行いません（reviewer の担当です）。

実装側の作業ツリーではブランチを切り替えていません。`git checkout` ・ `git switch` ・ `git stash` を実行していません。
確認は `git fetch origin` のうえ、一時的な作業ツリー（`git worktree add --detach`）で行いました。

## タイトル

`[実装] 日本語固有名詞の保持を実装します`

- 日本語です。ですます調です。絵文字・個人名・秘匿情報を含みません。
- 変更していません（そのままとしました）。

## 実測して確かめた数値

一時作業ツリーを `hero-prompt-forge-dev` イメージで動かし、実測しました。

| 本文の記載 | 実測 | 判定 |
|---|---|---|
| 変更したファイル 9 件 | 9 件（`gh pr view 149 --json files`） | 一致します |
| `.tsx` を 1 件も含まない | 含みません | 一致します |
| 固有名詞のテスト 35 件すべて成功 | 35 件 | 一致します |
| ローマ字のテスト 32 件すべて成功 | 32 件 | 一致します |
| 2 ファイル合計の実行結果 | 67 examples, 0 failures | 一致します |
| バックエンドのテスト全体 753 件 | 753 examples | 一致します |
| RuboCop 107 ファイル・指摘 0 件 | 107 files inspected, no offenses detected | 一致します |
| 自動の検査は 5 項目 | 5 項目すべて pass | 一致します |
| 検査の名前〈バックエンド（RuboCop / RSpec）〉 | 同名で存在します | 一致します |

本文に挙げたファイル名 9 件は、いずれも実物のパスと一致します。

参照している issue 番号も確かめました。#44 ・ #51（アートディレクションノート）・ #55（生成リクエスト API）・ #71（03 入力フォーム）・ #73（05 結果3案）は、いずれも本文の説明と実物の表題が一致します。

## 変換の例の突き合わせ

一時作業ツリーの実装（`Generation::ProperNoun` ・ `Generation::Romaji`）をそのまま動かして確かめました。

| 入力 | 実測の出力 | 本文の記載 | 判定 |
|---|---|---|---|
| `櫻花堂（おうかどう）の…` | `Oukadou, a shop name` | Oukadou | 一致します |
| `櫻花堂の…`（読みなし） | `櫻花堂, a shop name kept as written in Japanese` | Oukadou と書かれていました | **食い違いました。直しました** |
| `さくら堂の…` | `Sakuradou, a shop name` | Sakuradou | 一致します |
| `株式会社ミライ工房が…` | `Miraikoubou, a company name` | Miraikoubou | 一致します |
| `「さくら」という…` | `Sakura, a proper name` | Sakura | 一致します |
| `東海林写真館の…` | `東海林写真館, a shop name kept as written in Japanese` | 同左 | 一致します |
| `「コーヒー」` | `Kohi, a proper name` | Kohi | 一致します |
| `「のはら」` | `Nohara, a proper name` | Nohara | 一致します |

`の: no` と書いた場合の再現も行いました。値は `false`（真偽値）として読まれ、変換結果は `Falsehara` になります（`Hara` ではありません）。
現在の実装では、対応表の値が小文字の英字だけかを読み込み時に確かめ、`Generation::Romaji::InvalidDefinitionError` を投げて失敗させることを確認しました。

## 直した箇所

1. **「櫻花堂」の行**を「櫻花堂（おうかどう）」に直しました。読みが添えられていない「櫻花堂」は `Oukadou` になりません。あわせて、読みが無い場合は元の表記のまま残ることを 1 文で補いました。
2. **落とし穴の節の結果**を実測どおりに直しました。「`Hara` になってしまいます」を「`Nohara` ではなく `Falsehara` になってしまいます」に改めました。読みが「欠ける」のではなく `false` という文字の並びに化ける、という説明に直しました。あわせて、読み込み時に値を確かめてその場で失敗させることを補いました。
3. **まだ画面から通らないこと**を「何を変えたか」に明記しました。呼び出す受け口（issue #55）が無いため、この PR は部品を用意するところまでである旨を書きました。未実装のものを実装済みと読ませないためです。
4. **記録と控えの書き分け**を補いました。調査用の記録には種別と件数だけを残し、生成の下書きに添える控えにはお名前と「読み方が分からなかった」ことを残す、という区別を書きました。

反映は `gh pr edit 149 --title "…" --body-file <一時ファイル>` で行い、反映後の本文が渡した内容と一致することを確認しました。

## 文体・表記の確認

| 確認したこと | 結果 |
|---|---|
| タイトル・本文が日本語であること | 満たしています。英語は識別子・ファイルパス・生成 AI へ渡す英文・画面上のボタン名だけです |
| ですます調であること | 満たしています。だ・である調の文はありません |
| 絵文字がないこと | 満たしています（0 件） |
| 個人名がないこと | 満たしています。連絡先は `info@rictaworks.jp` です |
| `.claude/` 直下の md の内容を転記していないこと | 満たしています |
| 未実装のものを書いていないこと | 満たしています（4 の追記で明確にしました） |

## 非エンジニア向けユーザーテストの確認

| 確認したこと | 結果 |
|---|---|
| ブラウザだけで完了すること | 満たしています。ターミナル・コマンド・環境変数・ログの確認を含みません |
| 開始画面が明示されていること | 満たしています。PR のページの URL をリンクで示し、表題で正しいページかを確かめさせています |
| 1 手順 1 操作であること | 満たしています。全 7 手順が「押す」「スクロールする」「見る」の 1 操作です |
| 期待結果が画面に見えるもので書かれていること | 満たしています。「緑色のチェック印が付いています」等です |
| 失敗時の報告事項が書かれていること | 満たしています。画面の写真・URL・時刻（JST）です |
| 画面で確かめられない場合にその旨を書いていること | 満たしています。アプリの画面での手順が無いこと、その理由（issue #71 ・ #55 が未実装）、いつ確かめられるようになるか（issue #73 以降）を書いています |

リポジトリは公開設定ですので、GitHub のアカウントが無い方でも、この 3 つの確認はブラウザだけで行えます。

手順を捏造していません。アプリの画面での確認手順は書いていません。

## 後片付け

一時的な作業ツリーは、確認後に `mv` で `DELETE/pr149-worktree/` へ移しました。削除コマンドは使っていません。

## 判定

タイトルと本文の整備を完了しました。上記の 4 点を直し、本文の数値・ファイル名・件数・変換の例が、いずれも実物と一致することを確認しました。


---

# PR 整備（2回目） : PR #149

- 実施日時（JST）：2026-08-26 22:05
- 対象 PR：https://github.com/rictaworks/hero-prompt-forge/pull/149
- 対象 issue：#44 「[実装] 日本語固有名詞の保持を実装する」
- ブランチ：`feature/issue-44-proper-noun`（先頭 `6a99e67`）
- 実施者：pr-checker

## きっかけ

1 回目の整備のあと、コミットが 3 つ増えました。

| コミット | 内容 |
|---|---|
| `c846acd` | 一般名詞を名前として拾わないようにします |
| `582f476` | 手がかりを、名前と分かる書き方だけに絞ります |
| `6a99e67` | 名前であることを先に述べる書き方も拾います |

**仕組みの姿が大きく変わりましたので、本文を実測へ直しました。**

## 確認のやり方

実装側の作業ツリーではブランチを切り替えていません。`git checkout` ・ `git switch` ・ `git stash` を実行していません。
`git fetch origin` のうえ、一時的な作業ツリー（`git worktree add --detach 6a99e67`）を scratchpad 内に作り、そこで実測しました。リポジトリ直下に一時ファイルを作っていません。

- 変換の例：`ruby:3.4-slim` のコンテナで `Generation::ProperNoun` ・ `Generation::Romaji` をそのまま動かしました。
- 件数：`hero-prompt-forge-dev` のイメージで `bundle exec rspec` ・ `bundle exec rubocop` を動かしました。

## 実測した数値

| 本文の記載（直す前） | 実測 | 対応 |
|---|---|---|
| 変更したファイル 9 件 | 9 件 | 変わりません |
| `.tsx` を 1 件も含まない | 含みません | 変わりません |
| 固有名詞のテスト 35 件 | **72 件** | 直しました |
| ローマ字のテスト 32 件 | **42 件** | 直しました |
| バックエンドのテスト全体 753 件 | **800 件** | 直しました |
| RuboCop 107 ファイル・指摘 0 件 | 107 files inspected, no offenses detected | 変わりません |
| 自動の検査は 5 項目 | 5 項目すべて pass | 変わりません |

2 つのテストファイルをまとめて実行した結果は 114 examples, 0 failures でした（72 + 42 と一致します）。

## 変換の例の突き合わせ

| 入力 | 実測の出力 | 本文（直す前） | 対応 |
|---|---|---|---|
| `さくら堂の焼き菓子をお届けします。` | 拾いません | `Sakuradou, a shop name` | 直しました |
| `「さくら堂」という店の焼き菓子です。` | `Sakuradou, a proper name` | 記載なし | 足しました |
| `櫻花堂（おうかどう）の焼き菓子です。` | 拾いません | `Oukadou, a shop name` | 直しました |
| `「櫻花堂」（おうかどう）の焼き菓子です。` | `Oukadou, a proper name` | 記載なし | 足しました |
| `株式会社ミライ工房が運営します。` | `Miraikoubou, a company name` | 同左 | 変わりません |
| `「さくら」という名前のベーカリーです。` | `Sakura, a proper name` | 同左 | 変わりません |
| `「東海林写真館」という店の撮影プランです。` | `東海林写真館, a proper name kept as written in Japanese` | `a shop name kept as written…` | 直しました |
| `「コーヒー」という名前のカフェです。` | `Kohi, a proper name` | Kohi | 変わりません |
| `屋号は「はな花」です。` | `はな花, a proper name kept as written in Japanese` | 記載なし | 手がかりの例として足しました |
| `店名は「のはら」です。` | `Nohara, a proper name` | Nohara | 変わりません |

**意味説明が `a shop name` から `a proper name` に変わっています。** 屋号の語尾の規則がなくなり、かぎ括弧の規則へまとまったためです。本文の表記をすべて直しました。

誤検出をやめた分も実測しました。次の 5 文は、いずれも「拾いません」でした。

- `明るい部屋でくつろげます。`
- `近所の本屋へ寄ります。`
- `学校の食堂で食べます。`
- `「安心」をお届けします。`
- `囲炉裏（いろり）を囲みます。`
- `名前のとおり「明るい」雰囲気です。`

ローマ字の直しも実測しました。

| 読み | 実測 |
|---|---|
| しんいち | `Shin'ichi` |
| しにち | `Shinichi` |
| まっちゃ | `Matcha` |
| ゔぃれっじ | `Virejji` |
| コーヒー | `Kohi` |

`しんいち` と `しにち` が同じ綴りにならないこと、`まっちゃ` がヘボン式の `Matcha` になること、外来音のかなが対応表に加わったことを確かめました。

## 申し送りの確認

| issue | 表題 | 本文への記載 |
|---|---|---|
| #152 | [画面] 名前を確実に反映させる書き方を、入力フォームで案内します | 書きました |
| #153 | [不具合] 会社名の切り出しが「の」より後ろまで拾います | 書きました |

#153 は実測で再現しました。`株式会社ミライ工房の商品です。` を渡すと、`ミライ工房の商品` までを 1 つの名前として拾います。本文にはこの実例を載せました。

## 本文で直した箇所

1. **「なぜ変えたか」の表**を、いま拾える書き方の文へ入れ替えました。意味説明も `a proper name` へ直しました。
2. **「どうやって名前を見つけるか」**を、手がかり 2 つだけの表に作り替えました。屋号の語尾の行を外しました。
3. **「手がかりを 2 つに絞りました」の節**を新しく足しました。23 文中 20 文の誤検出を実測で見つけたこと、かぎ括弧だけ・読みだけを手がかりにしない理由（強調のかぎ括弧、一般名詞に添える読み）を、実例つきで書きました。
4. **「そのぶん、取りこぼしが増えます」の節**を新しく足しました。拾う書き方と拾わない書き方を表で並べ、**間違って拾うより取りこぼすほうが安全だと判断した**ことを書きました。
5. **「読み方が分からないときは、当てずっぽうにしません」の表**を、いま拾える文へ入れ替えました。
6. **「見つけた落とし穴」**へ、ローマ字の書き方の直し（`Shin'ichi` ・ `Matcha` ・外来音）を足しました。
7. **「確認した結果」の件数**を 72 / 42 / 800 へ直し、最新のコミット（`6a99e67`）で測り直した旨を明記しました。
8. **「申し送り」の節**を新しく足し、issue #152 と #153 を表題つきで書きました。
9. 「判断したこと」から、重複していた取りこぼしの記述を外し、4 の節へまとめました。

反映は `gh pr edit 149 --title "…" --body-file <scratchpad の一時ファイル>` で行い、反映後の本文が渡した内容と一致することを確認しました。

## 文体・表記の確認

| 確認したこと | 結果 |
|---|---|
| タイトル・本文が日本語であること | 満たしています。英語は識別子・ファイルパス・生成 AI へ渡す英文・画面上のボタン名だけです |
| ですます調であること | 満たしています。だ・である調の文はありません |
| 絵文字がないこと | 満たしています（0 件） |
| 個人名がないこと | 満たしています。連絡先は `info@rictaworks.jp` です |
| `.claude/` 直下の md の内容を転記していないこと | 満たしています |
| 未実装のものを書いていないこと | 満たしています。まだ画面から通らないこと、#152 と #153 は別で扱うことを明記しています |

タイトルは `[実装] 日本語固有名詞の保持を実装します` のままとしました。変える理由がありません。

## 非エンジニア向けユーザーテストの確認

| 確認したこと | 結果 |
|---|---|
| ブラウザだけで完了すること | 満たしています。ターミナル・コマンド・環境変数・ログの確認を含みません |
| 開始画面が明示されていること | 満たしています。PR のページの URL をリンクで示し、表題で正しいページかを確かめさせています |
| 1 手順 1 操作であること | 満たしています。全 7 手順が「押す」「スクロールする」「見る」の 1 操作です |
| 期待結果が画面に見えるもので書かれていること | 満たしています |
| 失敗時の報告事項が書かれていること | 満たしています。画面の写真・URL・時刻（JST）です |
| 画面で確かめられない場合にその旨を書いていること | 満たしています |

手順の中身は 1 回目から変えていません。検査は 5 項目のまま、`.tsx` は 1 件も含まないままですので、そのまま通ります。

## 後片付け

一時的な作業ツリーは、確認後に `mv` で `DELETE/` 配下へ移しました。削除コマンドは使っていません。

## 判定

本文を実測へ直し、2 回目の整備を完了しました。数値・変換の例・手がかりの説明・申し送りが、いずれも最新のコミット `6a99e67` の実物と一致します。

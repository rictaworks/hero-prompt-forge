# pr-checker 記録：PR #180（issue #171, #172 利用規約・プライバシーポリシー・特定商取引法に基づく表示への導線）

- 対象 PR：#180
- ブランチ：`feature/legal-pages`（`2442464`）
- 土台：`main`（`4a64c51`。**現在の `main` と同じです。** 取り込みは `MERGEABLE` です）
- 実施日時：2026-08-27（JST）
- 作業ツリー：確認用の作業ツリー `DELETE/pr180-checker-20260827` を新しく作り、そこで確かめました。**実装側の作業ツリー（`DELETE/impl-legal`）へ触れていません。`main` の作業ツリーでブランチを切り替えていません。**

## 判定

合格

タイトルはそのままとし、本文を規定の型（何を変えたか／なぜ変えたか／変更したファイル／ユーザーテスト手順／うまくいかなかったとき）へ組み直しました。**ユーザーテストの手順を、実際に画面を立ち上げて 1 手順ずつ突き合わせ、食い違っていた箇所を実測に合わせて直しました。** **本文に載っていた自動検査の数値が 3 つとも実測と違っていましたので、実測値へ直しました。**

## 反映した内容

- タイトル：`[文書] 利用規約・プライバシーポリシー・表示への導線を作ります (#171, #172)`（**変更していません。** 日本語・ですます調で、規約に沿っています）
- 本文：**組み直しました。** 日本語化は不要でした（もとから日本語・ですます調です）。**ユーザーテストの手順と、自動での確認の数値を直しました。**

## 画面を立ち上げて確かめた方法

**共有のデータベースへ負荷をかけないため、また他の確認を止めないため、次のようにしました。**

- コンテナの中では、**別の確認（PR #174 の作業ツリー）が 3000 番（画面）と 3001 番（API）を使っていました。** **これを止めていません。**
- 当方は、確認用の作業ツリーで `npm ci` を行い、**コンテナの中の 3010 番**で `next dev` を立ち上げ、コンテナの中から `curl` で読みました。
- 確認が終わったあと、**当方が立てた 3010 番だけ**を止めました。**3000 番・3001 番はそのまま動いています。**

**ホスト側の番号（3300 ・ 3301）とコンテナの中の番号（3000 ・ 3010 ・ 3001）を、本文で混ぜていません。** 本文に書いたのは **3300 番だけ**です。**コンテナの中の番号は本文に 1 つも書いていません**（反映後の本文を `grep` で確かめ、`3000` ・ `3001` の出現が 0 件であることを確認しました）。

## 画面と食い違っていた箇所（直しました）

**3 つの画面（`/terms` ・ `/privacy` ・ `/commerce`）とランディング（`/`）を実際に開き、描画された文字と CSS を 1 つずつ突き合わせました。**

| 更新前の本文 | 実際の画面 | 対応 |
|---|---|---|
| 確認 3「**2 つの青い文字（リンク）**があります」 | **青くありません。** `a { color: var(--text-body) }`（`src/styles/tokens/base.css`）で、`--text-body` は `--text` ＝ **`#e0e0e0`** です。**本文の段落（`.paragraph` も `var(--text)`）とまったく同じ色**で、**下線もありません**（`text-decoration: none`） | **「本文と同じ色の文字です。青くありませんし、下線もありません。」と書き直しました。** あわせて、**マウスを載せると金色（`--gold` ＝ `#c9a84c`）に変わることを目印として書き足しました** |
| 確認 3「さらに下へたどると…2 つの…があります」 | 位置の手がかりがありません。**実物は「第 9 条（お問い合わせ）」の下、〈制定日〉の行のすぐ上**に 2 行で並びます | **位置を明示しました** |
| 確認 1「「第一印象を、業務品質に。」の節より**さらに下**に、**横並びの 3 つの文字**があることを確かめてください」 | 見出しの下に本文と〈**Xでログイン**〉のボタンがあり、**その下**が帯です。3 つの文字は **`--grey-999`（`#999`）の小さな灰色の文字**で、`font-size: 12px` です | **〈Xでログイン〉のボタンを通り過ぎることを書き足しました。** ボタンの表記は画面の実物どおり（**空白を入れずに「Xでログイン」**）にしました。**「小さな灰色の文字」と書き、押せる目印（金色に変わる）も足しました** |
| 確認 2「大きく「利用規約」と出ることを確かめます」 | **その上に、小さく「2026 年 8 月 27 日」が出ます**（`SectionHeading` の `eyebrow` が `updatedAtValue`） | **「小さく 2026 年 8 月 27 日、その下に大きく利用規約」と書き直しました。** 書かないと「余計なものが出ている」と誤って報告されます |
| 確認 2「第 1 条（適用）から第 11 条（お問い合わせ）まで」 | **11 条で正しいです。** ただし途中の 9 つの見出し名が本文にありません | **11 条の見出し名をすべて表にしました。** 「番号が飛んでいないこと」を確かめる手順ですので、照合の対象が要ります |
| 確認 3「ブラウザの「戻る」でランディングへ戻り」／確認 4「ランディングへ戻り」 | **3 つの画面には、ランディングへ戻るボタンもロゴもヘッダーもありません**（`LegalPage` は `main` と本文と `Facts` だけを描画し、`layout.tsx` に共通ヘッダーがありません） | **準備の節へ「戻るときはブラウザの「戻る」ボタンをお使いください」を独立して書きました。** 確認 4 の「ランディングへ戻り」にも**同じ操作を明記**しました |
| 確認 3-2「reCAPTCHA と Google のことが書かれている」 | **第 4 条には reCAPTCHA と Gemini の 2 つ**が書かれています | **「reCAPTCHA と Gemini という 2 つの Google のサービス」と具体化しました** |
| 確認 3-3「送らないと書かれていることを確かめてください」 | 実物の文は「**本サービスは、この項目を送りません。**」です | **確かめる文をそのまま引用しました** |
| 確認 4「無償であることが書かれていることを確かめます」 | 第 4 条の実物は「**本サービスの利用料金は無償です。**」です | **引用に直し、〈こうなれば正解です〉の形にしました** |
| 確認 4「義務ではないが自主的に示していることが書かれている」 | 第 1 条の実物は「**本サービスは対価をいただきませんので、この義務の対象になりません。**」「**安心してお使いいただくために、提供者と連絡先を自主的に示します。**」です | **2 文とも引用しました** |
| 確認 4 に〈こうなれば正解です〉がありません | 判定の基準が読み手に伝わりません | **2 つとも〈こうなれば正解です〉を付けました** |
| 確認 5「`http://localhost:3300/terms` を直接開きます」 | **`/privacy` ・ `/commerce` も同じくログインを求めません**（`middleware.ts` が存在せず、認証の関所がありません） | **3 つとも開く手順にしました。** 受け入れ条件は 3 つの画面すべてです |
| 「**3 つのどれかが見当たらなければ不合格です。**」ほか、突き放した書き方が 3 か所 | 読み手は次に何をすればよいか分かりません | **末尾の〈うまくいかなかったとき〉へ誘導する書き方にしました** |
| 準備「開発の担当者に、画面を立ち上げてもらってください。手元の開発環境では、住所は `http://localhost:3300/` です」 | 住所が押せる形になっていません | **`<http://localhost:3300/>` の形にし、押せるようにしました。** 3300 番のままです（**コンテナの中の 3000 番へ書き換えていません**） |
| 〈うまくいかなかったとき〉の節がありません | 型（テンプレート）の必須項目です | **新設しました。** 画面の写真・URL・時刻（JST）・ブラウザの名前を求め、連絡先を `info@rictaworks.jp` としました |

## 実測した数値（本文の数値が 3 つとも違っていました）

**共有のデータベースが `the database system is in recovery mode` を返す状態でしたので、RSpec は 1 例も実行していません。** バックエンドの数値は、**この PR の先頭（`2442464`）に対する CI の実行（`33071199984`）のログ**から読みました。

| 検査 | 実測 | 本文の記載（更新前） | 判断 |
|---|---|---|---|
| RSpec | **1956 例・失敗 0 件**（CI ログ `1956 examples, 0 failures`） | 1783 例、失敗 0 件 | **食い違い。直しました** |
| RuboCop | **213 ファイル・指摘 0 件**（**手元で実測**。CI ログも `213 files inspected, no offenses detected` で一致） | 194 ファイル、指摘 0 件 | **食い違い。直しました** |
| Jest | **152 例・16 ファイル・失敗 0 件**（**手元で実測**。CI ログも `Tests: 152 passed, 152 total` で一致） | 86 例、失敗 0 件 | **食い違い。直しました** |
| Jest（今回足した本文の押さえ `src/strings/legal.test.ts`） | **20 例・失敗 0 件**（手元で実測） | 記載なし | **書き足しました** |
| 直書きの文言の検査（`npm run check:hardcode`） | 検出 0 件（**手元で実測**。「日本語を含む文字列リテラルはありません。」） | 検出 0 件 | 一致 |
| ESLint ・ 型検査 | いずれも指摘 0 件（CI のジョブが緑） | 指摘 0 件 | 一致 |
| 組み立て（`next build`） | 成功。**8 経路すべてが静的（`○ (Static)`）**：`/` ・ `/_not-found` ・ `/commerce` ・ `/presets` ・ `/privacy` ・ `/projects` ・ `/requests/new` ・ `/terms` | 「`/` ・ `/terms` ・ `/privacy` ・ `/commerce` を静的に作ります」 | 誤りではありませんが不完全です。**8 経路の一覧に直しました** |
| 変更ファイル数 | **14 件（+874 / −16 行）** | 記載なし | **書き足しました** |
| 自動検査（GitHub Actions） | **5 つとも緑**（実行 `33071199984`、`2442464`） | 記載なし | **書き足しました** |

**RSpec の 1783 例・RuboCop の 194 ファイル・Jest の 86 例は、いずれも実測と合いません。** 本文の数値は、実装の途中の値か、別の枝の値と思われます。**3 つとも実測値へ直しました。**

## 画面の内容を実測で照合した結果

**確認用の作業ツリーで `next dev` を立ち上げ、4 つの画面を読み込んで照合しました。**

| 画面 | 応答 | 見出しの数 | 実測 |
|---|---|---|---|
| `/terms` | 200 | **11 条**（第 1 条〜第 11 条。番号の飛びなし） | 本文の「全 11 条」と一致 |
| `/privacy` | 200 | **9 条**（第 1 条〜第 9 条。番号の飛びなし） | 本文の「全 9 条」と一致 |
| `/commerce` | 200 | **8 条**（第 1 条〜第 8 条。番号の飛びなし） | 本文の「全 8 条」と一致 |
| `/`（ランディング） | 200 | — | いちばん下に 3 つの導線と Cookie の一文が出ます |

| 照合した項目 | 実測 |
|---|---|
| ランディングの帯の位置 | `ContactSection`（「第一印象を、業務品質に。」）**の直後**に `FooterSection` が並びます。**ページのいちばん下です** |
| 帯の 3 つの行き先 | `<a href="/terms">` ・ `<a href="/privacy">` ・ `<a href="/commerce">`（`next/link`。**横並び**：`display: flex; gap: 28px`） |
| 帯の一文 | 「ログインの際に、状態を保つための Cookie を置きます。詳しくはプライバシーポリシーをご覧ください。」**本文の引用と一字一句一致します** |
| 3 つの画面のいちばん下 | **制定日（2026 年 8 月 27 日）・提供者（rictaworks）・お問い合わせ先（info@rictaworks.jp）の 3 行。** 3 つの画面すべてに出ます |
| Google への 2 つの導線 | `https://policies.google.com/terms` と `https://policies.google.com/privacy`。**`target="_blank"` ＋ `rel="noreferrer noopener"`** ですので、**新しいタブで開きます**（本文の記載と一致） |
| Google への導線が出る画面 | **`/privacy` だけ**です（`which === "privacy"` のときだけ描画します）。本文も `/privacy` の手順にだけ書いています |
| 認証の関所 | **`src/frontend/src/middleware.ts` が存在しません。** 4 つの画面はいずれもログインを求めません（本文の確認 5 と一致） |
| 連絡先 | `info@rictaworks.jp`。**個人名はありません** |
| 文体 | 3 文書とも「ですます調」です。「だ・である調」は見当たりません |

## 本文へ足した節

- **「なぜ変えたか」**（新設）。issue #171 と #172 を `Closes` で結び、**Cookie を置くこと・reCAPTCHA で Google へ合図を送ること・提供者が画面から分からないこと**の 3 つを困りごととして書きました。
- **「変更したファイル」**（新設）。14 件を、**画面（新設）・画面（変更）・バックエンド・記録**の 4 つに分けて並べました。
- **「ご用意いただくもの」**（準備へ追加）。ブラウザだけで足りること、インストールが不要なこと、**ログインが要らないこと**、所要およそ 5 分を書きました。
- **「戻りかた」**（準備へ追加）。**3 つの画面に戻る導線が無い**ため、ブラウザの「戻る」を使うことを最初に書きました。
- **「うまくいかなかったとき」**（新設）。画面の写真・URL・時刻（JST）・ブラウザの名前を求め、連絡先を `info@rictaworks.jp` としました。

もとの本文の「判断したこと」（IP アドレス・特定商取引法・置き場・導線の位置の 4 つ）は、**内容が実装と一致していましたので、そのまま残しました。** 「変更しなかったもの」も残しました（`requirements.md` ・ `app-ui/` が差分に無いことを確かめました）。

## ブラウザで確かめられないこと（本文に明記しました）

- **IP アドレスを送らなくなったこと**は、画面に現れません。本文では「**画面では確かめられません。自動のテストで固定しています**」と書きました。**手順を捏造していません。**
- **ログインした後の画面からの導線**は、この PR に含まれません。別のプルリクエストで実装中の持ち場ですので、本文でも「別の課題として足します」と残しました。
- **3 つの画面からランディングへ戻る導線が無いこと**を、**新たに「確かめられないこと」へ書き足しました。** 実測で確かめた事実です。導線の追加は別の課題としています。

## 制約の遵守

- **削除コマンドを一切生成していません。** 確認用の作業ツリー `DELETE/pr180-checker-20260827` は、そのまま `DELETE/`（ゴミ箱）に残しています。
- **実装側の作業ツリー（`DELETE/impl-legal`）へ触れていません。** ブランチも切り替えていません。
- **RSpec を 1 例も実行していません。** 共有のデータベースが復旧中（`the database system is in recovery mode`）でしたので、**負荷を重ねずに CI の実行から読みました。** 開発用のデータベースへ書き込んでいません。
- **他の確認が使っている開発サーバー（コンテナの 3000 番・3001 番）を止めていません。** 当方は 3010 番で立ち上げ、**確認後に自分の 3010 番だけ**を止めました。
- **本文にコンテナの中の番号（3000 ・ 3001 ・ 3010）を 1 つも書いていません。** 書いたのはホスト側の **3300** だけです。
- コマンドはすべて `docker compose exec -T dev bash -lc '...'` で実行しました。
- 本文は一時ファイル経由（`gh pr edit 180 --body-file`）で反映しました。反映後の本文を読み直し、文字化けが無いことを確かめました。
- コードレビューは行っていません（reviewer の担当です）。
- `.claude/` 直下の md の内容を、本文・記録へ転記していません。

## 申し送り

- **コミットの見出しが `[実装]`、PR のタイトルが `[文書]` です**（コミット `2442464` は「[実装] 利用規約・プライバシーポリシー・表示への導線を作ります」）。**内容は文書と画面の追加ですので、どちらでも通ります。** 取り込みの際にどちらへ寄せるかは、担当の方のご判断でお願いします。
- **タイトルの「表示への導線」は、読み手によっては指すものが分かりにくいです**（3 つ目は「特定商取引法に基づく表示」です）。**日本語・ですます調の規約は満たしていますので、変更していません。**
- **この PR には `test/pr180/` がありません。** 画面を足す PR ですので、ブラウザ操作の確認を足すかどうかは reviewer と担当の方のご判断でお願いします（**PR #179 でブラウザ操作の確認が自動検査へ入りますが、まだ `main` に取り込まれていないため、この PR の自動検査は 5 つです**）。
- **3 つの画面から、ランディングへ戻る導線がありません。** ヘッダーもロゴもないため、ブラウザの「戻る」以外に戻る方法がありません。**本文の「確かめられないこと」へ書きましたが、UI としては別の課題で足していただくのがよいと考えます。**
- **Google への 2 つの導線は、本文の段落とまったく同じ色（`#e0e0e0`）で、下線もありません。** 押せる場所だと気づきにくい状態です。**本文の手順では「マウスを載せると金色に変わる」を目印として書きましたが、体裁そのものの是非は reviewer のご判断でお願いします。**
- **本文にあった自動検査の 3 つの数値（RSpec 1783 ・ RuboCop 194 ・ Jest 86）が、いずれも実測と違っていました。** 実測値（1956 ・ 213 ・ 152）へ直しています。**以後の PR でも、本文の数値は反映の直前に取り直していただけますと助かります。**

連絡先は info@rictaworks.jp です。

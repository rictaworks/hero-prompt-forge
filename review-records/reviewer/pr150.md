# コードレビュー : PR #150（3 回分をまとめた記録です）

- 対象：PR #150「[実装] 生存確認の窓口と管理画面の入口を実装します」 / issue #62 ・ #64
- 回数：3 回（1 回目 要修正 / 2 回目 要修正 / 3 回目 合格）

## 判定

**合格です。** マージして構いません。

1 回目・2 回目に挙がった要修正 5 件は、3 回目のレビューですべて解消を実測しました。
残る指摘はありません。

- 平文の `/admin` は 301 で https へ転送されます
- `/health` ・ `/up` は平文でも 200 を返します（監視のための除外が実働しています）
- 印の無い書き込みが止まることを、検証用の入口で実測しました
- RSpec 783 例 0 失敗、RuboCop 指摘 0 件、ハードコード検査も通過しました

以下に、3 回分の記録をそのまま残します。

---

# コードレビュー : PR #150

- 実施日時（JST）：2026-08-26 21:30
- 対象：PR #150 `feature/issue-62-64-ops`（issue #62 ／ issue #64）
- 差分の取り方：`git diff origin/main...origin/feature/issue-62-64-ops`（実装側の作業ツリーでブランチを切り替えていません）
- 実行の確かめ方：`git archive` で取り出した写しを開発コンテナの内側（`/home/dev/pr150work`）へ置いて動かしました。実装側の作業ツリーには触れていません。

## 判定（1 回目）

**要修正** です。マージの前に、下記「要修正」の 5 件へ対応をお願いします。

**押さえておきたい点**：管理画面の入口そのものは、試した攻め手の範囲で破れませんでした。資格情報の未設定・空文字・ヘッダの作り替えのいずれでも、中身へ届きませんでした。指摘の中心は、**「掛けたつもりの守りが、実際には働いていないこと」** と **「受け入れ条件の確かめ方が、確かめになっていないこと」** です。

## Issue の受け入れ条件

### issue #62（ヘルスチェック）

| 受け入れ条件 | 判定 | 根拠 |
|---|---|---|
| 正常時に 200、DB 異常時に 503 を返します | 満たします | 本番設定で実行し、正常時 `200 {"status":"ok"}`、到達不能時 `503 {"status":"unavailable"}` を確認しました |
| Railway が注入する `PORT` で待ち受けます | **未達（確かめが不十分）** | 実装（`src/backend/config/puma.rb:33`）は妥当ですが、テストが振る舞いでなく原稿の文字列を見ています。要修正 3 を参照してください |
| RSpec のリクエストテストがあります | 満たします | `src/backend/spec/requests/health_spec.rb`（16 例） |

### issue #64（管理画面の BASIC 認証）

| 受け入れ条件 | 判定 | 根拠 |
|---|---|---|
| 未認証で管理画面へ到達できません | 満たします | 本番設定で 5 通りの入り方を試し、いずれも 401 でした（後述の実行結果） |
| 資格情報が環境変数から読まれます | **条件付き** | 実装は環境変数から読みます（`authenticates_admin.rb:45-46`）。ただし開発環境へ値が渡る経路がありません。要修正 2 を参照してください |
| RSpec のリクエストテストがあります | 満たします | `src/backend/spec/requests/admin_spec.rb`（11 例） |

### スコープ

- `src/backend/spec/config/locales_spec.rb` の作り直しは、`admin.headings` を足したことに伴う変更ですので、スコープ内と見ます。ただし検査の広がりに気になる点があります（提案 8）。
- そのほかにスコープ外の変更はありません。`.tsx` の変更が 1 件も無いことも確認しました。

## 各基準への適合

| 基準 | 判定 | 備考 |
|---|---|---|
| CC | 合致します | 個人名なし、連絡先は `info@rictaworks.jp`、ですます調、絵文字なし、JST、UTF-8 |
| OWASP10 | **要修正** | 要修正 1・4 が該当します |
| QC10 | **要修正** | 要修正 1・3 が該当します（守りが働かないことを検知できない、受け入れ条件を確かめていない） |
| CRAP | 保留 | 管理画面は素の HTML 1 枚で、判断材料がありません。提案 9 を参照してください |
| DP | 合致します | 認証を継承の土台へ寄せた判断、利用者向け API と管理画面の土台を分けた判断は妥当です |
| TM | **要修正** | 要修正 1・3 が該当します |

## 要修正

### 1. 管理画面の CSRF 対策が、実際には働いていません

- 場所：`src/backend/app/controllers/admin/application_controller.rb:20` の `protect_from_forgery with: :exception`
- あわせて：`src/backend/app/views/layouts/admin.html.erb:8` の `csrf_meta_tags`

Rails 8.1 の `protect_against_forgery?` は `allow_forgery_protection && session.enabled?` です（actionpack-8.1.3.1 `lib/action_controller/metal/request_forgery_protection.rb:620`）。本アプリは `src/backend/config/application.rb:59` で `config.api_only = true` とし、`:42` で `ActionDispatch::Cookies` だけを足しています。**セッションの仕組みを足していません。** そのためミドルウェアの並びにセッションがなく、`session.enabled?` が `false` になります。

結果として、次の 2 つが起きます。

1. `protect_from_forgery with: :exception` が**何もしません**。
2. `csrf_meta_tags` が**空文字を出します**（実際に本番設定の応答の `<head>` に token の meta がありません）。

本番設定で実際に確かめました。管理画面の土台へ POST の経路を 1 本足し、`Origin: https://evil.example.com` を付けてトークン無しで送ったところ、**200 で通りました。**

```
Rails.env=production
allow_forgery_protection=true
session.enabled?=false
protect_against_forgery?=false
cross-origin POST without CSRF token: status=200 body=probe-ok
```

いまは管理画面に GET しかありませんので、ただちに悪用される状態ではありません。ただし **BASIC 認証の資格情報は、ブラウザが同じ生成元への後続の要求へ自動で付けます。** #65〜#68 で状態を変える操作が入った時点で、そのまま外部サイトから叩ける穴になります。

**テストでは気づけません。** `src/backend/config/environments/test.rb:31` で `allow_forgery_protection = false` としているためです。念のため、写しの側でこれを `true` に変えて `admin_spec.rb` を流しましたが、**11 例すべてそのまま通りました**（`session.enabled?` が `false` である以上、どちらでも結果が変わらないためです）。

お願いしたいこと（どちらかです）。

- 管理画面の経路にだけセッションの仕組みを足し、CSRF 検査が実際に働くことを**リクエストテストで確かめます**（トークン無しの POST が弾かれること）。
- もしくは、いまは CSRF 対策の対象外だと決め、**働いていない宣言を残しません。** そのうえで #65 の着手条件として申し送ります。

いずれにしても、**「掛けたつもりで掛かっていない」状態を残さないでください。** CLAUDE.md の「フォールバックを禁止します／想定外は握りつぶさず失敗させます」に反します。

### 2. 開発環境では、管理画面を開けません

- 場所：`docker-compose.yml` の `dev.environment`、および `.env.example` の追記

`.env.example` へ `ADMIN_BASIC_AUTH_USER` / `ADMIN_BASIC_AUTH_PASSWORD` を足しましたが、`docker-compose.yml` の `dev` サービスは `env_file:` を持たず、`environment:` に列挙した 5 つ（`TZ` / `APP_ENV` / `DATABASE_URL` / `TEST_DATABASE_URL` / `NEXT_PUBLIC_APP_ENV`）しか渡していません。**`.env` へ書いても、アプリまで届きません。**

実際に開発コンテナの中を確かめました。

```
$ docker exec hero-prompt-forge-dev-1 env | grep -E "DATABASE_URL|APP_ENV"
DATABASE_URL=postgres://hero:***@db:5432/hero_prompt_forge_development
TEST_DATABASE_URL=postgres://hero:***@db:5432/hero_prompt_forge_test
APP_ENV=development
NEXT_PUBLIC_APP_ENV=development
（ADMIN_BASIC_AUTH_* はありません）
```

このままでは、開発者が `/admin` を開くと 401 になり、何を入れても `MissingCredentialsError` で 500 になります。**issue #64 の「開発者用の管理画面」という目的が、開発環境で果たせません。** `docker-compose.yml` へ 2 つの受け渡しを足してください。

### 3. `PORT` の受け入れ条件を、原稿の文字列で確かめています

- 場所：`src/backend/spec/requests/health_spec.rb:74-78`

```ruby
it 'PORT を読みます' do
  config = Rails.root.join('config/puma.rb').read
  expect(config).to include('PORT')
end
```

これは **`config/puma.rb` のどこかに `PORT` という 5 文字があること**しか確かめていません。コメントに `PORT` と書いてあるだけでも通ります。実装（`config/puma.rb:33` の `port ENV.fetch('PORT', 3000)`）自体は妥当ですが、**受け入れ条件「Railway が注入する `PORT` で待ち受けます」の確かめになっていません。** TM に反します。

あわせて、リポジトリの中に**本番の起動の仕方を決めた記述がありません**（本番用 Dockerfile・Procfile・`railway.json` のいずれもありません）。`.github/workflows/release-deploy.yml` は `src/backend` から `railway up` を実行するだけです。起動の仕方が固定されていないと、`config/puma.rb` が読まれる保証がありません。

お願いしたいこと。

- `config/puma.rb` を実際に読み込ませ、`PORT` を変えたときに待ち受けが変わることを確かめる形へ直してください。
- 本番の起動の仕方を、リポジトリの中で決めてください（決めない判断であれば、その理由を `ENV/PRODUCTION.md` へ残してください）。

### 4. BASIC 認証を足したのに、本番で HTTPS を強めていません

- 場所：`src/backend/config/environments/production.rb:27`（`# config.assume_ssl = true`）、`:30`（`# config.force_ssl = true`）

BASIC 認証は、**資格情報を要求のたびに送ります。** いずれもコメントのままですので、http で届いた要求はそのまま処理され、資格情報が平文で流れる経路が残ります。**この PR で初めてパスワードを外へ晒す入口ができましたので、この PR の中で判断してください。**

Railway の前段で必ず TLS 終端されるため不要、という判断であれば、その根拠を `ENV/PRODUCTION.md` へ書いたうえで申し送ってください。

### 5. 利用者の目に触れる文字列が、ソースへ直書きされています

- 場所：`src/backend/app/controllers/concerns/authenticates_admin.rb:30` の `'hero-prompt-forge admin'`

これは **BASIC 認証の realm** で、**ブラウザの入力ダイアログにそのまま表示されます。** 実際の応答でも確認しました。

```
www-authenticate="Basic realm=\"hero-prompt-forge admin\""
```

CLAUDE.md の「文字列リテラルをファイルまたはデータベースへ分離します」に反します。`test/hardcode/check-hardcoded-strings.mjs` は**日本語を含むものしか見ていない**（`const JAPANESE = ...`）ため、この直書きは検査をすり抜けています。設定側へ出してください。あわせて、検査が英数字だけの表示文言を拾えないことも申し送ります。

## 提案

1. `/health` が記録を埋めます。`production.rb:43` の `config.silence_healthcheck_path` は `/up` のままです。外形監視は数十秒ごとに叩きますので、`/health` を静かにするか、静かにしない理由を残してください。
2. `/health` に上限時間がありません。データベースが応答を返さないとき、要求が待ち続けます。`RAILS_MAX_THREADS` の既定は 3 ですので、監視の再試行だけで受け口が埋まります。上限時間を付けることを勧めます。
3. `/health` は誰でも叩けて、1 回ごとにデータベースへ問い合わせます。回数の制限がありません。認証を求めない判断そのものは妥当だと考えますが、**「認証を求めない」と「無制限に働かせる」は別です。** ごく短い間の結果を使い回すか、回数を絞ることを勧めます。
4. `health_controller.rb:31` の `ActiveRecord::Base.connection.execute(...)` は、返ってきた結果を片付けていません。`select_value` などへ寄せると、結果の後始末も要りません。あわせて `ActiveRecord::Base.connection` は Rails 7.2 以降で `lease_connection` / `with_connection` へ寄せる方向です。
5. 管理画面に、合言葉の試行回数の制限がありません。Rails 8 の `rate_limit` が使えます。
6. 401 の本文が Rails 既定の英語（`HTTP Basic: Access denied.`）です。「日本語版のみ」「ですます調」から外れます。開発者しか見ませんので急ぎませんが、直せる箇所です。
7. `.github/workflows/release-deploy.yml` の確認は `vars.BACKEND_HEALTH_URL` を叩きます。**この PR で `/health` ができましたので、その値の指す先を `/health` へ揃えてください。** `/up` のままでは、リリース後の確認がデータベース到達性を見ません。
8. `locales_spec.rb` の見出し例外は、**`headings` という名前のキーであれば、どの深さでも、その配下すべて**をですます調の検査から外します。30 文字以内・句点なしという歯止めは良い工夫ですが、だ・である調の見出しは通ります。深さを 1 階層に限る、または見出しに許す語尾を決めることを勧めます。
9. `layouts/admin.html.erb` は素の HTML で、`app-ui` の見た目に寄せていません。いまは入口 1 枚だけですので許容しますが、#65 以降で画面が増えるときに CRAP の判断ができる形へ寄せてください。アイコンは Font Awesome です。
10. テストの置き場が `src/backend/spec/` で、CLAUDE.md の `test/pr<番号>/` と食い違います。既存の踏襲ですので今回は流しますが、**規約の側を実態へ合わせる**か、申し送りが要ります。
11. `SPEC/api/` に、増えた 2 つの入口の記載がありません。

## 良かった点

- **資格情報が未設定・空文字なら、その場で止まります。** 空文字と照合して通す作りにしていません。実際に未設定・空文字の両方で確かめ、いずれも中身へ届きませんでした。
- **比べ方が正しいです。** `ActiveSupport::SecurityUtils.secure_compare` を使い、`&&` ではなく `&` で利用者名と合言葉の両方を必ず比べています（`authenticates_admin.rb:38-39`）。片方で止めない判断は妥当です。
- **掛け忘れが起きない作りです。** 認証を土台の継承へ寄せています。画面ごとに書く作りより堅いです。
- **死活監視の答えが最小限です。** 失敗の形も含めて確かめましたが、内部の作りは出ていません。
- **利用者のログインでは管理画面へ届きません。** テストで押さえてあります。
- **まだ無い画面への導線を出していません。**
- PR 本文はですます調で、絵文字がなく、連絡先も規約どおりです。

## 確認した実行結果

### 本番設定（`RAILS_ENV=production`）での入口の様子

| 試したこと | 結果 |
|---|---|
| 認証なしで `/admin` | 401（`WWW-Authenticate: Basic realm="hero-prompt-forge admin"`） |
| 利用者名だけ違えて `/admin` | 401 |
| `Authorization: Basic `（中身が空） | 401 |
| `Authorization: Bearer abc`（別の方式） | 401 |
| `basic`（小文字の方式名）＋正しい資格情報 | 200（方式名の大小は区別しない仕様どおりです） |
| 正しい資格情報で `/admin` | 200、画面が返ります（**本番設定でも画面の仕組みは働きます**） |
| 認証なしで `/admin.json` | 401 |
| 正しい資格情報で `/admin.json` | 406（`{"status":406,"error":"Not Acceptable"}`。中身は漏れません） |
| `/health`（正常時） | 200 `{"status":"ok"}` |
| `/health`（データベースへ届かないとき） | 503 `{"status":"unavailable"}`、記録に `[health] データベースへ到達できません error=ActiveRecord::DatabaseConnectionError` |
| `/up` | 200（Rails 既定の緑の画面） |

**「本番で画面が返るか」は問題ありませんでした。** 一方で、同じ応答の `<head>` に CSRF の meta がありません。これが要修正 1 です。

### テストの実行

- `spec/requests/admin_spec.rb` ／ `spec/requests/health_spec.rb` ／ `spec/config/locales_spec.rb`：27 例、失敗 0 件
- `config/environments/test.rb` の `allow_forgery_protection` を `true` へ変えて `admin_spec.rb` を再実行：11 例、失敗 0 件（**変えても結果が変わりません＝この観点はテストで守られていません**）
- PR の自動検査 5 項目：すべて通っています

## 申し送り

- 要修正 1 は、#65〜#68 で状態を変える操作が入る前に決着させてください。**先に画面が増えると、そのまま穴になります。**
- 要修正 3・4 に関わる本番の決めごと（起動の仕方・TLS の強制）は、`ENV/PRODUCTION.md` に書いてあるはずの内容です。追跡対象外のため差分から読めませんでした。**すでに決まっているのであれば、その旨を PR へ一言足してください。** 決まっていなければ、この PR で決めてください。
- ハードコード検査が英数字だけの表示文言を拾えない件（要修正 5 の後半）は、この PR の範囲を超えます。別 issue として起票することを勧めます。
- 一時的な作業の写しは、コンテナの中（`/home/dev/pr150work`）に置いたもののみです。作業ツリー側に作った `review-pr150-tmp` は `DELETE/` へ移してください（こちらでは削除しません）。


---

# コードレビュー : PR #150（再確認）

- 実施日時（JST）：2026-08-26 21:40
- 対象：コミット `40e3179`（差分は `git diff 8565069..40e3179`）
- 前回の記録：`pr150-reviewer.md`
- 確かめ方：`git archive` で取り出した写しを開発コンテナの内側（`/home/dev/pr150work/pr150b`）へ置き、**本番設定（`RAILS_ENV=production`）で実際に要求を通しました。** 実装側の作業ツリーには触れていません。

## 判定（2 回目）

**要修正** です。**残り 1 件です。**

**5 件のうち 4 件は、確かに直っていることを実測しました。** とくに CSRF 対策は、こちらの手で本番相当の設定を組み、外部の生成元からトークン無しの書き込みを送って**止まること**を確認しました。対応の速さと丁寧さに感謝します。

残る 1 件は **要修正 4（本番の TLS）** です。**`force_ssl` を有効にした一方で `assume_ssl` も有効にしたため、平文の要求が転送されず、そのまま応答されます。** ねらいであった「BASIC 認証の資格情報を平文で流さない」が達成できていません。**直し方は 1 行です。**

## 前回の要修正 5 件の再確認

| # | 内容 | 判定 | 確かめ方 |
|---|---|---|---|
| 1 | 管理画面の CSRF 対策が働いていない | **解消** | 本番設定で実測。後述 |
| 2 | 開発環境へ資格情報が渡らない | **解消** | `docker compose config` で解決を確認 |
| 3 | `PORT` の確かめが原稿の文字列だった | **解消** | 設定を実際に評価する形へ。テストも実行 |
| 4 | 本番で TLS を強めていない | **未達** | 平文の `/admin` が 200 で応答します。後述 |
| 5 | realm の直書き | **解消** | 定数へ分離。妥当と判断します |

### 1. CSRF 対策（解消）

`src/backend/config/application.rb` でセッションの仕組みを明示的に組み込んだ判断は正しいです。**本番設定で、こちらの手で確かめました。**

```
== cross-origin POST no token : status=422（前回は 200 でした）
== GET /admin                 : status=200, csrf-token の meta あり
== Set-Cookie                 : _hpf_admin_session=...; path=/; httponly; samesite=lax; secure
```

- **外部の生成元（`Origin: https://evil.example.com`）からのトークン無しの書き込みが 422 で止まります。**
- `csrf_meta_tags` が実際に token を出すようになりました（前回は空でした）。
- クッキーに `httponly` ・ `samesite=lax` ・ `secure` が付きます。**鍵の名前を利用者向けと分けた判断も妥当です。**
- ミドルウェアの並びを数えました。**セッションは 1 つだけで、二重に積まれていません。** 並び順も `ActionDispatch::Cookies` → `ActionDispatch::Session::CookieStore` で正しいです。

検証用の経路（`Admin::SpecProtectedController`）を足した判断も妥当です。**本番の経路表に現れないことを実測しました**（本番設定で経路を全件出し、`/admin/spec/protected` が無いことを確認しました）。利用者向けの `SpecProtectedController` と同じ作りで、一貫しています。

### 2. 開発環境の資格情報（解消）

`docker-compose.yml` の `dev.environment` へ 2 つ足りました。`docker compose config` で解決結果を確認しました。他の変数が `${VAR:?...}`（未設定なら起動を止める）なのに対し、ここだけ `${VAR:-}`（未設定なら空）にした判断は妥当です。**管理画面を触らない人の `docker compose up` を止めないためです。** アプリ側が未設定で止まりますので、守りは緩んでいません。

### 3. `PORT` の確かめ（解消）

`config/puma.rb` を `Puma::Configuration` で実際に読み込み、`options[:binds]` を見る形になりました。**書きぶりではなく、決まった番号を見ています。** `PORT=4567` のとき 4567、未設定のとき 3000 の 2 通りを押さえた点も良いです。`ensure` で元の値へ戻す作りになっていることも確認しました。

なお、**本番の起動の仕方（Procfile / 本番用 Dockerfile / `railway.json`）は、今回も追加されていません。** `railway up` が Nixpacks を通して `config/puma.rb` を読む前提です。**この前提を `ENV/PRODUCTION.md` へ書き残してください**（申し送り）。

### 4. 本番の TLS（未達）

**`assume_ssl = true` が `force_ssl = true` の転送を打ち消しています。**

`ActionDispatch::AssumeSSL` は、ミドルウェアの並びの**いちばん先頭**に入り、すべての要求に `HTTPS=on` を立てます。その後ろの `ActionDispatch::SSL` は「すでに TLS の上だ」と判断しますので、**転送を一度も行いません。** 実測した並びです。

```
ActionDispatch::AssumeSSL   ← ここで HTTPS=on になります
ActionDispatch::SSL         ← そのため転送しません
...
```

本番設定での実測です。

| 要求 | 現状（`assume_ssl = true`） | `assume_ssl` を外した場合 |
|---|---|---|
| 平文の `GET /admin`（資格情報つき） | **200（そのまま応答します）** | **301 → `https://.../admin`** |
| `X-Forwarded-Proto: http` の `GET /admin` | **200** | **301** |
| `X-Forwarded-Proto: https` の `GET /admin` | 200、クッキーに `secure` | 200、クッキーに `secure` |
| 平文の `GET /health` | 200 | **200（除外が意図どおり働きます）** |

つまり、いまの設定では次の 2 つが起きています。

1. **平文で届いた要求に、BASIC 認証の資格情報を求め、受け取ってしまいます。** 要修正 4 で塞ぎたかった経路が残っています。
2. **`config.ssl_options` の `/up` ・ `/health` の除外が、一度も働きません。** 転送そのものが起きないためです。**働かない記述が残ることは、要修正 1（書いたつもりで守られていない）と同じ形です。**

お願いしたいこと。**`config.assume_ssl` を外してください（または `false` にしてください）。** Railway は `X-Forwarded-Proto` を付けますので、`assume_ssl` が無くても Rails は TLS を正しく見分けます。上の表のとおり、外した場合に

- 平文の `/admin` は https へ転送されます、
- `X-Forwarded-Proto: https` の要求ではクッキーに `secure` が付いたままです、
- **`/health` は平文のままでも 200 を返します（除外が効きます）**、

の 3 つが同時に成り立ちます。**皆さんがねらったとおりの姿になります。**

### 5. realm の直書き（解消）

`REALM` 定数へ出し、開発者向けの印を付けた対応で問題ありません。**利用者に見えない、開発者だけが見る名前**という整理は妥当です。

## いただいたご質問への回答

### 「死活監視だけは平文でも答える」という判断は妥当か

**判断そのものは妥当です。** 外形監視サービスには転送に追従しないものがあり、稼働しているのに落ちていると判定される事故は実際に起きます。**返すのが状態の 1 項目だけで、資格情報も個人情報も乗らない**ため、平文で流れて失うものがない、という整理も正しいです。

ただし前述のとおり、**いまの設定ではその除外が一度も働きません。** `assume_ssl` を外してはじめて、この判断が実装として意味を持ちます。

あわせて 2 点だけ申し添えます。

- 除外の対象を `/up` と `/health` に限った点は良いです。**入口を増やすときに、ここへ足さない**運用を守ってください。
- `force_ssl` は HSTS（`max-age=63072000; includeSubDomains`）を返します。実測で確認しました。**`rictaworks.jp` のサブドメイン全体に効きますので、他のサブドメインで平文の運用が無いかだけご確認ください。**

### セッションを組み込んだことで、利用者向けの API に影響が出ていないか

**出ていません。** 修正前後の写しを並べ、本番設定で同じ要求を通して見比べました。

| 要求 | 修正前 | 修正後 |
|---|---|---|
| `GET /auth/start` | 500、`Set-Cookie` なし | 500、`Set-Cookie` なし |
| `GET /auth/callback` | 500、`Set-Cookie` なし | 500、`Set-Cookie` なし |
| `DELETE /auth/session` | 500 | 500 |
| `GET /health` | 200、`Set-Cookie` なし | 200、`Set-Cookie` なし |

- **利用者向けの応答に `_hpf_admin_session` は付きません。** セッションへ書き込む処理が無いためです。
- 500 は**修正前後で同じ**で、こちらの検証環境に `X_OAUTH_CLIENT_ID` ・ `FRONTEND_BASE_URL` を置いていないことが原因です（`KeyError (key not found: "X_OAUTH_CLIENT_ID")`）。**この PR による後退ではありません。**
- CSRF 対策は `ActionController::Base` を継承するもの（＝管理画面）にだけ掛かります。利用者向けは `ActionController::API` ですので、対象外のままです。**意図どおりです。**
- 枠組みが用意する経路（`/rails/conductor/...`）も、修正前後ともに 403 です。開いていません。

## 提案

前回の提案のうち、まだ手が付いていないものを再掲します。**いずれもマージを止めるものではありません。**

1. `production.rb` の `silence_healthcheck_path` が `/up` のままです。`/health` も静かにしてください。
2. `/health` に上限時間がありません。`RAILS_MAX_THREADS` の既定は 3 です。
3. `/health` に回数の制限がありません。1 回ごとにデータベースへ問い合わせます。
4. `health_controller.rb:31` の `ActiveRecord::Base.connection.execute` は、結果を片付けていません。
5. 管理画面に、合言葉の試行回数の制限がありません。
6. 401 の本文が Rails 既定の英語です。
7. `release-deploy.yml` の `vars.BACKEND_HEALTH_URL` を `/health` へ揃えてください。
8. `locales_spec.rb` の見出し例外が、どの深さの `headings` でも効きます。
9. 管理画面の見た目を `app-ui` へ寄せる件（#65 以降）。
10. テストの置き場と CLAUDE.md の `test/pr<番号>/` の食い違い。
11. `SPEC/api/` に 2 つの入口の記載がありません。

今回の差分について、新しく 3 点です。

12. `config/application.rb` で、鍵と `same_site` ・ `httponly` を `config.session_store` と `config.middleware.use` の**両方へ書いています。** 片方だけ直すと食い違います。1 か所へまとめるか、まとめられない理由を注記してください。
13. 新しいテストの `before` が `Rails.application.routes.draw` で経路表を丸ごと差し替えます。`after` の `reload_routes!` が走らないと、後続のテストへ影響します。既存の作りに合わせたものですので今回は流しますが、**経路の差し替えを 1 か所の補助へまとめる**ことを勧めます。
14. 同じ `after` が `ActionController::Base.allow_forgery_protection = false` と**決め打ちで戻します。** 元の値を控えて戻す形が安全です。

## 確認した実行結果

### 本番設定（`RAILS_ENV=production`）

```
-- セッションのミドルウェア数 = 1（二重に積まれていません）
-- force_ssl=true assume_ssl=true
== 外部の生成元からのトークン無し POST : 422（前回は 200）
== GET /admin : 200 / csrf-token の meta あり
   Set-Cookie: _hpf_admin_session=...; path=/; httponly; samesite=lax; secure
== HSTS : max-age=63072000; includeSubDomains
== 平文の GET /admin   : 200（転送されません）  ← 要修正 4
== 平文の GET /health  : 200
== /admin/spec/protected : 本番の経路表にありません
```

`assume_ssl` を外した写しでの比較。

```
-- force_ssl=true assume_ssl=false
== 平文の GET /admin              : 301 → https://admin.example.com/admin
== X-Forwarded-Proto: http の /admin : 301
== X-Forwarded-Proto: https の /admin : 200（クッキーに secure が付きます）
== 平文の GET /health             : 200（除外が働きます）
```

### テストと書き方の検査

- RSpec 全件：**779 例、失敗 0 件**（修正前の写しは 773 例 0 件。増えた 6 例が今回の追加分です）
- RuboCop：110 ファイル、指摘 0 件
- 補足：1 度目の全件実行で `spec/models/quota_consumption_spec.rb:97`（並列投入）が 1 件落ちました。**単体で 3 回、全件で 1 回、いずれも通りましたので、こちらの検証環境における一時的な揺れと判断します。** この PR による後退ではありません（修正前の写しでも全件 0 件でした）。**ただし、並列を扱うテストが検証環境によって揺れる事実は残ります。** 別 issue として様子を見ることを勧めます。

## 申し送り

- **要修正 4 を直したら、そのまま合格で構いません。** 直し方は `config.assume_ssl` を外す 1 行です。他に条件は付けません。
- 直したあと、**平文の `/admin` が 301 になること**と**平文の `/health` が 200 のままであること**の 2 つを、リクエストテストで固定してください。**同じ間違いが二度起きない形にしておきたいためです。**
- 本番の起動の仕方（`config/puma.rb` が読まれる前提）を `ENV/PRODUCTION.md` へ書き残してください。
- 提案 1〜11 は、#65 の着手前に一度まとめて棚卸しすることを勧めます。とくに 5（試行回数の制限）は、管理画面に操作が増える前に入れておきたいものです。
- 一時的な作業の写しは、開発コンテナの中（`/home/dev/pr150work`）にのみ置きました。**作業ツリーには何も残していません。**


---

# コードレビュー : PR #150（3回目）

- 実施日時（JST）：2026-08-26 21:50
- 対象：コミット `910af09`（差分は `git diff 40e3179..910af09`）
- 前回までの記録：`pr150-reviewer.md` ／ `pr150-reviewer-2.md`
- 確かめ方：`git archive` で取り出した写しを開発コンテナの内側（`/home/dev/pr150work/pr150c`）へ置き、**本番設定（`RAILS_ENV=production`）で実際に要求を通しました。** 実装側の作業ツリーには触れていません。

## 判定（3 回目）

**合格** です。マージして構いません。

**当初の要修正 5 件は、すべて解消したことを実測しました。** 残る指摘はありません。以下に挙げるものは、いずれもマージを止めない提案と申し送りです。

**とくに良かった点を 1 つ挙げます。** `assume_ssl` を「書かない」ではなく **`false` と明示的に書いた**判断です。既定値に頼ると、将来だれかが良かれと思って有効にしたとき、**転送が黙って止まります。** 明示してあれば、変えるときに理由を考えます。**この PR で 2 度繰り返した「書いたつもりで守られていない」という失敗の型に対する、正しい手当てです。**

## 要修正 4（本番の TLS）の再確認

`src/backend/config/environments/production.rb` の `config.assume_ssl = false` により、`ActionDispatch::AssumeSSL` がミドルウェアの並びから消えました。**先頭が `ActionDispatch::SSL` になっています。**

本番設定での実測です。

| 要求 | 前回（`assume_ssl = true`） | 今回（`assume_ssl = false`） |
|---|---|---|
| 平文の `GET /admin`（資格情報つき） | 200（そのまま応答） | **301 → `https://admin.example.com/admin`** |
| `X-Forwarded-Proto: http` の `GET /admin` | 200 | **301** |
| `X-Forwarded-Proto: https` の `GET /admin` | 200 | **200**（クッキーに `secure`、`csrf-token` の meta あり） |
| 平文の `GET /health` | 200 | **200（除外が働きます）** |
| 平文の `GET /up` | 200 | **200（除外が働きます）** |

**ねらったとおりの姿になりました。**

- **BASIC 認証の資格情報が、平文の経路で求められることはなくなりました。** 平文で `/admin` を叩くと、認証の窓が出る前に https へ転送されます。
- **死活監視だけは平文でも答えます。** `config.ssl_options` の除外が、はじめて実際に働くようになりました。前回は「働かない記述」でしたので、これで記述と挙動が一致します。
- HSTS（`max-age=63072000; includeSubDomains`）は https の応答にだけ付きます。転送の応答には付きません。**正しい振る舞いです。**

### 配置先（Railway）で本当に動くかを確かめました

「`assume_ssl` を外すと、転送が延々と繰り返されるのではないか」という懸念を持ちましたので、`X-Forwarded-Proto` の扱いを確かめました。

| 転送元 | `X-Forwarded-Proto: https` のときの結果 |
|---|---|
| 私有アドレス（`10.0.0.5`） | 200（転送しません） |
| 公開アドレス（`203.0.113.9`） | 200（転送しません） |
| `REMOTE_ADDR` なし | 200（転送しません） |

**Rails は転送元の住所によらず `X-Forwarded-Proto` を見ます。** Railway の前段がどの住所から渡してきても、転送が繰り返される事故は起きません。**懸念は当たりませんでした。**

## テストの確認

新しく足った 4 例（`spec/requests/health_spec.rb` の「本番での TLS の扱い」）は、**いまの環境の設定ではなく、本番の設定ファイルを読み込んで**確かめる形になりました。前回の指摘に沿った直し方です。除外の判定を実際に呼んで `/health` と `/admin` の両方を見ている点も良いです。

実行結果です。

| 検査 | 結果 |
|---|---|
| RSpec（全件） | **783 例、失敗 0 件** |
| RSpec（`--order random` で全件） | **783 例、失敗 0 件** |
| RSpec（`health_spec.rb` 単体） | 14 例、失敗 0 件 |
| RuboCop | 110 ファイル、指摘 0 件 |
| ハードコードの検査 | 日本語を含む文字列リテラルはありません |

**順序を変えても通ります。** 経路表を差し替えるテストと `allow_forgery_protection` を切り替えるテストが、他へ影響を残していないことを確認しました（前回の提案 13・14 に関わる点です）。

## 5 件の最終確認

| # | 内容 | 判定 | 根拠 |
|---|---|---|---|
| 1 | 管理画面の CSRF 対策が働いていない | **解消** | 外部の生成元からのトークン無し POST が 422。`csrf-token` の meta が出ます |
| 2 | 開発環境へ資格情報が渡らない | **解消** | `docker compose config` で解決を確認 |
| 3 | `PORT` の確かめが原稿の文字列だった | **解消** | 設定を実際に評価して番号を見る形へ |
| 4 | 本番で TLS を強めていない | **解消** | 平文の `/admin` が 301、`/health` が 200 |
| 5 | realm の直書き | **解消** | 定数へ分離 |

## Issue の受け入れ条件（最終）

### issue #62（ヘルスチェック）

- 正常時 200 ／ DB 異常時 503：**満たします**（本番設定で実測）
- `PORT` で待ち受けます：**満たします**（設定を評価して 4567 ／ 3000 を確認）
- RSpec のリクエストテスト：**満たします**（14 例）

### issue #64（管理画面の BASIC 認証）

- 未認証で到達できません：**満たします**（資格情報なし・空文字・別方式・ヘッダ改変のいずれも 401。平文の経路は認証の前に転送されます）
- 資格情報が環境変数から読まれます：**満たします**（未設定・空文字はその場で失敗させます。開発環境へも渡ります）
- RSpec のリクエストテスト：**満たします**

**スコープ外の変更はありません。**

## 各基準への適合（最終）

| 基準 | 判定 |
|---|---|
| CC | 合致します |
| OWASP10 | 合致します |
| QC10 | 合致します |
| CRAP | 判断を保留します（管理画面は入口 1 枚のみ。#65 以降で判断します） |
| DP | 合致します |
| TM | 合致します |

## 提案（マージを止めません）

今回の差分について 2 点です。

1. **`config.assume_ssl = false` の置き場所です。** `assume_ssl` を説明する長い注記の下に `force_ssl` の注記が続き、その内側に `config.assume_ssl = false` が入っています。**読み手が、どちらの注記がどちらの設定のものか取り違えます。** `assume_ssl` の注記の直下へ移してください。1 行の移動です。
2. **`production_settings` の作り方が壊れやすいです**（`spec/requests/health_spec.rb`）。`production.rb` の中身を文字列で置き換えて `instance_eval` しています。**いまは通りますが、`production.rb` の末尾にブロックが増えると、`sub(/end\s*\z/, '')` が別の `end` を落とします。** 設定の値を見る形は残しつつ、**「平文の `/admin` が転送される」「平文の `/health` が 200 のまま」を要求として確かめる**テストを 1 組足すと、作りに依存しない形で固定できます（こちらでは実測済みですので、期待値はそのまま使えます）。

前回までの提案のうち、まだ手が付いていないものを再掲します。**いずれも #65 の着手前に棚卸しすることを勧めます。**

3. `production.rb` の `silence_healthcheck_path` が `/up` のままです。`/health` も静かにしてください。
4. `/health` に上限時間がありません（`RAILS_MAX_THREADS` の既定は 3 です）。
5. `/health` に回数の制限がありません。
6. `health_controller.rb` の `ActiveRecord::Base.connection.execute` が結果を片付けていません。
7. **管理画面に、合言葉の試行回数の制限がありません。** 操作が増える前に入れておきたいものです。
8. 401 の本文が Rails 既定の英語です。
9. `release-deploy.yml` の `vars.BACKEND_HEALTH_URL` を `/health` へ揃えてください。
10. `locales_spec.rb` の見出し例外が、どの深さの `headings` でも効きます。
11. 管理画面の見た目を `app-ui` へ寄せる件（#65 以降）。
12. テストの置き場と CLAUDE.md の `test/pr<番号>/` の食い違い。
13. `SPEC/api/` に 2 つの入口の記載がありません。
14. `config/application.rb` で、鍵と `same_site` ・ `httponly` を 2 か所へ書いています。
15. テストが `Rails.application.routes.draw` で経路表を丸ごと差し替えます（補助へまとめる案）。
16. 同じテストが `allow_forgery_protection` を決め打ちの `false` で戻します（元の値を控える案）。

## 申し送り

- **利用者向けの経路も転送の対象になりました。** 平文の `GET /auth/start` が 301 を返します。**正しい挙動です**が、フロントエンドがバックエンドを `http://` で呼んでいないことだけ、`ENV/PRODUCTION.md` の設定でご確認ください。呼んでいれば、`POST` は 307 で転送されるとはいえ、往復が 1 回増えます。
- **転送は守りの本体ではありません。** 攻める側は自分で `X-Forwarded-Proto: https` を付けて転送を避けられますが、それで損なわれるのは攻める側自身の通信だけです。**利用者を守るのは HSTS と `secure` のクッキーで、いずれも入っていることを実測しました。**
- 本番の起動の仕方（`config/puma.rb` が読まれる前提）を `ENV/PRODUCTION.md` へ書き残してください。
- ハードコード検査が英数字だけの表示文言を拾えない件は、別 issue として起票することを勧めます。
- 一時的な作業の写しは、開発コンテナの中（`/home/dev/pr150work`）にのみ置きました。**作業ツリーには何も残していません。**

## この PR を通しての所見

**3 回のやり取りで、指摘のすべてに根拠つきで応えていただきました。** とくに、指摘を「言われたから直す」ではなく、**同じ失敗の型（書いたつもりで守られていない）を将来も防ぐ形へ落とし込んで**いる点を評価します。`assume_ssl` の明示、検証用の書き込み経路、設定を実際に評価するテストは、いずれもその形です。

**当初の PR は、テストがすべて緑でありながら、守りが 2 か所で働いていませんでした。** 実行して確かめないと分からない類のものです。今後 `src/backend` に外向きの入口を足す PR では、**本番設定で 1 度は要求を通す**ことを、手順として置くことを勧めます。


---


# reviewer 記録 : PR #170（issue #69 01 ランディングを実装する）

- 実施日時（JST）：2026-08-27 07:16
- 対象：2 回目 `4bd00da`（土台は `main` = `195f1d9`）
- 1 回目：`4cd4013` ・ `4a1b2e7`（指摘 F1・F2・R2〜R6・P1〜P7）
- 作業ツリー：`DELETE/review-worktrees/pr170c-20260827-reviewer`（`git worktree add --detach` 済みです。実装側の作業ツリーは読み取りのみで、分岐を切り替えていません）

## 2 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 土台 | **`main` と一致します**（`merge-base` = `195f1d9` = `main`）。積み直し済みです |
| 自動検査 | **5 ジョブすべて pass** |
| PR の差分 | 22 ファイル。#69 の変更と、合意済みの `docker-compose.yml` の取り込みだけです |
| フロントエンドのテスト | **61 例、失敗 0 件**（PR 本文の記載は 58 例です） |
| 書き方の検査（ESLint） | **指摘 0 件** |
| ハードコードの検査 | **検出 0 件** |
| 開発フックの検査 | **23 例、失敗 0 件** |
| テストの置き場の検査 | **3 例、失敗 0 件** |
| ブラウザ操作の確認（Playwright） | **9 例すべて緑**（温まった開発サーバーに対して 22.9 秒） |
| `alert()` / `confirm()` / `prompt()` | **ありません** |
| 絵文字 | **ありません。** アイコンは Font Awesome です |
| ファイルのエンコード | **すべて UTF-8 です** |
| 外部への通信 | **ありません。** 描き出された HTML に外部の場所が 1 つも出ません |

### 実際に描き出された画面で確かめたこと

開発サーバーへ `curl` を当てて、出力そのものを検めました。

| 場面 | 結果 |
|---|---|
| ログインの入口 | **`href="/auth/start"` がちょうど 3 か所です。** バックエンドの場所は 1 文字も出ません |
| ログインの入口の描き方 | **3 か所とも素の入口です。** 画面の中の移動の仕組みで描かれていません（F1 の直しが効いています） |
| ワードマーク | **`hero-prompt-forge` です。** `veyra` の綴りは出力に 1 件もありません（R6 の直しが効いています） |
| アカウント名 | **`@rictaworks` です** |
| 共有されたときの見え方 | **`og:title` ・ `og:description` ・ `og:url` ・ `og:image` ・ `og:type` ・ `twitter:*` がそろっています**（R3 の直しが効いています） |
| 正となる場所 | **canonical が出ます** |
| 外部の配信先 | **ありません。** 書体もアイコンも同梱です |

### 1 回目の指摘への対応

| 番号 | 内容 | 確認の結果 |
|---|---|---|
| **F1** | 開いただけでログインの手続きが始まります | **直っています。** `Button` に `external` が入り、3 か所とも素の入口で描かれます |
| **F2** | ブラウザ操作のテストが失敗します | **直っています。** 見出しを完全一致で探す形になり、9 例すべて緑です |
| **R2** | 添え書きがモックと違います | **直っています。** 改行の位置とアカウント名の色（金）がモックと一致します |
| **R3** | 公開画面としての必須の設定 | **直っています。** 実出力で OGP と canonical を確認しました |
| **R4** | 画像の出どころとライセンスの記録 | **直っています。** 画像の中身がモックと同一（md5 一致）であることも確かめました |
| **R5** | 文言の検査が、変異試験で抜けます | **おおむね直っています。** 語尾で見る形になりました |
| **R6** | 公開画面に他社の名称が出ます | **直っています。** ただし後戻りを止めるテストがありません（要修正 3） |
| **P1** | 読み込まれていない CSS | **対応済み。** `DELETE/unused-2026-08-27/` へ移動しています（削除ではありません） |
| **P2** | ログインの入口を設定へ寄せる | **対応済み。** `src/config/backend.ts` の `LOGIN_PATH` です |
| **P3** | 組み立て時に焼き込まれることの記載 | **対応済み。** `ENV/DEVELOPMENT.md` ・ `ENV/PRODUCTION.md` に記載を確認しました |
| **P7** | 規約・ポリシーへの導線 | **対応済み。** issue #172 が立っています |
| **P4・P5・P6** | 見送り | **受け入れます。** 申し送りに残っています |

### 変異試験（10 種）

| # | 変異 | 結果 |
|---|---|---|
| 1 | 中継先が未設定のときに既定値へ寄せます | **赤** |
| 2 | 中継先の変数名を `NEXT_PUBLIC_` で始めます | **赤** |
| 3 | 公開先が未設定のときに既定値へ寄せます | **赤** |
| 4 | 「だ・である調」の文を文言へ混ぜます | **赤** |
| 5 | ログインの入口へバックエンドの場所を書きます | **赤（Playwright のみ）。** Jest では素通りします |
| 6 | 「だ・である調」の短い文を `labels` へ隠します | **生き残ります** |
| 7 | `Button` が `external` を無視します | **生き残ります** |
| 8 | ワードマークをモックの既定値へ戻します | **生き残ります** |
| 9 | アカウント名をモックの値へ戻します | **生き残ります** |
| 10 | 見出しの完全一致を部分一致へ戻します | 赤（F2 の直しが効いています） |

**10 種のうち 4 種が生き残りました。** そのうち 3 種は、**1 回目に指摘して直していただいた箇所そのもの**です。

### Issue の受け入れ要件（issue #69）

| 受け入れ条件 | 判定 | 根拠 |
|---|---|---|
| `app-ui/index.html` と見た目が一致します | **満たします** | 節の並び・体裁・数値・色・余白がモックのままです。**`app-ui/` を 1 文字も書き換えていません** |
| 文言が設定ファイルから読まれます | **満たします** | すべて `src/strings/ja.ts` から引きます。ハードコードの検査が検出 0 件です |
| Playwright のテストがあります | **満たします** | `test/pr170/landing.spec.ts` に 9 例あり、実測で全件緑です |

**スコープ外の変更**：`docker-compose.yml` への環境変数の取り込みが入っています。**PR #168 のレビューで「#170 側で直すこと」を合意した分ですので、受け入れます。**

### 各基準への適合

| 基準 | 判定 | 備考 |
|---|---|---|
| CC | **適合** | 他社の名称を公開画面から外しています |
| OWASP10 | **適合** | 秘匿値の混入はありません。バックエンドの場所はブラウザへ配られません |
| QC10 | **一部** | 直した箇所に対する回帰の押さえが足りません（要修正 2・3） |
| CRAP | **適合（モックのとおり）** | 添え書きと eyebrow の対比が目安を下回りますが、モックの指定どおりです |
| DP | **適合** | フォールバックがありません。未設定はその場で失敗します |
| TM | **一部** | 直した箇所が変異試験で生き残ります |

## 指摘

### 要修正

- **要修正 1：`.env.example` の `E2E_BASE_URL` が、コンテナから届かない番号です。** 例が `http://localhost:3300` ですが、**3300 はホスト側の番号で、コンテナの中では何も待ち受けていません。** 例の側を `3000` に直してください。
- **要修正 2：F1 の直しに、どのテストも当たりません。** `external` を無視するように変えても、Jest も Playwright も緑のままです。`ui.test.tsx` へ、`external` を渡したときに先読みの仕組みで描かれないことを固定する例を足してください。
- **要修正 3：R6 の直し（ワードマーク）とアカウント名に、どのテストも当たりません。** モックの既定値へ戻しても全例が緑のままです。**「モックの既定値が公開の文言に含まれないこと」を検めるテストを足してください。**
- **要修正 4：ブラウザ操作の確認が、自動検査のどのジョブからも呼ばれません。** 変異 5 は Playwright だけが捕まえます。ジョブを足すか、記述を実態に合わせてください。**別 issue へ切り出す判断でも構いません。**
- **要修正 5：初めて動かす人は、ブラウザ操作の確認に必ず失敗します。** 初回の `page.goto("/")` が組み立ての完了を待ちます。**先に温める手当てか、待ち方を変える手当てを入れてください。**

### 提案

- **提案 6**：公開画面に応答の見出し（セキュリティヘッダー）がありません。別 issue で構いません。
- **提案 7**：`labels` の逃げ道が、まだ完全には塞がっていません。この PR では受け入れます。
- **提案 8**：設定の値の形を検めていません。専用の例外で失敗させてください。
- **提案 9**：`ENV/PRODUCTION.md` に、`NEXT_PUBLIC_BASE_URL` も焼き込まれる旨を足してください。
- **提案 10**：PR 本文と記録の「Jest 58 例」は、実測では 61 例です。
- **提案 11**：セキュリティレビュー記録の回数の数え方が食い違っています。
- **提案 12**：テストの後片づけに警告が 1 件出ます。別の機会に追ってください。

## 判定

条件付き合格

**受け入れ要件 3 項目は、すべて実測で満たされます。** 1 回目に挙げた 2 件と要修正 5 件・提案 7 件は、いずれも直っているか、妥当な見送りとして片づいています。PR は `MERGEABLE` ・ `CLEAN` で、自動検査 5 ジョブが緑です。

**残るのは、直した箇所を守るテストです。** 10 種の変異のうち 4 種が生き残り、そのうち 3 種は 1 回目に指摘した箇所そのものでした。

要修正 1（`.env.example` の番号）と要修正 2・3（後戻りを止めるテスト）を本 PR で直していただければ、**合格とします。** 要修正 4（自動検査への組み込み）と要修正 5（初回の待ち時間）は、**別 issue へ切り出す判断でも構いません。** 提案 6〜12 は、この PR での対応を求めません。

連絡先は info@rictaworks.jp です。

---

## 3 回目

- 実施日時（JST）：2026-08-27 07:27
- 対象：`17cf1f2`（土台は `main` = `195f1d9`）
- 作業ツリー：`DELETE/review-worktrees/pr170d-20260827-reviewer`（`git worktree add --detach` 済みです。実装側の作業ツリーは読み取りのみで、分岐を切り替えていません）

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 自動検査 | **5 ジョブすべて pass**（実行 `33019597916`） |
| PR の差分 | **25 ファイル。#69 の変更と、合意済みの取り込みだけです。** 2 回目から増えたのは、直しの 4 ファイルと記録の 3 ファイルです |
| フロントエンドのテスト | **66 例、失敗 0 件**（2 回目は 61 例。**+5 例**） |
| 書き方の検査（ESLint） | **指摘 0 件** |
| ハードコードの検査 | **検出 0 件** |
| ブラウザ操作の確認（Playwright） | **冷えた開発サーバーに対して 9 例すべて緑。27.1 秒** |

### 要修正への対応

| 指摘 | 判定 | 根拠 |
|---|---|---|
| **要修正 1**（`E2E_BASE_URL` の見本が届かない番号です） | **直っています** | `.env.example` が `http://localhost:3000` になりました。**コンテナの中から見た番号であること**が、理由として添えられています |
| **要修正 2**（外への出口の直しに、どのテストも当たりません） | **直っています** | 画面の中の移動の仕組みを印の付いた入口へ差し替え、**印が付かないこと・付くことの両方**を固定しました。**変異が赤くなります** |
| **要修正 3**（ワードマークとアカウント名に、どのテストも当たりません） | **直っています** | `strings/index.test.ts` に 3 例が入りました。**変異が赤くなります** |
| **要修正 4**（自動検査に組み込まれていません） | **切り出しを受け入れます** | **issue #173 が立っています** |
| **要修正 5**（初めて動かす人は必ず失敗します） | **直っています** | **組み立ての結果を一切持たない状態から始めて、9 例すべてが緑です** |
| **提案 9**（`NEXT_PUBLIC_BASE_URL` も焼き込まれます） | **対応済み** | `ENV/PRODUCTION.md` に入りました |
| **提案 11**（記録の回数の数え方） | **対応済み** | 5 回目・6 回目まで番号が通りました |

### 変異試験（11 種）

| # | 変異 | 2 回目 | 3 回目 |
|---|---|---|---|
| M7 | `Button` が `external` を無視します（常に先読みの仕組みで描きます） | 生き残り | **赤** |
| M7b | `Button` が `external` を無視します（常に素の入口で描きます） | — | **赤** |
| M8 | ワードマークをモックの既定値へ戻します | 生き残り | **赤（2 例）** |
| M9 | アカウント名をモックの値へ戻します | 生き残り | **赤（2 例）** |
| M13 | ワードマークを、禁じた語ではない別の名前へ変えます | — | **赤** |
| M14 | アカウント名を、よく似た別の名前へ変えます | — | **赤** |
| M15 | 本文の途中へモックの名前を紛れ込ませます | — | **赤** |
| M16 | ワードマークを大文字だけのモックの名前にします | — | **赤** |
| M17 | **本文へ大文字だけのモックの名前を紛れ込ませます** | — | **生き残ります** |
| M5 | ログインの入口へバックエンドの場所を書きます | 赤（Playwright のみ） | 赤（Playwright のみ。issue #173 で自動化します） |
| M10 | 見出しの完全一致を部分一致へ戻します | 赤 | 赤 |

**2 回目に生き残った 3 種は、いずれも赤くなりました。** 上下どちらへ倒しても捕まること、禁じた語を避けて別の名前へ差し替えても捕まることまで確かめています。

### 冷えた開発サーバーでの確認

**組み立ての結果を一切持たない作業ツリーから開発サーバーを立ち上げ、待ち受けが始まった直後に流しました。**

| 例 | 時間 |
|---|---|
| 1 例目 | **10.0 秒** |
| 2 例目以降 | いずれも 2.1 秒以内 |
| 合計 | **27.1 秒。9 例すべて緑** |

2 回目は同じ条件で 2 例が落ち、90 秒でも間に合いませんでした。**初めて動かす人がつまずく場所は無くなりました。**

### 指摘

要修正はありません。

- **提案 13：禁じた語の一覧が、大文字小文字を見分けます。** 本文の途中へ大文字だけで書くと素通りします。照合を小文字へそろえてから見るだけで塞がります。**この PR での対応を求めません。**
- **提案 14：外への出口のテストが、直し方そのものを固定しています。** いまの直し方を続ける限り困りません。覚えとして残します。
- **提案 12（2 回目からの持ち越し）**：**今回の実行では出ませんでした。取り下げます。**

### Issue の受け入れ要件（issue #69）

| 受け入れ条件 | 判定 | 根拠 |
|---|---|---|
| `app-ui/index.html` と見た目が一致します | **満たします** | 今回の直しは見た目に触れていません。**`app-ui/` を 1 文字も書き換えていません** |
| 文言が設定ファイルから読まれます | **満たします** | **モックの既定値が戻らないことも、テストで固定されました** |
| Playwright のテストがあります | **満たします** | **冷えた開発サーバーで全件緑です** |

### 各基準への適合

| 基準 | 判定 | 備考 |
|---|---|---|
| CC | **適合** | 他社の名称が戻らないことを、テストで固定しました |
| OWASP10 | **適合** | 秘匿値の混入はありません。外部への通信がありません |
| QC10 | **適合** | 2 回目に足りなかった回帰の押さえが入りました |
| CRAP | **適合（モックのとおり）** | 対比の目安を下回る箇所は申し送り済みです |
| DP | **適合** | フォールバックがありません |
| TM | **適合** | 直した箇所が変異試験で捕まります |

## 判定

合格

3 回のレビューで挙げた重大 2 件・要修正 6 件・提案 14 件は、すべて解決したか、妥当な見送り・別 issue への切り出しとして片づきました。

**2 回目に残した唯一の懸念、「いま動くものは正しいが、いつ黙って戻っても気づけない」は解消しました。** 生き残っていた 3 種の変異は、いずれも赤くなります。**冷えた開発サーバーでも、ブラウザ操作の確認 9 例が 27.1 秒で緑です。**

受け入れ要件 3 項目は実測で満たされ、6 つの基準すべてに適合します。PR は `MERGEABLE` ・ `CLEAN` で、自動検査 5 ジョブが緑です。**マージして差し支えありません。**

マージの後に、**issue #173** を進めてください。**併せて issue #172 と、画像の利用条件の確認を、本番公開の前に片づけてください。**

連絡先は info@rictaworks.jp です。

# reviewer 記録 : PR #185（issue #78 ・ #79 README と SPEC を、実装から起こします）

- 実施日時（JST）：2026-08-27 22:32
- 対象：1 回目 `2bc7219`（土台は `bf4cb46`。`main` と同じ位置で、遅れはありません）
- 作業ツリー：`DELETE/review-worktrees/pr185-20260827-reviewer`（`git worktree add --detach` 済みです）
- 実装側の作業ツリー `DELETE/impl-78` は読み取りだけに留めました
- 検証用のデータベース：使いません（`src/` を 1 文字も変えていないため、RSpec の全例は流していません）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 土台 | **`main`（`bf4cb46`）を取り込み済みです。** `git rev-list --left-right --count origin/main...HEAD` は `0 2` で、**遅れ 0 ・ 先行 2** です |
| PR の差分 | **2 コミット・12 ファイル**（1073 行の追加、10 行の削除） |
| `src/` の変更 | **1 文字もありません**（`git diff --name-only origin/main...HEAD -- src requirements.md app-ui` が 0 件です） |
| `requirements.md` ・ `app-ui/` | **1 文字も変わっていません** |
| 自動検査 | **6 ジョブすべて pass**（実行 `33076484284`） |
| 文書の検査（`test:docs`） | **5 例、失敗 0 件**（この作業ツリーで流し直しました） |
| セキュリティレビュー記録の指紋 | **2 つとも一致します**（`4bc2cbad60cbe35a` ・ `b4d90746ac3afb6e` を差分から再計算しました） |
| 絵文字 | **ありません**（README ・ SPEC 7 ファイル ・ テストを走査しました） |
| ですます調 | **満たします。** だ・である調の混入はありません |
| 秘匿値・本番の住所 | **ありません**（後述） |
| ファイルのエンコード・改行 | **9 ファイルとも UTF-8 ・ LF です** |
| 図が描けること | PR 本文の「6 図すべて成功」は、Mermaid CLI が手元に無いため再現していません。**GitHub の描画に必要なコード塀の対応は、検査が押さえています** |

### issue #79（README にページ一覧と API 一覧を書く）の受け入れ条件

| # | 受け入れ条件 | 判定 | 実測 |
|---|---|---|---|
| 1 | ページ名と URL がリンクで書かれています | **満たします** | 10 行の表があり、10 件ともリンクです。`src/frontend/src/app` の下の `page.tsx` は 10 件で、**過不足がありません** |
| 2 | API のタイトルとエンドポイント URL、`SPEC/api` へのリンクが書かれています | **満たします** | 14 行すべてにタイトル・経路・`SPEC/api/README.md` への案内があります。**14 件とも `SPEC/api/README.md` に契約が実在します**（1 件ずつ突き合わせました） |
| 3 | 未実装のものを書きません | **満たします** | 10 画面・14 経路・ログインと死活監視の 4 経路・管理画面の 4 パスを `src/frontend/src/app` と `src/backend/config/routes.rb` へ 1 件ずつ当てました。**実装に無いものはありません** |

**README の記述は、実装と細部まで合っています。** 次の 4 点を実装で確かめました。

| README の記述 | 実装 | 判定 |
|---|---|---|
| 自動ログインは `APP_ENV` が `development` ・ `test` で、その利用者が居るときだけ効きます | `app/controllers/concerns/authenticates_user.rb:64-70`（`AppEnvironment.developer_shortcuts_allowed?` → 環境変数 → `User.find_by`） | **一致します** |
| 本番では、値が設定されていても無視します | 同 65 行目で、条件が偽なら環境変数を読む前に `nil` を返します | **一致します** |
| `/health` は、データベースへ到達できることまで含めて答えます | `app/controllers/health_controller.rb`（`SELECT 1` を投げ、届かなければ 503） | **一致します** |
| 画面からの呼び出しは、画面の中の中継を通ります | `src/frontend/next.config.ts` ＋ `src/config/backend.ts`（`BACKEND_INTERNAL_URL` はサーバー側だけが読みます。`NEXT_PUBLIC_` ではありません） | **一致します** |

### issue #78（SPEC にリバースエンジニアリング図を作る）の受け入れ条件

| # | 受け入れ条件 | 判定 |
|---|---|---|
| 1 | 実装済みのものだけを書きます | **満たしません**（重大 1 ・ 2 ・ 3 ・ 4） |
| 2 | 図は Mermaid で書きます | **満たします**（6 文書とも Mermaid です。塀の対応は検査が押さえます） |
| 3 | requirements.md と食い違う場合は差分を明記します | **満たしません**（重大 5。書かれた 2 点は正確ですが、書き漏らしが 2 件あります） |

### 図と実装の突き合わせ

#### `SPEC/state.md` × `PromptRequest::TRANSITIONS` — **一致します**

`src/backend/app/models/prompt_request.rb:36-45` の 8 状態を 1 本ずつ当てました。

| 実装の遷移 | 図 | 判定 |
|---|---|---|
| `draft → queued` ・ `draft → rejected` | あります | 一致します |
| `queued → generating` | あります | 一致します |
| `generating → completed` ・ `degraded_completed` ・ `failed` | 3 本ともあります | 一致します |
| `failed → queued` | あります | 一致します |
| `completed → archived` ・ `degraded_completed → archived` | 2 本ともあります | 一致します |
| `rejected → []` ・ `archived → []` | 終端として描いています | 一致します |

**過不足はありません。** 枠の扱いの表（`rejected` は予約しない・`failed` は返す）も、`Quota::Settlement.next_status_for` ・ `PromptRequests::Acceptance` の実装どおりです。

#### `SPEC/er.md` × `db/schema.rb` — **表と列は一致します。読み取りの要点に 1 か所の誤りがあります**

- **表は 11 件で過不足がありません。** `solid_queue_*` ・ `solid_cache_entries` を除く業務の表と 1 対 1 で対応します。
- **列も 11 表すべてを 1 つずつ当てました。** 型・注記・一意（`users.x_user_id` ・ `evaluation_notes.prompt_output_id` ・ `rule_dictionaries.version`）まで合っています。
- **結び付きも外部キーと一致します。** `prompt_requests |o--o| quota_consumptions` を任意にしている点、`rule_dictionaries` ・ `metric_events` が外部キーを持たない点も、`schema.rb` の `add_foreign_key` の並びどおりです。
- **ただし「読み取りの要点」の 1 文が実装と逆です**（重大 3）。

#### `SPEC/class.md` × 実装 — **食い違いが複数あります**（重大 1）

#### `SPEC/sequence.md` × 実装 — **図 2 の順序が実装と逆です**（重大 3）。ほかの 5 本は一致します

#### `SPEC/dfd.md` × 実装 — **一致します**

「外へ出す情報」の表を 1 行ずつ当てました。reCAPTCHA へ**要求元のアドレスを送らない**ことは `bot_protection/recaptcha_verifier.rb:76`（引数そのものを置いていません）で、`sessions` が識別子のハッシュだけを持つことは `schema.rb` の `token_digest` で、`metric_events` が誰の分かを持たないことは `app/models/metric_event.rb` で確かめました。**すべて記述どおりです。**

#### `SPEC/usecase.md` × 実装 — **利用者の操作が 1 件、実装にありません**（重大 2）

### 変異試験（文書の検査が実際に効くことの確認）

**11 種を、この作業ツリーの写しへ 1 か所ずつ変異を入れて流しました**（`node --test test/docs/docs.test.mjs`）。

| # | 変異 | 結果 |
|---|---|---|
| M0 | 変異なし（基準） | **緑**（5 例、失敗 0 件） |
| M1 | 実在しない画面（`/dashboard`）を README のページ一覧へ足します | **赤（1 例）** |
| M2 | 実装済みの画面（`/presets`）を README から落とします | **赤（1 例）** |
| M3 | `SPEC/README.md` の索引から `state.md` を落とします | **赤（1 例）** |
| M4 | `SPEC/er.md` の Mermaid の塀を閉じ忘れます | **赤（1 例）** |
| M5 | **実装に無い動詞で経路を足します**（`DELETE /api/v1/projects/:id`） | **緑のまま（検出できません）** |
| M6 | **実装に無い経路を足します**（`GET /api/v1/dashboard`） | **緑のまま（検出できません）** |
| M7 | 実装に無い画面へ書き換えます（`/projects` → `/projects/list`） | **赤（2 例）** |
| M8 | 実装に無い経路を足します（`POST /api/v1/prompt_requests/:id/regenerate`） | **赤（1 例）** |
| M9 | `SPEC/` へ文書を足し、索引へ載せません | **赤（1 例）** |
| M10 | 実装に無い経路を足します（`POST /api/v1/prompt_outputs/:id/copy`） | **赤（1 例）** |

**5 つの検めのうち 4 つは、しっかり効きます。** 画面の両方向（書きすぎ・書き漏らし）と、索引・塀の押さえは、変異を入れると必ず赤くなります。

**経路の検めだけ、穴が 2 つあります**（提案 1）。`test/docs/docs.test.mjs:88-98` は、**経路の末尾の語が `routes.rb` の本文に現れるか、同じ名前の控えのファイルがあるか**しか見ません。

- **動詞を見ていません。** `DELETE /api/v1/projects/:id` は、`routes.rb` が `only: %i[index create update]` と明示して**作っていない**経路ですが、末尾の語が `projects` であるため通ります。
- **`routes.rb` のどこかに現れる語を使えば通ります。** `GET /api/v1/dashboard` は、管理画面の `root to: 'dashboard#show'` の語に当たって通ります。**利用者向けの API として実在しないのに、緑のままです。**

### 秘匿情報の露出

| 観点 | 方法 | 結果 |
|---|---|---|
| 本番の住所 | 全文検索（`https?://`） | **ありません。** README にあるのは `http://localhost:3300` だけです |
| バックエンドのドメイン | 目視 ・ 全文検索 | **ありません。** 「画面の中の中継を通ります」と書くに留めています |
| 管理画面の住所 | 目視 | **パスだけです**（`/admin` ほか）。本番の住所は書いていません |
| 環境変数の値 | 目視 | **名前だけです**（`DEVELOPMENT_AUTO_LOGIN_X_USER_ID` ・ `BACKEND_INTERNAL_URL`）。値は 1 つも書いていません |
| `.claude/` の内容 | 全文検索 | **1 文字も転記されていません** |
| 個人名 | 目視 | **ありません。** 連絡先は `info@rictaworks.jp` です |
| Railway ・ Vercel の実際の住所 | 全文検索 | **ありません** |

**この観点では、指摘はありません。**

### 各基準への適合

| 基準 | 判定 | 根拠 |
|---|---|---|
| CC | **適合** | 権利物の持ち込み・第三者の表示物がありません。連絡先は所定のもの 1 つです |
| OWASP10 | **適合** | 秘匿値・本番の住所・内部の作りの露出がありません。検査は読み取りだけで、外部へ通信しません |
| QC10 | **満たしません** | **記述の正しさの観点で、実装と食い違う記載が残っています**（重大 1 〜 5）。**検査が緑であることが、記述の正しさを保証していません** |
| CRAP | **適合** | 見出し・表・強調の使い方が 7 文書で揃っています。近接・反復とも保たれています |
| DP | **適合** | 検査は既存の `node --test` に乗せ、新しい仕組みを作っていません。既存ジョブへ 1 手順だけ足す判断も、YAGNI ・ KISS に沿います |
| TM | **おおむね適合** | 文書に対する自動の検めを新設した点は評価できます。**ただし経路の検めが弱く、変異が 2 種生き残ります**（提案 1）。図と実装の突き合わせを見る例はありません（提案 2） |

### プロジェクト固有の決まり

| 決まり | 判定 |
|---|---|
| 実装が `src/` の下にあること | **該当しません。** `src/` を 1 文字も変えていません |
| 開発用の道具が `src` の外にあること | **満たします**（`test/docs/` ・ `.github/`） |
| フォールバックが無いこと・例外処理があること | **満たします。** 検査は読めなければその場で落ちます。既定値へ寄せる書き方はありません |
| デバッグトレースが追えること | **満たします。** 失敗した文言に、どの画面・どの文書が原因かを名指しで出します |
| 制御構文以外がクラスまたは関数に入ること | **満たします**（`read()` ・ `documentedPages()` ・ `implementedPages()` ・ `documentedEndpoints()`） |
| グローバル変数が無いこと | **満たします。** 定数 2 つ（`HERE` ・ `ROOT`）は `const` で、書き換えません |
| 文字列リテラルの分離とハードコードの検査 | **満たします。** `check:hardcode` は自動検査で緑です |
| ネイティブの `alert()` 等を使わないこと | **満たします** |
| 環境の判定と本番での近道の遮断 | **満たします。** README が近道の条件と「本番では効かないこと」を明記し、実装と一致します |
| 車輪の再発明をしないこと | **満たします。** Node 標準の `node:test` と `node:fs` だけです |
| フォロワー判定を自前で作らないこと | **満たします。** 図はいずれも `x-follower-gate` へ問い合わせる形で描いています |
| 秘匿値を `.env` から読むこと | **満たします。** 文書には名前だけを書いています |
| ですます調 | **満たします** |
| 日本語版のみ・多言語化なし | **満たします** |
| 絵文字なし | **満たします** |
| JST ・ UTF-8 | **満たします** |

## 指摘

### 重大（取り込みの前に直していただきたいもの）

- **重大 1：`SPEC/class.md` に、実装に無いクラスの中身と、実装に無いメソッドが並んでいます。**

  issue #78 の受け入れ条件「実装済みのものだけを書きます」に反します。1 つずつ挙げます。

  | 図の記載 | 実装 | 場所 |
  |---|---|---|
  | `PromptPackage` の項目は `main_prompt` ・ `negative_prompt` ・ `parameters` ・ `art_direction_note` です | **`Generation::PromptPackage` の項目は `variation` ・ `formatted` ・ `note` ・ `draft` です。** 4 つとも図に載っていません | `SPEC/class.md:39-44` ／ `src/backend/app/services/generation/prompt_package.rb:10` |
  | `ModelAdapter` の `format(素材)` は `PromptPackage` を返します（`ModelAdapter --> PromptPackage`） | **`Adapters::ModelAdapter#format(draft)` が返すのは `Adapters::Formatted` です。** `PromptPackage` を作るのは `PromptGenerationService#packaged` です。**`Formatted` は図に 1 つも出てきません** | `SPEC/class.md:26` ・ `58` ／ `model_adapter.rb:96` ・ `120`、`adapters/formatted.rb:18`、`prompt_generation_service.rb:183` |
  | `StyleSpec` に `decide(inputs, dictionary)` があります | **`decide` はありません。** 公開の入口は `apply(draft)` です（`decide` は別物件の `people.decide` です） | `SPEC/class.md:12-14` ／ `style_spec.rb:101` ・ `150` |
  | `Draft` に `build(仕様)` があります | **`build` はありません。** `initialize` ・ `add` ・ `replace` です（`build` を持つのは別の `VariationBuilder` です） | `SPEC/class.md:15-17` ／ `draft.rb:30` ・ `53` ・ `63` |
  | `Delivery` に `+store(request, packages)` があります | **公開の入口は `call(packages)` です**（`initialize(prompt_request)`）。`store` は**非公開**で、引数は `package` 1 つです | `SPEC/class.md:182` ／ `prompt_requests/delivery.rb:16` ・ `22` ・ `60` |
  | `Reservation` に `+refund!(prompt_request)` があります | **`refund!` はありません。** 返還は `settle!` が `Settlement.next_status_for` の答えに従って行います | `SPEC/class.md:151` ／ `quota/reservation.rb:31` ・ `49`（`def refund!` は全文検索で 0 件です） |
  | `NarrativeAdapter` は `<<mixin>>` で、`DalleAdapter` ・ `NanoBananaAdapter` が**依存**します。`DalleAdapter` は `ModelAdapter` を**直接**継承します | **`NarrativeAdapter` はクラスで、`ModelAdapter` を継承します。** `DalleAdapter` ・ `NanoBananaAdapter` は **`NarrativeAdapter` の子**です。`ModelAdapter` を直接継承するのは `MidjourneyAdapter` ・ `StableDiffusionAdapter` だけです | `SPEC/class.md:32-35`（`<<mixin>>`）・ `52`（`ModelAdapter <|-- DalleAdapter`）・ `54`（同 `NanoBananaAdapter`） ／ `narrative_adapter.rb:45`、`dalle_adapter.rb:22`、`nano_banana_adapter.rb:14`、`midjourney_adapter.rb:15`、`stable_diffusion_adapter.rb:18` |

  **直し方は 2 通りあります。** 実装の名前と項目へ合わせるか、図から落とすかです。とくに `PromptPackage` は、**名前は実在するのに中身が別物**ですので、読んだ方が `package.main_prompt` を書いて動かないところまで進みます。`Formatted` を図へ足し、`PromptPackage --> Formatted` の関係を描くのが素直だと思います。

- **重大 2：`SPEC/usecase.md` の UC12「判定を取り直します」は、利用者の操作としては実装されていません。**

  `SPEC/usecase.md:25`（`UC12["判定を取り直します"]`）と `46`（`利用者 --> UC12`）です。

  **実装を全文検索しました。** `Auth::RecheckService` を呼ぶ場所は `src/backend/app/controllers/admin/users_controller.rb:33` の 1 か所だけです。`src/backend/config/routes.rb` の `namespace :api do namespace :v1 do` の中に、再判定の経路はありません。画面側（`src/frontend/src`）にも、再判定を押す導線はありません（`pending` を扱う表示は `useRequest.ts` の生成の状態だけで、プラン値ではありません）。

  **同じ操作は UC22「判定を取り直します（代行）」として、管理者側にもう 1 つ挙がっています。** 実装があるのは、こちらだけです。

  **この文書は末尾で「この図に無い操作は、実装していません」と書いています。** 裏返して読むと「図にある操作は実装されています」と受け取られます。**利用者の欄に、無い操作が 1 つ載っている状態です。** UC12 を落とすか、「ログインをし直すと判定を取り直します」という実際の挙動（`auth_controller.rb:52` が毎回のログインで `plan_updater.refresh` を呼びます）に書き改めてください。

- **重大 3：枠の予約と、お申し込みの行を作る順序が、実装と逆に書かれています。**

  実装は `src/backend/app/services/prompt_requests/acceptance.rb:56-63` です。**`draft(project, normalized)` で行を作ってから、`ensure_allowed!` を通り、`enqueue` の中の `Quota::Reservation.reserve!(user:, prompt_request: request)` で予約します**（同 123 行目）。冒頭のコメント（同 11-16 行目）も「3. 記録を作ります（`draft`）… 5. 枠を予約します」と、その順序を明示しています。

  | 文書 | 記載 | 判定 |
  |---|---|---|
  | `SPEC/sequence.md:62-63` | 「本日の枠を予約します」→「お申し込みを作ります」 | **逆です** |
  | `SPEC/er.md:124` | 「枠の予約は、お申し込みの行を作る前に行うためです」 | **逆です** |
  | `SPEC/state.md:26`（表） | 「`draft`｜受け付けた直後です｜まだ予約しません」 | **正しいです** |

  **SPEC の中で食い違っています。** `quota_consumptions.prompt_request_id` が空を許すのは、`Quota::Reservation.reserve!` が `prompt_request:` を省略して呼べる形だから（同 32 行目の既定値 `nil`）であって、受付の経路が行より先に予約するからではありません。**理由の説明ごと直してください。**

- **重大 4：`SPEC/state.md` のクォータの図に、実装済みの遷移が 2 本ありません。**

  `SPEC/state.md:44-48` は `reserved → confirmed` ・ `reserved → refunded` の 2 本だけを描き、`confirmed` ・ `refunded` をどちらも終端にしています。**実装はそうなっていません。**

  | 実装の遷移 | 実装の場所 | 図 |
  |---|---|---|
  | `refunded → reserved` | `src/backend/app/models/quota_consumption.rb:32`（`'refunded' => %w[reserved]`）。**実際に通ります**：`quota/reservation.rb:125` で状態を見て、`142-146` の `reclaim!` が `transition_to!('reserved', …)` を呼びます | **ありません** |
  | `confirmed → refunded`（管理者の手動リセット） | `quota_consumption.rb:79`（`reset_by_admin!`）。`admin/users_controller.rb:52` は**予約中の枠を弾きます**ので、リセットが当たるのは主に `confirmed` の行です | **ありません** |

  **この 2 本は、まさにこの節が本文で説明している機能です。** 本文は「管理者は手動でリセットできます（`reset_by_admin`）」と書いていますが、**図では `confirmed` から出る線がありません。** 「返還後、当日中に作り直せます」（`quota_consumption.rb:22` のコメント）も図から読み取れません。**本文と図が食い違っています。**

  なお `reset_by_admin!` は `TRANSITIONS` を通らない専用の入口です（同 70-78 行目のコメント）。**その旨も添えて描いていただけると、読んだ方が `transition_to!` で同じことをしようとして失敗せずに済みます。**

- **重大 5：`SPEC/README.md` の「requirements.md との差」に、書き漏らしが 2 件あります。**

  **書かれた 2 点は、どちらも正確です。** 実装で確かめました。

  | 書かれた差 | 実測 |
  |---|---|
  | LangSmith の追跡を有効にしていません | `Generation::LangchainGemini` は `Langchain::LLM::GoogleGemini` を継ぎ、上書きするのは呼び出し先・鍵・待ち時間だけです。追跡の設定は全文検索で 0 件です。**記載どおりです** |
  | 測定軸 8 つのうち 6 つです | `Metrics::UsageSummary#call` が返すのは `requests` ・ `degraded` ・ `evaluation` ・ `regeneration` ・ `quota` で、`MetricEvent::AXES` は `quota_exhausted` ・ `quota_reclaimed` の 2 つです。**合わせて 6 軸**で、「コピーされた案の分布」と「稼働率」はありません。**記載どおりです** |

  **問題は、その次の 1 行です。** 「**上の 2 点以外に、仕様と食い違う実装はありません。**」と断言しています。**次の 2 件が漏れています。**

  | requirements.md | いまの実装 |
  |---|---|
  | 6「フォロー直後の判定漏れに対して、**ユーザー操作による手動再判定の導線を設ける**。再判定にはクールダウン（レート制限保護）を必須とする」 | **利用者向けの導線がありません。** `Auth::RecheckService`（クールダウン 5 分つき）は実装済みですが、**呼ぶのは管理画面だけ**です（`admin/users_controller.rb:33`）。利用者向けの経路も画面も存在しません。**重大 2 と同じ穴です** |
  | 7.3「LLM ジョブの失敗率・縮退モード発生を**アラート対象とする**」「X API のエラー率を監視し…」「**通知先を定義**し…」 | **通知・アラートの実装がありません**（`alert` ・ `通知` ・ `webhook` ・ `slack` を `src/backend/app` ・ `src/backend/config` で全文検索して 0 件です）。`/health` の応答までは実装済みです |

  **差を書き漏らすと、実装が仕様に届いているように読めます。** この節はまさに、そうならないために置いた節です。**2 件を足すか、「差の洗い出しはこの 2 点に絞りました」と範囲を明示するか**を選んでください。断言の 1 行は、そのままでは残せません。

### 提案（次の issue へ回してよいもの）

- **提案 1：経路の検めが、動詞と語の当てずっぽうを通します。** `test/docs/docs.test.mjs:88-98` は末尾の語しか見ません。実測で、**実装が明示的に作っていない `DELETE /api/v1/projects/:id`（`only: %i[index create update]`）と、利用者向けには存在しない `GET /api/v1/dashboard`（管理画面の `dashboard#show` の語に当たります）が、どちらも緑のまま通りました。** 画面の検めと同じように、`routes.rb` から動詞と経路の組を取り出して突き合わせる形にすると塞がります。**この PR が新設した押さえですので、次の issue で強めていただければ十分です。**
- **提案 2：図が実装と食い違っても、検査は緑のままです。** 今回の重大 1 〜 4 は、5 つの検めのどれにも当たりませんでした。**すべて機械で押さえるのは重すぎますが、状態遷移図だけは安く押さえられます。** `PromptRequest::TRANSITIONS` ・ `QuotaConsumption::TRANSITIONS` を読み、`SPEC/state.md` の `A --> B` の並びと集合で突き合わせる例を 1 つ置けば、**重大 4 は自動で赤くなります。**
- **提案 3：`archived` へ進む経路が、実装にありません。** `PromptRequest::TRANSITIONS` には `completed → archived` がありますが、`transition_to!('archived')` を呼ぶ場所は全文検索で 0 件です（画面側は `ja.ts:72` ・ `RequestStatus.tsx:107` で表示だけ用意しています）。`SPEC/state.md:34` の表は「記録として畳みました」と、操作があるかのように読めます。**「定めはありますが、進める操作はまだありません」と一言添えると正確になります。**
- **提案 4：定時の拾い直しが拾うものは、2 種類です。** `SPEC/state.md:53` は「決着だけが残った行を拾い直します」と 1 種類だけを挙げていますが、`ReclaimPromptRequestsJob#perform` は `stale_ids`（動きの無い `generating`）と `unsettled_ids`（決着済みなのに枠が予約のまま）の**2 種**を拾います。1 行足すだけで合います。
- **提案 5：README に載っていない経路が 1 本あります。** `routes.rb` の `get 'up' => 'rails/health#show'` です。Rails の既定で、`/health` との違いも `routes.rb` に書かれています。**載せるか、載せない理由を書くか**を決めておくと、次に読む方が迷いません。
- **提案 6：管理画面は、一覧に載っている 4 パス以外にも経路があります。** 版の作成（`/admin/rule-dictionaries/new`）・公開（`POST …/publish`）・利用者の詳細（`/admin/users/:id`）・再判定・枠のリセットです。**操作は `SPEC/usecase.md` が挙げていますので実害はありません**が、README の表に「主なページです」と一言あると、抜けと読まれずに済みます。
- **提案 7：テストの置き場が `test/pr<番号>/` ではありません。** `test/docs/` です。**`test/hardcode/` ・ `test/hooks/` ・ `test/harness/` と同じ「持ち場ごとの検査」の並びですので、この置き方に合理性があります。** ただし issue の前提は `test/pr<番号>/` と書いています。**置き場の決まりを実態に合わせて書き換える issue**を立てておくと、次から迷いません。

## 後片づけ

**削除は行っていません。** 手動で片づけていただく必要があるものは、次のとおりです。

- レビュー用の作業ツリー `DELETE/review-worktrees/pr185-20260827-reviewer`
- 変異試験に使った写し `C:\tmp\pr185review`（WSL からは `/mnt/c/tmp/pr185review` です）。**開発機に Linux 側の Node がありませんでしたので、Windows 側の Node で流すために、README ・ `SPEC/*.md` ・ `test/docs/` ・ `src/frontend/src/app` ・ `src/backend/config/routes.rb` ・ `src/backend/app/controllers` だけを写しました。** 秘匿値は含みません

**実装側の作業ツリー `DELETE/impl-78` は、読み取りだけに留めました。** 1 ファイルも変えていません。

## 判定

要修正

**issue #79（README）は、受け入れ条件 3 件をすべて満たします。** 10 画面・14 経路を実装へ 1 件ずつ当て、過不足がないことを確かめました。自動ログイン・`/health` ・ 画面の中の中継についての記述も、実装と細部まで一致します。**秘匿情報の露出もありません。** 本番の住所・バックエンドのドメイン・環境変数の値・`.claude/` の内容は、1 か所も出ていません。

**文書に対する自動の検めを新設した点は、この PR のいちばんの値打ちです。** 変異を 11 種入れて流し、画面の書きすぎ・書き漏らし・索引の漏れ・塀の閉じ忘れが、いずれも確かに赤くなることを実測しました。

**取り込みを止めるのは、issue #78 の側です。** 受け入れ条件「実装済みのものだけを書きます」と「requirements.md と食い違う場合は差分を明記します」を、いまの `SPEC/` は満たしていません。

- **`SPEC/class.md`** に、実装に無いメソッドが 4 つ（`StyleSpec.decide` ・ `Draft.build` ・ `Reservation.refund!` ・ 公開されていない `Delivery.store`）並び、`PromptPackage` の項目 4 つが実装と別物で、継承の親子も入れ替わっています（重大 1）。
- **`SPEC/usecase.md`** の利用者の欄に、実装の無い操作が 1 つ載っています（重大 2）。
- **`SPEC/sequence.md` と `SPEC/er.md`** が、枠の予約とお申し込みの行を作る順序を実装と逆に書いています。同じ PR の `SPEC/state.md` は正しく書いており、**SPEC の中で食い違っています**（重大 3）。
- **`SPEC/state.md`** のクォータの図に、実装済みの遷移が 2 本ありません。**そのうち 1 本は、同じ節が本文で説明している管理者の手動リセットそのものです**（重大 4）。
- **`SPEC/README.md`** の「上の 2 点以外に、仕様と食い違う実装はありません」が成り立ちません。**利用者による手動再判定の導線（requirements.md 6）と、アラート・通知（同 7.3）が実装にありません**（重大 5）。

**書かれた 2 点の差そのものは、どちらも正確でした。** 隠さずに書く姿勢は、そのまま活かしていただきたいと思います。**足りないのは、洗い出しの範囲だけです。**

**重大 1 〜 5 を直していただければ、取り込める状態になります。** `src/` を 1 文字も変えない PR ですので、**直すのは文書の記述だけ**です。実装側の修正は要りません。**提案 1 〜 7 は、この PR を止めるものではありません。次の issue へまとめて回してください。**

連絡先は info@rictaworks.jp です。

---

- 実施日時（JST）：2026-08-27 23:58
- 対象：2 回目 `1c987ff`（1 回目は `2bc7219`。修正は 1 コミットです）
- 作業ツリー：`DELETE/review-worktrees/pr185-20260827-reviewer2`（`git worktree add --detach` 済みです）
- 実装側の作業ツリー `DELETE/impl-78` は、今回も読み取りだけに留めました
- RSpec の全例は流していません（`src/` を 1 文字も変えていないためです）

## 2 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 土台 | `git rev-list --left-right --count origin/main...1c987ff` は **`1 3`** です。**`main` に 1 コミット（`5841c48` 1 回目のレビュー記録）だけ先行があります。** 文書だけの記録ですので、取り込みの妨げにはなりません |
| 修正の差分 | **1 コミット・10 ファイル**（241 行の追加、35 行の削除） |
| `src/` の変更 | **1 文字もありません**（`git diff --name-only origin/main...1c987ff -- src requirements.md app-ui` が 0 件です） |
| 自動検査 | **6 ジョブすべて pass**（実行 `33078186780`） |
| 文書の検査（`test/docs/docs.test.mjs`） | **7 例、失敗 0 件**（この作業ツリーの写しで流し直しました。5 例から 2 例増えています） |
| ファイルのエンコード | **11 ファイルとも UTF-8 です** |
| 絵文字 | **ありません**（README ・ SPEC ・ `test/README.md` ・ 検査を走査しました） |
| ですます調 | **満たします。** 追加した行の文末を機械で拾い、だ・である調の混入がないことを確かめました |
| 秘匿値・本番の住所 | **ありません。** `https?://` は `http://localhost:3300` ・ `http://localhost:3000` だけです。`secret` ・ `api_key` ・ `railway.app` ・ `vercel.app` の類は 0 件です |

### 重大 1 〜 5 の再検証

#### 重大 1（`SPEC/class.md`）— **7 点のうち 7 点が直っています。ただし 1 か所を新しく取り違えています**

| 1 回目の指摘 | いまの記載 | 実装 | 判定 |
|---|---|---|---|
| `PromptPackage` の項目が別物です | `variation` ・ `formatted` ・ `note` ・ `draft` ・ `degraded?()` | `prompt_package.rb:10`（`Struct.new(:variation, :formatted, :note, :draft)`）・ 同 `15`（`degraded?`） | **直りました** |
| `Formatted` が図にありません | `Formatted` を新設し、`main_prompt` ・ `negative_prompt` ・ `parameters` ・ `prompt` ・ `notes` を挙げます | `app/services/adapters/formatted.rb:18`（同じ 5 項目・同じ並び） | **直りました** |
| `ModelAdapter.format` の返り値 | `+format(draft) Formatted` | `model_adapter.rb:96`（`@return [Formatted]`）・ 同 `116-126`（`Formatted.new(...)`） | **直りました** |
| 包む持ち場が図にありません | `PromptGenerationService --> PromptPackage` ・ `PromptPackage --> Formatted` を足し、本文で「整えるのと、包むのは別の持ち場です」と説明します | `prompt_generation_service.rb:178-186`（`packaged` が `Package.new(formatted: …ModelAdapter.for(...).format(draft))`。`Package = PromptPackage` は同 `73`） | **直りました** |
| `StyleSpec.decide` が実装にありません | `+apply(draft) Draft` | `style_spec.rb:101` | **直りました** |
| `Draft.build` が実装にありません | `+add(...)` ・ `+replace(...)` | `draft.rb:53` ・ `63` | **直りました** |
| `Reservation.refund!` が実装にありません | 落として、`Settlement` と `Reservation --> Settlement` を足しました | `quota/reservation.rb:31` ・ `49`、`quota/settlement.rb:33` | **直りました** |
| `Delivery.store` が非公開です | `+call(packages)` | `prompt_requests/delivery.rb:22`（`store` は同 `60` の非公開のまま） | **直りました** |
| 継承の親子が入れ替わっています | `ModelAdapter <|-- NarrativeAdapter` ・ `NarrativeAdapter <|-- DalleAdapter` ・ `NarrativeAdapter <|-- NanoBananaAdapter` ・ `ModelAdapter <|-- MidjourneyAdapter` ・ 同 `StableDiffusionAdapter` | `narrative_adapter.rb:45` ・ `dalle_adapter.rb:22` ・ `nano_banana_adapter.rb:14` ・ `midjourney_adapter.rb:15` ・ `stable_diffusion_adapter.rb:18` | **直りました。** `<<mixin>>` の表記も外れています |

**残りの記載も、この機会に 1 つずつ実装へ当てました。** `GeminiClient.refine(instruction:, lines:)` ・ `LangchainGemini#http_post` ・ `LlmSettings.load` ・ `AuthenticatesUser#current_user` ・ `AuthenticatesAdmin#admin_actor` ・ `VerifiesHumans#verify_human!` ・ `Admin::Credentials.from_env` ・ `.actor_for` ・ `RecaptchaVerifier#call` ・ `AppEnvironment.current` ・ `.developer_shortcuts_allowed?` ・ `Session.find_alive` ・ `#revoke!` ・ `FollowerGateClient#decide` ・ `QuotaDay.of` ・ `GeneratePromptJob#perform` ・ `#claimed?` ・ `#stale?` ・ `ReclaimPromptRequestsJob#perform` ・ `#stale_ids` ・ `#unsettled_ids` は、**すべて実在します。**

**ただし 1 か所、直したときに取り違えています**（新規 1。後述します）。`LlmRefiner` の公開の入口を `+call(draft) Draft` に書き換えていますが、**実装に `call` はありません。**

#### 重大 2（`SPEC/usecase.md` UC12）— **直っています**

- `SPEC/usecase.md:25` は `UC12["ログインし直して、判定を取り直します"]` になりました。
- **実際の挙動と一致します。** `src/backend/app/controllers/auth_controller.rb:48-53` の `complete_login` が、**ログインのたびに** `plan_updater.refresh(user)` を呼びます。`Auth::PlanUpdater#refresh`（`plan_updater.rb:35`）は判定サービスへ問い合わせ、`PLAN_BY_DECISION`（`full → active` ・ `restricted → pending`）でプラン値へ落とします。**ですので、ログインし直せば判定は取り直されます。**
- **押しものが無いことも、そのとおり書いてあります**（`SPEC/usecase.md:71-75`）。「押しもの（手動の再判定の導線）は `requirements.md` 6 が求めていますが、まだ実装していません」と明記し、`SPEC/README.md` の差の一覧へも送っています。**文書どうしが食い違わなくなりました。**
- 利用者向けの再判定の経路が無いことは、今回も `src/backend/config/routes.rb` の `namespace :api do namespace :v1 do` を通しで読んで確かめました。**ありません。** `Auth::RecheckService` を呼ぶのは `admin/users_controller.rb` だけです。

#### 重大 3（予約と行を作る順序）— **主眼は直っています。付けた一文に、なお取り違えがあります**

| 文書 | いまの記載 | 実装 | 判定 |
|---|---|---|---|
| `SPEC/sequence.md:62-63` | 「お申し込みを作ります（draft）」→「本日の枠を予約します」 | `prompt_requests/acceptance.rb:53-59`（`draft(project, normalized)` → `ensure_allowed!` → `enqueue`）・ 同 `123`（`enqueue` の中で `Quota::Reservation.reserve!`） | **直りました** |
| `SPEC/sequence.md:72` の本文 | 「お申し込みの行を先に作り、そのあとで枠を予約します」 | 同上 | **直りました** |
| `SPEC/er.md:124` | 「`prompt_request_id` は空を許します。枠の予約は、お申し込みに結び付けずに行うこともできるためです（`reserve!` は `prompt_request:` を省いて呼べます）。受付の経路では、行を作ってから予約しますので、結び付きます」 | `quota/reservation.rb:31`（既定値 `prompt_request: nil`） | **直りました。理由の説明も実装どおりです** |

**ただし `SPEC/sequence.md:72` に足した「差し戻しは、そのどちらよりも先に判定します。」は、半分だけ正しいです**（新規 3。後述します）。

#### 重大 4（`SPEC/state.md` のクォータの図）— **直っています**

| 実装の遷移 | 実装の場所 | 図（`SPEC/state.md:46-53`） |
|---|---|---|
| `reserved → confirmed` ・ `reserved → refunded` | `quota_consumption.rb:30`（`'reserved' => %w[confirmed refunded]`） | あります |
| `refunded → reserved` | 同 `32`（`'refunded' => %w[reserved]`）。`reservation.rb:142-148` の `reclaim!` が通します | **足されました**（「当日中に、もう一度作ります」） |
| `confirmed → refunded` | `quota_consumption.rb:72-75`（`reset_by_admin!`） | **足されました**（「管理者が手動でリセットします」） |
| `confirmed` からの定めは空 | 同 `31`（`'confirmed' => []`） | `confirmed --> [*]` として描いています |

**「この 1 本だけは、定めた遷移の表を通りません」という断りも足しました**（`SPEC/state.md:56`）。`quota_consumption.rb:63-71` のコメントと一致します。**本文と図の食い違いは解消しています。**

なお、本文が引く `Quota::Reservation#reclaim!` は、実装では **`class << self` の中の非公開の手続き**です（`reservation.rb:26` ・ `133` ・ `142`）。`#` は本来、公開の instance の入口を指す書き方です（新規 4）。

#### 重大 5（`SPEC/README.md` の差の一覧）— **断言は外れました。書かれた 4 点は正確です。なお漏れが 2 件あります**

- 「上の 2 点以外に、仕様と食い違う実装はありません。」は**外れました。** 代わりに「見落としがあれば、見つけた時点でこの表へ足します。**『差はもうありません』とは書きません。**」になっています。**指摘のとおりの直しです。**
- **足した 2 行の中身は、どちらも正確です。**
  - 6「利用者の操作による手動の再判定の導線」→ 実装は管理画面だけ（`admin/users_controller.rb:33`）。**実測どおりです。**
  - 7.3「アラート対象とする」「通知先を定義する」→ `alert` ・ `通知` ・ `webhook` ・ `slack` を `src/backend/app` ・ `src/backend/config` で全文検索して 0 件です。**実測どおりです。**
- **ただし、その 2 行が GitHub では表として描かれません**（新規 2。後述します）。
- **さらに 2 件、`requirements.md` と突き合わせて漏れを見つけました**（後述の「なお挙がっていない差」）。

### 提案 1 ・ 2 ・ 3 ・ 4 ・ 5 ・ 7 の対応

| 提案 | 対応 | 実装との突き合わせ | 判定 |
|---|---|---|---|
| 1 | 経路の検めを、`routes.rb` から動詞と経路の組を組み立てる形へ直しました（`test/docs/docs.test.mjs:88-125`） | `only: %i[...]` の操作だけを数え、`resource`（単数）は識別子を付けません | **効きます**（変異 N6 ・ N7 ・ N11 が赤くなります） |
| 2 | 状態遷移図の検めを 2 つ足しました（同 `140-207`） | `PromptRequest::TRANSITIONS` ・ `QuotaConsumption::TRANSITIONS` を読みます。**取り出した組は 9 本と 3 本で、取りこぼしも拾いすぎもありません**（抽出結果を直接確かめました） | **効きます**（変異 N1 ・ N3 ・ N4 ・ N12 ・ N13 ・ N14 が赤くなります） |
| 3 | 「`archived` へ進める操作は、まだありません」を足しました | `transition_to!('archived')` は `src/backend/app` で 0 件です | **正確です** |
| 4 | 「2 種類の行を拾い直します」へ直しました | `reclaim_prompt_requests_job.rb:50-70`（`stale_ids` ・ `unsettled_ids`）。冒頭のコメント `15-18` とも一致します | **正確です** |
| 5 | `GET /up` を README へ足し、`/health` との違いを書きました。管理画面の表に「主なページです」も足しました | `routes.rb:66`（`get 'up' => 'rails/health#show'`）・ `routes.rb:13`（`get 'health'`） | **正確です** |
| 7 | 道具の検査を `test/pr<番号>/` へ置かない理由を `test/README.md` へ書きました | — | **筋の通った説明です** |

### 変異試験（18 種）

**この作業ツリーの写しへ、1 か所ずつ変異を入れて `node --test test/docs/docs.test.mjs` を流しました。**

| # | 変異 | 期待 | 結果 |
|---|---|---|---|
| M0 | 変異なし（基準） | 緑 | **緑（7 例、失敗 0 件）** |
| N1 | クォータの図から `refunded --> reserved` を落とします | 赤 | **赤（1 例）** |
| N2 | クォータの図から `confirmed --> refunded` を落とします | 緑 | **緑**（表を通らない 1 本のため。記録の申告どおりです） |
| N3 | 生成リクエストの図から `failed --> queued` を落とします | 赤 | **赤（1 例）** |
| N4 | 生成リクエストの図へ、実装に無い遷移を足します（`rejected --> queued`） | 赤 | **赤（1 例）** |
| N5 | クォータの図へ、実装に無い遷移を足します（`confirmed --> reserved`） | 赤 | **緑のまま（検出できません）** |
| N6 | README へ `DELETE /api/v1/projects/:id` を足します | 赤 | **赤（1 例）。1 回目に生き残った変異が捕まります** |
| N7 | README へ `GET /api/v1/dashboard` を足します | 赤 | **赤（1 例）。1 回目に生き残った変異が捕まります** |
| N8 | README へ `GET /api/v1/users` を足します（管理画面の資源に当たります） | 赤 | **緑のまま（検出できません）** |
| N9 | README へ `POST /api/v1/rule_dictionaries` を足します（同上） | 赤 | **緑のまま（検出できません）** |
| N10 | README から実装済みの経路を落とします（`GET /api/v1/presets`） | 赤 | **緑のまま（検出できません）** |
| N11 | README へ `PATCH /api/v1/prompt_requests/:id` を足します（`update` は実装していません） | 赤 | **赤（1 例）** |
| N12 | 実装（`quota_consumption.rb`）へ遷移を 1 本足します | 赤 | **赤（1 例）** |
| N13 | 実装（`prompt_request.rb`）から遷移を 1 本落とします | 赤 | **赤（1 例）** |
| N14 | クォータの図の字下げを 2 文字にします | 赤 | **赤（1 例）** |
| N15 | README のページ一覧から `/presets` を落とします | 赤 | **赤（2 例）** |
| N16 | SPEC の索引から `state.md` を落とします | 赤 | **赤（1 例）** |
| N17 | `SPEC/er.md` の塀を 1 つ壊します | 赤 | **赤（1 例）** |
| N18 | README の API 一覧から `GET /up` を落とします | 赤 | **緑のまま（検出できません）** |

**足した 2 つの検めは、実際に効きます。** 図から落としても（N1 ・ N3）、図へ足しても（N4）、実装を変えても（N12 ・ N13）、字下げを崩しても（N14）赤くなります。**提案 1 の直しも効きます。** 1 回目に生き残った 2 種（N6 ・ N7）が、どちらも捕まるようになりました。

**1 回目に赤かった 4 種（N15 ・ N16 ・ N17 と画面の両方向）は、いまも赤いままです。壊していません。**

**残る穴は 4 つです。** いずれも**次の issue で構いません。**

- **N5：クォータの図は、実装に無い遷移を足しても通ります。** この検めだけが片道（`missing` しか見ません）だからです。`confirmed --> refunded` の 1 本を通すための割り切りですが、**その 1 本を除いた両方向の照合にできます。**
- **N8 ・ N9：経路の検めが、名前空間を見ていません。** `implementedEndpoints()` は `routes.rb` 全体を読みますので、**管理画面の資源（`admin/users` ・ `admin/rule-dictionaries`）が、利用者向けの経路として通ります。** 1 回目の指摘（`GET /api/v1/dashboard`）と同じ種類の穴が、形を変えて残っています。照合が `path.endsWith(...)`（`docs.test.mjs:130`）で、前の部分を見ないためです。
- **N10 ・ N18：経路は片道の検めです。** 画面は両方向（書きすぎ・書き漏らし）を見ますが、**経路は「書いたものが実在するか」しか見ません。** 実装した経路を README から落としても緑です。

### 新しく壊れた箇所

- **新規 1：`SPEC/class.md:18-20` の `LlmRefiner` に、実装に無いメソッドを書いています。**

  ```
  class LlmRefiner {
      +call(draft) Draft
  }
  ```

  **実装に `call` はありません。** `src/backend/app/services/generation/llm_refiner.rb` の公開の入口は `refine(draft)` です（同 `58` 行目）。`def call` は同ファイルに 0 件で、呼び出し側も `degraded_composer.rb:50` の `refiner.refine(draft)` です。

  **1 回目の指摘（`+refine(素材) 素材`）は、実は正しい記載でした。** それを直しに行って、**正しかったものを誤りへ変えています。**

  **同じ文書の中で食い違ってもいます。** `SPEC/class.md:88-90`（「LLM の呼び出し」の図）は `LlmRefiner { +refine(素材) }` のままです。**1 つの文書が、同じクラスの入口を 2 通りに書いています。** issue #78 の受け入れ条件「実装済みのものだけを書きます」に反します。**`+refine(draft) Draft` へ戻してください。**

- **新規 2：`SPEC/README.md` の「requirements.md との差」へ足した 2 行が、表として描かれません。**

  `SPEC/README.md:41` が表の最後の行、**`42` が空行**、`43` ・ `44` が足した 2 行です。**空行が入ると、そこで表が終わります。** 続く 2 行には見出し行も区切り行もありませんので、GitHub Flavored Markdown では表になりません。

  **GitHub の描画で確かめました**（`gh api -X POST /markdown`）。足した 2 行は `<table>` に入らず、**`<p>` の中に `|` を含んだ地の文として出ます。**

  ```
  <p>| 6「フォロー直後の判定漏れに対して、<strong>利用者の操作による…</strong>」 | … |<br>
  | 7.3「LLM ジョブの失敗率・…」 | … |</p>
  ```

  **重大 5 で足していただいた、いちばん大事な 2 行が、崩れて出ます。** `DOCS/CRAP.md` の整列・反復の観点にも反します。**42 行目の空行を 1 つ取れば直ります。**

- **新規 3：`SPEC/sequence.md:72` に足した一文が、半分だけ正しいです。**

  「**差し戻しは、そのどちらよりも先に判定します。** ですので、差し戻しは枠を使いません。」と書いています。**「そのどちら」は、直前の文の「お申し込みの行を作ること」と「枠を予約すること」です。**

  **実装は `prompt_requests/acceptance.rb:53-59` です。**

  ```
  request = draft(project, normalized)   # 3. 行を作ります
  ensure_allowed!(request, normalized)   # 4. 禁止入力を調べます
  enqueue(request)                       # 5. 枠を予約します
  ```

  **差し戻しの判定は、行を作った「あと」です。** 枠の予約より先であることは、そのとおりです。**同ファイル `11-16` のコメントも「3. 記録を作ります（`draft`）… 4. 禁止入力を調べます … 5. 枠を予約します」と、その順序を明示しています。**

  図の側も同じ読み違いをしています。`SPEC/sequence.md:62` は、`draft` を作る手順を **`else 見つかりません` の枝の中**へ置いています。実装では、`draft` の行は**枝へ入る前**に作られます。差し戻しの枝が「差し戻しとして記録します」と書くのは、**すでにある `draft` の行を `rejected` へ進めること**です（`acceptance.rb:93-95` の `transition_to!('rejected', …)`）。

  **「差し戻しは、枠の予約より先に判定します」と書けば、実装と一致します。** 図は、`draft` を作る手順を `alt` の外へ出していただくのが正確です。

- **新規 4：`SPEC/state.md:55` が、非公開の手続きを `#` で引いています。**

  「返還した枠は、その日のうちにもう一度使えます（`Quota::Reservation#reclaim!`）」と書いていますが、**`reclaim!` は `class << self` の中にあり、しかも `private` の下です**（`quota/reservation.rb:26` ・ `133` ・ `142`）。外から呼べません。

  **重大 1 で「公開されていない `Delivery.store` を書かない」と直したのと、同じ種類の記載です。** 公開の入口は `Quota::Reservation.reserve!` です。**`reserve!` を引くか、「`reclaim!` という内側の手続きが行います」と地の文で書くかにしてください。**

- **軽微：`SPEC/class.md` の `DegradedComposer` ・ `PromptGenerationService` の入口が、呼ばれ方とずれています。**

  - `DegradedComposer` は `+degraded?(draft) bool` だけになりました。**`degraded?` は `self.` の付いたクラスの手続きです**（`degraded_composer.rb:38`）。`PromptGenerationService --> DegradedComposer` の矢印が指す実際の呼び出しは `composer.compose(...)` です（`prompt_generation_service.rb:169`）。**`+compose(draft) Draft` を残したうえで `degraded?` を足すのが正確です。**
  - `PromptGenerationService` は `+call() PromptPackage[]` になりました。実装は `call(raw)` です（同 `92`）。**引数が消えています。**
  - `AdminCredentials` は、実装では `Admin::Credentials` です（`app/services/admin/credentials.rb:15`）。**図の名前とクラスの名前が違います。**

  **いずれも次の issue で構いません。**

### なお挙がっていない差（`SPEC/README.md`）

**`requirements.md` を 1 節ずつ通して突き合わせました。** 表に無い差が、なお 2 件あります。

| `requirements.md` | いまの実装 |
|---|---|
| 6「フォロワーIDリストをサーバー側に**キャッシュ**し、認証で取得したユーザーIDと照合して判定する。**初回のみ全件取得し、以降は差分取得**とする」／ 8 の ER 図の **`follower_checks` 表**（`is_follower` ・ `checked_at` ・ `cooldown_until`）／ 11 のクラス図の **`FollowerCacheService`**（`syncDiff()` ・ `isFollower()`） | **どれも実装にありません。** 判定は外部の `x-follower-gate` へ委ね、ログインのたびに問い合わせます（`auth_controller.rb:52` → `plan_updater.rb:36`）。保持するのは `users.plan` ・ `users.plan_checked_at` ・ `users.recheck_available_at` の 3 列だけです（`db/schema.rb:295-303`）。**`follower_checks` 表は `schema.rb` に存在しません。** これは CLAUDE.md の「フォロワー判定を新規実装しません」に沿った正しい判断ですが、**`requirements.md` との差であることに変わりはありません。** 7.3 の「X API のエラー率を監視し、フォロワーキャッシュの差分取得失敗を検知する」も、同じ理由で当てはまりません |
| 4.4 の最終項「クォータはプラン値と独立した仕組みとし、将来のプラン拡張（回数の異なるプランの追加）時に**クォータ上限をプラン値から導出できる構造とする**」 | **導出の仕組みがありません。** 上限 1 回は `quota_consumptions` の `(user_id, quota_day)` の一意索引に焼き込まれています（`db/schema.rb:107`）。`app/services/quota/` に上限を表す定数も、プラン値から回数を引く入口もありません（`DAILY_LIMIT` ・ `limit_for` の類は 0 件です） |

**「見落としがあれば、見つけた時点でこの表へ足します」と断ってありますので、断言の問題は解けています。** この 2 件は、**次の issue で足していただければ十分です。**

### 各基準への適合

| 基準 | 判定 | 根拠 |
|---|---|---|
| CC | **適合** | 権利物の持ち込み・第三者の表示物がありません。連絡先は所定のもの 1 つです |
| OWASP10 | **適合** | 秘匿値・本番の住所・内部の作りの露出がありません。足した検めは読み取りだけで、外部へ通信しません |
| QC10 | **おおむね適合** | **記述の正しさは、1 回目から大きく上がりました。** 重大 1 〜 5 の指摘 12 点のうち 11 点が実装と一致します。**残るのは、直しの過程で入った 4 か所です**（新規 1 〜 4）。**検めが記述の正しさを支える範囲も、状態遷移図と経路まで広がりました** |
| CRAP | **満たしません（1 か所）** | `SPEC/README.md:42` の空行で表が割れ、**足した 2 行が地の文として崩れて出ます**（新規 2）。整列・反復の観点に反します。ほかの 7 文書は揃っています |
| DP | **適合** | 検めは既存の `node --test` に乗せたままです。新しい仕組みを作っていません。遷移の定めを実装から読む作りは、書き写しを避ける点で DRY に沿います |
| TM | **適合** | **1 回目の弱点（経路の検め）を塞ぎ、図と実装を突き合わせる例を 2 つ足しました。** 18 種の変異のうち 14 種が赤くなります。**届かない 1 本（`confirmed --> refunded`）を、隠さず記録に残した点も評価できます。** 残る 4 つの穴は、次の issue で構いません |

### プロジェクト固有の決まり

| 決まり | 判定 |
|---|---|
| 実装が `src/` の下にあること | **該当しません。** `src/` を 1 文字も変えていません |
| 開発用の道具が `src` の外にあること | **満たします**（`test/docs/`）。`test/README.md` に、置き場の理由も書かれました |
| フォールバックが無いこと・例外処理があること | **満たします。** 足した検めは、読めなければその場で落ちます。既定値へ寄せる書き方はありません |
| デバッグトレースが追えること | **満たします。** 足した 2 つの検めは、食い違った遷移を名指しで出します |
| 制御構文以外がクラスまたは関数に入ること | **満たします**（`implementedEndpoints()` ・ `implementedTransitions()` ・ `documentedTransitions()` を新設しています） |
| グローバル変数が無いこと | **満たします。** 追加も `const`（`ACTIONS`）です |
| 文字列リテラルの分離とハードコードの検査 | **満たします。** `check:hardcode` は自動検査で緑です |
| ネイティブの `alert()` 等を使わないこと | **満たします** |
| 環境の判定と本番での近道の遮断 | **満たします**（README の記述は 1 回目から変わっていません） |
| 車輪の再発明をしないこと | **満たします。** Node 標準の `node:test` と `node:fs` だけです |
| フォロワー判定を自前で作らないこと | **満たします。** 図はいずれも `x-follower-gate` へ問い合わせる形です |
| 秘匿値を `.env` から読むこと | **満たします。** 文書には名前だけを書いています |
| ですます調 | **満たします** |
| 日本語版のみ・多言語化なし | **満たします** |
| 絵文字なし | **満たします** |
| JST ・ UTF-8 | **満たします** |

### 後片づけ

**削除は行っていません。** 手動で片づけていただく必要があるものは、次のとおりです。

- レビュー用の作業ツリー `DELETE/review-worktrees/pr185-20260827-reviewer2`
- 変異試験に使った写し `/mnt/c/tmp/pr185review2`（Windows からは `C:\tmp\pr185review2` です）。**開発機に Linux 側の Node がありませんので、Windows 側の Node で流すために、README ・ `SPEC/*.md` ・ `test/docs/` ・ `src/frontend/src/app` ・ `routes.rb` ・ `prompt_request.rb` ・ `quota_consumption.rb` だけを写しました。** 秘匿値は含みません
- 1 回目の写し `C:\tmp\pr185review` も、まだ残っています

**実装側の作業ツリー `DELETE/impl-78` は、今回も読み取りだけに留めました。** 1 ファイルも変えていません。

## 判定

条件付き合格

**重大 1 〜 5 は、5 件とも直っています。** 指摘した 12 点を 1 つずつ実装へ当て直し、**11 点が一致しました。** `PromptPackage` の項目・`Formatted` の新設・継承の親子・`StyleSpec.apply` ・ `Draft.add` / `replace` ・ `Delivery.call` ・ `Reservation.settle!` は、いずれも実装のとおりです。UC12 は実際の挙動（ログインのたびに `plan_updater.refresh` が走ります）に沿う書き方になり、順序の食い違い（`SPEC/sequence.md` ・ `SPEC/er.md`）も解け、クォータの図には実装済みの 2 本が入り、断言の 1 行は外れました。

**提案 1 と 2 の対応も、実際に効きます。** 変異を 18 種入れて流し、**1 回目に生き残った 2 種（`DELETE /api/v1/projects/:id` ・ `GET /api/v1/dashboard`）が、どちらも捕まる**ことを実測しました。足した 2 つの検めは、図から落としても・図へ足しても・実装を変えても・字下げを崩しても赤くなります。**届かない 1 本を隠さず記録へ残した姿勢も、そのまま続けていただきたいと思います。**

**取り込みの条件として、2 か所だけ直してください。** どちらも 1 行です。

1. **`SPEC/README.md:42` の空行を 1 つ取ってください。** 空行が表を割っており、**重大 5 で足していただいた 2 行が、GitHub では `|` を含んだ地の文として崩れて出ます。** GitHub の描画で実測しました。いちばん大事な 2 行ですので、このままにはできません。
2. **`SPEC/class.md:19` の `+call(draft) Draft` を `+refine(draft) Draft` へ戻してください。** `Generation::LlmRefiner` に `call` はありません（`llm_refiner.rb:58` は `refine`）。**同じ文書の 88-90 行目は `refine` のままで、1 つの文書が同じクラスの入口を 2 通りに書いています。** 1 回目に正しかった記載を、直しに行って誤りへ変えてしまった箇所です。

**この 2 行が直れば、取り込んでいただいて差し支えありません。** `src/` を 1 文字も変えない PR で、自動検査は 6 ジョブすべて緑、`mergeable` は `CLEAN` です。秘匿情報の露出もありません。

**次の issue へ回していただきたいものは、次の 4 件です。取り込みを止めるものではありません。**

- **記述の細部**：`SPEC/sequence.md:72` の「差し戻しは、そのどちらよりも先に判定します」（実装では、差し戻しの判定は**行を作ったあと・枠の予約より前**です）。`SPEC/state.md:55` が非公開の `Quota::Reservation#reclaim!` を引いていること。`SPEC/class.md` の `DegradedComposer` ・ `PromptGenerationService.call()` ・ `AdminCredentials` の名前。
- **検めの残る穴 4 つ**：クォータの図へ実装に無い遷移を足しても通ること（片道の照合のため）。経路の検めが名前空間を見ず、**管理画面の資源（`GET /api/v1/users` ・ `POST /api/v1/rule_dictionaries`）が利用者向けの経路として通ること**。経路が片道で、実装した経路を README から落としても通ること。
- **差の一覧の追記 2 件**：`follower_checks` 表とフォロワーキャッシュの差分取得（`requirements.md` 6 ・ 8 ・ 11。`x-follower-gate` へ委ねた結果です）。クォータ上限をプラン値から導出する構造（同 4.4）。
- **1 回目の提案 6**：README の管理画面の表は「主なページです」と断り書きが入りましたので、**解けています。** 提案 7 も `test/README.md` に理由が書かれ、**置き場の決まりを実態へ合わせる issue**を立てるだけになりました。

**土台について 1 点だけ添えます。** このブランチは `main` より 1 コミット（`5841c48` 1 回目のレビュー記録）遅れています。文書だけの記録で、`mergeStateStatus` は `CLEAN` ですので、取り込みの妨げにはなりません。

連絡先は info@rictaworks.jp です。

# reviewer 記録 : PR #185（issue #78 ・ #79 README と SPEC を、実装から起こします）

- 実施日時（JST）：2026-08-27 22:32
- 対象：1 回目 `2bc7219`（土台は `bf4cb46`。`main` と同じ位置で、遅れはありません）
- 作業ツリー：`DELETE/review-worktrees/pr185-20260827-reviewer`（`git worktree add --detach` 済みです）
- 実装側の作業ツリー `DELETE/impl-78` は読み取りだけに留めました
- 検証用のデータベース：使いません（`src/` を 1 文字も変えていないため、RSpec の全例は流していません）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 土台 | **`main`（`bf4cb46`）を取り込み済みです。** `git rev-list --left-right --count origin/main...HEAD` は `0 2` で、**遅れ 0 ・ 先行 2** です |
| PR の差分 | **2 コミット・12 ファイル**（1073 行の追加、10 行の削除） |
| `src/` の変更 | **1 文字もありません**（`git diff --name-only origin/main...HEAD -- src requirements.md app-ui` が 0 件です） |
| `requirements.md` ・ `app-ui/` | **1 文字も変わっていません** |
| 自動検査 | **6 ジョブすべて pass**（実行 `33076484284`） |
| 文書の検査（`test:docs`） | **5 例、失敗 0 件**（この作業ツリーで流し直しました） |
| セキュリティレビュー記録の指紋 | **2 つとも一致します**（`4bc2cbad60cbe35a` ・ `b4d90746ac3afb6e` を差分から再計算しました） |
| 絵文字 | **ありません**（README ・ SPEC 7 ファイル ・ テストを走査しました） |
| ですます調 | **満たします。** だ・である調の混入はありません |
| 秘匿値・本番の住所 | **ありません**（後述） |
| ファイルのエンコード・改行 | **9 ファイルとも UTF-8 ・ LF です** |
| 図が描けること | PR 本文の「6 図すべて成功」は、Mermaid CLI が手元に無いため再現していません。**GitHub の描画に必要なコード塀の対応は、検査が押さえています** |

### issue #79（README にページ一覧と API 一覧を書く）の受け入れ条件

| # | 受け入れ条件 | 判定 | 実測 |
|---|---|---|---|
| 1 | ページ名と URL がリンクで書かれています | **満たします** | 10 行の表があり、10 件ともリンクです。`src/frontend/src/app` の下の `page.tsx` は 10 件で、**過不足がありません** |
| 2 | API のタイトルとエンドポイント URL、`SPEC/api` へのリンクが書かれています | **満たします** | 14 行すべてにタイトル・経路・`SPEC/api/README.md` への案内があります。**14 件とも `SPEC/api/README.md` に契約が実在します**（1 件ずつ突き合わせました） |
| 3 | 未実装のものを書きません | **満たします** | 10 画面・14 経路・ログインと死活監視の 4 経路・管理画面の 4 パスを `src/frontend/src/app` と `src/backend/config/routes.rb` へ 1 件ずつ当てました。**実装に無いものはありません** |

**README の記述は、実装と細部まで合っています。** 次の 4 点を実装で確かめました。

| README の記述 | 実装 | 判定 |
|---|---|---|
| 自動ログインは `APP_ENV` が `development` ・ `test` で、その利用者が居るときだけ効きます | `app/controllers/concerns/authenticates_user.rb:64-70`（`AppEnvironment.developer_shortcuts_allowed?` → 環境変数 → `User.find_by`） | **一致します** |
| 本番では、値が設定されていても無視します | 同 65 行目で、条件が偽なら環境変数を読む前に `nil` を返します | **一致します** |
| `/health` は、データベースへ到達できることまで含めて答えます | `app/controllers/health_controller.rb`（`SELECT 1` を投げ、届かなければ 503） | **一致します** |
| 画面からの呼び出しは、画面の中の中継を通ります | `src/frontend/next.config.ts` ＋ `src/config/backend.ts`（`BACKEND_INTERNAL_URL` はサーバー側だけが読みます。`NEXT_PUBLIC_` ではありません） | **一致します** |

### issue #78（SPEC にリバースエンジニアリング図を作る）の受け入れ条件

| # | 受け入れ条件 | 判定 |
|---|---|---|
| 1 | 実装済みのものだけを書きます | **満たしません**（重大 1 ・ 2 ・ 3 ・ 4） |
| 2 | 図は Mermaid で書きます | **満たします**（6 文書とも Mermaid です。塀の対応は検査が押さえます） |
| 3 | requirements.md と食い違う場合は差分を明記します | **満たしません**（重大 5。書かれた 2 点は正確ですが、書き漏らしが 2 件あります） |

### 図と実装の突き合わせ

#### `SPEC/state.md` × `PromptRequest::TRANSITIONS` — **一致します**

`src/backend/app/models/prompt_request.rb:36-45` の 8 状態を 1 本ずつ当てました。

| 実装の遷移 | 図 | 判定 |
|---|---|---|
| `draft → queued` ・ `draft → rejected` | あります | 一致します |
| `queued → generating` | あります | 一致します |
| `generating → completed` ・ `degraded_completed` ・ `failed` | 3 本ともあります | 一致します |
| `failed → queued` | あります | 一致します |
| `completed → archived` ・ `degraded_completed → archived` | 2 本ともあります | 一致します |
| `rejected → []` ・ `archived → []` | 終端として描いています | 一致します |

**過不足はありません。** 枠の扱いの表（`rejected` は予約しない・`failed` は返す）も、`Quota::Settlement.next_status_for` ・ `PromptRequests::Acceptance` の実装どおりです。

#### `SPEC/er.md` × `db/schema.rb` — **表と列は一致します。読み取りの要点に 1 か所の誤りがあります**

- **表は 11 件で過不足がありません。** `solid_queue_*` ・ `solid_cache_entries` を除く業務の表と 1 対 1 で対応します。
- **列も 11 表すべてを 1 つずつ当てました。** 型・注記・一意（`users.x_user_id` ・ `evaluation_notes.prompt_output_id` ・ `rule_dictionaries.version`）まで合っています。
- **結び付きも外部キーと一致します。** `prompt_requests |o--o| quota_consumptions` を任意にしている点、`rule_dictionaries` ・ `metric_events` が外部キーを持たない点も、`schema.rb` の `add_foreign_key` の並びどおりです。
- **ただし「読み取りの要点」の 1 文が実装と逆です**（重大 3）。

#### `SPEC/class.md` × 実装 — **食い違いが複数あります**（重大 1）

#### `SPEC/sequence.md` × 実装 — **図 2 の順序が実装と逆です**（重大 3）。ほかの 5 本は一致します

#### `SPEC/dfd.md` × 実装 — **一致します**

「外へ出す情報」の表を 1 行ずつ当てました。reCAPTCHA へ**要求元のアドレスを送らない**ことは `bot_protection/recaptcha_verifier.rb:76`（引数そのものを置いていません）で、`sessions` が識別子のハッシュだけを持つことは `schema.rb` の `token_digest` で、`metric_events` が誰の分かを持たないことは `app/models/metric_event.rb` で確かめました。**すべて記述どおりです。**

#### `SPEC/usecase.md` × 実装 — **利用者の操作が 1 件、実装にありません**（重大 2）

### 変異試験（文書の検査が実際に効くことの確認）

**11 種を、この作業ツリーの写しへ 1 か所ずつ変異を入れて流しました**（`node --test test/docs/docs.test.mjs`）。

| # | 変異 | 結果 |
|---|---|---|
| M0 | 変異なし（基準） | **緑**（5 例、失敗 0 件） |
| M1 | 実在しない画面（`/dashboard`）を README のページ一覧へ足します | **赤（1 例）** |
| M2 | 実装済みの画面（`/presets`）を README から落とします | **赤（1 例）** |
| M3 | `SPEC/README.md` の索引から `state.md` を落とします | **赤（1 例）** |
| M4 | `SPEC/er.md` の Mermaid の塀を閉じ忘れます | **赤（1 例）** |
| M5 | **実装に無い動詞で経路を足します**（`DELETE /api/v1/projects/:id`） | **緑のまま（検出できません）** |
| M6 | **実装に無い経路を足します**（`GET /api/v1/dashboard`） | **緑のまま（検出できません）** |
| M7 | 実装に無い画面へ書き換えます（`/projects` → `/projects/list`） | **赤（2 例）** |
| M8 | 実装に無い経路を足します（`POST /api/v1/prompt_requests/:id/regenerate`） | **赤（1 例）** |
| M9 | `SPEC/` へ文書を足し、索引へ載せません | **赤（1 例）** |
| M10 | 実装に無い経路を足します（`POST /api/v1/prompt_outputs/:id/copy`） | **赤（1 例）** |

**5 つの検めのうち 4 つは、しっかり効きます。** 画面の両方向（書きすぎ・書き漏らし）と、索引・塀の押さえは、変異を入れると必ず赤くなります。

**経路の検めだけ、穴が 2 つあります**（提案 1）。`test/docs/docs.test.mjs:88-98` は、**経路の末尾の語が `routes.rb` の本文に現れるか、同じ名前の控えのファイルがあるか**しか見ません。

- **動詞を見ていません。** `DELETE /api/v1/projects/:id` は、`routes.rb` が `only: %i[index create update]` と明示して**作っていない**経路ですが、末尾の語が `projects` であるため通ります。
- **`routes.rb` のどこかに現れる語を使えば通ります。** `GET /api/v1/dashboard` は、管理画面の `root to: 'dashboard#show'` の語に当たって通ります。**利用者向けの API として実在しないのに、緑のままです。**

### 秘匿情報の露出

| 観点 | 方法 | 結果 |
|---|---|---|
| 本番の住所 | 全文検索（`https?://`） | **ありません。** README にあるのは `http://localhost:3300` だけです |
| バックエンドのドメイン | 目視 ・ 全文検索 | **ありません。** 「画面の中の中継を通ります」と書くに留めています |
| 管理画面の住所 | 目視 | **パスだけです**（`/admin` ほか）。本番の住所は書いていません |
| 環境変数の値 | 目視 | **名前だけです**（`DEVELOPMENT_AUTO_LOGIN_X_USER_ID` ・ `BACKEND_INTERNAL_URL`）。値は 1 つも書いていません |
| `.claude/` の内容 | 全文検索 | **1 文字も転記されていません** |
| 個人名 | 目視 | **ありません。** 連絡先は `info@rictaworks.jp` です |
| Railway ・ Vercel の実際の住所 | 全文検索 | **ありません** |

**この観点では、指摘はありません。**

### 各基準への適合

| 基準 | 判定 | 根拠 |
|---|---|---|
| CC | **適合** | 権利物の持ち込み・第三者の表示物がありません。連絡先は所定のもの 1 つです |
| OWASP10 | **適合** | 秘匿値・本番の住所・内部の作りの露出がありません。検査は読み取りだけで、外部へ通信しません |
| QC10 | **満たしません** | **記述の正しさの観点で、実装と食い違う記載が残っています**（重大 1 〜 5）。**検査が緑であることが、記述の正しさを保証していません** |
| CRAP | **適合** | 見出し・表・強調の使い方が 7 文書で揃っています。近接・反復とも保たれています |
| DP | **適合** | 検査は既存の `node --test` に乗せ、新しい仕組みを作っていません。既存ジョブへ 1 手順だけ足す判断も、YAGNI ・ KISS に沿います |
| TM | **おおむね適合** | 文書に対する自動の検めを新設した点は評価できます。**ただし経路の検めが弱く、変異が 2 種生き残ります**（提案 1）。図と実装の突き合わせを見る例はありません（提案 2） |

### プロジェクト固有の決まり

| 決まり | 判定 |
|---|---|
| 実装が `src/` の下にあること | **該当しません。** `src/` を 1 文字も変えていません |
| 開発用の道具が `src` の外にあること | **満たします**（`test/docs/` ・ `.github/`） |
| フォールバックが無いこと・例外処理があること | **満たします。** 検査は読めなければその場で落ちます。既定値へ寄せる書き方はありません |
| デバッグトレースが追えること | **満たします。** 失敗した文言に、どの画面・どの文書が原因かを名指しで出します |
| 制御構文以外がクラスまたは関数に入ること | **満たします**（`read()` ・ `documentedPages()` ・ `implementedPages()` ・ `documentedEndpoints()`） |
| グローバル変数が無いこと | **満たします。** 定数 2 つ（`HERE` ・ `ROOT`）は `const` で、書き換えません |
| 文字列リテラルの分離とハードコードの検査 | **満たします。** `check:hardcode` は自動検査で緑です |
| ネイティブの `alert()` 等を使わないこと | **満たします** |
| 環境の判定と本番での近道の遮断 | **満たします。** README が近道の条件と「本番では効かないこと」を明記し、実装と一致します |
| 車輪の再発明をしないこと | **満たします。** Node 標準の `node:test` と `node:fs` だけです |
| フォロワー判定を自前で作らないこと | **満たします。** 図はいずれも `x-follower-gate` へ問い合わせる形で描いています |
| 秘匿値を `.env` から読むこと | **満たします。** 文書には名前だけを書いています |
| ですます調 | **満たします** |
| 日本語版のみ・多言語化なし | **満たします** |
| 絵文字なし | **満たします** |
| JST ・ UTF-8 | **満たします** |

## 指摘

### 重大（取り込みの前に直していただきたいもの）

- **重大 1：`SPEC/class.md` に、実装に無いクラスの中身と、実装に無いメソッドが並んでいます。**

  issue #78 の受け入れ条件「実装済みのものだけを書きます」に反します。1 つずつ挙げます。

  | 図の記載 | 実装 | 場所 |
  |---|---|---|
  | `PromptPackage` の項目は `main_prompt` ・ `negative_prompt` ・ `parameters` ・ `art_direction_note` です | **`Generation::PromptPackage` の項目は `variation` ・ `formatted` ・ `note` ・ `draft` です。** 4 つとも図に載っていません | `SPEC/class.md:39-44` ／ `src/backend/app/services/generation/prompt_package.rb:10` |
  | `ModelAdapter` の `format(素材)` は `PromptPackage` を返します（`ModelAdapter --> PromptPackage`） | **`Adapters::ModelAdapter#format(draft)` が返すのは `Adapters::Formatted` です。** `PromptPackage` を作るのは `PromptGenerationService#packaged` です。**`Formatted` は図に 1 つも出てきません** | `SPEC/class.md:26` ・ `58` ／ `model_adapter.rb:96` ・ `120`、`adapters/formatted.rb:18`、`prompt_generation_service.rb:183` |
  | `StyleSpec` に `decide(inputs, dictionary)` があります | **`decide` はありません。** 公開の入口は `apply(draft)` です（`decide` は別物件の `people.decide` です） | `SPEC/class.md:12-14` ／ `style_spec.rb:101` ・ `150` |
  | `Draft` に `build(仕様)` があります | **`build` はありません。** `initialize` ・ `add` ・ `replace` です（`build` を持つのは別の `VariationBuilder` です） | `SPEC/class.md:15-17` ／ `draft.rb:30` ・ `53` ・ `63` |
  | `Delivery` に `+store(request, packages)` があります | **公開の入口は `call(packages)` です**（`initialize(prompt_request)`）。`store` は**非公開**で、引数は `package` 1 つです | `SPEC/class.md:182` ／ `prompt_requests/delivery.rb:16` ・ `22` ・ `60` |
  | `Reservation` に `+refund!(prompt_request)` があります | **`refund!` はありません。** 返還は `settle!` が `Settlement.next_status_for` の答えに従って行います | `SPEC/class.md:151` ／ `quota/reservation.rb:31` ・ `49`（`def refund!` は全文検索で 0 件です） |
  | `NarrativeAdapter` は `<<mixin>>` で、`DalleAdapter` ・ `NanoBananaAdapter` が**依存**します。`DalleAdapter` は `ModelAdapter` を**直接**継承します | **`NarrativeAdapter` はクラスで、`ModelAdapter` を継承します。** `DalleAdapter` ・ `NanoBananaAdapter` は **`NarrativeAdapter` の子**です。`ModelAdapter` を直接継承するのは `MidjourneyAdapter` ・ `StableDiffusionAdapter` だけです | `SPEC/class.md:32-35`（`<<mixin>>`）・ `52`（`ModelAdapter <|-- DalleAdapter`）・ `54`（同 `NanoBananaAdapter`） ／ `narrative_adapter.rb:45`、`dalle_adapter.rb:22`、`nano_banana_adapter.rb:14`、`midjourney_adapter.rb:15`、`stable_diffusion_adapter.rb:18` |

  **直し方は 2 通りあります。** 実装の名前と項目へ合わせるか、図から落とすかです。とくに `PromptPackage` は、**名前は実在するのに中身が別物**ですので、読んだ方が `package.main_prompt` を書いて動かないところまで進みます。`Formatted` を図へ足し、`PromptPackage --> Formatted` の関係を描くのが素直だと思います。

- **重大 2：`SPEC/usecase.md` の UC12「判定を取り直します」は、利用者の操作としては実装されていません。**

  `SPEC/usecase.md:25`（`UC12["判定を取り直します"]`）と `46`（`利用者 --> UC12`）です。

  **実装を全文検索しました。** `Auth::RecheckService` を呼ぶ場所は `src/backend/app/controllers/admin/users_controller.rb:33` の 1 か所だけです。`src/backend/config/routes.rb` の `namespace :api do namespace :v1 do` の中に、再判定の経路はありません。画面側（`src/frontend/src`）にも、再判定を押す導線はありません（`pending` を扱う表示は `useRequest.ts` の生成の状態だけで、プラン値ではありません）。

  **同じ操作は UC22「判定を取り直します（代行）」として、管理者側にもう 1 つ挙がっています。** 実装があるのは、こちらだけです。

  **この文書は末尾で「この図に無い操作は、実装していません」と書いています。** 裏返して読むと「図にある操作は実装されています」と受け取られます。**利用者の欄に、無い操作が 1 つ載っている状態です。** UC12 を落とすか、「ログインをし直すと判定を取り直します」という実際の挙動（`auth_controller.rb:52` が毎回のログインで `plan_updater.refresh` を呼びます）に書き改めてください。

- **重大 3：枠の予約と、お申し込みの行を作る順序が、実装と逆に書かれています。**

  実装は `src/backend/app/services/prompt_requests/acceptance.rb:56-63` です。**`draft(project, normalized)` で行を作ってから、`ensure_allowed!` を通り、`enqueue` の中の `Quota::Reservation.reserve!(user:, prompt_request: request)` で予約します**（同 123 行目）。冒頭のコメント（同 11-16 行目）も「3. 記録を作ります（`draft`）… 5. 枠を予約します」と、その順序を明示しています。

  | 文書 | 記載 | 判定 |
  |---|---|---|
  | `SPEC/sequence.md:62-63` | 「本日の枠を予約します」→「お申し込みを作ります」 | **逆です** |
  | `SPEC/er.md:124` | 「枠の予約は、お申し込みの行を作る前に行うためです」 | **逆です** |
  | `SPEC/state.md:26`（表） | 「`draft`｜受け付けた直後です｜まだ予約しません」 | **正しいです** |

  **SPEC の中で食い違っています。** `quota_consumptions.prompt_request_id` が空を許すのは、`Quota::Reservation.reserve!` が `prompt_request:` を省略して呼べる形だから（同 32 行目の既定値 `nil`）であって、受付の経路が行より先に予約するからではありません。**理由の説明ごと直してください。**

- **重大 4：`SPEC/state.md` のクォータの図に、実装済みの遷移が 2 本ありません。**

  `SPEC/state.md:44-48` は `reserved → confirmed` ・ `reserved → refunded` の 2 本だけを描き、`confirmed` ・ `refunded` をどちらも終端にしています。**実装はそうなっていません。**

  | 実装の遷移 | 実装の場所 | 図 |
  |---|---|---|
  | `refunded → reserved` | `src/backend/app/models/quota_consumption.rb:32`（`'refunded' => %w[reserved]`）。**実際に通ります**：`quota/reservation.rb:125` で状態を見て、`142-146` の `reclaim!` が `transition_to!('reserved', …)` を呼びます | **ありません** |
  | `confirmed → refunded`（管理者の手動リセット） | `quota_consumption.rb:79`（`reset_by_admin!`）。`admin/users_controller.rb:52` は**予約中の枠を弾きます**ので、リセットが当たるのは主に `confirmed` の行です | **ありません** |

  **この 2 本は、まさにこの節が本文で説明している機能です。** 本文は「管理者は手動でリセットできます（`reset_by_admin`）」と書いていますが、**図では `confirmed` から出る線がありません。** 「返還後、当日中に作り直せます」（`quota_consumption.rb:22` のコメント）も図から読み取れません。**本文と図が食い違っています。**

  なお `reset_by_admin!` は `TRANSITIONS` を通らない専用の入口です（同 70-78 行目のコメント）。**その旨も添えて描いていただけると、読んだ方が `transition_to!` で同じことをしようとして失敗せずに済みます。**

- **重大 5：`SPEC/README.md` の「requirements.md との差」に、書き漏らしが 2 件あります。**

  **書かれた 2 点は、どちらも正確です。** 実装で確かめました。

  | 書かれた差 | 実測 |
  |---|---|
  | LangSmith の追跡を有効にしていません | `Generation::LangchainGemini` は `Langchain::LLM::GoogleGemini` を継ぎ、上書きするのは呼び出し先・鍵・待ち時間だけです。追跡の設定は全文検索で 0 件です。**記載どおりです** |
  | 測定軸 8 つのうち 6 つです | `Metrics::UsageSummary#call` が返すのは `requests` ・ `degraded` ・ `evaluation` ・ `regeneration` ・ `quota` で、`MetricEvent::AXES` は `quota_exhausted` ・ `quota_reclaimed` の 2 つです。**合わせて 6 軸**で、「コピーされた案の分布」と「稼働率」はありません。**記載どおりです** |

  **問題は、その次の 1 行です。** 「**上の 2 点以外に、仕様と食い違う実装はありません。**」と断言しています。**次の 2 件が漏れています。**

  | requirements.md | いまの実装 |
  |---|---|
  | 6「フォロー直後の判定漏れに対して、**ユーザー操作による手動再判定の導線を設ける**。再判定にはクールダウン（レート制限保護）を必須とする」 | **利用者向けの導線がありません。** `Auth::RecheckService`（クールダウン 5 分つき）は実装済みですが、**呼ぶのは管理画面だけ**です（`admin/users_controller.rb:33`）。利用者向けの経路も画面も存在しません。**重大 2 と同じ穴です** |
  | 7.3「LLM ジョブの失敗率・縮退モード発生を**アラート対象とする**」「X API のエラー率を監視し…」「**通知先を定義**し…」 | **通知・アラートの実装がありません**（`alert` ・ `通知` ・ `webhook` ・ `slack` を `src/backend/app` ・ `src/backend/config` で全文検索して 0 件です）。`/health` の応答までは実装済みです |

  **差を書き漏らすと、実装が仕様に届いているように読めます。** この節はまさに、そうならないために置いた節です。**2 件を足すか、「差の洗い出しはこの 2 点に絞りました」と範囲を明示するか**を選んでください。断言の 1 行は、そのままでは残せません。

### 提案（次の issue へ回してよいもの）

- **提案 1：経路の検めが、動詞と語の当てずっぽうを通します。** `test/docs/docs.test.mjs:88-98` は末尾の語しか見ません。実測で、**実装が明示的に作っていない `DELETE /api/v1/projects/:id`（`only: %i[index create update]`）と、利用者向けには存在しない `GET /api/v1/dashboard`（管理画面の `dashboard#show` の語に当たります）が、どちらも緑のまま通りました。** 画面の検めと同じように、`routes.rb` から動詞と経路の組を取り出して突き合わせる形にすると塞がります。**この PR が新設した押さえですので、次の issue で強めていただければ十分です。**
- **提案 2：図が実装と食い違っても、検査は緑のままです。** 今回の重大 1 〜 4 は、5 つの検めのどれにも当たりませんでした。**すべて機械で押さえるのは重すぎますが、状態遷移図だけは安く押さえられます。** `PromptRequest::TRANSITIONS` ・ `QuotaConsumption::TRANSITIONS` を読み、`SPEC/state.md` の `A --> B` の並びと集合で突き合わせる例を 1 つ置けば、**重大 4 は自動で赤くなります。**
- **提案 3：`archived` へ進む経路が、実装にありません。** `PromptRequest::TRANSITIONS` には `completed → archived` がありますが、`transition_to!('archived')` を呼ぶ場所は全文検索で 0 件です（画面側は `ja.ts:72` ・ `RequestStatus.tsx:107` で表示だけ用意しています）。`SPEC/state.md:34` の表は「記録として畳みました」と、操作があるかのように読めます。**「定めはありますが、進める操作はまだありません」と一言添えると正確になります。**
- **提案 4：定時の拾い直しが拾うものは、2 種類です。** `SPEC/state.md:53` は「決着だけが残った行を拾い直します」と 1 種類だけを挙げていますが、`ReclaimPromptRequestsJob#perform` は `stale_ids`（動きの無い `generating`）と `unsettled_ids`（決着済みなのに枠が予約のまま）の**2 種**を拾います。1 行足すだけで合います。
- **提案 5：README に載っていない経路が 1 本あります。** `routes.rb` の `get 'up' => 'rails/health#show'` です。Rails の既定で、`/health` との違いも `routes.rb` に書かれています。**載せるか、載せない理由を書くか**を決めておくと、次に読む方が迷いません。
- **提案 6：管理画面は、一覧に載っている 4 パス以外にも経路があります。** 版の作成（`/admin/rule-dictionaries/new`）・公開（`POST …/publish`）・利用者の詳細（`/admin/users/:id`）・再判定・枠のリセットです。**操作は `SPEC/usecase.md` が挙げていますので実害はありません**が、README の表に「主なページです」と一言あると、抜けと読まれずに済みます。
- **提案 7：テストの置き場が `test/pr<番号>/` ではありません。** `test/docs/` です。**`test/hardcode/` ・ `test/hooks/` ・ `test/harness/` と同じ「持ち場ごとの検査」の並びですので、この置き方に合理性があります。** ただし issue の前提は `test/pr<番号>/` と書いています。**置き場の決まりを実態に合わせて書き換える issue**を立てておくと、次から迷いません。

## 後片づけ

**削除は行っていません。** 手動で片づけていただく必要があるものは、次のとおりです。

- レビュー用の作業ツリー `DELETE/review-worktrees/pr185-20260827-reviewer`
- 変異試験に使った写し `C:\tmp\pr185review`（WSL からは `/mnt/c/tmp/pr185review` です）。**開発機に Linux 側の Node がありませんでしたので、Windows 側の Node で流すために、README ・ `SPEC/*.md` ・ `test/docs/` ・ `src/frontend/src/app` ・ `src/backend/config/routes.rb` ・ `src/backend/app/controllers` だけを写しました。** 秘匿値は含みません

**実装側の作業ツリー `DELETE/impl-78` は、読み取りだけに留めました。** 1 ファイルも変えていません。

## 判定

要修正

**issue #79（README）は、受け入れ条件 3 件をすべて満たします。** 10 画面・14 経路を実装へ 1 件ずつ当て、過不足がないことを確かめました。自動ログイン・`/health` ・ 画面の中の中継についての記述も、実装と細部まで一致します。**秘匿情報の露出もありません。** 本番の住所・バックエンドのドメイン・環境変数の値・`.claude/` の内容は、1 か所も出ていません。

**文書に対する自動の検めを新設した点は、この PR のいちばんの値打ちです。** 変異を 11 種入れて流し、画面の書きすぎ・書き漏らし・索引の漏れ・塀の閉じ忘れが、いずれも確かに赤くなることを実測しました。

**取り込みを止めるのは、issue #78 の側です。** 受け入れ条件「実装済みのものだけを書きます」と「requirements.md と食い違う場合は差分を明記します」を、いまの `SPEC/` は満たしていません。

- **`SPEC/class.md`** に、実装に無いメソッドが 4 つ（`StyleSpec.decide` ・ `Draft.build` ・ `Reservation.refund!` ・ 公開されていない `Delivery.store`）並び、`PromptPackage` の項目 4 つが実装と別物で、継承の親子も入れ替わっています（重大 1）。
- **`SPEC/usecase.md`** の利用者の欄に、実装の無い操作が 1 つ載っています（重大 2）。
- **`SPEC/sequence.md` と `SPEC/er.md`** が、枠の予約とお申し込みの行を作る順序を実装と逆に書いています。同じ PR の `SPEC/state.md` は正しく書いており、**SPEC の中で食い違っています**（重大 3）。
- **`SPEC/state.md`** のクォータの図に、実装済みの遷移が 2 本ありません。**そのうち 1 本は、同じ節が本文で説明している管理者の手動リセットそのものです**（重大 4）。
- **`SPEC/README.md`** の「上の 2 点以外に、仕様と食い違う実装はありません」が成り立ちません。**利用者による手動再判定の導線（requirements.md 6）と、アラート・通知（同 7.3）が実装にありません**（重大 5）。

**書かれた 2 点の差そのものは、どちらも正確でした。** 隠さずに書く姿勢は、そのまま活かしていただきたいと思います。**足りないのは、洗い出しの範囲だけです。**

**重大 1 〜 5 を直していただければ、取り込める状態になります。** `src/` を 1 文字も変えない PR ですので、**直すのは文書の記述だけ**です。実装側の修正は要りません。**提案 1 〜 7 は、この PR を止めるものではありません。次の issue へまとめて回してください。**

連絡先は info@rictaworks.jp です。

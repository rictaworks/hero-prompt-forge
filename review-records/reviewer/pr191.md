# reviewer 記録 : PR #191（issue #190 database.yml が全体を読み込むため、本番起動時に TEST_DATABASE_URL 不足で落ちる）

- 実施日時（JST）：2026-08-31
- 対象：`229c2f8`（2回のコミット。2回目はCI不合格を受けたRuboCop指摘の解消）
- 土台：`main` と同じ位置です（`git rev-list --left-right --count origin/main...HEAD` は `0 2`、遅れ 0・先行 2）
- `mergeable`：**`MERGEABLE`** ／ `mergeStateStatus`：**`CLEAN`**

## 実測

| 観点 | 結果 |
|---|---|
| 自動検査（CI、最終実行 `33320902542`） | **6 ジョブすべて pass**（バックエンド RuboCop/RSpec・フロントエンド・本番と同じ設定での読み込み確認・開発フックの検査・ハードコードの検査・ブラウザ操作の確認） |
| バックエンド（RuboCop / RSpec） | 手元でも実行。RuboCop 214ファイル・指摘0件、RSpec 全体 1985例・失敗0件、新規スペック 5例・失敗0件 |
| 実機再現（issue本文の症状） | 手元の dev コンテナで、`RAILS_ENV=production`・`DATABASE_URL` のみ設定・`TEST_DATABASE_URL` 未設定の状態で `bin/rails db:prepare` を実行し、修正前に起きていた `KeyError` が起きず成功することを確認（検証用DBは確認後に削除済み） |
| `production-boot` ジョブ自体の検証力 | CI実行ログを確認。`env:` に `TEST_DATABASE_URL` が無く、本番相当の条件（`DATABASE_URL` のみ）で `bin/rails db:prepare` を実行して pass しており、issue の指摘どおり検出力が回復している |
| セキュリティレビュー記録の指紋 | `f6546841bc4b08d3` を差分（`database.yml` ・ `ci.yml`）から再計算し一致を確認 |
| `requirements.md` ・ `app-ui/` | 1文字も変わっていません（`git diff --name-only origin/main...HEAD -- requirements.md app-ui` は0件） |
| 絵文字 | ありません |
| ですます調 | コメント・レビュー記録とも満たします |
| 個人名 | ありません。連絡先は info@rictaworks.jp |

## issue #190 の受け入れ条件ごとの判定

| # | 項目 | 判定 | 実測 |
|---|---|---|---|
| 1 | `RAILS_ENV=production` で `TEST_DATABASE_URL` 未設定でも `db:prepare` が成功する | **満たします** | 上記の実機再現のとおり |
| 2 | `production-boot` ジョブが `TEST_DATABASE_URL` を設定しない状態になり、この種の不具合を検出できる | **満たします** | `ci.yml` の差分・CI実行ログのとおり |
| 3 | `test:` 節の既定値が効くことを単体テストで固定する | **満たします** | `database_yaml_spec.rb`（5例） |
| 4 | マージ後、Railway 本番の `backend` サービスを実際に再デプロイし、プレースホルダーが無くても正常に起動することを確認する | **未実施（マージ後に行う工程）** | この記録の対象外。マージ後、デスクトップから本番再デプロイ・確認を別途実施する |

## CC / OWASP10 / QC10 / CRAP / DP / TM

| 基準 | 判定 |
|---|---|
| CC | 該当なし（法務・表記事項に関わる変更はありません） |
| OWASP10 | 合格（セキュリティレビュー記録のとおり。本番の接続先には既定値を持たせておらず、`DATABASE_URL` 未設定時は従来どおり失敗する） |
| QC10 | 該当なし（設定ファイルとCI定義の変更のみで、画面・SEOへの影響はありません） |
| CRAP | 該当なし（UIの変更はありません） |
| DP | 満たします。YAGNI（既定値は development 節と同型に留めた）・KISS（`ENV.fetch` の第2引数を足すだけ）・DRY（`STALE_AFTER` 等の既存の書き写し禁止パターンは踏んでいません） |
| TM | 満たします。単体テスト（RSpec）・CI の `production-boot` ジョブでの実機相当検証の両方で確認しています |

## プロジェクト固有の決まり

| 観点 | 判定 |
|---|---|
| 実装が `src/` 配下 | 満たします |
| フォールバックが無いこと | 満たします。`production:` 節には既定値を追加していません |
| 秘匿値 | ソースに書かれていません（`ci_boot_check_only_not_a_real_secret` は変更前から既存のプレースホルダー） |
| 絵文字 | ありません |
| 車輪の再発明 | ありません |

## 申し送り

- 受け入れ条件4（Railway本番の実再デプロイ確認）は、マージ後・リリース段の工程として別途実施する。

## 判定

合格

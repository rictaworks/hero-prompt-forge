# reviewer 記録 : PR #193（issue #192 本番のセキュリティヘッダ欠落・GitHub Actions可変タグ参照）

- 実施日時（JST）：2026-08-31
- 対象：`d44ed79`（1コミット）
- 土台：`main`と同じ位置（先行1・遅れ0）
- `mergeable`：**`MERGEABLE`** ／ `mergeStateStatus`：**`CLEAN`**

## 実測

| 観点 | 結果 |
|---|---|
| 自動検査（CI、最終実行 `33333395807`） | **6ジョブすべてpass**（バックエンドRuboCop/RSpec・フロントエンドESLint/型検査/Jest/ビルド・本番と同じ設定での読み込み確認・開発フックの検査・ハードコードの検査・ブラウザ操作の確認） |
| CSPが既存のE2Eフロー（フォーム送信・reCAPTCHA等）を壊していないか | CI の「ブラウザ操作の確認（Playwright）」が既存スイートを含めて pass。個別に本番相当ビルド（`npm run start`）を起動し、`/`・`/requests/new` でコンソールにCSP違反（`Refused to ...`）が出ないことも確認済み（security-review記録に実測あり） |
| ヘッダの実測 | `curl -sSI` で `Content-Security-Policy`・`X-Content-Type-Options: nosniff`・`X-Frame-Options: DENY` の3つが応答に含まれることを確認 |
| GitHub Actionsのpin | `actions/checkout`（6箇所）・`ruby/setup-ruby`（3箇所）・`actions/setup-node`（4箇所）が全てSHA参照になっていることを実測。SHAが意図したバージョン（`checkout` v4.4.0・`setup-node` v4.4.0）を指すことをGitHub APIで確認済み |
| セキュリティレビュー記録の指紋 | `4dc6a9d0ea6a7329` を差分から再計算し一致を確認 |
| `requirements.md`・`app-ui/` | 1文字も変わっていません |
| 絵文字 | ありません |
| ですます調 | 満たします |
| 個人名 | ありません。連絡先はinfo@rictaworks.jp |

## issue #192 の完了条件ごとの判定

| # | 項目 | 判定 | 実測 |
|---|---|---|---|
| 1 | 本番相当環境で3ヘッダが揃うこと | **満たします** | 上記のとおり実測済み |
| 2 | ci.yml・release-deploy.ymlの全`uses:`がSHA参照になること | **満たします** | 全13箇所を確認 |
| 3 | 既存のRSpec/Jest/Playwrightが引き続き成功すること | **満たします** | CI・手元とも成功 |

## CC / OWASP10 / QC10 / CRAP / DP / TM

| 基準 | 判定 |
|---|---|
| CC | 該当なし |
| OWASP10 | 合格（A05 セキュリティ設定の不備・A06 脆弱で古いコンポーネントの是正に該当。詳細はsecurity-review記録） |
| QC10 | 該当なし（画面の見た目・SEOへの変更なし） |
| CRAP | 該当なし（UIの変更なし） |
| DP | 満たします。YAGNI（既知の欠落3ヘッダのみ対応、nonce化等の追加改修は見送り理由を明記）・KISS・DRY |
| TM | 満たします。単体テスト・CI・実機ブラウザ確認の3系統で検証 |

## 申し送り

- 本文に記載のとおり、実装作業中にWSL側のgitオブジェクト破損（Windowsクラッシュに起因）が発生し、リポジトリを再クローンして変更を再適用しています。最終的な差分・検証結果はこの記録のとおりで、内容に欠落はありません。
- `script-src`の`'unsafe-inline'`除去（nonce化）は別issue送りとする旨がsecurity-review記録に明記されています。

## 判定

合格

# reviewer 記録 : PR #187（issue #182 レビューで見つかった、後戻りを止める押さえを足す）

- 実施日時（JST）：2026-08-29
- 対象：1 回目 `67e0c75`（土台は `5459e18` = `main`。遅れはありません）
- 作業ディレクトリ：`~/github/rictaworks/hero-prompt-forge`（ブランチ `feature/issue-182-regression-guards` を直接検めました）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 土台 | `main`（`5459e18`）と同じ位置から分岐。遅れはありません |
| PR の差分 | 1 コミット・22 ファイル（301 行の追加、30 行の削除） |
| `requirements.md` ・ `app-ui/` | **1 文字も変わっていません**（`git diff --name-only origin/main...HEAD -- requirements.md app-ui` が 0 件） |
| CI（GitHub Actions） | **6 ジョブすべて pass**（ハードコードの検査・バックエンド RuboCop/RSpec・フロントエンド ESLint/型検査/Jest/ビルド・ブラウザ操作の確認 Playwright・本番と同じ設定での読み込み確認・開発フックの検査） |
| ローカルでの再実行（`bundle exec rspec`） | **1974 例、失敗 0** |
| ローカルでの再実行（`npx jest`） | **158 例、失敗 0** |
| `bundle exec rubocop app spec` | 182 ファイル、指摘 0 |
| `npm run test:all` | 成功（hooks・hardcode・harness・docs・check:hardcode すべて緑） |
| セキュリティレビュー記録の指紋 | `220fb673b5b96d89` が、`git diff origin/main...HEAD -- . ':(exclude)review-records/'` の再計算値と**一致します** |

### issue #182 の受け入れ条件

| # | 受け入れ条件 | 判定 | 実測 |
|---|---|---|---|
| 1 | 「塞ぐもの」9 項目が、変異を入れると赤になります | **満たします** | 9 項目すべてについて、PR 本文に記載された手順（実装を壊す→既存テストが緑のまま通ることを確認→テスト追加→再度壊して赤を確認→復元）が実際に踏まれていることを、各項目のテストコード（コミット差分）とその近傍のコメントで確認しました。以下、項目ごとに実装とテストを突き合わせました |
| 2 | 「片付けるもの」7 項目が片付きます | **満たします**（後述） |
| 3 | 既存のテストが、すべて緑のままです | **満たします**（RSpec 1974 例・Jest 158 例、CI 6 ジョブとも失敗 0） |

### 「塞ぐもの」9 項目の個別確認

| # | 項目 | 追加されたテスト | 実装との対応 | 判定 |
|---|---|---|---|---|
| 1 | 取り出しの契約（`Representation#to_h`）から `inputs` を落とす変異 | `spec/requests/api/v1/prompt_requests_spec.rb`「入力条件の取り出し」 | `app/services/prompt_requests/representation.rb:70` の `body = base.merge(inputs: prompt_request.inputs)` を直接検めます | **満たします** |
| 2 | 一覧の「同じ条件で作る」から `request_id` を落とす変異 | `src/frontend/src/components/projects/index.test.tsx`（新規） | `RequestRow` を公開し、`href` を直接検めます。`components/projects/index.tsx:184` の `request_id=${request.id}` を対象にしています | **満たします** |
| 3 | API 呼び出しの `credentials` を `include` へ変える変異 | `src/frontend/src/lib/api.test.ts`（新規） | `lib/api.ts:83` の `credentials: "same-origin"` を、`fetch` のモックで直接検めます | **満たします** |
| 4 | 定時の拾い直しの間隔（5分）を大きく変える変異 | `spec/jobs/reclaim_prompt_requests_job_spec.rb`「間隔は、置き去りと見なすまでの時間と釣り合っています」 | `config/recurring.yml:25` の `schedule: every 5 minutes` を直接検めます | **満たします** |
| 5 | 人物の構図の役割を落とす変異 | `spec/services/adapters/narrative_roles_spec.rb`「人物の構図を、構図の述語で述べます（選び直した 2 番目の案）」 | `app/services/generation/variation_notes.rb:48` の `rewritten_person_safety` を対象にしています。**先頭の案（variation 0）では、選び直した構図がたまたま当てた構図と一致するため検出できない**ことをコメントに明記しており、実測（別途 `rails runner` での確認）とも一致します | **満たします** |
| 6 | 弱めた配色の役割を落とす変異 | `spec/services/generation/conflict_resolver_spec.rb`「弱めた配色の役割を残します」 | `app/services/generation/style_palette.rb:84` の `note_for` を対象にしています | **満たします** |
| 7 | 管理画面の照合を早く打ち切る書き方（`&` → `&&`）にする変異 | `spec/services/admin/credentials_spec.rb`「利用者名が違っても、合言葉の照合を省きません」「合言葉が違っても、両方の照合を行います」 | `app/services/admin/credentials.rb:54` の `matches?` を対象にしています。`secure_compare` の呼び出し回数を `have_received(:secure_compare).twice` で直接検めます | **満たします** |
| 8 | 雛形の属性検査から一重引用符の側を落とす変異 | `test/hardcode/hardcode.test.mjs`「一重引用符の属性の中の文言も検出します」 | 見本（`ng.html.erb`）へ `<img alt='一重引用符の直書きです'>` を追加。`check-hardcoded-strings.mjs:80` の `matchAll(/=\s*'([^']*)'|=\s*"([^"]*)"/g)` を対象にしています | **満たします** |
| 9 | 認証が通らなかったときに実施者が残らないことの直接の検め | `spec/requests/admin_spec.rb`「利用者名が違っても、実施者は残りません」「合言葉が違っても、実施者は残りません」 | `request` スペックの `controller` アクセサ経由で、`AuthenticatesAdmin#admin_actor`（`private attr_reader`）を `.send` で直接読みます。認証そのものの成否（401）とは独立に確かめる形で、issue の指摘（間接の守りに留まっていた点）に正しく応えています | **満たします** |

**いずれの項目も、テストコード中のコメントに「変異を入れると緑のままだった」実測の経緯が明記されており、PR 本文の記述と食い違いはありません。**

### 「片付けるもの」7 項目の個別確認

| # | 項目 | 判定 | 実装 |
|---|---|---|---|
| 1 | 未使用の文言 3 件の削除 | **満たします** | `src/frontend/src/strings/ja.ts` から `exceptions.labels.resultLink` ・ `requestStatus.labels.degradedQuote` ・ `requestStatus.labels.openResultPlain` の 3 行が削除されています。**削除後も他の参照が無いことを確認しました**（`grep` で 0 件） |
| 2 | `PERSON_SAFETY_ROLE` の重複定義の解消 | **満たします** | `style_spec.rb:70` が `VariationBuilder::PERSON_SAFETY_ROLE` を参照する形になり、独自の文字列リテラルを持ちません |
| 3 | `index_with(...).invert` の解消 | **満たします** | `style_spec.rb:219` の `applied_note` が `{ PERSON_SAFETY_ROLE => decision[:compositions].first }` という直接の組み立てに変わり、2 件以上になっても値を欠落させる余地のある変換（`.invert`）を使いません |
| 4 | `resetLoader()` の本番束からの除去 | **満たします** | `lib/recaptcha.ts` から `resetLoader` の named export が削除されました。テスト（`recaptcha.test.ts`）は `jest.resetModules()` でモジュールを丸ごと読み直す形に変わり、6 例とも成功しています |
| 5 | `error.tsx` の `<button>` を `Button` へ | **満たします** | `app/error.tsx` が `@/components/ui` の `Button` を使う形に変わりました |
| 6 | `AuthenticatesAdmin::USER_NAME_KEY` / `PASSWORD_KEY` の重複解消 | **満たします** | `authenticates_admin.rb` から両定数が削除され、4 つの spec ファイル（`admin_spec.rb` ・ `admin/metrics_spec.rb` ・ `admin/rule_dictionaries_spec.rb` ・ `admin/users_spec.rb`）の参照が `Admin::Credentials::USER_NAME_KEY` / `PASSWORD_KEY` へ揃っています。**`MissingCredentialsError` の別名は残していますが、issue はこの別名を対象にしておらず、指摘には当たりません** |
| 7 | `ok.html.erb` の `admin.labels.logo`（実在しない鍵）の解消 | **満たします** | `admin.headings.title`（`ja.yml:95` に実在）へ変更されています |

### 各基準への適合

| 基準 | 判定 | 根拠 |
|---|---|---|
| CC | **適合** | 権利物・第三者表示物の持ち込みはありません。連絡先の記載変更もありません |
| OWASP10 | **適合** | 秘匿値の混入なし（gitleaks 検出 0）。認証・認可のロジック自体は不変で、直接の検めが追加されたのみです。危険な描画（XSS）を生む変更はありません |
| QC10 | **適合** | `error.tsx` のエラーハンドリング（QC10）は、`Button` コンポーネントへの統一で見た目・挙動とも劣化していません |
| CRAP | **適合** | UI の見た目に影響する変更は `error.tsx` の 1 箇所のみで、既存の `Button` コンポーネントへ寄せる変更のため、既存デザインとの整合が保たれます |
| DP | **適合** | DRY（定数重複の解消）・KISS（`index_with(...).invert` の単純化）・Fail Fast（テスト専用コードの本番除去）のいずれにも沿います。過剰な抽象化や新しい仕組みの追加はありません |
| TM | **適合** | 変異試験（mutation testing）の手法そのものを実践しており、TM の観点（テストが実際に効くことの検証）を体現しています |

### プロジェクト固有の決まり

| 決まり | 判定 |
|---|---|
| 実装が `src/` 配下にあること・開発用スクリプトが `src` の外にあること | **満たします**（`test/hardcode/` は既存の置き場どおり） |
| フォールバックが無いこと | **満たします。** 新規の分岐・既定値への逃がしはありません |
| 文字列リテラルの分離・ハードコード検査 | **満たします。** `check:hardcode` が緑です |
| ですます調 | **満たします。** コミットメッセージ・テストのコメントとも、だ・である調の混入はありません |
| 日本語版のみ・多言語化コードなし | **満たします** |
| 絵文字なし | **満たします** |
| 秘匿値がソースに無いこと | **満たします。** gitleaks 検出 0 |
| 車輪の再発明をしていないこと | **満たします。** `jest.resetModules()` は Jest 標準機能で、独自のモジュールリセット機構を作っていません |

## 指摘

指摘はありません。

## 判定

合格

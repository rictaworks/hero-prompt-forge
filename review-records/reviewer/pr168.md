# reviewer 記録 : PR #168（issue #61 reCAPTCHA を適用する）

- 実施日時（JST）：2026-08-27
- 対象：1 回目 `fccfe5f`、2 回目 `c956063`、3 回目 `9074fe2`（土台は `main`）
- 作業ツリー：`DELETE/review-worktrees/pr168*-20260827-reviewer`（いずれも `git checkout --detach` 済みです）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| 全体のテスト | 1680 例、失敗 0 件（新規 37 例） |
| 書き方の検査 | 187 ファイル、指摘 0 件 |
| ハードコードの検査 | 検出 0 件 |
| 秘匿値の露出 | 403 / 503 の本文・見出し・記録を実測。混入 0 件 |
| 要求側の値での切り替え | 本文の項目と見出し 3 通りを付けて投入。いずれも 403。**飛ばせません** |
| 外部への通信 | 全遮断。新規の 37 例はすべて差し替え済みです |
| **変異試験** | 12 種のうち **10 種は赤、2 種が生き残りました** |

### 受け入れ要件

issue #61 の 3 項目を**満たします。** スコープ外の変更はありません。

### 指摘

- **要修正 1**：`requirements.md` の引用が誤っています（5.1 ではなく 5.2 です）。**6 か所**。
- **要修正 2**：「環境だけで決まる」ことを守るテストがありません。**抜け道を入れても 37 例すべてが緑のままでした。** 記録の記述が実態と合っていません。
- **要修正 3**：「断られた場合」のテストが弱く、**別の理由で緑になっています。**
- **要修正 4**：`RECAPTCHA_SECRET_KEY` が開発の実行環境へ届きません。
- **要修正 5**：`SPEC/api/README.md` の表の並びと説明。
- **提案 6〜13**：理由づけの書き直し・設定の型と範囲・秘密鍵の入口・起動時の確認・`Retry-After`・プライバシーポリシー・画面側への申し送り・判断の重複。

## 判定（1 回目）

要修正

不合格はありません。実装そのものは受け入れ要件を満たしますが、**変異試験で 2 種の変異が生き残り、そのうち 1 つは記録が「テストで確かめた」と書いている項目です。**

---

## 2 回目

### 実測

| 観点 | 結果 |
|---|---|
| 全体のテスト | **1693 例、失敗 0 件**（新規 50 例） |
| **変異試験** | **22 種のうち 21 種が赤。生き残り 1 種** |
| 鍵が空文字・空白だけの場合 | 本番でいずれも失敗します |
| 設定が壊れている場合 | 設定の誤りとして失敗します |

### 確認の結果

- **前回生き残った 2 種は、どちらも赤くなりました。** 要求側から飛ばす道を 4 通りに広げても、いずれも赤です。
- **設定の型と範囲の検めは働いています。** 7 種の変異すべてが赤で、**型と範囲を別々に崩しても捕まります。**
- **`SPEC/api/README.md` の表が整いました。**
- **1 回目の 10 種は、赤のままです。**

### 判断を求められた 2 点

- **要修正 4 を PR #170 側で直すこと**：**受け入れます。** 同じファイルを 2 つの PR で触らない判断は妥当です。ただし `.env.example` の衝突と、#170 まで開発の分岐を試せないことを申し送りへ足してください。
- **提案 9・10・13 の見送り**：**3 件とも受け入れます。**

### 指摘

- **要修正 14**：**PR の土台が動いており、このままではマージできません。** `CONFLICTING` で、14 ファイルが衝突し、差分が 36 ファイルになっており、**自動検査が 1 件も走っていません。**
- **要修正 15**：PR 本文の `requirements.md 5.1` が直っていません。
- **提案 16**：空文字の鍵を固定するテストが 1 例あると、`.presence` の意味が残ります。
- **提案 17**：設定のテストが `tmp/` へ YAML を毎回積み増します（**1 回の実行につき 7 ファイル**）。

## 判定（2 回目）

条件付き合格

reCAPTCHA の実装そのものは、2 回目で仕上がりました。残るのは、積み直しと PR 本文の 1 か所です。

---

## 3 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`**（2 回目は `CONFLICTING`） |
| PR の差分 | **11 ファイル。#61 の変更だけです**（2 回目は 36 ファイル） |
| 自動検査 | **5 ジョブすべて pass** |
| 全体のテスト | **1781 例、失敗 0 件** |
| 書き方の検査 | **194 ファイル、指摘 0 件** |
| 差分の指紋 | **`ac0f60601e1e8841`。記録の値と一致します** |
| **変異試験** | **20 種すべて赤。生き残りゼロ** |
| `tmp/` のファイル | **21 回まわしても 1 のままです** |

### 積み直しで落ちていないこと

**中心の 4 ファイルは、積み直し前と 1 バイトも違いません。** 2 回目に「衝突する」と申し上げた 3 ファイルも、`before_action :verify_human!` ・ `human_verification_failed` の文言 ・ `SPEC` の表の並びを含め、**#61 の内容がすべて残っています。**

### 実測した振る舞い

| 場面 | 結果 |
|---|---|
| 通った場合 | `201` |
| 通らなかった場合 | `403`。**秘密鍵も、Google の理由の符号も含みません** |
| 照合できない場合 | `503`。当たり障りのない文言です |
| 失敗時の副作用 | 記録・枠・案のいずれも増えません |

### 指摘

要修正はありません。

- **提案 18**：記録の「変更の範囲」の表で、テストの例数が古いままです（23 例・14 例 → 実測は 34 例・19 例）。

### 受け入れ要件（issue #61）

| 受け入れ条件 | 判定 |
|---|---|
| 検証に失敗した場合に生成を行いません | **満たします**（実測） |
| シークレットが環境変数から読まれます | **満たします。** URL へ載せず、応答・記録・例外にも出ません。**空文字も「無い」として扱います** |
| 開発環境での扱いが明示されています | **満たします。** 分かれ方は環境だけで決まることを、テストと変異試験の両方で確かめました |

## 判定

合格

3 回のレビューで挙げた要修正 15 件・提案 17 件は、すべて解決または妥当な見送りとして片づきました。**20 種の変異試験に生き残りが無く**、受け入れ要件 3 項目は実測で満たされています。PR は `MERGEABLE` で、自動検査 5 ジョブが緑です。**マージして差し支えありません。**

マージのときは、`.env.example` が PR #170 と同じ位置で衝突しますので、**両方の節を残して解いてください。**

連絡先は info@rictaworks.jp です。

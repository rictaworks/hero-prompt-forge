# reviewer 記録 : PR #179（issue #173 ブラウザ操作の確認を自動検査へ組み込みます）

- 実施日時（JST）：2026-08-27 21:50
- 対象：1 回目 `32d4120`（土台は `be10d2d`。`main` は `6e97fd8` まで進んでいます）
- 作業ツリー：`DELETE/review-worktrees/pr179` ・ `DELETE/review-worktrees/pr179-merge`（いずれも `git worktree add --detach` 済みです）
- 検証用のデータベース：`hero_prompt_forge_test_179`（RSpec 用）・ `hero_prompt_forge_e2e179`（`e2e` ジョブの再現用）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 土台 | **`main` より 3 コミット遅れています**（`ded2f5b` ・ `5b791e7` ・ `6e97fd8`）。**積み直しが要ります**（条件 1） |
| PR の差分 | **1 コミット・4 ファイル**（241 行の追加、1 行の削除）。**スコープ外の変更はありません** |
| `src/` の変更 | **1 文字もありません**（`git diff --name-only` で確認しました） |
| 自動検査 | **6 ジョブすべて pass**（実行 `33065520678`） |
| `e2e` ジョブの実測 | **1 分 45 秒**。**Playwright 11 例、失敗 0 件**（ジョブの記録を直接読みました） |
| 置き場の検査（`test:harness`） | **6 例、失敗 0 件** |
| 束ねた検査（`test:hooks` ・ `test:hardcode` ・ `test:harness`） | **34 例、失敗 0 件** |
| 直書きの文言の検査（`check:hardcode`） | **検出 0 件** |
| RSpec（環境判定・認証に絞りました） | **15 例、失敗 0 件**（`spec/services/app_environment_spec.rb` ・ `spec/requests/authentication_spec.rb`） |
| セキュリティレビュー記録の指紋 | **一致します**（`8fbb3357f0536c21` を差分から再計算しました） |
| `requirements.md` ・ `app-ui/` ・ `src/` | **1 文字も変わっていません** |
| 絵文字 | **ありません** |
| グローバル変数 | **ありません**（追加分は `workflow()` 関数に入っています） |
| 秘匿値の直書き | **ありません。** 合言葉は自動検査の中で使い捨てる `hero_ci_only` です（既存 3 ジョブと同じ方式です） |
| ファイルのエンコード・改行 | **4 ファイルとも UTF-8 ・ LF です** |
| ですます調 | **満たします**（ジョブのコメント・`test/README.md` ・ テストの記述とも） |

**自動検査は、実際にブラウザを開いています。** ジョブの記録に `Running 11 tests using 2 workers` と、11 例の日本語の名前が並び、`11 passed (5.0s)` で終わっています。**`自動ログインの利用者を用意しました: 1000000000000000001` も記録に出ています。** 「ジョブは足したが中身は動いていない」という状態ではありません。

### issue #173 の受け入れ条件（5 件）

| # | 受け入れ条件 | 判定 | 実測 |
|---|---|---|---|
| 1 | `test/pr*/**/*.spec.ts` が `main` への取り込みの前に自動で走ります | **満たします（条件 1 つき）** | `on: pull_request` で走り、11 例が緑です。**ただし `main` に増えた `test/pr174/screens.spec.ts` は、この PR の実行では走っていません** |
| 2 | 画面・バックエンド・データベースを立ち上げてから走ります | **満たします** | 順番も、起動の待ち合わせも入っています。押さえが効くことを変異で確かめました（変異 C） |
| 3 | **接続先は開発サーバーだけです。本番を指した場合はその場で失敗します** | **満たします** | 実際に壊して確かめました（下記） |
| 4 | ジョブが重い場合は、走らせる条件を決めて記載します | **満たします** | **絞らないと決め**、理由を `test/README.md` と記録へ書いています。実測 1 分 45 秒で、重くありません |
| 5 | `test/README.md` の記述と実態が一致します | **満たします** | 5 つの手順が、`ci.yml` の手順とそのまま対応します |

#### 受け入れ条件 3 を、実際に壊して確かめました

**3 通りの与え方で確かめました。**

| 与えた接続先 | 結果 |
|---|---|
| `https://hero.rictaworks.jp`（本番の住所） | **その場で失敗します。** `playwright.config.ts:19` が例外を投げ、テストは 1 例も動きません |
| `https://hero.rictaworks.jp/#http://localhost:3000`（本番の住所に開発の住所を混ぜたもの） | **その場で失敗します。** 同じく `playwright.config.ts:19` です |
| 未設定 | **その場で失敗します。** `playwright.config.ts:13` が例外を投げます |

**「その場で失敗する」ことも確かめました。** 一覧の取得（`npx playwright test --list`）の時点で落ちますので、**本番へ 1 度も接続しません。** 既定値へ寄せる書き方もありません。

### 変異試験（押さえが効くことの確認）

**5 種を、この作業ツリーで実際に流し直しました。**

| # | 変異 | 結果 |
|---|---|---|
| A | `e2e` ジョブを丸ごと外します | **赤（3 例）**。PR 本文の記載どおりです |
| B | 接続先を本番の住所（`https://hero.rictaworks.jp`）へ変えます | **赤（1 例）**。PR 本文の記載どおりです |
| C | 起動より先に `npx playwright test` を置きます | **赤（1 例）**。PR 本文の記載どおりです |
| D | ジョブに `if: false` を足します | **生き残ります**（6 例とも緑のまま。提案 2） |
| E | `on: pull_request:` に `paths: ['src/frontend/**']` を足します | **生き残ります**（6 例とも緑のまま。提案 2） |
| B' | 接続先を `https://hero.rictaworks.jp/#http://localhost:3000` へ変えます | **置き場の検査は生き残ります**（提案 1）。**ただし `playwright.config.ts` が実行時に止めます** |

**A ・ B ・ C は、PR 本文と記録の記載どおりでした。** 数も一致します。

### 開発向けの自動ログインが、本番の守りを弱めていないか

**弱めていません。** 1 つずつ確かめました。

| 観点 | 確かめた内容 | 結果 |
|---|---|---|
| 近道が効く条件 | `AppEnvironment.developer_shortcuts_allowed?` は `development` ・ `test` のときだけ真です（`app/services/app_environment.rb:30`） | **本番では必ず偽です** |
| 近道の入口 | `AuthenticatesUser#development_user` は、条件が偽なら**最初の 1 行で `nil` を返します**（`app/controllers/concerns/authenticates_user.rb:65`）。環境変数はその後でしか読みません | **本番では設定しても効きません** |
| 未知の値の扱い | `AppEnvironment.current` は既知の 3 語以外を例外にします。既定値へ寄せません | **フォールバックはありません** |
| 自動検査での検め | `production-boot` ジョブが `APP_ENV=production` で `raise "本番で開発向けの近道が有効です" if AppEnvironment.developer_shortcuts_allowed?` を毎回流しています | **緑です**（この PR でも pass） |
| テスト | `app_environment_spec.rb` ・ `authentication_spec.rb` を流しました | **15 例、失敗 0 件** |
| 配置への波及 | `DEVELOPMENT_AUTO_LOGIN_X_USER_ID` の出どころを全文検索しました。**`ci.yml` の `e2e` ジョブ（ジョブの中だけ）・ `docker-compose.yml` の `dev`（既定は空）・ `.env.example`（値は空）だけです** | **本番の配置設定に 1 か所も入っていません** |
| 値そのもの | `1000000000000000001` は X の実在の識別子ではなく、ジョブの中で 1 名だけ作って捨てます | **実在の利用者に触れません** |
| 記録への漏れ | 失敗時に出すのは Rails と Next の起動の記録だけです。環境変数の一覧を出す手順はありません | **秘匿値は出ません** |

**この PR は、本番側の設定・手順・環境変数を 1 つも触っていません。** 近道は「自動検査の走行環境の中でだけ真になる条件」に閉じています。**守りは、この PR の前後で変わりません。**

**1 点だけ、次の issue へ回したい先回りがあります。** `e2e` ジョブは画面を `NEXT_PUBLIC_APP_ENV: development` で組み立てます。いまは画面側の `developerShortcutsAllowed()` を使っている場所が 1 つもありませんので実害はありませんが、**画面に近道が入ったあと、ブラウザでの確認は「開発の見た目」しか見なくなります。**「本番の見た目に近道が出ない」ことを、ブラウザで押さえる手立てが要ります（提案 3）。

### 各基準への適合

| 基準 | 判定 | 根拠 |
|---|---|---|
| CC | **適合** | 表示物・権利物・文言の追加がありません。画面を 1 つも変えていません |
| OWASP10 | **適合** | 秘匿値の持ち込みがありません。近道が本番へ広がらないことを実測しました。ジョブの権限は読み取りのみです |
| QC10 | **適合** | 例外処理の観点で、接続先の不備は**その場で失敗**します。既定値へ寄せる書き方はありません |
| CRAP | **該当しません** | 画面の見た目・並び・文言を 1 つも変えていません |
| DP | **適合** | Fail Fast を保っています。ジョブの手順が小さく分かれ、名前が中身と一致します。自作の仕組みを増やしていません |
| TM | **適合** | 受け入れテスト（ブラウザ操作）を自動検査へ載せる変更そのものです。**押さえが効くことを変異で確かめています**（A ・ B ・ C） |

### プロジェクト固有の決まり

| 決まり | 判定 |
|---|---|
| 実装が `src/` の下にあること | **該当しません。** `src/` を 1 文字も変えていません |
| 開発用の道具が `src` の外にあること | **満たします**（`.github/` ・ `test/`） |
| フォールバックが無いこと・例外処理があること | **満たします。** 起動の待ち合わせは、待てなければ記録を出して落とします |
| デバッグトレースが追えること | **おおむね満たします。** 失敗時に両方の起動の記録を出します。**ただし Playwright 側の記録が残りません**（提案 4） |
| 制御構文以外がクラスまたは関数に入ること | **満たします**（`workflow()` を切り出しています） |
| 文字列リテラルの分離とハードコードの検査 | **満たします。** `check:hardcode` は検出 0 件です |
| ネイティブの `alert()` 等を使わないこと | **満たします** |
| 環境の判定と本番での近道の遮断 | **満たします**（上記のとおり実測） |
| 車輪の再発明をしないこと | **満たします。** 走らせるのは Playwright と GitHub Actions の既存の仕組みだけです |
| フォロワー判定を自前で作らないこと | **該当しません。** この PR は触れていません |
| 秘匿値を `.env` から読むこと | **満たします。** ジョブに秘匿値はありません |
| ですます調 | **満たします** |
| 日本語版のみ・多言語化なし | **満たします** |
| 絵文字なし | **満たします** |
| JST ・ UTF-8 | **満たします**（`env: TZ: Asia/Tokyo` を引き継ぎます） |

## 指摘

### 条件（マージの前に片づけていただきたいもの）

- **条件 1：`main` を取り込んでから、`e2e` ジョブが緑になることを確かめてください。**

  この PR の土台は `be10d2d` です。**その後 `main` へ入った PR #174 が `test/pr174/screens.spec.ts` を足しています。** このファイルは `playwright.config.ts` の `testMatch`（`**/pr*/**/*.spec.ts`）に当たりますので、**取り込んだあとの `e2e` ジョブは、この PR の実行では 1 度も走っていない 40 例あまりを一緒に走らせます。**

  **走らない見込みが濃厚です。** ジョブの手順をそのまま再現して確かめました。`bin/rails db:prepare` と「自動ログインの利用者を作る」を流したあとの状態は、次のとおりです。

  - `db/seeds.rb` が作るのは規則辞書だけです。**生成の要求も、案件も、1 件も作りません。**
  - 実測で `PromptRequest.count` は **0** でした。
  - 一方 `test/pr174/screens.spec.ts` は、`/requests/1` に `Package Ready` ・ `DEGRADED MODE` ・ `NOT REFINED` が出ること、`/projects` に「過去の生成」と「縮退」の印が並ぶこと、`1 日 1 回`の枠を使い切ると `NEXT RESET` が出ることを求めます。**いずれも、あらかじめ用意された記録が要ります。**

  **`main` は保護されていません**（`branches/main/protection` は 404 です）。取り込む前に自動検査を流し直す仕組みがありませんので、**このまま取り込むと `main` の自動検査が赤くなります。**

  お願いしたいことは、次のどちらかです。

  1. `main` を取り込み、`e2e` ジョブが緑になることを確かめてから取り込む。必要なら、**確認用の記録を用意する手順を `e2e` ジョブへ 1 つ足す**（`bin/rails runner` で案件と要求を 1 件ずつ作る等）。
  2. それが重い場合は、**この PR ではいったん `test/pr174/` を対象から外し**（`playwright.config.ts` ではなく `e2e` ジョブの引数で範囲を指定する等）、**記録の用意を次の issue へ切り出す。** その場合は `test/README.md` の「必ず走ります」の記述も、実態に合わせて直してください。

  **なお、共用の PostgreSQL が復旧処理に入ったため、ブラウザでの実地確認までは進めていません。** 上の判断は、`db/seeds.rb` の中身と `PromptRequest.count = 0` の実測、および `test/pr174/screens.spec.ts` の要求内容から導いています。

### 提案（次の issue へ回してよいもの）

- **提案 1：接続先の検査の正規表現に、終端の錨がありません。** `test/harness/test-directories.test.mjs` の `/https?:\/\/(localhost|127\.0\.0\.1)/` は行の途中の一致でも通ります。実測で、`https://hero.rictaworks.jp/#http://localhost:3000` を与えても 6 例とも緑のままでした。**同じ形の穴が `playwright.config.ts:19` にもあります。** こちらは行頭の錨はありますが終端がなく、`http://localhost.evil.example.com` を**受け入れます**（実測でテストの一覧が出ました）。**issue #173 が「既存の守りをそのまま使う」と定めた部分ですので、この PR の担当ではありません。** ホスト名を切り出して完全一致で見る形へ寄せる issue を立ててください。
- **提案 2：ジョブが「黙って外れる」道が、まだ 2 つ残っています。** ジョブを丸ごと削る変異（A）は 3 例が赤になりますが、**`if: false` を 1 行足す変異（D）と、`on: pull_request:` に `paths:` を足す変異（E）は、どちらも 6 例とも緑のままです。** いまの押さえは「文字列があるか」だけを見ています。**ジョブに `if:` が付いていないこと・ `on:` に `paths:` が無いことを見る例を足せば塞がります。** 走らせる条件を絞らないという判断（受け入れ条件 4）と、そのまま対になります。
- **提案 3：ブラウザでの確認が、本番の見た目を 1 度も見ません。** `e2e` ジョブは画面を `NEXT_PUBLIC_APP_ENV: development` で組み立てます。いまは画面側で `developerShortcutsAllowed()` を使っている場所が無いため実害はありませんが、**近道が画面に入った時点で「本番の見た目に近道が出ない」ことの押さえが空になります。** 本番の設定で組み立てた画面に対して、近道が出ないことだけを見る例を別に置く形が考えられます。
- **提案 4：落ちたときに、Playwright 側の記録が残りません。** `playwright.config.ts` は `retries: 0` ・ `trace: "on-first-retry"` ですので、**追跡の記録が 1 度も採られません。** 失敗時に採られるスクリーンショット・追跡・報告書を、ジョブの成果物として残す手順が無いため、**赤くなったときに読めるのはサーバーの起動の記録だけです。** プロジェクトの決まり（デバッグトレースが追えること）に照らすと、成果物の保存を 1 手順足しておくと後が楽になります。
- **提案 5：`e2e` ジョブに、実行時間の上限がありません。** 既存の `hooks` ジョブは、実行そのものに時間の上限を付けています。`e2e` ジョブは、起動の待ち合わせこそ 60 回で打ち切りますが、**組み立て・ブラウザの導入・確認の各手順と、ジョブ全体に上限がありません。** `timeout-minutes` を 1 行置いておくと、暴走したときに早く落ちます。
- **提案 6：動かす仕掛けの版が、印ではなく札で止めてあります。** `actions/checkout@v4` ・ `ruby/setup-ruby@v1` ・ `actions/setup-node@v4` です。既存の 5 ジョブと同じ書き方ですので**この PR の担当ではありません**が、供給の経路を固めるなら、全ジョブをまとめて指紋で止める issue を立てておくとよいと思います。

## 後片づけ

**削除は行っていません。** 手動で片づけていただく必要があるものは、次のとおりです。

- レビュー用の作業ツリー `DELETE/review-worktrees/pr179` ・ `DELETE/review-worktrees/pr179-merge`
- 検証用のデータベース `hero_prompt_forge_test_179` ・ `hero_prompt_forge_e2e179`
- `DELETE/review-worktrees/pr179-merge/src/frontend/node_modules`（開発機の同じ内容を複製したものです）と、同じ場所の `.next`

**レビューの途中で、共用の PostgreSQL が復旧処理に入りました。** 21:25 ごろから、データ用ディレクトリ全体の同期に 800 秒を超えて掛かっています。**こちらが立てた Rails と Next.js は、気づいた時点で止めました**（開発機で動いていた 3001 ・ 3300 の組は触っていません）。**同時に重い実行が重なると落ちる**という申し送りは、PR #178 のレビューに続いて今回も再現しました。

## 判定

条件付き合格

**issue #173 の受け入れ条件 5 件は、5 件とも満たしています。** とくに「接続先は開発サーバーだけです。本番を指した場合はその場で失敗します」は、本番の住所・本番の住所に開発の住所を混ぜたもの・未設定の 3 通りで実際に壊し、**いずれも一覧の取得の時点で例外になり、本番へ 1 度も接続しない**ことを確かめました。

**「ジョブが黙って外れないこと」の押さえは、PR 本文の記載どおりに効きます。** ジョブを丸ごと外すと 3 例、接続先を本番へ向けると 1 例、起動より先に確認を置くと 1 例が、それぞれ赤くなることを流し直して確かめました。

**開発向けの自動ログインは、本番の守りを弱めていません。** 近道は `AppEnvironment.developer_shortcuts_allowed?` が真の環境でしか効かず、入口の 1 行目で断たれます。`production-boot` ジョブが毎回それを検めており、この PR でも緑です。**環境変数の出どころを全文検索しても、本番の配置設定に 1 か所も入っていません。** この PR は本番側の設定・手順を 1 つも触っていません。

PR は **`MERGEABLE` ・ `CLEAN`**、**自動検査 6 ジョブが緑**（`e2e` は 1 分 45 秒・Playwright 11 例が緑）、**束ねた検査 34 例・失敗 0 件**、**直書きの検査は検出 0 件**、**セキュリティレビュー記録の指紋も一致**します。**`src/` ・ `requirements.md` ・ `app-ui/` は 1 文字も変わっていません。** 各基準にも適合します。

**条件 1（`main` を取り込んでから `e2e` ジョブが緑になることを確かめること）だけ、取り込みの前に片づけてください。** `main` に増えた `test/pr174/screens.spec.ts` は、この PR の実行では 1 度も走っておらず、**ジョブの用意する記録が 0 件のままでは通らない見込みです。** 記録を用意する手順を 1 つ足すか、対象をいったん絞るかを選んでください。**提案 1〜6 は、この PR を止めるほどのものではありません。次の issue へまとめて回してください。**

連絡先は info@rictaworks.jp です。

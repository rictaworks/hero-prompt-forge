# reviewer 記録：PR #166（issue #55・#56）

- 対象：`feature/issue-54-async-job..feature/issue-55-56-api`（差分のみ。#54 の実装は対象外です）
- 検証したコミット：`5410c3a`
- 検証した作業ツリー：`DELETE/review-worktrees/pr166-20260827-reviewer`
- 実施日：2026-08-27（JST）

## 1. 実際に動かして確かめたこと

| 実行 | 結果 |
|---|---|
| `bundle exec rspec`（全体） | 1558 例、失敗 0 件 |
| `bundle exec rspec spec/requests/api/v1/prompt_requests_spec.rb` | 41 例、失敗 0 件 |
| `bundle exec rubocop` | 174 ファイル、指摘 0 件 |
| ハードコード検査 | 検出 0 件 |

### 1-1. 禁止入力でクォータを消費しないこと（順序の固定）

**実装は正しいことを確認しました。** そのうえで**テストによる順序の固定が不十分**でした。順序を入れ替えると既存の「枠を使いません」は落ちますが、**その日にすでに「返還済み」の枠がある場合は検知できません。** 取り直しは行の更新であり、件数が変わらないためです。実測では、入れ替えた実装のもとで返還済みの枠が `reserved` へ戻りました。**件数ではなく状態で固定してください。**

### 1-2. 上限到達時のリセット時刻（境界 02:59 / 03:00）

**ずれはありませんでした。** 02:59:59 → `2026-08-27T03:00:00+09:00`、03:00:00 → `2026-08-28T03:00:00+09:00`。ただしテストは `hour == 3` と `/\d+月\d+日/` のみで、**日付を固定していません。**

### 1-3. 他人の資源で 404 を返すこと

**漏れはありませんでした。** 所有の絞り込みを外すと既存の 2 例が落ちます。存在しない識別子と他人の識別子は同じ本体を返します。プラン値が有効でない利用者は一律 403 です。

### 1-4. プラン値の判定が入力の検証より先であること

**先です。** `before_action :require_authorized_plan!` により、行動へ入る前に判定されます。

### 1-5. 質量攻撃（Mass Assignment）

**`params` を `create!` へ直に渡していません。** `status` / `degraded` / `dictionary_version` / `id` を混ぜても既定のままでした。ただし `project_id` の受け取り方に穴があります（要修正 1・2）。

### 1-6. 応答からの漏れ

**混ざりませんでした。** `rejection_reason` に秘匿値を入れた状態でも、応答は状態名と文言だけでした。

### 1-7. `details.reasons` に `matched` を含める判断

**記録には残っていません。** しかし**応答の側は契約と食い違います。** SPEC は `details` について「個人情報・秘匿値を入れません」と定めています。**判断そのものは妥当だと考えますが、契約が現状の実装を許していません。**

### 1-8. 状態名と縮退の印

**一致しました。** 縮退の印は要求と案の両方から引けました。

### 1-9. N+1 問い合わせ

**起きていません。** 3 案がそろった要求に対する問い合わせは 4 件でした。

## 2. 受け入れ要件の確認

issue #55 の 4 項目・issue #56 の 4 項目とも**満たします。** 要件に無い変更は見当たりません。

## 3. 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | 要修正（要修正 4） |
| OWASP10 | 要修正（要修正 1・2） |
| QC10 | 要修正（要修正 1・2・3） |
| CRAP | 対象外 |
| DP | 適合 |
| TM | 要修正（要修正 5・6） |

## 4. 指摘

### 要修正 1：`project_id` を配列で送ると 500 になります

`params.require(:project_id)` は配列をそのまま通します。`find([1, 1])` が配列を返し、`create!(project: 配列)` で落ちます。**ログインした利用者であれば誰でも引き起こせます。** `params.expect(:project_id)` へ揃えてください。

### 要修正 2：`project_id` が無いと、契約の形で失敗を返しません

`ActionController::ParameterMissing` が素通りし、`code` / `message` / `next_action` を持たない応答になります。`BaseController` へ `rescue_from` を足してください。

### 要修正 3：`details.reasons` の中身が SPEC と食い違います

SPEC 側に「`details` には要求した本人の入力の写し（`matched`）を含めます。第三者の情報は含めません」と例外を明記し、エンドポイントの表へ `matched` と `prompt_request_id` を書き足してください。

### 要修正 4：差し戻した記録に、サービス概要の原文が残ります

`matched` を記録に残さない判断の根拠は「記録は保管期間が長い」でした。**ところが `matched` を含む原文そのものが、同じ記録の別の列（`inputs`）に残ります。** 配慮が効いていません。

### 要修正 5：順序の固定が、返還済みの枠を取り直す道筋を捉えません

件数ではなく状態（`refunded` のまま）で見る例を足してください。

### 要修正 6：上限到達の時刻テストが、日付を固定していません

`travel_to` を用い、02:59:59 と 03:00:00 で `details.reset_at` を完全一致で確かめてください。

### 提案

- 時刻の書式を `time.formats.*` と `I18n.l` へ寄せられます。
- `errors.forbidden_input.message` と `prompt_requests.failure.rejected.message` が同じ文です。
- 断られた要求の記録が際限なく増えます。**この入口が本番へ出る前に #61 を通す**必要があります。
- `RuleDictionary::MissingCurrentError` 等が 500 になります。運用で起こり得る失敗は 503 で契約の形へ寄せることをご検討ください。
- `test/pr166/` がありません。運用として揃えるか、README を実態へ合わせるかを決めてください。

## 5. 良かった点

- **順序に意味があることを、コードのコメントで残しています。**
- **他人の資源を 404 に寄せる判断が、入口の 1 か所に集まっています。**
- **記録と応答で、残すものを分けています。**
- **N+1 が出ていません。**

## 判定（1 回目）

要修正

受け入れ要件は 8 項目とも満たし、実際に動かした 9 つの観点はいずれも設計の意図どおりに動きました。一方で、要修正 1 と 2 は、ログインした利用者が容易に引き起こせる、契約の形を外れた 500 と 400 です。3 は SPEC と実装の食い違い、4 は配慮が別の列で打ち消されている点、5 と 6 はテストが振る舞いを十分に固定していない点です。

連絡先は info@rictaworks.jp です。


---
---

# reviewer 記録：PR #166（issue #55・#56）2 回目

- 検証したコミット：`9f08292`
- 検証した作業ツリー：`DELETE/review-worktrees/pr166b-20260827-reviewer`
- 実施日：2026-08-27（JST）

## 1. 実際に動かして確かめたこと

| 実行 | 結果 |
|---|---|
| `bundle exec rspec`（全体） | 1584 例、失敗 0 件 |
| `bundle exec rubocop` | 175 ファイル、指摘 0 件 |
| ハードコード検査 | 検出 0 件 |
| `npm run test:hooks` ・ `test:harness` | 23 件・3 件、失敗 0 件 |

## 2. 要修正 6 件への対応

### 要修正 1（配列で 500）— **500 は消えました。ただし契約の形にはなっていません**

`params.expect(:project_id)` により 500 にはなりません。**しかし本番相当の応答は `{"status":400,"error":"Bad Request"}` でした。** 要修正 2 が入っていないためです。

### 要修正 2（契約の形で返しません）— **入っていません**

**`rescue_from ActionController::ParameterMissing` が、どこにもありません。** `src/backend/` 全体を探しても一致は 0 件です。

**あわせて、記録と PR 本文が、入っていないものを「入れた」と書いています。** 該当するテストも 1 例もありません。

### 要修正 3（`details.reasons` が SPEC と食い違います）— **一致しました**

項目名も並びも一致します。種別の 3 つも `forbidden_inputs.yml` と一致しました。

### 要修正 4（記録に原文が残ります）— **落ちました。判断も妥当です**

`service_summary` はありません。選択肢の値は残っています。**「差し戻した以上、この文章で作ることはありません」という理由づけに異論はありません。** `rejected` は終端で `queued` へ戻る道がなく、`.inputs` を読むのはジョブだけで、`GET` は `inputs` を返しません。**利用者から取り上げてもいません。** 差し戻していない要求では残ることも実測しました。

### 要修正 5（返還済みの枠を捉えません）— **赤くなります**

順序を入れ替えると 2 例が赤くなりました（件数 0 → 1、`refunded` → `reserved`）。

### 要修正 6（日付を固定していません）— **赤くなります**

`reset_at` の退行を作ると 2 例が赤くなりました。**旧来の `hour == 3` の例は緑のままでした。** 狙いどおりです。

## 3. 提案への対応

- **`formats` を句点の検査から外す判断に賛成します。** Rails の標準の置き場であり、書式は句点で終われず、`collect_headings` 側の検査には掛かります。ひとつだけ、`non_sentence_keys` は鍵の名前だけを見ますので、**道筋で押さえる**ことをご検討いただけます（提案）。
- 同じ文が 2 か所にある件は、**理由が読めます。**
- reCAPTCHA の申し送りは PR 本文にあります。
- **`test/pr166/` を置かないことを受け入れます。** ただし**要修正 8 を直すことが条件です。**
- 503 へ寄せる件は申し送りで結構です。

## 4. 1 回目の 9 観点の回帰

**いずれも壊れていません。** 所有の絞り込みを外すと 2 例が赤くなり、質量攻撃も防がれ、N+1 も 4 件のままでした。**自由に書いていただいた文章が記録の側へ回り込む経路もありません。**

## 5. 受け入れ要件

issue #55 の 4 項目・issue #56 の 4 項目とも**満たします。** 要件に無い変更は見当たりません。

## 6. 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | 適合 |
| OWASP10 | 要修正（要修正 7） |
| QC10 | 要修正（要修正 7・8・9） |
| CRAP | 対象外 |
| DP | 適合 |
| TM | 適合 |

## 7. 指摘

### 要修正 7：`ActionController::ParameterMissing` を受け止めていません

本番相当の本体は `{"status":400,"error":"Bad Request"}` でした。**`error` は文字列で返りますので、画面側が `error.message` を読むと、何も出せません。** `BaseController` へ足し、`invalid_input`（400）へ寄せてください。**`project_id` を省いた場合・配列で送った場合の 2 例をテストへ足し、`code` ・ `message` ・ `next_action` が入ることまで確かめてください。**

### 要修正 8：記録と PR 本文に、行っていない検証が書かれています

CLAUDE.md は「検証・承認の記録を版管理対象のログに残します」と定めています。**行っていない検証が「テスト」と記された記録は、記録が無いことよりも重い問題です。**

### 要修正 9：PR 本文のユーザーテストが、書かれたとおりでは実行できません

`ENV/DEVELOPMENT.md` のとおり、ホスト側は **3301** です。**3000 番には別のリポジトリが載っています。**

### 提案

- 契約の失敗の例（`quota_exhausted`）が、同じ文書の表と食い違います。
- `non_sentence_keys` を道筋で押さえられます。
- `Acceptance` は `service_summary` を含む行をいったん書き、あとで落とします。**行の古い版は掃除が走るまで残ります。**

## 8. 良かった点

- **退行を模した実測に、ことごとく耐えました。**
- **「件数では捉えられない」という指摘に、状態で答えています。**
- **落とすものと残すものを、利用者の立場で分けています。**
- **時刻の書式を Rails の標準の置き場へ寄せました。**

## 判定（2 回目）

要修正

要修正 3・4・5・6 と提案への対応は、いずれも実測で解けていることを確かめました。1 回目の 9 観点も壊れていません。残るのは要修正 2 です。**`rescue_from ActionController::ParameterMissing` は入っておらず、記録と PR 本文が「足しました」と書いています。** 加えて、ユーザーテストが記載どおりでは実行できません。

連絡先は info@rictaworks.jp です。

---
---

# 2 回目の指摘への対応（実装側の記録）

- 実施日時（JST）：2026-08-27
- 対象：`feature/issue-55-56-api`（`71056a8`）

| 指摘 | 対応 |
|---|---|
| 要修正 7 | **`BaseController` へ `rescue_from ActionController::ParameterMissing` を実際に足しました。** 同じ変更を別のブランチ（PR #167）へ書いてしまっていたのが原因です。**識別子が欠けている場合・配列の場合・連想配列の場合の 3 通りについて、契約の 4 項目がそろうこと・項目名が添うこと・記録も枠も作らないことを固定しました（18 例）。** |
| 要修正 8 | **セキュリティレビュー記録を、実態へ直しました。** 入れ忘れていたことと、その原因を明記しました。**「テスト」と書くのは、そのテストを実際に走らせて緑になったときだけにします。** |
| 要修正 9 | **PR 本文の入口を `http://localhost:3301/...` へ直しました。** ブラウザに JSON がそのまま出ることも書き添えました。 |
| 提案（契約の例） | **`quota_exhausted` の例を、実装が返すものへ合わせました。** |

**RSpec 1611 例・失敗 0 件、RuboCop 175 ファイル・指摘 0 件、ハードコード検査 検出 0 件です。**

**最終の判定は、下の 3 回目の節にあります。**


---
---

# reviewer 記録：PR #166（issue #55・#56）3 回目

- 検証したコミット：`71056a8`（土台は `b1ef6f3`）
- 検証した作業ツリー：`DELETE/review-worktrees/pr166c-20260827-reviewer`
- 実施日：2026-08-27（JST）

## 1. 実際に動かして確かめたこと

| 実行 | 結果 |
|---|---|
| `bundle exec rspec`（全体） | **1611 例、失敗 0 件** |
| `bundle exec rspec spec/requests/api/v1/prompt_requests_spec.rb` | **66 例、失敗 0 件** |
| `bundle exec rubocop` | 175 ファイル、指摘 0 件 |
| ハードコード検査 | 検出 0 件 |
| 差分の指紋の再計算 | **記録の `07c1b4e7a2d704c6` と一致しました** |

## 2. 要修正 7（`ParameterMissing` を受け止めていません）— **解けました**

### コードに入っていること

`base_controller.rb` 21 行目に `rescue_from ActionController::ParameterMissing` があります。**2 回目に「無い」と申し上げたものが、今度は実在します。**

### 本番相当の設定で、契約の 4 項目を備えること

`show_detailed_exceptions = false` へ切り替えて実測しました。**4 通り（欠落・配列・連想配列・`inputs` ごと空）とも同じ本体でした。**

```
status=400
{"error":{"code":"invalid_input","message":"入力の内容に誤りがあります。",
          "next_action":"赤く示された項目を直してから、もう一度お試しください。",
          "details":{"fields":[{"field":"project_id","reason":"missing"}]}}}
```

### 送られた値を返さないこと

`project_id` に合言葉を入れて送りました。**応答の本体のどこにも現れませんでした。**

### 変異試験

`rescue_from` を外すと **9 例**が赤くなります（18 例ではありません）。**残る 9 例は、状態コードと副作用の無さを別に固定しており、赤くならないのが正しい姿です。** 申し送りの書き方だけが実測と食い違います。

`details` へ値を足す退行では、**「項目名を添えます」の 3 例が赤くなりました。**

## 3. 要修正 8（記録に行っていない検証が書かれています）— **本筋は解けました**

記録は、2 回目の誤りを隠さずに書き直しています。**事実と原因の両方**、そして**次に同じことを起こさないための決まり**を書いています。

**この直し方に異論はありません。** 誤りを消さずに残し、原因と再発を防ぐ決まりまで書いた記録は、消してやり直した記録より役に立ちます。

**ただし、数字が 2 か所ずれています**（要修正 10）。

## 4. 要修正 9（ユーザーテストが実行できません）— **そのとおりに実行できました**

Rails を起動し、ホストの `http://localhost:3301` へ当てました。

- 手順 1：`401` ・ 「ログインが必要です。」**合格です。**
- 手順 2：`404` ・ 「お探しのものが見つかりませんでした。」**合格です。**

**ブラウザしか使わない方が、書かれたとおりに辿り着けます。**

## 5. 提案への対応

- **契約の例が表と食い違う件**：直りました。**実装から一字一句そのまま取り出せる状態になりました。**
- **`non_sentence_keys` を道筋で押さえる件の見送り**：**受け入れます。**
- **`draft` の古い版が残る件の見送り**：**受け入れます。**

## 6. 2 回目までの観点が壊れていないこと

**いずれも変異試験と実測で確かめ、ひとつも緩んでいません。**

## 7. 受け入れ要件

issue #55 の 4 項目・issue #56 の 4 項目とも、**8 項目すべてを満たします。**

## 8. 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | 適合 |
| OWASP10 | 適合 |
| QC10 | 条件付き適合（要修正 10） |
| CRAP | 対象外 |
| DP | 適合 |
| TM | 適合 |

## 9. 指摘

### 要修正 10：記録と PR 本文の例数が、実測と合いません

**コードとテストに直すところはありません。数字だけです。**

| 場所 | 記載 | 実測 |
|---|---|---|
| 記録 21 行目 | 48 例 | **66 例** |
| 記録 89 行目・PR 本文 | API は 69 例 | **66 例** |

**全体の 1611 例と、RuboCop 175 ファイル、ハードコード検査 0 件は一致しました。**

### 提案 1：`params.expect` より先に、規則辞書を引きにいきます

Ruby は受け手を引数より先に評価しますので、**規則辞書を引く問い合わせが `project_id` の検査より先に走ります。** 公開済みの版が無い状態で `project_id` を省くと 500 になります。**マージの条件にはしません。** `project_id` の取り出しを 1 行前へ出すだけで、契約の形が規則辞書の健康状態に左右されなくなります。

### 提案 2：変異試験で赤くなる例数の書き方

「契約の形を押さえた 9 例が赤くなり、状態コードと副作用を押さえた 9 例は緑のままです」と書き分けていただくと、次に読む方が誤解しません。

## 10. 良かった点

- **誤りを消さずに残しました。取り繕うより難しい直し方を選んでいます。**
- **契約の例が、実装から取り出せる状態になりました。**
- **ユーザーテストが、書かれたとおりに通ります。**
- **添えるものを絞っています。** 値を返す退行はテストが捉えます。
- **これまでに固定した振る舞いが、ひとつも緩んでいません。**

## 判定（3 回目）

条件付き合格

要修正 7 ・ 9 と、契約の例に関する提案は、**いずれも実際に動かして解けていることを確かめました。** 2 回目までに固定した 9 つの観点も、変異試験でひとつも緩んでいません。要修正 8 の本筋である記録の正直さも解けており、差分の指紋も再計算して一致しました。

**条件は要修正 10 の 1 件だけです。** 記録と PR 本文の例数を、実測の `66 例` へ直してください。**コードとテストに直すところはありません。**

連絡先は info@rictaworks.jp です。

---
---

# 3 回目の条件への対応（実装側の記録）

- 実施日時（JST）：2026-08-27

| 条件 | 対応 |
|---|---|
| 要修正 10（例数が実測と合いません） | **セキュリティレビュー記録の 2 か所と PR 本文を、実測の `66 例` へ直しました。** |
| 提案 2（変異試験の例数の書き方） | **記録へ「契約の形を押さえた 9 例が赤くなり、状態コードと副作用を押さえた 9 例は緑のままです」と書き分けました。** |
| 提案 1（規則辞書を引く順序） | **見送ります。** マージの条件ではなく、申し送りにある 503 の検討とあわせて別の機会に判断します。 |

**コードとテストへの変更はありません。**

## 判定

条件付き合格

3 回目のレビューの判定です。**条件（記録と PR 本文の例数を実測へ直すこと）は、`66 例` へ直して満たしました。**

受け入れ要件は 8 項目すべてを満たし、2 回目までに固定した 9 つの観点も緩んでいません。**コードとテストへの変更はありません。**

# reviewer 記録：PR #166（issue #55・#56）

- 対象：`feature/issue-54-async-job..feature/issue-55-56-api`（差分のみ。#54 の実装は対象外です）
- 検証したコミット：`5410c3a`
- 検証した作業ツリー：`DELETE/review-worktrees/pr166-20260827-reviewer`
- 実施日：2026-08-27（JST）

## 1. 実際に動かして確かめたこと

| 実行 | 結果 |
|---|---|
| `bundle exec rspec`（全体） | 1558 例、失敗 0 件 |
| `bundle exec rspec spec/requests/api/v1/prompt_requests_spec.rb` | 41 例、失敗 0 件 |
| `bundle exec rubocop` | 174 ファイル、指摘 0 件 |
| ハードコード検査 | 検出 0 件 |

### 1-1. 禁止入力でクォータを消費しないこと（順序の固定）

**実装は正しいことを確認しました。** そのうえで**テストによる順序の固定が不十分**でした。順序を入れ替えると既存の「枠を使いません」は落ちますが、**その日にすでに「返還済み」の枠がある場合は検知できません。** 取り直しは行の更新であり、件数が変わらないためです。実測では、入れ替えた実装のもとで返還済みの枠が `reserved` へ戻りました。**件数ではなく状態で固定してください。**

### 1-2. 上限到達時のリセット時刻（境界 02:59 / 03:00）

**ずれはありませんでした。** 02:59:59 → `2026-08-27T03:00:00+09:00`、03:00:00 → `2026-08-28T03:00:00+09:00`。ただしテストは `hour == 3` と `/\d+月\d+日/` のみで、**日付を固定していません。**

### 1-3. 他人の資源で 404 を返すこと

**漏れはありませんでした。** 所有の絞り込みを外すと既存の 2 例が落ちます。存在しない識別子と他人の識別子は同じ本体を返します。プラン値が有効でない利用者は一律 403 です。

### 1-4. プラン値の判定が入力の検証より先であること

**先です。** `before_action :require_authorized_plan!` により、行動へ入る前に判定されます。

### 1-5. 質量攻撃（Mass Assignment）

**`params` を `create!` へ直に渡していません。** `status` / `degraded` / `dictionary_version` / `id` を混ぜても既定のままでした。ただし `project_id` の受け取り方に穴があります（要修正 1・2）。

### 1-6. 応答からの漏れ

**混ざりませんでした。** `rejection_reason` に秘匿値を入れた状態でも、応答は状態名と文言だけでした。

### 1-7. `details.reasons` に `matched` を含める判断

**記録には残っていません。** しかし**応答の側は契約と食い違います。** SPEC は `details` について「個人情報・秘匿値を入れません」と定めています。**判断そのものは妥当だと考えますが、契約が現状の実装を許していません。**

### 1-8. 状態名と縮退の印

**一致しました。** 縮退の印は要求と案の両方から引けました。

### 1-9. N+1 問い合わせ

**起きていません。** 3 案がそろった要求に対する問い合わせは 4 件でした。

## 2. 受け入れ要件の確認

issue #55 の 4 項目・issue #56 の 4 項目とも**満たします。** 要件に無い変更は見当たりません。

## 3. 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | 要修正（要修正 4） |
| OWASP10 | 要修正（要修正 1・2） |
| QC10 | 要修正（要修正 1・2・3） |
| CRAP | 対象外 |
| DP | 適合 |
| TM | 要修正（要修正 5・6） |

## 4. 指摘

### 要修正 1：`project_id` を配列で送ると 500 になります

`params.require(:project_id)` は配列をそのまま通します。`find([1, 1])` が配列を返し、`create!(project: 配列)` で落ちます。**ログインした利用者であれば誰でも引き起こせます。** `params.expect(:project_id)` へ揃えてください。

### 要修正 2：`project_id` が無いと、契約の形で失敗を返しません

`ActionController::ParameterMissing` が素通りし、`code` / `message` / `next_action` を持たない応答になります。`BaseController` へ `rescue_from` を足してください。

### 要修正 3：`details.reasons` の中身が SPEC と食い違います

SPEC 側に「`details` には要求した本人の入力の写し（`matched`）を含めます。第三者の情報は含めません」と例外を明記し、エンドポイントの表へ `matched` と `prompt_request_id` を書き足してください。

### 要修正 4：差し戻した記録に、サービス概要の原文が残ります

`matched` を記録に残さない判断の根拠は「記録は保管期間が長い」でした。**ところが `matched` を含む原文そのものが、同じ記録の別の列（`inputs`）に残ります。** 配慮が効いていません。

### 要修正 5：順序の固定が、返還済みの枠を取り直す道筋を捉えません

件数ではなく状態（`refunded` のまま）で見る例を足してください。

### 要修正 6：上限到達の時刻テストが、日付を固定していません

`travel_to` を用い、02:59:59 と 03:00:00 で `details.reset_at` を完全一致で確かめてください。

### 提案

- 時刻の書式を `time.formats.*` と `I18n.l` へ寄せられます。
- `errors.forbidden_input.message` と `prompt_requests.failure.rejected.message` が同じ文です。
- 断られた要求の記録が際限なく増えます。**この入口が本番へ出る前に #61 を通す**必要があります。
- `RuleDictionary::MissingCurrentError` 等が 500 になります。運用で起こり得る失敗は 503 で契約の形へ寄せることをご検討ください。
- `test/pr166/` がありません。運用として揃えるか、README を実態へ合わせるかを決めてください。

## 5. 良かった点

- **順序に意味があることを、コードのコメントで残しています。**
- **他人の資源を 404 に寄せる判断が、入口の 1 か所に集まっています。**
- **記録と応答で、残すものを分けています。**
- **N+1 が出ていません。**

## 判定（1 回目）

要修正

受け入れ要件は 8 項目とも満たし、実際に動かした 9 つの観点はいずれも設計の意図どおりに動きました。一方で、要修正 1 と 2 は、ログインした利用者が容易に引き起こせる、契約の形を外れた 500 と 400 です。3 は SPEC と実装の食い違い、4 は配慮が別の列で打ち消されている点、5 と 6 はテストが振る舞いを十分に固定していない点です。

連絡先は info@rictaworks.jp です。

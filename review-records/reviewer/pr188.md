# reviewer 記録 : PR #188（issue #183 上限到達の見せ方を、モックへ寄せます）

- 実施日時（JST）：2026-08-29 22:40
- 対象：`b90b089`（土台は `4ac9f02` = `main`。`git rev-list --left-right --count origin/main...HEAD` は `0 1` で、遅れ 0 ・ 先行 1）
- 作業ツリー：`~/github/rictaworks/hero-prompt-forge`（ブランチ `feature/issue-183-quota-mock-parity`、実装した本人が引き続き確認しています）
- 検証用のデータベース：使いました（docker compose の `db` ・ `dev`）

## 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| CI（GitHub Actions） | **6 ジョブすべて pass**（ハードコードの検査 ・ バックエンド ・ フロントエンド ・ Playwright ・ 本番と同じ設定での読み込み確認 ・ 開発フックの検査。実行 `33233785151`） |
| PR の差分 | 1 コミット・11 ファイル（311 行追加、9 行削除） |
| `requirements.md` ・ `app-ui/` | **1 文字も変わっていません**（`git diff --name-only origin/main...HEAD -- requirements.md app-ui` が 0 件） |
| RSpec（バックエンド全体） | **1980 例、失敗 0 件**（この作業ツリーで実行） |
| Jest（フロントエンド全体） | **167 例、失敗 0 件**（この作業ツリーで実行） |
| RuboCop（変更ファイル） | **14 ファイル検査、指摘 0 件** |
| ESLint ・ `tsc --noEmit` | **いずれも指摘 0 件** |
| `check:hardcode` | **日本語を含む文字列リテラルはありません** |
| セキュリティレビュー記録の指紋 | `review-records/security-review/feature-issue-183-quota-mock-parity.md` の指紋 `dcbf9a6b6f03f749` を、`git diff --cached -- . ':(exclude)review-records/'` で再計算し、一致を確認しました |
| 絵文字 | ありません（差分 11 ファイルを走査） |
| ですます調 | 満たします。だ・である調の混入はありません |
| 秘匿値 | ありません（gitleaks 済み。後述） |

## issue #183 の受け入れ条件

| # | 受け入れ条件 | 判定 | 実測 |
|---|---|---|---|
| 1 | 上限到達の画面が、モックと一致します。または、一致させない項目について理由が記録に残ります | **満たします** | モック（`app-ui/degraded.html` 30〜60行目）の3行「消費の帰属」「状態 — confirmed（完了により確定）」「返還される場合」のうち、前後2行は既存実装のまま一致。3行目「状態」は、**上限到達の元になった消費（`QuotaConsumption`）の実際の状態が `confirmed` のときだけ**出す実装にし（`src/frontend/src/components/exceptions/index.tsx` の `QuotaPanel`）、`reserved`（前の生成が進行中で未完了）のときに出さない理由を、実装のコメント・PR本文・本記録の3か所に明記しました。「本日の結果を見る」も同様に `confirmed` のときだけ出し、押した先（`/requests/:id/result`）は既存の結果画面（`RequestStatus.tsx` の `Actions` と同一の遷移先）に揃えています |
| 2 | 使われていない文言が残りません | **満たします** | 追加した3つの文言キー（`exceptions.labels.quotaStateHeading` ・ `quotaStateConfirmed` ・ `resultLink`）は、いずれも `QuotaPanel` から参照されています（`git grep` で1件ずつ確認）。`npm run check:hardcode` も「日本語を含む文字列リテラルはありません」で通過しています |
| 3 | `app-ui/` を書き換えていません | **満たします** | 上記のとおり、差分に `app-ui/` は含まれません |

## 実装の判断（PR本文の記載との突き合わせ）

- **「reserved のときは状態行を出さない」という判断が、実装・テストの両方に反映されています。** `src/backend/spec/requests/api/v1/prompt_requests_spec.rb` の「本日の枠を使い切っている場合」ブロックに、`status` が `reserved` として返ること・`result_prompt_request_id` キー自体が無いことを固定するテストがあります。フロントエンド側も `exceptions.test.tsx` に、`status="reserved"` のとき行と導線がどちらも出ないことを確かめるテストがあります
- **`confirmed` のときの実測も、契約からReactの描画まで一気通貫で確かめています。** `Quota::Reservation` の単体テスト（`reservation_spec.rb`）で `ExhaustedError#status` ・ `#prompt_request_id` を、API リクエストテストで `details.status` ・ `details.result_prompt_request_id` を、コンポーネントテストで実際に表示される文言と `<a href="/requests/42/result">` を、それぞれ確認しています
- **他人の情報の露出が無いことを確認しました。** `result_prompt_request_id` の取り出し元は常に `current_user` に属する `QuotaConsumption` で、遷移先の `GET /api/v1/prompt_requests/:id` も既存の `owned` スコープで別途守られています（詳細はセキュリティレビュー記録）
- **422 forbidden_input の `details.prompt_request_id` という既存の前例に倣った拡張であり、新しい設計を持ち込んでいません。** 結果画面への導線も、`RequestStatus.tsx` の `Actions` コンポーネントと同じ `Button variant="outline" icon="chevron-right" href` の形をそのまま使っています（車輪の再発明の回避）

## 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | 満たします（個人名の記載なし。連絡先の記載もなし＝対象外） |
| OWASP10 | 満たします（セキュリティレビュー記録のとおり。認可・入力検証・注入・秘匿値のいずれも新規の懸念なし） |
| QC10 | 満たします（既存のアクセシブルな `Button` ・ `Link` コンポーネントを再利用しており、新規のマークアップ上の懸念なし） |
| CRAP | 満たします（`.factsRow` ・ `.actions` の既存スタイルを再利用し、既存の結果画面の導線と見た目をそろえています＝反復・整列） |
| DP | 満たします（YAGNI：`reserved` 用の新しい文言を作らず行自体を省略。DRY：`Button` と `prompt_request_id` の前例を再利用。Fail Fast：既存の `resetAt` の厳格な検査は維持しつつ、新規の任意項目は妥当な範囲で緩やかに扱っています） |
| TM | 満たします（TDD：モデル層→API層→コンポーネント層の3層でテストを追加し、各層とも赤→緑を確認したうえで実装。全体テスト実行で回帰なしを確認） |

## 指摘

指摘はありません。

## 判定

合格

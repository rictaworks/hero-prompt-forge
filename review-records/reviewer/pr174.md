# reviewer 記録 : PR #174（issue #70 ・ #71 ・ #152 ・ #72 ・ #73 ・ #74 ・ #75 ・ #76 ・ #77 画面 02〜09 を実装する）

- 実施日時（JST）：2026-08-27 08:40
- 対象：1 回目 `bcbd499`（土台は `main` = `84cbe4a`）
- 作業ツリー：`DELETE/review-worktrees/pr174-20260827-reviewer`（`--detach` 済みです。実装側の作業ツリーは読み取りのみです）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 自動検査 | **5 ジョブすべて pass** |
| PR の差分 | **46 ファイル** |
| RSpec | **1790 例、失敗 0 件** |
| RuboCop | **196 ファイル、指摘 0 件** |
| Jest | **88 例、失敗 0 件** |
| ESLint ・ 型検査 ・ ハードコードの検査 | **いずれも指摘 0 件** |
| Playwright（`--workers=1`） | **24 例すべて緑。17.8 秒** |
| Playwright（設定どおりの並列） | **2 回流して 2 回とも落ちました**（重大 8） |
| `alert()` 等 | **1 か所もありません** |
| `app-ui/` ・ `requirements.md` | **1 文字も書き換えていません** |

### バックエンドの契約変更（返す範囲を広げていないか）

**広がっていません。** 案の識別子・評価メモ・`GET /api/v1/session` のいずれも、すでに見えていた範囲の中です。

### reCAPTCHA の分かれ方

**いま動いているものは、環境だけで決まります。** ただし**それを守るテストがありません**（要修正 9）。

### 変異試験（13 種）

| # | 変異 | 結果 |
|---|---|---|
| M1 | 上部バーの並びへ `admin` を足します | **赤** |
| M2 | 開発向けの近道として `/admin` への入口を外枠へ足します | **生き残ります** |
| M3 | 本番で鍵が無いときの失敗を外します | **赤** |
| M4 | 環境ではなく別の変数で Bot 対策を切ります | **生き残ります** |
| M5 | 呼び出しの `credentials` を `include` へ変えます | **生き残ります** |
| M6 | 日時の帯を `UTC` へ変えます | **生き残ります** |
| M7 | 評価メモを、記録の無い案にも付けます | **赤** |
| M8 | ログイン中の利用者へ X のユーザー ID を足します | **赤** |
| M9 | 上部バーの管理の表示名を別の語へ変えて並べます | **赤** |
| M10 | 選択肢の呼び名を 1 つ落とします | **生き残ります。実行時に画面全体が落ちます** |
| M11 | お名前の書き方の例を 3 つから 1 つへ減らします | Playwright だけが赤 |
| M12 | 上限到達の次回リセット時刻を出さないようにします | **生き残ります** |
| M13 | 差し戻しの理由を出さないようにします | **生き残ります** |

**13 種のうち 6 種が生き残りました。** そのうち 5 種は受け入れ条件そのものに当たる箇所です。

## 指摘

### 重大

- **重大 1：`.env.example` に、取り込みの衝突の印がそのまま入っています。** PR #170 のレビューで直した `E2E_BASE_URL` が壊れています。
- **重大 2：`app-ui/degraded.html` と見た目が一致しません（issue #76）。** 節の見出し・次回リセットの添え書き・3 行の素性・「本日の結果を見る」・縮退の案の見出しと本文の引用・**差し戻しの入力の写しと下線**・「入力を修正する」が実装にありません。**見出しだけが宙に浮きます。** API が返さないために出せない項目は、**出せない理由を PR 本文へ書き分けてください。**
- **重大 3：「同じ条件で作る」が、同じ条件になりません（issue #70 ・ #72）。** 引き継ぐのは業種とスタイル系統だけです。**API が入力条件を返しませんので、いまの契約では復元できません。** 契約へ `inputs` を足すか、文言を実際の働きに合わせてください。

### 要修正

- **要修正 4：`GET /api/v1/session` が `SPEC/api/README.md` にありません。**
- **要修正 5：選択肢の並びが両側で一致することを、どのテストも検めません。**
- **要修正 6：選択肢の呼び名を 1 つ落とすと、画面全体が落ちます。** 逃がす先（`error.tsx`）がありません。
- **要修正 7：上限到達と差し戻しの画面に、テストが 1 件もありません。**
- **要修正 8：ブラウザ操作の確認が、設定どおりに流すと落ちます。** 順序への依存があります。
- **要修正 9：Bot 対策の分かれ方を、環境以外で切り替える抜け道を足しても、検査が緑です。**
- **要修正 10：開発向けの近道を画面へ足しても、どのテストも捕まえません（issue #77）。**
- **要修正 11：Bot 対策の読み込みが一度失敗すると、二度と取り直せません。**
- **要修正 12：本文の写し取りが、安全でない場所では静かに壊れます。**
- **要修正 13：生成中（04）の見出しが、終わった状態でも「Generating」のままです。**

### 提案

- **提案 14**：使われていない文言と CSS があります。
- **提案 15**：`error={name === "" ? undefined : undefined}` は、どちらへ転んでも同じ値です。
- **提案 16**：サイト名の `maxLength={100}` が直書きです。
- **提案 17**：`" / "` の区切りが直書きです。
- **提案 18**：履歴・一覧（02）の 2 つの枠に、目に見える見出しがありません。
- **提案 19**：結果 3 案（05）の PARAMETERS に、機械の鍵がそのまま出ます。
- **提案 20**：`ErrorNotice` の見出しが、API の符号そのものです。
- **提案 21**：評価メモ（06）の本文の抜粋が、途中の行で切れて見えます。
- **提案 22**：`QuotaPanel` の逃がし方が、契約上起きない場合のフォールバックです。
- **提案 23**：状態を変える経路への守りの重ね掛けをご検討ください。
- **提案 24**：`exceptions.module.css` の注記が実態と合っていません。

## 判定

要修正

**受け入れ要件 24 項目のうち、8 項目を満たし、13 項目が一部、3 項目を満たしません。**

**マージを止める理由は 3 つです。** `.env.example` の衝突の印、issue #76 の見た目の不一致、「同じ条件で作る」が同じ条件にならないことです。

**併せて、守りの薄さが目立ちます。** 13 種の変異のうち 6 種が生き残り、そのうち 5 種は受け入れ条件そのものに当たる箇所でした。

**重大 1〜3 と要修正 4〜13 を本 PR で直していただければ、再度拝見します。** 提案 14〜24 は、この PR での対応を求めません。

連絡先は info@rictaworks.jp です。

---

## 2 回目

- 実施日時（JST）：2026-08-27 21:12
- 対象：`68fc03c`（`3158f55` ・ `68fc03c` の 2 コミットでの対応後）
- 作業ツリー：`DELETE/review-worktrees/pr174c-20260827-reviewer`（`--detach` 済みです。実装側の作業ツリー `DELETE/impl-71` は読み取りのみで、書き換えていません）

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 自動検査 | **5 ジョブすべて pass**（run `33028031709`） |
| PR の差分 | **51 ファイル**（1 回目は 46 ファイルでした） |
| RSpec | **1799 例、失敗 0 件** |
| RuboCop | **197 ファイル、指摘 0 件** |
| Jest | **15 ファイル・132 例、失敗 0 件**（1 回目は 88 例でした） |
| ESLint ・ 型検査 ・ ハードコードの検査 | **いずれも指摘 0 件** |
| Playwright（**設定どおり**の実行） | **`test/pr174` は 36 例、`test/pr170` と併せて 47 例。緑になることを確かめました**（下記の申し送りをご覧ください） |
| `alert()` 等 | **1 か所もありません** |
| `app-ui/` ・ `requirements.md` | **1 文字も書き換えていません** |

### 重大 1〜3 の再検証

**重大 1：直りました。** `.env.example` は `main` と 1 バイトも違いません（差分なし）。`E2E_BASE_URL` は 1 つで、値は `http://localhost:3000` です。**このファイルは PR の差分からも外れました。** 取り込みの印はリポジトリ全体にありません。

**重大 2：おおむね直りましたが、2 点残ります。**

直ったことを実測しました。

| モックの要素 | 実測 |
|---|---|
| 節の見出し | **出ます。** 受け取り済みは `PACKAGE` ／ `Package Ready`、差し戻し・失敗は `EXCEPTIONS` ／ `Limits & Fallbacks` で、添え書きも付きます |
| 次回リセットの添え書き | **出ます。** `NEXT RESET` ／ `03:00` ／ `JST / 2026/08/28 03:00` |
| 縮退の案の見出しと本文の引用 | **出ます。** `REQUEST #1 / VARIATION 01` ・ `被写体主導` ・ `v2026.08.4` ・ `NOT REFINED` と、組み立てた本文の引用 |
| 差し戻しの入力の写しと下線 | **出ます。** 送った文章がそのまま出て、`有名人` に印が付きます |
| 「入力を修正する」 | **出ます。** 押すと差し戻しの表示が消えます |

残るのは次の 2 点です。

- **上限到達の素性が 2 行です。** モック（`app-ui/degraded.html`）は 3 行で、**「状態 — confirmed（完了により確定）」がありません。** 実装は「消費の帰属」と「返還される場合」だけです。
- **「本日の結果を見る」が出ません。** 文言 `exceptions.labels.resultLink` を `src/frontend/src/strings/ja.ts` へ足しながら、**どの画面からも呼んでいません**（実装側の全文検索で 0 件）。1 回目に「API が返さないために出せない項目は、出せない理由を PR 本文へ書き分けてください」と申し上げましたが、**PR 本文は 1 回目のままです。**

**重大 3：直りました。** 実測です。

- `GET /api/v1/prompt_requests/1` が `inputs` を返します。**一覧（`GET /api/v1/prompt_requests`）には載りません。**
- `/requests/new?project_id=1&request_id=1` を開くと、**業種 `medical` ・ スタイル系統 `photoreal` ・ 生成モデル `midjourney` ・ トーン `trust` ・ サービス概要「「あおぞら歯科」という歯科医院です。」・余白の位置 `left` ・ 画角 `16:9` ・ 保存先のプロジェクト `1`** が、そのまま入ります。
- 一覧の「同じ条件で作る」の行き先が `/requests/new?project_id=…&request_id=…` になっています。
- 差し戻した記録（`id` 2）の `inputs` に `service_summary` が残っていないことも確かめました。`SPEC/api/README.md` の記述と実際が一致します。

### 要修正 4〜13 の再検証

| # | 判定 | 根拠（実測） |
|---|---|---|
| 4 | **直りました** | `SPEC/api/README.md` に経路と節が入りました。実応答は `{"display_name":"@rictaworks","plan":"active"}` で、**X のユーザー ID を返しません** |
| 5 | **直りました** | `src/backend/spec/config/input_choices_contract_spec.rb`。画面側の定義を読み取って比べます。読み取れない書き方に変わった場合も落ちます |
| 6 | **直りました** | 呼び名を 1 つ落とすと Jest が赤。画面は `src/frontend/src/app/error.tsx` が受け、**「想定していない問題が起きました。」と「もう一度読み込む」**を出します（ブラウザで実測） |
| 7 | **直りました** | `exceptions.test.tsx` 21 例と、ブラウザ操作 7 例（差し戻し 4 ・ 上限到達 3） |
| 8 | **直りました** | `playwright.config.ts` を `fullyParallel: false` ・ `workers: 1` にし、**設定どおりの実行で 47 例が緑**になることを実測しました |
| 9 | **直りました** | 4 つの別の環境変数を立てても切り替わりません |
| 10 | **直りました** | `/admin` で始まる入口が 1 つも無いことを 4 画面で確かめます |
| 11 | **直りました** | 失敗したら持ち回しを解きます。取り直せることを Jest で固定しました |
| 12 | **直りました** | 呼び出しの手前で受け止めます（`navigator.clipboard` そのものが無い場合） |
| 13 | **直りました** | `Package Ready` ／ `Limits & Fallbacks`。**終わった状態で `Generating` が出ないことも固定しました** |

### 変異試験

**1 回目に生き残った 7 種を流し直し、今回の直しに対する 2 種を足しました。**

| # | 変異 | 結果 |
|---|---|---|
| M2 | 開発向けの近道として `/admin` への入口を外枠へ足します | **赤（殺せました）** |
| M4 | 環境ではなく別の変数で Bot 対策を切ります | **赤** |
| M5 | 呼び出しの `credentials` を `include` へ変えます | **生き残ります**（Jest 132 例すべて緑） |
| M6 | 日時の帯を `UTC` へ変えます | **赤** |
| M10 | 選択肢の呼び名を 1 つ落とします | **赤。** あわせて画面全体が落ちなくなりました |
| M12 | 上限到達の次回リセット時刻を出さないようにします | **赤** |
| M13 | 差し戻しの理由を出さないようにします | **赤** |
| M14 | 取り出しの契約から `inputs` を落とします | **生き残ります**（RSpec 1799 例すべて緑） |
| M15 | 一覧の「同じ条件で作る」から `request_id` を落とします | **生き残ります**（Jest 132 例すべて緑。ブラウザ操作にも例がありません） |

**1 回目の 7 種のうち 6 種を殺せました。** 残る 1 種（M5）と、今回足した 2 種（M14 ・ M15）が生き残ります。

### 受け入れ条件

| issue | 判定 | 備考 |
|---|---|---|
| #70 | **満たします** | 複製が実際に条件を引き継ぎます。縮退の印も出ます |
| #71 | **満たします** | 画面で止まり、差し戻しの理由・印・修正の導線が出ます。`alert()` を使いません |
| #152 | **満たします** | 案内・例 3 つ・設定ファイルからの読み出し・ブラウザ操作の例。読みが決まらない名前の告知は issue #51 の実装で担保されます |
| #72 | **満たします** | 状態に応じて表示と見出しが変わり、失敗時に作り直しの導線が出ます |
| #73 | **満たします** | 3 案・コピー・縮退の印・推奨パラメータの呼び名 |
| #74 | **満たします** | 上限到達中でも記録できることは RSpec が担保します |
| #75 | **満たします** | 保存と呼び出しをブラウザ操作で確かめます |
| #76 | **一部です** | 次回リセット時刻・曖昧なエラーを出さないことは満たします。**モックとの一致に 2 点残ります**（上記の重大 2） |
| #77 | **満たします** | 変異 M2 を殺せることまで確かめました |

### 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | **適合します** |
| OWASP10 | **適合します** |
| QC10 | **おおむね適合します。** 後戻りを止める仕組みに抜けがあります（M5 ・ M14 ・ M15） |
| CRAP | **おおむね適合します。** 重大 2 の残りと、提案 F |
| DP | **おおむね適合します。** 提案 E |
| TM | **おおむね適合します。** 上と、ブラウザ操作の揺れ |

### プロジェクト固有の決まり

実装は `src/` の中、開発用のものは外にあります。フォールバックを置かず、無い値はその場で失敗させます（`MissingResetAtError`）。デバッグトレースが追えます。グローバル変数はありません（`let` の持ち回しを入れ物へ収めました）。文字列は設定へ分離され、検出のテストが緑です。`alert()` 等を使いません。環境で分岐し、開発向けの近道を本番の画面へ出しません。フォロワー判定を自作していません。秘匿値をソースへ書いていません。ですます調です。日本語版のみです。絵文字がなく、アイコンは Font Awesome です。時刻は JST、ファイルは UTF-8 です。

### 新しい指摘

#### 要修正

- **要修正 A：重大 3 の直しに、後戻りを止めるテストが 1 つもありません。** 取り出しの契約から `inputs` を落としても（M14）、一覧の「同じ条件で作る」から `request_id` を落としても（M15）、**RSpec 1799 例・Jest 132 例・ブラウザ操作 36 例のすべてが緑のままです。** 受け入れ条件（#70「過去案の再表示・複製ができます」）そのものに当たる箇所です。
- **要修正 B：PR 本文が 1 回目のままです。** 申し送りに**いまは誤りである記述**が 2 つ残ります。「`.env.example` に取り込みの跡が残っています。マージの前に直す必要があります。」（すでに直っています）と、「`--workers=1` で実行してください。並列のままだと落ちます。」（設定そのものを直しましたので、この指示は要りません）です。件数も古いままです（**46 ファイル → 51 ファイル、Jest 88 例 → 132 例、ブラウザ操作 24 例 → 36 例**）。あわせて、**`test/pr174/screens.spec.ts` の手順 23〜29 に対応する利用者向けの手順が本文にありません**（本文は 22 までです）。`test/README.md` は 1 対 1 の対応を求めます。

#### 提案

- **提案 C：送信のボタンが、画面の組み上がる前に押されると、素の GET 送信になります。** 実測で `/requests/new?` へ遷移し、**入れた内容が黙って消えました。** 入力の欄に `name` が無いため、送信そのものは何も運びません。組み上がるまでボタンを押せなくする形をご検討ください。**ブラウザ操作の確認が時折落ちるのも、これが原因です。**
- **提案 D：使われない文言が 3 つ増えました。** `exceptions.labels.resultLink` ・ `requestStatus.labels.degradedQuote` ・ `requestStatus.labels.openResultPlain` です。1 回目の提案 14 と同じ形です。
- **提案 E：`resetLoader()` が本番の束にも入ります。** テスト専用の抜け道を外へ出さない形をご検討ください。`Object.assign(loader, new Loader())` も、読み手を驚かせます。
- **提案 F：`src/frontend/src/app/error.tsx` の「もう一度読み込む」が素の `<button>` です。** ほかの画面は `Button` を使います。逃がし先だけ見た目が変わります。
- **提案 G：ハードコードの検査に除外の一覧（`FROM_MOCK`）が入りました。** いまは 1 件で、根拠も書かれています。**増やす前に、押しものの文言だけを別の入れ物へ分ける形をご検討ください。**

### 申し送り（ブラウザ操作の確認の揺れ）

**設定どおりに 8 回流しました。4 回が全緑、4 回で 1〜6 例が時間切れになりました。** 落ちた例はいずれも**確かめている中身の誤りではなく、待ちの不足**です。内訳は、初回の組み立て待ち（画面を初めて開いたとき）と、**画面が組み上がる前にボタンを押したこと**（提案 C）です。落ちた例だけを流し直すと必ず緑になります（`いまの条件を保存できます` は 3 回連続で緑、`差し戻しの理由と直し方が出ます` も緑）。

**同じ機械で他の検査が同時に動いていました**（load average 5〜11、別の作業ツリーの `next build` と `rspec`）。**1 回目に指摘した順序への依存（要修正 8）は解消しています。** 残るのは待ちの不足です。

連絡先は info@rictaworks.jp です。

## 判定

条件付き合格

**重大 1 ・ 2 ・ 3 と要修正 4〜13 のうち、11 件が直り、重大 2 に 2 点が残ります。** 残る 2 点（上限到達の素性の 1 行と「本日の結果を見る」）は、**いずれも情報を足す向きの不足で、誤った案内を出すものではありません。** 上限到達の画面は、次回のリセット時刻・消費の帰属・上限に関係なく続けられることを、すでに正しく伝えています。**マージを止める理由には届きません。**

**残件は次の Issue へ回してください。**

- **重大 2 の残り 2 点**：上限到達の素性の「状態」と、「本日の結果を見る」。出さない場合は、**出せない理由を PR 本文か記録へ書き分けてください。**
- **要修正 A**：重大 3 の直しに、後戻りを止めるテストを足してください（M14 ・ M15）。あわせて M5 もご検討ください。
- **提案 C ・ D ・ E ・ F ・ G**、および 1 回目の提案 20 ・ 21 ・ 23。

**要修正 B（PR 本文）は、マージの前に直してください。** 申し送りにいまは誤りである記述が残っており、**そのまま読むと「取り込みの跡が残ったままマージした」と受け取られます。** 本文の整備は pr-checker の担当です。

連絡先は info@rictaworks.jp です。

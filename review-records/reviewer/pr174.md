# reviewer 記録 : PR #174（issue #70 ・ #71 ・ #152 ・ #72 ・ #73 ・ #74 ・ #75 ・ #76 ・ #77 画面 02〜09 を実装する）

- 実施日時（JST）：2026-08-27 08:40
- 対象：1 回目 `bcbd499`（土台は `main` = `84cbe4a`）
- 作業ツリー：`DELETE/review-worktrees/pr174-20260827-reviewer`（`--detach` 済みです。実装側の作業ツリーは読み取りのみです）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`CLEAN`** |
| 自動検査 | **5 ジョブすべて pass** |
| PR の差分 | **46 ファイル** |
| RSpec | **1790 例、失敗 0 件** |
| RuboCop | **196 ファイル、指摘 0 件** |
| Jest | **88 例、失敗 0 件** |
| ESLint ・ 型検査 ・ ハードコードの検査 | **いずれも指摘 0 件** |
| Playwright（`--workers=1`） | **24 例すべて緑。17.8 秒** |
| Playwright（設定どおりの並列） | **2 回流して 2 回とも落ちました**（重大 8） |
| `alert()` 等 | **1 か所もありません** |
| `app-ui/` ・ `requirements.md` | **1 文字も書き換えていません** |

### バックエンドの契約変更（返す範囲を広げていないか）

**広がっていません。** 案の識別子・評価メモ・`GET /api/v1/session` のいずれも、すでに見えていた範囲の中です。

### reCAPTCHA の分かれ方

**いま動いているものは、環境だけで決まります。** ただし**それを守るテストがありません**（要修正 9）。

### 変異試験（13 種）

| # | 変異 | 結果 |
|---|---|---|
| M1 | 上部バーの並びへ `admin` を足します | **赤** |
| M2 | 開発向けの近道として `/admin` への入口を外枠へ足します | **生き残ります** |
| M3 | 本番で鍵が無いときの失敗を外します | **赤** |
| M4 | 環境ではなく別の変数で Bot 対策を切ります | **生き残ります** |
| M5 | 呼び出しの `credentials` を `include` へ変えます | **生き残ります** |
| M6 | 日時の帯を `UTC` へ変えます | **生き残ります** |
| M7 | 評価メモを、記録の無い案にも付けます | **赤** |
| M8 | ログイン中の利用者へ X のユーザー ID を足します | **赤** |
| M9 | 上部バーの管理の表示名を別の語へ変えて並べます | **赤** |
| M10 | 選択肢の呼び名を 1 つ落とします | **生き残ります。実行時に画面全体が落ちます** |
| M11 | お名前の書き方の例を 3 つから 1 つへ減らします | Playwright だけが赤 |
| M12 | 上限到達の次回リセット時刻を出さないようにします | **生き残ります** |
| M13 | 差し戻しの理由を出さないようにします | **生き残ります** |

**13 種のうち 6 種が生き残りました。** そのうち 5 種は受け入れ条件そのものに当たる箇所です。

## 指摘

### 重大

- **重大 1：`.env.example` に、取り込みの衝突の印がそのまま入っています。** PR #170 のレビューで直した `E2E_BASE_URL` が壊れています。
- **重大 2：`app-ui/degraded.html` と見た目が一致しません（issue #76）。** 節の見出し・次回リセットの添え書き・3 行の素性・「本日の結果を見る」・縮退の案の見出しと本文の引用・**差し戻しの入力の写しと下線**・「入力を修正する」が実装にありません。**見出しだけが宙に浮きます。** API が返さないために出せない項目は、**出せない理由を PR 本文へ書き分けてください。**
- **重大 3：「同じ条件で作る」が、同じ条件になりません（issue #70 ・ #72）。** 引き継ぐのは業種とスタイル系統だけです。**API が入力条件を返しませんので、いまの契約では復元できません。** 契約へ `inputs` を足すか、文言を実際の働きに合わせてください。

### 要修正

- **要修正 4：`GET /api/v1/session` が `SPEC/api/README.md` にありません。**
- **要修正 5：選択肢の並びが両側で一致することを、どのテストも検めません。**
- **要修正 6：選択肢の呼び名を 1 つ落とすと、画面全体が落ちます。** 逃がす先（`error.tsx`）がありません。
- **要修正 7：上限到達と差し戻しの画面に、テストが 1 件もありません。**
- **要修正 8：ブラウザ操作の確認が、設定どおりに流すと落ちます。** 順序への依存があります。
- **要修正 9：Bot 対策の分かれ方を、環境以外で切り替える抜け道を足しても、検査が緑です。**
- **要修正 10：開発向けの近道を画面へ足しても、どのテストも捕まえません（issue #77）。**
- **要修正 11：Bot 対策の読み込みが一度失敗すると、二度と取り直せません。**
- **要修正 12：本文の写し取りが、安全でない場所では静かに壊れます。**
- **要修正 13：生成中（04）の見出しが、終わった状態でも「Generating」のままです。**

### 提案

- **提案 14**：使われていない文言と CSS があります。
- **提案 15**：`error={name === "" ? undefined : undefined}` は、どちらへ転んでも同じ値です。
- **提案 16**：サイト名の `maxLength={100}` が直書きです。
- **提案 17**：`" / "` の区切りが直書きです。
- **提案 18**：履歴・一覧（02）の 2 つの枠に、目に見える見出しがありません。
- **提案 19**：結果 3 案（05）の PARAMETERS に、機械の鍵がそのまま出ます。
- **提案 20**：`ErrorNotice` の見出しが、API の符号そのものです。
- **提案 21**：評価メモ（06）の本文の抜粋が、途中の行で切れて見えます。
- **提案 22**：`QuotaPanel` の逃がし方が、契約上起きない場合のフォールバックです。
- **提案 23**：状態を変える経路への守りの重ね掛けをご検討ください。
- **提案 24**：`exceptions.module.css` の注記が実態と合っていません。

## 判定

要修正

**受け入れ要件 24 項目のうち、8 項目を満たし、13 項目が一部、3 項目を満たしません。**

**マージを止める理由は 3 つです。** `.env.example` の衝突の印、issue #76 の見た目の不一致、「同じ条件で作る」が同じ条件にならないことです。

**併せて、守りの薄さが目立ちます。** 13 種の変異のうち 6 種が生き残り、そのうち 5 種は受け入れ条件そのものに当たる箇所でした。

**重大 1〜3 と要修正 4〜13 を本 PR で直していただければ、再度拝見します。** 提案 14〜24 は、この PR での対応を求めません。

連絡先は info@rictaworks.jp です。

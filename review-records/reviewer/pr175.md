# reviewer 記録 : PR #175（issue #65 ・ #66 ・ #67 ・ #68 管理画面）

- 実施日時（JST）：2026-08-27 09:15
- 対象：1 回目 `200e3a1`（土台は `main` = `84cbe4a`）
- 作業ツリー：`DELETE/review-worktrees/pr175-20260827-reviewer`（`--detach` 済みです。実装側の作業ツリーは読み取りのみです）
- 検証用のデータベース：`hero_prompt_forge_r175`（共用の検査用データベースと分けました）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** ／ `mergeStateStatus` は **`UNSTABLE`** |
| 自動検査 | **5 ジョブ中 4 ジョブが pass、1 ジョブが fail**（バックエンド） |
| CI での RSpec | **1835 例、5 失敗** |
| 開発機での RSpec | **1835 例、失敗 0 件** |
| 管理画面の RSpec | **54 例、失敗 0 件** |
| RuboCop | **203 ファイル、指摘 0 件** |
| ハードコードの検査 | **検出 0 件**（ただし `.erb` は対象外です。要修正 3） |
| PR の差分 | **24 ファイル。スコープ外の変更はありません** |
| `raw` / `html_safe` / `sanitize` | **1 か所もありません** |
| 移行 | **1 件。`admin_actions` を新しく作るだけです。既存の表に触れません** |

### 自動検査が赤い理由

落ちる 5 例は、いずれも **issue #66 の受け入れ条件そのものを検める例**です。`KeyError: key not found: "FOLLOWER_GATE_BASE_URL"` です。**開発機では緑になり、CI では赤になります。**

### 各 issue の受け入れ条件

| issue | 判定 |
|---|---|
| #65（規則辞書） | **3 項目とも満たします。** 編集内容が生成へ届くところまで通しで確かめています |
| #66（利用者とプラン値） | **証明されていません。** 実装はありますが、検める 5 例が CI で落ちます |
| #67（クォータの手動リセット） | **3 項目とも満たします** |
| #68（利用状況） | **一部です。** 内部の識別子については守りがありません |

### 変異試験（15 種）

**15 種のうち 6 種が生き残りました。**

| # | 変異 | 結果 |
|---|---|---|
| M3 ・ M3b | 読めない JSON を通します | **赤** |
| M4 ・ M4b | 公開済みの版を書き換えます | **赤** |
| M5 | 手動リセットの印を残しません | **赤** |
| M6 | 記録の実施者を決め打ちにします | **赤** |
| M7 | **要求の偽装の守りを外します** | **生き残ります** |
| M8 | **プラン値を書き換える別の経路を足します** | **生き残ります** |
| M9 | **集計へ内部の識別子の軸を足します** | **生き残ります** |
| M10 | 定義に無い軸を足します | **赤** |
| M11 | **「記録していない軸」の断り書きを消します** | **生き残ります** |
| M12 | **通らなかった資格情報の名前も控えます** | **生き残ります** |
| M14 | 手動リセットを定めの遷移だけで行います | **赤（6 例）** |
| — | **`.erb` へ日本語を直書きします** | **生き残ります** |

M14 が赤くなったことは**良い知らせです。** 専用の入口が要ることが、実測で裏づけられました。

### `reset_by_admin!` は妥当か

**考え方は妥当です。** 定め自体を緩めず、専用の入口を 1 つだけ設けた判断を支持します。**足りないのは、その入口の門番です**（要修正 1）。

### 移行は後方互換か

**後方互換です。** 足すのは 1 表だけで、既存の表に触れません。段階適用に耐えます。

## 指摘

### 未達

- **未達 1：自動検査が赤いままです。issue #66 の受け入れ条件が、自動検査で証明されていません。** `FollowerGateClient#initialize` の既定引数が環境変数を引きます。**環境変数の有無で結果が変わるテストにしないでください。** 併せて、PR 本文の「失敗 0 件」を実態に合わせてください。

### 要修正

- **要修正 1：手動リセットが、生成中の枠にも当たります。** `reserved`（ジョブが動いている最中）の枠をリセットすると、**成果物は届くのに、ジョブは失敗のまま残ります。** 門番を入口へ足してください。
- **要修正 2：読めない JSON を弾いたとき、書いていただいた内容を丸ごと捨てます。** 隣の経路とも扱いが揃っていません。
- **要修正 3：ハードコードの検査が `.erb` を見ていません。** この PR は 9 枚の `.erb` を足しました。**いまは正しく書かれているからこそ、後戻りを止める仕掛けが要ります。**
- **要修正 4：手動リセットの有無に「公開済み」と出ます。** 版の状態のための文言を流用しています。
- **要修正 5：この PR が足した守りのうち、5 つに後戻りを止める押さえがありません。** 少なくとも M7（要求の偽装）・M8（プラン値の経路）・M9（内部の識別子）は、この PR で塞いでください。

### 提案

- **提案 1**：記録の日時に年がありません。**監査の記録ですので、年を入れてください。**
- **提案 2**：規則辞書の公開の記録が、どの画面にも出ません。
- **提案 3**：画面に機械の値がそのまま出ます。開発者だけが見る画面ですので、対応を求めません。
- **提案 4**：`admin.headings.user` が、どこからも引かれていません。
- **提案 5**：見出しと表の行が、同じ文言を二重に使っています。
- **提案 6**：一覧に区切りがありません。
- **提案 7**：利用状況の集計に、期間の絞り込みがありません。
- **提案 8**：いま使う版が無いときに、白紙の下書きを黙って出します。
- **提案 9**：一般の画面から `/admin` へ繋がないことを固定するテストがあると安心です。
- **提案 10**：すでに返還済みの枠にも「リセットしました」と答えます。

## 判定

要修正

**#65 と #67 は受け入れ条件をすべて満たします。** 実装の考え方はいずれも筋が通っており、変異試験でも要る仕掛けであることが裏づけられました。移行は後方互換で、XSS の守りは行き届いており、スコープ外の変更もありません。

**それでも、いまはマージできません。**

**第一に、自動検査が赤いままです**（未達 1）。**第二に、生成中の枠を手動リセットすると、動いているジョブが決着できなくなります**（要修正 1）。**第三に、この PR が足した守りのうち 5 つが、変異試験で生き残ります**（要修正 5）。

**未達 1 と要修正 1 ・ 2 ・ 3 ・ 4、および要修正 5 のうち M7 ・ M8 ・ M9 を直していただければ、合格とします。**

連絡先は info@rictaworks.jp です。

---

## 2 回目

- 実施日時（JST）：2026-08-27 09:59
- 対象：`facb436`（土台は `main` = `84cbe4a`）
- 1 回目からの積み増し：`6b0349c` ・ `facb436` の 2 つ。**12 ファイル、351 行の追加と 15 行の削除**です
- 作業ツリー：`DELETE/review-worktrees/pr175b-20260827-reviewer`（`--detach` 済みです）
- 検証用のデータベース：`hero_prompt_forge_r175b` ・ `hero_prompt_forge_r175b_srv`

### 実測

| 観点 | 1 回目 | 2 回目 |
|---|---|---|
| `mergeable` | `MERGEABLE` ／ **`UNSTABLE`** | **`MERGEABLE`** ／ **`CLEAN`** |
| 自動検査 | 5 ジョブ中 **1 ジョブが fail** | **5 ジョブすべて pass** |
| CI での RSpec | **1835 例、5 失敗** | **1850 例、失敗 0 件** |
| 管理画面の RSpec | 54 例 | **68 例、失敗 0 件** |
| **判定サービスの環境変数を外した状態** | **落ちます（5 失敗）** | **68 例すべて緑です** |
| RuboCop | 指摘 0 件 | **指摘 0 件** |
| ハードコードの検査 | 検出 0 件（`.erb` は対象外） | **検出 0 件（`.erb` を含みます）** |

**ご報告の「管理画面の 54 例」は 1 回目の数です。** 実測では **68 例**です。**記録の数だけ合わせてください。**

### ご質問への回答

1. **M7 ・ M8 ・ M9 は 3 つとも赤くなります。** 上下どちらへ倒しても、別の書き方へ差し替えても捕まります。
2. **門番は守ります。** 外す変異でも、見る状態を取り違える変異でも赤くなります。**動いているサーバーでも確かめました。**
3. **`.erb` の検査は、地の文と `<% %>` の中の両方を捕まえます。** 注記と「開発者向け」の印は正しく対象外です。**HTML の属性だけは捕まりません**（提案 12）。
4. **自動検査は 5 ジョブとも pass です。**

### 1 回目の指摘への対応

| 指摘 | 判定 |
|---|---|
| **未達 1**（自動検査が赤い） | **直っています。** 3 つの環境変数を外した状態で 68 例が緑です |
| **要修正 1**（生成中の枠にも当たります） | **直っています。** 動いているサーバーで、断られること・状態が変わらないこと・記録が増えないことを実測しました |
| **要修正 2**（書きかけの内容を捨てます） | **直っています。** 3 つの欄とも残り、`"` は逃がされています |
| **要修正 3**（`.erb` を見ていません） | **直っています** |
| **要修正 4**（「公開済み」の流用） | **直っています。** 「なし」→「あり」と変わることを実測しました |
| **要修正 5 / M7 ・ M8 ・ M9** | **3 つとも直っています** |
| **要修正 5 / M11 ・ M12** | **切り出しを受け入れます。** ただし **issue がまだ立っていません** |
| **提案 1**（記録の日時に年） | **対応済み。** `2026年8月27日 09:57` と出ます |
| **追加**（ブラウザで開けません） | **直っています。** 5 つの画面がすべて 200 です |

### 変異試験（14 種）

| # | 変異 | 1 回目 | 2 回目 |
|---|---|---|---|
| M7 ・ M7b | 要求の偽装の守りを外します | 生き残り | **赤（3 例 ・ 5 例）** |
| M8 ・ M8b | プラン値を書き換える経路を足します | 生き残り | **赤（各 1 例）** |
| M9 ・ M9b | 集計へ内部の識別子の軸を足します | 生き残り | **赤（各 1 例）** |
| M15 ・ M16 | 生成中の枠の門番を外す／取り違えます | — | **赤（3 例 ・ 10 例）** |
| M17 | 書きかけの内容を、また空へ戻します | — | **赤（2 例）** |
| M18 | お知らせの仕組みを外します | — | **赤（1 例）** |
| M20 | 経路を 1 本減らします | — | **赤（10 例）** |
| M22 | 判定サービスの差し替えを外します | — | **赤（5 例）** |
| M19 ・ M21 | 記録の書式から年を落とす／見出しを移す（参考） | — | 生き残ります（提案 13 ・ 14） |

**1 回目に生き残った 3 種は、いずれも赤くなりました。** 「その 1 本だけを塞いだ」形ではありません。

### 実際に動いているサーバーでの確認

**開発サーバー（3020 ・ 3021）には触れていません。** 別の番号（3399）と別のデータベースで立て、確認の後に止めました。

| 場面 | 結果 |
|---|---|
| 管理画面の 5 つの入口 | **すべて 200 です** |
| 資格情報なし | **401 です** |
| 印の無い書き込み | **422 です** |
| 版を作った／公開した後のお知らせ | **移った先の画面に出ます** |
| 読めない JSON | **3 つの欄とも残ります。`"` は逃がされています** |
| **生成中の枠へリセット** | **断られます。状態も記録も変わりません** |
| 確定済みの枠へリセット | **通ります。手動リセットの欄が「あり」になります** |
| 管理の操作の記録の日時 | **`2026年8月27日 09:57`。年が入っています** |

### Issue の受け入れ条件

**4 つの issue の 12 項目は、すべて実測で満たされます。** とりわけ issue #66 は、**判定サービスの環境変数を外した状態で緑になること**を確かめました。

**スコープ外の変更はありません。** `ActionDispatch::Flash` の組み込みは、**管理画面が画面として成り立つために要るもの**ですので、範囲内として受け入れます。

### 各基準への適合

| 基準 | 1 回目 | 2 回目 |
|---|---|---|
| CC | 適合 | **適合** |
| OWASP10 | 一部 | **適合** |
| QC10 | 一部 | **適合** |
| CRAP | 一部 | **適合** |
| DP | 一部 | **適合** |
| TM | 一部 | **適合** |

### 指摘

**要修正はありません。**

- **提案 11**：`.erb` の検査そのものに、テストがありません。**せっかく広げた検査が、黙って元へ戻せます。** 見本を 1 枚足していただくだけで塞がります。
- **提案 12**：HTML の属性の中の日本語は捕まりません。**いまの 9 枚に属性の文言はありませんので、急ぎません。**
- **提案 13**：記録の日時の書式に、テストがありません。
- **提案 14**：内訳の見出しを `<td>` へ移すと、識別子の検めが外れます。**意図した割り切りです。**
- **持ち越し**：M11 ・ M12 の切り出しを受け入れます。ただし **issue がまだ立っていません。** 提案 11〜14 と併せて 1 本にまとめ、**必ず立ててください。**

### 後片づけ

削除は行っていません。**手動で片づけていただく必要があるもの**：検証用のデータベース 3 つと、レビュー用の作業ツリー 2 つ、変異試験の駆動スクリプトです。

## 判定

合格

1 回目に挙げた**未達 1 件・要修正 5 件・提案 1 件のすべてが解決しました。**

**1 回目に懸念した「いま動くものは正しいが、黙って戻っても気づけない」は解消しました。** 14 種の変異のうち 12 種が捕まり、残る 2 種はいずれも参考の域です。**直しそのものが効いていることも、M20 ・ M22 で裏づけました。**

**4 つの issue の受け入れ条件 12 項目は、すべて実測で満たされます。** 実際に動いているサーバーでも確かめました。

PR は **`MERGEABLE` ・ `CLEAN`** で、**自動検査 5 ジョブが緑**、RSpec **1850 例・失敗 0 件**、RuboCop **指摘 0 件**、ハードコードの検査 **検出 0 件**です。6 つの基準すべてに適合します。**マージして差し支えありません。**

マージの前後で、**M11 ・ M12 と提案 11〜14 をまとめた issue を必ず立ててください。**

連絡先は info@rictaworks.jp です。

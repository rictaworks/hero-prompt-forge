# reviewer 記録 : PR #180（issue #171 ・ #172 利用規約・プライバシーポリシー・表示への導線を作ります）

- 実施日時（JST）：2026-08-27 21:56
- 対象：1 回目 `2442464`（土台は `main` = `4a64c51`）
- 作業ツリー：`DELETE/review-worktrees/pr180`（`git worktree add --detach` 済みです。実装側の作業ツリー `DELETE/impl-legal` は読み取りのみで、1 文字も書き換えていません）
- 検証用のデータベース：`hero_prompt_forge_test_180`（他のレビューと分けました）

## 1 回目

### 実測

| 観点 | 結果 |
|---|---|
| `mergeable` | **`MERGEABLE`** |
| 土台 | **`main`（`4a64c51`）の上に積み直し済みです。** `main` はその後 `ffa645c`（記録のコミット）へ進んでいますが、`src/` の差はありません |
| PR の差分 | **1 コミット・14 ファイル**（874 行の追加、16 行の削除）。**スコープ外の変更はありません** |
| 自動検査 | **5 ジョブすべて pass**（`33071199984`、`2442464` に対して実行されています） |
| Jest（この作業ツリー） | **152 例、失敗 0 件**（CI の実測と一致します） |
| RSpec（例数） | **1956 例**（`--dry-run` で実測しました。CI も **1956 例・失敗 0 件**です） |
| RSpec（この作業ツリーで実行） | `recaptcha_verifier_spec` ・ `prompt_requests_spec` の **106 例、失敗 0 件**。`human_verification_spec` も緑です |
| 型検査（`tsc --noEmit`）・ ESLint | **いずれも指摘 0 件** |
| 組み立て（`next build`） | **成功**。`/` ・ `/terms` ・ `/privacy` ・ `/commerce` を静的に作ります（静的 8 経路・動的 3 経路） |
| 画面の実測 | `next start` で配って `curl` で確かめました。**ランディング最下部に 3 つの導線**、**`/privacy` に Google の 2 リンク**、**`/terms` に第 1 条〜第 11 条**が出ます |
| 直書きの文言の検査（`check:hardcode`） | **検出 0 件** |
| セキュリティレビュー記録の指紋 | **一致しません**（記録は `baa8d1b4840a6a82`、再計算は `f780601de19c378c`。後述の条件 3） |
| `requirements.md` ・ `app-ui/` | **1 文字も変わっていません** |
| `alert()` / `confirm()` / `prompt()` | **ありません** |
| `dangerouslySetInnerHTML` | **ありません** |
| 絵文字 | **ありません** |
| グローバル変数 | **ありません** |
| 秘匿値の直書き | **ありません** |
| ファイルのエンコード・改行 | **すべて UTF-8 ・ LF です** |

**PR 本文と実装側のセキュリティレビュー記録は「Jest 86 例 ・ RSpec 1783 例」と書いていますが、実測と CI はいずれも「Jest 152 例 ・ RSpec 1956 例」です**（後述の条件 4）。積み直しの前に数えた値が、そのまま残っているものと見られます。

### 法務の本文と実装の突き合わせ

**`src/frontend/src/strings/legal.ts` の記述を、1 項目ずつ実装で確かめました。**

| 本文の記述 | 実装 | 判定 |
|---|---|---|
| 保持する個人関連情報は X のユーザー ID と表示名だけです（プライバシー第 1 条） | `users` 表の列は `x_user_id` ・ `display_name` ・ `plan` ・ `plan_checked_at` ・ `recheck_available_at` のみで、**個人関連情報は 2 つだけです** | **一致します**（requirements.md 5.3） |
| メールアドレス・住所・電話番号は取得しません | 該当する列も入力もありません | **一致します** |
| ほかに保存するもの（プロジェクト・入力条件・3 案・プリセット・評価メモ） | requirements.md 4.3 の付随機能と一致します | **一致します** |
| Cookie は 2 種類です（プライバシー第 3 条） | `hpf_session`（`authenticates_user.rb:19`）と、手続きの途中の値（`auth_controller.rb:9-10` の `hpf_oauth_state` ・ `hpf_oauth_verifier`） | **おおむね一致します**（後述の提案 4） |
| 広告・行動の追跡の Cookie は利用しません | 画面に解析の仕込みがありません（`gtag` ・ `@vercel/analytics` とも不在です） | **一致します** |
| reCAPTCHA v3 を生成の実行で使います（プライバシー第 4 条・利用規約第 7 条） | `verifies_humans.rb` が生成の投入で照合します | **一致します** |
| Gemini へ送るのは英語の語句と指示文だけです | `llm_refiner.rb` は、**日本語を含む素材を外して**指示文と英語の素材だけを渡します（`refinable` が `grep_v(JAPANESE)`） | **一致します**（requirements.md 5.2） |
| `remoteip` を送りません（プライバシー第 5 条） | `app/` に `remote_ip` の参照は 1 件も残っていません。送る項目は `secret` と `response` の 2 つだけです | **一致します**（変異試験で実測） |
| 3 案の構成（利用規約第 1 条） | requirements.md 4.1 の出力と一致します。**ネガティブは対応モデルのみ**で、`model_adapter.rb` の `negative_prompt?` が分けています | **一致します** |
| 禁止事項の 3 つ（利用規約第 4 条） | `config/forbidden_inputs.yml` の種別は `real_person` ・ `brand_logo` ・ `third_party_work` の 3 つです | **一致します** |
| 検出した場合は生成せず、理由を添えて返します | `ForbiddenDetector` ＋ 差し戻しの応答。**網羅を謳っていない点も、実装の但し書きと合っています** | **一致します** |
| 1 アカウント 1 日 1 回・JST 午前 3 時にリセット（利用規約第 6 条） | `Quota::QuotaDay`（`ZONE = 'Asia/Tokyo'` ・ `RESET_HOUR = 3`） | **一致します**（requirements.md 4.4） |
| 受け付けた時点で消費し、跨いでも帰属は変わりません | 予約消費（`Quota::Reservation`）と一意制約 | **一致します** |
| 失敗したら返還し、禁止入力は消費しません | `Quota::Settlement#next_status_for`（`failed` → `refunded`、差し戻しは投入前） | **一致します** |
| 上限に達したら、次のリセット時刻を出します | `prompt_requests_controller.rb:124` が `reset_at` を添え、`ja.yml:216` が文言を持ちます。画面側にも上限到達の表示があります | **一致します** |
| 縮退した生成である旨を案に明記します（利用規約第 9 条） | requirements.md 5.1 と、画面の縮退の文言 | **一致します** |
| 無償で提供します（利用規約第 9 条・特商法の表示） | **課金の実装がありません。** ER 図にも決済の実体がありません | **妥当です**（後述の提案 8） |
| **再判定の操作が画面にあります（利用規約第 2 条）** | **利用者向けの入口がありません。** `recheck` の経路は `routes.rb:56` の管理画面の下だけで、`Auth::RecheckService` を呼ぶのは `admin/users_controller.rb:33` だけです | **合っていません**（後述の条件 1） |

### issue #171 の受け入れ条件

| 条件 | 判定 | 根拠 |
|---|---|---|
| プライバシーポリシーに、reCAPTCHA の利用と送信先が書かれています | **満たします** | 第 4 条・第 5 条。`curl` で表示を実測しました |
| `remoteip` を送るかどうかの判断と、その理由が記録に残っています | **満たします** | PR 本文・セキュリティレビュー記録・`verifies_humans.rb` と `recaptcha_verifier.rb` のコメント・プライバシー第 5 条の 4 か所に残っています |
| 画面から、プライバシーポリシーへ辿れます | **満たします** | ランディング最下部 → `/privacy`。実測しました |

**「決めること」の 4 項目も、すべて判断が示されています。** 置き場（画面の一部）・reCAPTCHA の明記・`remoteip`（送らない）・Google への導線（プライバシーポリシーの末尾）です。**ただし、その導線が見た目でリンクと分かりません**（後述の条件 2）。

### issue #172 の受け入れ条件

| 条件 | 判定 | 根拠 |
|---|---|---|
| 画面から、利用規約とプライバシーポリシーへ辿れます | **満たします** | ランディング最下部の 3 つの導線。**ログインの前に読めます**（認証を求めません） |
| 保持する情報が requirements.md 5.3 と一致しています | **満たします** | `users` 表の列と一致します |
| すべて「ですます調」で書かれています | **満たします** | `strings` の木に載せてあり、文末の検査が効きます。**変異で赤になることを実測しました** |
| `app-ui/` を書き換えていません | **満たします** | 差分に `app-ui/` はありません |

**「決めること」も満たしています。** 生成物の扱い（利用規約第 5 条）・保持する情報と Cookie（プライバシー第 1 条・第 3 条）・特定商取引法の要否の判断が書かれています。

**「ログイン後の画面から辿れるか」について。** 受け入れ条件は「画面から辿れます」です。**ランディングは公開の画面ですので、条件そのものは満たします。** ログイン後の画面（`AppShell`）からは、上部バーのロゴ（`/`）→ ランディング最下部、の **2 手**になります。**本 PR を止めるほどのものではありません。次の issue へ回してください**（後述の提案 2）。

### 特定商取引法に基づく表示の要否

**判断は妥当です。** `requirements.md` には「無償」「料金」「課金」「有償」「決済」のいずれの記述もなく、**対価を取る仕組みが仕様のどこにもありません**（ER 図にも決済の実体がありません）。**対価を得る取引がなければ、同法の表示義務の対象になりません。** それでも提供者と連絡先を自主的に示す立て付けは、過不足のない置き方です。

### 変異試験（12 種）

**すべて、この作業ツリーで実際に流し直した結果です。**

| # | 変異 | 結果 |
|---|---|---|
| B1 | `remoteip` を送る形へ戻します（引数を復活させ、`form` へ載せます） | **赤（1 例）** |
| B2 | 引数だけ受け取って捨てます | **赤（1 例）** |
| B3 | 引数を使わず、内側でアドレスを載せます | **赤（2 例）** |
| B4 | 呼び出す側（`verifies_humans.rb`）がアドレスを渡します | **赤（7 例・`human_verification_spec.rb`）**。※`prompt_requests_spec.rb` だけでは緑のままです |
| F1 | ランディングからフッターを外します | **生き残ります**（Jest 152 例すべて緑）。ESLint が「未使用」として気づくだけです |
| F2 | どの住所でも利用規約を出します（`legal[which]` → `legal.terms`） | **生き残ります**（ESLint も捕まえません） |
| F3 | Google への導線を外します | **生き残ります**（ESLint の「未使用」のみ） |
| F4 | フッターの行き先を `#` にします | **生き残ります**（ESLint も捕まえません） |
| F5 | 「送りません」を「必要に応じて送る場合があります」へ差し替えます | **赤（1 例）** |
| F6 | 本文へ URL を書きます | **赤（1 例）** |
| F7 | だ・である調を混ぜます（「日本法である。」） | **赤（1 例）** |
| F8 | 文言の木から `legal` を外します | **赤（1 例）** |

**`remoteip` を送らないことは、4 方向から押さえられています。** 引数を置き直しても、受け取って捨てても、内側で載せても、呼び出す側が渡しても、いずれかのテストが赤になります。**この PR の要である判断は、きちんと固定されています。**

**一方、画面の描画にはテストがありません。** F1 〜 F4 は **4 件とも生き残ります**。とくに **F2（プライバシーポリシーの住所を開いても利用規約が出る）** と **F4（導線がどこへも行かない）** は、**受け入れ条件そのものが壊れる変異**であるにもかかわらず、緑のままです。ESLint が F1 ・ F3 を拾うのは「関数が未使用になる」ためで、**関数ごと消せば通ります。** 押さえとしては数えられません。

### 各基準への適合

| 基準 | 判定 | 根拠 |
|---|---|---|
| CC | **条件付き** | 連絡先は `info@rictaworks.jp` で、個人名がありません。**実装に無い操作を案内している点だけ、条件 1 として直してください。** 免責の書き方は提案 6 に回します |
| OWASP10 | **適合** | 3 つのページは**認証を求めず、書き換える経路もありません**（読むだけの静的な出力です）。外への導線は `https` の 2 件で、`rel="noreferrer noopener"` ・ `target="_blank"` が付いています。`dangerouslySetInnerHTML` を使いません。**第三者へ渡す項目を 1 つ減らしており、守りは強くなっています**（変異試験で実測） |
| QC10 | **適合** | フォールバックはありません。文言は 1 か所に集め、直書きの検出は 0 件です。例外の握りつぶしもありません |
| CRAP | **条件付き** | 反復：色・字・余白をデザイントークンだけで組み、モックの体裁に合わせています。**モックの `_ds` は「footer body」「footer link」の値を持っており、フッターはデザインシステムの想定の内側です。** 整列・近接も保たれています。**対比だけが弱く、条件 2 ・ 提案 1 ・ 10 ・ 11 を挙げます** |
| DP | **おおむね適合** | 関心が分かれています（文言 `legal.ts` ／ 画面 `legal/index.tsx` ／ 外への出口）。**引数を置かないことで「渡した時点で失敗させる」形は、Fail Fast の良い当て方です。** 提案 9 ・ 13 だけ残します |
| TM | **要補強** | 文言の単体テストは効きます（変異 4 件が赤）。**描画のテストがありません**（変異 4 件が生き残ります）。**受け入れ条件を直接に検める層が欠けています** |

### プロジェクト固有の決まり

| 決まり | 判定 |
|---|---|
| 実装が `src/` の下にあること | **満たします** |
| 開発用の道具が `src` の外にあること | **満たします**（この PR は道具を足していません） |
| フォールバックが無いこと・例外処理があること | **満たします** |
| デバッグトレースが追えること | **満たします**（照合は `traced` の中です） |
| 制御構文以外がクラスまたは関数に入ること | **満たします** |
| 文字列リテラルの分離とハードコードの検査 | **満たします**（本文は `strings/legal.ts`、検出 0 件） |
| ネイティブの `alert()` 等を使わないこと | **満たします** |
| 環境の判定と本番での近道の遮断 | **満たします**（この PR は分岐を増やしていません） |
| 車輪の再発明をしないこと | **満たします** |
| フォロワー判定を自前で作らないこと | **満たします**（判定は `x-follower-gate` のままです） |
| 秘匿値を `.env` から読むこと | **満たします** |
| ですます調 | **満たします**（Jest が検め、変異で赤になります） |
| 日本語版のみ・多言語化なし | **満たします**（`legal.ts` の冒頭にも明記されています） |
| 絵文字なし・アイコンは Font Awesome | **満たします** |
| JST ・ UTF-8 | **満たします** |

## 指摘

### 条件（マージ前に直していただきたいもの）

- **条件 1：利用規約 第 2 条が、いまの実装に無い操作を案内しています。** `src/frontend/src/strings/legal.ts:56` の「その場合は、画面に用意した再判定の操作をお試しください。再判定には、続けて実行できない待ち時間を設けています。」です。**利用者向けの再判定の入口はありません。** `recheck` の経路は `src/backend/config/routes.rb:56` の管理画面の下だけで、`Auth::RecheckService` を呼ぶのは `src/backend/app/controllers/admin/users_controller.rb:33` だけです。画面側にも「再判定」の文言・操作がありません（`src/frontend/src/strings/ja.ts` ・ `src/frontend/src/components` とも検出 0 件）。**ログインのたびに `Auth::PlanUpdater` が走りますので、実際の直し方は「あらためてログインし直してください」です。** その形へ書き換えてください。**利用者向けの再判定の導線そのものは requirements.md 6 が求めていますので、別の issue で足してください。**
- **条件 2：Google への導線が、見た目でリンクと分かりません。** `src/frontend/src/components/legal/legal.module.css` の `.links` に文字色も下線もありません。既定の `src/frontend/src/styles/tokens/base.css:16` が当たり、色は `--text-body`（= `--text` = `#e0e0e0`）です。**本文（`.paragraph`）とまったく同じ色で、下線もありません。** 押せると分かるのは、当てたときに金色へ変わる瞬間だけです。**PR 本文のユーザーテストは「2 つの青い文字（リンク）」と書いていますが、実際は青くありません。** 非エンジニアの方は探し当てられません。**CSS 1 行（色または下線）と、PR 本文の言い直しで直ります。** issue #171 が挙げた「バッジを隠す場合の代わりの表示」としての実効に関わりますので、条件に置きます。
- **条件 3：セキュリティレビュー記録の指紋が、いまの差分と一致しません。** 記録（`review-records/security-review/feature-legal-pages.md`）は `baa8d1b4840a6a82` ですが、`2442464` の差分から再計算すると **`f780601de19c378c`** です（同じ計算方法で `a35bc09` を計算すると、PR #178 の記録の値と一致しますので、計算は合っています）。**積み直しで差分の本文が変わったためです。** **変更の中身は変わっていませんので、レビューの結論はそのままで構いません。指紋の値だけを直してください。**
- **条件 4：例数の記載が実測と大きく違います。** PR 本文と記録は「Jest 86 例 ・ RSpec 1783 例」ですが、**この作業ツリーでも CI でも Jest 152 例 ・ RSpec 1956 例**です。**積み直しの前の値が残っています。数字だけを直してください。**
- **条件 5：「別の PR と衝突するので後回しにする」という理由が、いまの状況と合いません。** PR 本文と記録は「画面 02 〜 09 は PR #174 で実装中です。同じ持ち場を二重に触ると、取り込みで衝突します」と書いていますが、**PR #174 はすでに `main`（`4a64c51`）へ取り込まれており**、**いま開いているもう 1 本の PR #179 は `src/frontend` を 1 ファイルも触りません**（変更は `.github/` ・ `test/` ・ 記録だけです）。**後回しにする判断そのものは妥当です**（画面 02 〜 09 のフッターは designer の判断が要ります）。**理由の書き方だけを、いまの状況に合わせて直してください。**

### 提案（次の issue へ回してよいもの）

- **提案 1：法務のページが行き止まりです。** `/terms` と `/commerce` には、`<a>` が **1 つもありません**（実測）。`/privacy` にあるのは Google への 2 件だけで、**どちらも本サービスの外へ出ます。** 検索や共有された住所から直接お越しになった方は、**戻る操作以外に本サービスへ入る道がありません。** 上部にロゴ（`/` へ）を置くか、末尾に「トップへ戻る」を足してください。
- **提案 2：ログイン後の画面（`AppShell`）にフッターがありません。** ロゴ → ランディング最下部の 2 手になります。**受け入れ条件は満たしますが、Cookie を置いたあとの画面から 1 手で辿れる形が望ましいです。** `src/frontend/src/components/app/AppShell.tsx` へ同じ帯を足す形が素直です。
- **提案 3：画面の描画にテストがありません。** 変異 F1 〜 F4 が生き残ります。**`legal/index.tsx` と `landing` のフッターに、レンダリングのテストを 1 本ずつ足せば塞がります**（`which` ごとに題が変わること・3 つの行き先が正しいこと・Google の 2 件が出ること）。既に `AppBar.test.tsx` という前例があります。
- **提案 4：Cookie の数え方が、ひとつだけ曖昧です。** 実装では、手続きの途中の Cookie は `hpf_oauth_state` と `hpf_oauth_verifier` の **2 つ**です（`src/backend/app/controllers/auth_controller.rb:9-10`）。「2 種類」は正しいのですが、「**もう 1 つは**」を「**もう 1 種類は**」に直すと、数と種類が食い違いません。
- **提案 5：サーバーの記録に残る要求元のアドレスに触れていません。** プライバシー第 5 条は「Google へ送らない」ことだけを述べています。Rails の既定の要求ログには、要求元のアドレスが残ります。**`requirements.md` 5.3 と同じ言い回しですので、この PR を止めるものではありません。** 保存期間・扱いの一文を足すと、より正確になります。
- **提案 6：免責（利用規約第 9 条）が、全部免責の書き方です。** 利用者に消費者が含まれる場合、消費者契約法 8 条により、故意または重大な過失がある場合の全部免責は無効となり得ます。**「当社の故意または重大な過失による場合を除きます」の但し書き**を検討してください。無償の提供ですので影響は限られますが、書いておくほうが安全です。
- **提案 7：ログインの押しものの近くに、同意のひとことがありません。** 利用規約第 1 条は「ご利用いただいた時点で同意」というみなしの立て付けです。**押しものの近くに「ログインすると利用規約に同意したものとみなします」を添えると、画面と条文が揃います。** モックに無い要素ですので、designer の判断を仰いでください。
- **提案 8：「無償で提供します」は、`requirements.md` に明文がありません。** 課金の仕組みも仕様にありませんので矛盾はしませんが、**無償で提供する方針そのものは director の判断です。** どこかに記録を残しておくと、あとから根拠を辿れます。
- **提案 9：`LegalPage` の中の `const document` が、DOM の `document` を覆います**（`src/frontend/src/components/legal/index.tsx:32`）。動きに影響はありませんが、**surprise の少ない命名**の観点から、`article` や `doc` などへ寄せてください。
- **提案 10：制定日が 2 か所に出ます。** 見出しの上（`SectionHeading` の eyebrow）と、末尾の `Facts` です。**上のほうには「制定日」の目印がありません**ので、日付だけが宙に浮いて見えます。片方に寄せるか、上にも目印を添えてください。
- **提案 11：フッターの添え書きが読みにくい配色です。** `--grey-666`（`#666`）・11.5px で、`--dark2`（`#111`）との対比はおよそ **3.3 : 1** です（4.5 : 1 に届きません）。**デザインシステムの「footer body」の値どおりですので、この PR を止めるものではありません。** ただし、この一文は Cookie の告知です。**告知として読ませたい文ですので、対比を上げる価値があります。**
- **提案 12：`src/frontend/src/config/screens.ts` に 3 つの場所を載せていません。** あの一覧はモックの番号と結びつくものですので、そのままでも構いません。**載せない方針であることを、あの一覧のコメントに 1 行書いておくと迷いません。**
- **提案 13：Google の場所（URL）が画面の中にあります**（`GOOGLE_LINKS`）。`src/frontend/src/config/` に置き場があります。**いまの置き方でも動きますが、外部の宛先は設定側へ寄せるほうが揃います。**

## 後片づけ

**削除は行っていません。** 手動で片づけていただく必要があるものは、次のとおりです。

- 検証用のデータベース `hero_prompt_forge_test_180`
- レビュー用の作業ツリー `DELETE/review-worktrees/pr180`
- `DELETE/trash-pr180/broken-node_modules-symlink`（作業ツリーを作る際に生じた、行き先の無いリンクを移したものです）

**なお、レビューの開始時に共用の PostgreSQL が復旧処理中でした。** 復旧を待ってから検証しています。**全例の実行は行わず、関係する範囲（106 例）と例数の数え上げ（`--dry-run`）にとどめました。**

## 判定

条件付き合格

**issue #171 の受け入れ条件 3 件、issue #172 の受け入れ条件 4 件は、いずれも満たしています。** 画面から辿れること・保持する情報が `requirements.md` 5.3 と一致すること・ですます調であること・`app-ui/` を書き換えていないことを、いずれも実測しました。

**この PR の要である「要求元のアドレスを送らない」という判断は、きちんと固定されています。** 引数を置き直しても、受け取って捨てても、内側で載せても、呼び出す側が渡しても、いずれかのテストが赤になります。**4 方向から押さえられていることを、変異試験で 1 件ずつ確かめました。** 判断と理由も、コード・記録・ポリシーの本文の 3 か所に残っています。

**法務の本文は、実装とよく突き合わされています。** 保持する情報・Cookie・3 案の構成・禁止事項・利用回数・縮退の明記・Gemini へ送る範囲は、**いずれも実装と一致します。** `requirements.md` に無いことを書き足してもいません。**唯一、利用規約 第 2 条の「画面に用意した再判定の操作」だけが、いまの実装にありません**（条件 1）。**特定商取引法に基づく表示が義務でないという判断も妥当です。** 対価を取る仕組みが仕様のどこにもないことを確かめました。

**ログイン後の画面からの導線は、この PR に無くて構いません。** 受け入れ条件は「画面から辿れます」であり、公開のランディングから辿れます。**次の issue へ回してください**（提案 2）。**ただし、後回しにする理由の書き方だけは、いまの状況に合わせて直してください**（条件 5）。

**残る弱さは、画面の描画にテストが無いことです**（提案 3）。**プライバシーポリシーの住所を開いても利用規約が出る**という変異が、緑のまま通ります。**いますぐ壊れているわけではありません**（実際の表示は `curl` で確かめました）ので、次の issue で塞いでください。

**条件 1 〜 5 を直していただければ、マージして差し支えありません。** 条件 1 と条件 2 は本文と CSS の小さな直しで、条件 3 〜 5 は記載の直しだけです。**提案 1 〜 13 は、この PR を止めるほどのものではありません。次の issue へまとめて回してください。**

連絡先は info@rictaworks.jp です。

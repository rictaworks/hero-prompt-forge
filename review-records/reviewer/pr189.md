# reviewer 記録 : PR #189（issue #184 本番へ出す前に片づけること）

- 実施日時（JST）：2026-08-29 16:15
- 対象：`617e602`（4 回のコミット。1・2回目はCI不合格を受けた作り直し）
- 土台：`main`（`cad5079`）と同じ位置です（`git rev-list --left-right --count origin/main...617e602` は `0 4`、遅れ 0・先行 4）
- `mergeable`：**`MERGEABLE`** ／ `mergeStateStatus`：**`CLEAN`**
- 実装は実装側の作業ツリー（このセッションの担当分）で直接確認しました

## 実測

| 観点 | 結果 |
|---|---|
| 自動検査（CI、最終実行 `33240086201`） | **6 ジョブすべて pass**（バックエンド・フロントエンド・本番と同じ設定での読み込み確認・開発フックの検査・ハードコードの検査・ブラウザ操作の確認） |
| フロントエンド（ESLint / `tsc --noEmit` / Jest / ビルド） | 手元でも実行。ESLint 指摘 0 件、`tsc --noEmit` 指摘 0 件、Jest **18 スイート・175 例・失敗 0 件**、`npm run build` 成功（`/requests/new` は静的プリレンダリング対象と確認） |
| ハードコードの検査（`test:hardcode` ・ `check:hardcode`） | 手元で実行。仕組みのテスト 9 例・失敗 0 件、直書きの文言検出 0 件 |
| 開発フックの検査（`test:hooks` ・ `test:harness`） | 手元で実行。**29 例・失敗 0 件**（23 + 6） |
| 文書の検査（`test:docs`） | 手元で実行。7 例・失敗 0 件 |
| ブラウザ操作の確認（Playwright、新規 `test/pr189`） | 手元で **開発サーバー（`npm run dev`）・本番相当の組み立て（`npm run start`）の両方**で 3 例・失敗 0 件。CI（本番相当の組み立て）でも 3 例・失敗 0 件 |
| ブラウザ操作の確認（Playwright、既存 `test/pr-setup` ・ `pr170` ・ `pr174`） | CI で **47 例すべて pass**。手元では 50 例中 2 例が落ちましたが、原因は長期間共有されてきた開発用データベースへの蓄積（後述）で、この PR の変更とは無関係と判断しました |
| セキュリティレビュー記録の指紋 | 4 つとも一致します（`992bc19d1917be61` ・ `1b98d5e73df12887` ・ `9332b0cc8ff3791f` ・ `da75a7f6bf5b65a2` を差分から再計算しました） |
| `requirements.md` ・ `app-ui/` | 1 文字も変わっていません（`git diff --name-only origin/main...617e602 -- requirements.md app-ui` は 0 件） |
| 絵文字 | ありません |
| ですます調 | 満たします。だ・である調の混入はありません |
| 個人名 | ありません。連絡先は `info@rictaworks.jp` |

## issue #184 の片付け項目ごとの判定

| # | 項目 | 判定 | 実測 |
|---|---|---|---|
| 1 | 送る本文の項目名（`generation_config`）を実際の鍵で確かめる | **「本番公開の前に必ず行うこと」として記載**（issue の受け入れ条件が許容する形） | `ENV/PRODUCTION.md`（`.gitignore` の対象で、このリポジトリのコミットには含まれません）へ、未確認である旨・失敗時の症状（`temperature`・`max_output_tokens` が黙って無視される）・確かめ方を記載しました。本番用の有償枠の鍵は未発行（`ENV/PRODUCTION.md` の「現在の状態」も「未構築」のまま）のため、この PR の範囲で実地確認はできません |
| 2 | `ENV/PRODUCTION.md` へ有償枠の鍵を使う旨を書く | **対応済み（既存）** | 同ファイルの「Gemini の鍵」節にすでに記載済みでした（実測：「有償の枠で発行した鍵を Railway Variables の `GEMINI_API_KEY` へ入れます」等）。二重記載はしていません |
| 3 | 組み上がる前の送信でGET遷移になる | **満たします** | `NewRequestForm.tsx` に `useHydrated()`（`useSyncExternalStore` ベース）を追加し、送信ボタンを `disabled={submitting \|\| !ready}` にしました。`test/pr189/hydration-guard.spec.ts` で、`domcontentloaded` 直後に素のクリックを送り込んでも `?` 付き URL へ遷移しないこと・組み上がれば押せるようになること・組み上がったあとの画面止めは従来どおりであることを確認しています |
| 4 | イラスト・抽象＋ブランドカラーの組み合わせが必ず失敗する | **満たします** | 原因は `Generation::StylePalette`（`src/backend/app/services/generation/style_palette.rb`）が、ブランドカラー指定時に配色指定（`palette`）を無条件で弱めるのに対し、`illustration`・`abstract` の 2 系統だけが `style_spec_rules` の `required` に `palette` を含む（`src/backend/config/rule_dictionary/initial.yml`）ため、と実装を突き合わせて特定しました。バックエンドは変更せず、`validate()` に `hasBrandColorStyleConflict()` を追加して画面で止めます。`app-ui/` は変更していません。単体テスト 8 件を追加（各スタイル系統 × 有無の組み合わせ、書き方誤りとの優先順位） |
| 5 | `test/README.md` の作業ツリー置き場が2通り | **満たします** | `DELETE/review-worktrees/` へ一本化。`.gitignore` の未使用となった `/.review/` 行も削除しました。PR #179 の pr-checker の指摘（実運用はほぼ全て `DELETE/` を使っていた）と実測が一致します |

## 実測した Playwright の失敗 2 件（既存 `test/pr174`、手元のみ・この PR の対象外）

手元での全例の実行（50 例）で、以下 2 件が落ちました。**CI（`33240086201`）では同じ 2 件を含む 47 例すべて pass しており、この PR の変更とは無関係と判断します。**

| 例 | 症状 | 判断根拠 |
|---|---|---|
| プリセット（07）保存した条件を入力フォームへ呼び出せます | プリセット読込後も業種欄が空 | 失敗時のスナップショットに `プロジェクト` の選択肢が数十件重複して並んでおり、長期間再利用されてきた共有の開発用データベースへ、過去の複数セッションの作業が蓄積していることが直接の原因と見られます（`review-records/pr-checker/pr178.md` の申し送りと同種の既知の共有資源問題） |
| 上限到達と差し戻し（08）禁止入力の差し戻しが画面に出ます | 送信後も入力フォームのまま。REJECTED も出ない | 同上。**いずれの失敗例でも、失敗の直前に複数回の `selectOption` / `fill` が先行して成功しており、この PR が追加した「組み上がる前の disabled」はすでに解除された状態でボタンが押されています。** したがってこの PR の変更が原因ではありません |

## CC / OWASP10 / QC10 / CRAP / DP / TM

| 基準 | 判定 |
|---|---|
| CC | 該当なし（法務・表記事項に関わる変更はありません） |
| OWASP10 | 合格（セキュリティレビュー記録のとおり。該当する変更なし） |
| QC10 | 該当なし（フロントエンドの検証ロジック追加のみで、SEO・構造化データ等への影響はありません） |
| CRAP | 満たします。新しいエラー表示は既存の `Field` のエラー表示（`brandColorFormat` と同じ枠）をそのまま使っており、対比・反復・整列・近接のいずれも既存パターンから外れていません |
| DP | 満たします。YAGNI（必要最小限の検証のみ追加）・KISS（`validate()` への素直な追加、`useSyncExternalStore` は React 標準のイディオム）・DRY（既存のフィールドエラー表示を再利用） |
| TM | 満たします。単体テスト（Jest）・ブラウザ操作の確認（Playwright）の双方で検証し、CI（クリーンな環境）でも確認しています |

## プロジェクト固有の決まり

| 観点 | 判定 |
|---|---|
| 実装が `src/` 配下、開発用スクリプトが外 | 満たします（`test/pr189/` は `src` の外） |
| フォールバックが無いこと | 満たします。バックエンドの規則は変更せず、画面側の防止に留めています |
| 文字列リテラルの分離 | 満たします。`newRequest.errors.brandColorStyleConflict` を `ja.ts` へ追加し、`check:hardcode` で確認済み |
| ネイティブの `alert()` 等 | 使っていません |
| 車輪の再発明 | ありません。`useSyncExternalStore` は React 標準 API です |
| 秘匿値 | ソースに書かれていません |
| 絵文字 | ありません |

## 申し送り

- **CI が 2 回連続で不合格になり、その都度実装ではなくテストの作り方を直しました。** 1 回目はネットワーク遅延で「組み上がる前」を狙う方式が環境（開発サーバー／本番相当の組み立て）によって速さが違い安定しなかったため、2 回目は `/requests/new` が `useSearchParams()` によって本番相当の組み立てでは `BAILOUT_TO_CLIENT_SIDE_RENDERING`（クライアント側だけで描く扱い）になり、送信ボタンを含む本体が最初の HTML に含まれないためです。**最終的に、タイミングにも描画方式にも依存しない検査（`domcontentloaded` 直後に素のクリックを送り込み、`?` 付き URL へ遷移しないことを直接確かめる）へ落ち着きました。**
- 上記の経緯は `review-records/security-review/feature-production-launch-cleanup.md` に 4 回分すべて記録しています。

## 判定

合格

# コードレビュー : PR #80

- 実施日時（JST）：2026-08-25 10:06
- 対象 Issue：#1

## 判定

合格

## Issue の受け入れ条件

| 条件 | 判定 | 根拠 |
|---|---|---|
| `docker compose up -d` で `db` と `dev` が起動する | 合格 | `docker compose ps` で db が healthy、dev が Up |
| `dev` 内で `node -v` と `ruby -v` が応答する | 合格 | v22.23.2 / 3.3.12 |
| `db` に PostgreSQL 16 以上が起動し `dev` から接続できる | 合格 | PostgreSQL 16.15 に `psql` で接続成功 |
| 注意事項を `ENV/DEVELOPMENT.md` に追記する | 合格 | dev コンテナの節とポート表を追記（`ENV/` は追跡対象外のためローカルのみ） |

## 各基準への適合

| 基準 | 判定 | 備考 |
|---|---|---|
| CC | 合格 | 第三者の著作物を同梱していません。取得元は公式イメージと NodeSource のみです |
| OWASP10 | 合格 | 実行ユーザーを非 root にし、ポートを 127.0.0.1 に束縛しています。外部取得は署名検証を経ています |
| QC10 | 合格 | 対象となる画面がまだ無いため、該当する検査項目がありません |
| CRAP | 対象外 | 画面の変更を含みません |
| DP | 合格 | 公式イメージを土台にしており、車輪の再発明をしていません |
| TM | 対象外 | テスト基盤は issue #6・#7・#8 で用意します |

## プロジェクト固有の決まり

| 観点 | 判定 |
|---|---|
| 実装が `src/` 配下にあること | 合格（`src/.gitkeep` のみ） |
| フォールバックが無いこと | 合格 |
| グローバル変数が無いこと | 対象外 |
| 文字列リテラルの分離 | 対象外（画面文言を含みません） |
| `alert()` / `confirm()` / `prompt()` の不使用 | 合格 |
| 時刻が JST・ファイルが UTF-8 | 合格 |
| 絵文字の不使用 | 合格 |
| コンテンツがですます調 | 合格 |

## 指摘

指摘はありません。

開発用データベースのパスワードが `docker-compose.yml` に直値で入っていますが、127.0.0.1 に束縛された開発専用の値であり、issue #5 で `.env` からの読み込みへ移す予定がセキュリティレビュー記録に明記されています。提案としても保留とします。

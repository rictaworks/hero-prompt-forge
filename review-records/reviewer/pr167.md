# reviewer 記録 : PR #167（issue #57・#58・#59・#60）

- 実施日時（JST）：2026-08-27
- 対象：`feature/issue-55-56-api..feature/issue-57-60-resource-api`（1 回目）／ `origin/main..origin/feature/issue-57-60-resource-api`（2・3 回目）
- 作業ツリー：`DELETE/review-worktrees/pr167*-20260827-reviewer`（いずれも `git checkout --detach` でブランチを解放済みです）
- 実行環境：dev コンテナ。**検証専用のデータベースを作り、実装側の作業ツリーと共有の試験用データベースに触れていません。**

## 1 回目

### 実測

| 項目 | 結果 |
|---|---|
| 全体のテスト | 1643 例、失敗 0 件 |
| 今回の 4 ファイル | 59 例、失敗 0 件 |
| 書き方の検査 | 115 ファイル、指摘 0 件 |
| ハードコードの検査 | 検出 0 件 |

### 変異試験（絞り込みを外すと本当に赤くなるか）

**7 とおりの変異を実際に当てました。**

| 変異 | 結果 |
|---|---|
| M1 プロジェクトの絞り込みを `Project.all` へ | **赤 3 件** |
| M2 プリセットの絞り込みを `Preset.all` へ | **赤 4 件** |
| M3 評価メモの案の絞り込みを削除 | **赤 5 件** |
| M4 履歴の絞り込みを `PromptRequest.all` へ | **赤 2 件** |
| M5 受け取る項目へ `:user_id` を足す | **赤 1 件** |
| M6 一覧から `degraded` を落とす | **赤 2 件** |
| M8 一覧から `outputs_count` を落とす | **赤 1 件** |
| M7 `includes(:prompt_outputs)` を外す | **緑のまま（検知しません）** |

### 受け入れ要件

issue #57・#58・#59・#60 の**受け入れ条件をすべて満たします。** スコープ外の変更は見当たりません。

### 指摘

- **R1【要修正】** セキュリティレビューの記録が、コミットされた差分と対応していません。**記録を書いたあとに実装が変わっています。**
- **R2【要修正】** `project[brand_settings]` の `{}` 許可が広すぎます。**鍵と大きさに歯止めがありません。** 20 万文字を含む設定が 600,031 バイトで保存できました。
- **R3【要修正】** 生成履歴の一覧に `project_id` がありません。**一覧を見ても、その履歴がどのプロジェクトのものか分かりません。**
- **S1〜S5【提案】** 先読みの固定・`rescue_from` の書き写し・契約への応答の形の追記・見出しの閉じ括弧・頁分けの上限。

## 判定（1 回目）

要修正

重大な指摘は 0 件で、受け入れ条件はすべて満たしています。R1・R2・R3 の 3 点は、マージの前に直していただく必要があります。

---

## 2 回目

### 実測

| 項目 | 結果 |
|---|---|
| 全体のテスト | **1728 例、失敗 0 件** |
| 書き方の検査 | 128 ファイル、指摘 0 件 |
| ハードコードの検査 | 検出 0 件 |

### 依頼された 6 点

- **`brand_settings` の歯止め**：**鍵と大きさの両方で働いています。** 決まっていない鍵・20 万文字・500 色のいずれも 400 になります。**上限の値（2000 文字）と鍵の一覧（`people` ・ `tone` ・ `colors`）は妥当と判断します。**
- **プリセットの入力条件の上限**：10 万文字で 400、100 文字で 201 です。
- **生成履歴の `project_id`**：**返します。** 問い合わせ回数は 4 件のままで、N+1 は起きていません。
- **変異試験**：**11 とおりを当て直し、1 回目の M1〜M6・M8 はすべて壊れずに赤くなりました。** 新しい歯止め（M10〜M12）も外すと赤くなります。
- **指紋**：いまの差分と一致しませんでした。
- **契約と実装**：`GET /api/v1/prompt_requests/:id` の応答例だけが以前のままでした。

### 指摘

- **R4【要修正】** 空のファイル（`src/backend/spec/models/preset_spec.rb.append`、0 バイト）が `src/` に混入しています。
- **R5【要修正】** `GET /api/v1/prompt_requests/:id` の応答例に `project_id` がありません。
- **R6【要修正】** セキュリティレビューの記録を、いまの差分に合わせてください。
- **S6〜S10【提案】** 呼び名の食い違い・既存データがある環境での注意・上限が JSON の長さであること・サービス概要の上限からの導出・リクエストのテストの追加。

### 見送り（S1・S2・S5）

**3 件とも、見送りを受け入れます。**

## 判定（2 回目）

条件付き合格

R2 と R3 が対応済みであることを、変異試験と API の実測で確認しました。残るのは R4・R5・R6 の 3 点で、**いずれも実装の動きではなく、残骸・契約・記録の話です。**

---

## 3 回目

### 実測

| 項目 | 結果 |
|---|---|
| 全体のテスト | **1728 例、失敗 0 件**（記録の値と一致します） |
| 書き方の検査 | **189 ファイル**、指摘 0 件 |
| ハードコードの検査 | 検出 0 件 |

### 条件の確認結果

- **R4**：**`src/` の差分から消えていることを確かめました。** `DELETE/stray-2026-08-27/` へ移っています（blob は同じ＝移動です）。**削除の操作はありません。**
- **R5**：**契約の例と、実装が返すものが一致することを、API を通して実測しました。** 受付・取り出し・一覧・プロジェクト・プリセット・評価メモの 6 経路すべてで一致します。**受付の応答でも `project_id` が返ります。**
- **R6**：**指紋 `15bdb3d8b3454dcf` が、いまの差分から再計算して一致しました。** コミットごとの 4 つの値もそれぞれ一致します。件数（1728 例・189 ファイル）と「モデルは 2 件変えました」の記述も実測と合っています。

### 変異試験（11 とおり）

**2 回目とまったく同じ結果です。壊れていません。**

### 提案（S6〜S10）の扱い

**5 件とも、申し送りへ残す扱いを受け入れます。** 記録の申し送りに、こちらの指摘の趣旨がそのまま残っていることを確認しました。

### 気付き（マージを妨げません）

- **記録の見出しが「2 回目」のままです。** 見出しの回数だけ実態へ合わせておくと親切です。
- **`DELETE/stray-2026-08-27/preset_spec.rb.append` は、追跡されたままです。** `git mv` による移動ですので、`.gitignore` は効きません（無視は追跡済みのファイルへ働かないためです）。**中身が 0 バイトですので害はありません。** 追跡を外す操作は削除に当たりますので行わず、記録の説明を実態へ合わせるのが安全です。

### 3 回を通しての総括

**受け入れ条件（issue #57・#58・#59・#60）は、3 回を通じてすべて満たされています。** 他人の資源を操作できないこと・持ち主を要求の値で決めないこと・質量攻撃の余地が無いこと・上限に達していても閲覧と記録ができること・縮退の印が履歴に残ること・N+1 が起きないこと・失敗の応答が契約の形であることを、**いずれも実際に動かして確かめました。**

## 判定

合格

3 つの条件（R4・R5・R6）は、いずれも実測で解消を確認しました。2 回目に確かめた 11 とおりの変異もすべて健在で、テスト 1728 例・失敗 0 件、RuboCop 指摘 0 件、ハードコード検出 0 件です。**マージして差し支えありません。**

連絡先は info@rictaworks.jp です。

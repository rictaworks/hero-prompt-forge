# コードレビュー : PR #121

- 実施日時（JST）：2026-08-26 10:15
- 対象 Issue：#120

## 判定

合格

## Issue の受け入れ条件

| 条件 | 判定 | 根拠 |
|---|---|---|
| CI の「バックエンド（RuboCop / RSpec）」が成功します | 合格 | 実行 32917916406 が成功。4 つの仕事すべてが成功しています |
| テスト用のデータベースへ初期データを流しません | 合格 | `.github/workflows/ci.yml` 55 行目を `bin/rails db:test:prepare` に変えています。初期データの内容を確かめるテストは `spec/models/rule_dictionary_spec.rb` 109 行目で自ら `db/seeds.rb` を読み込む書き方を保っています |
| 「本番と同じ設定での読み込み確認」は初期データを流す手順を保ちます | 合格 | `ci.yml` 132 行目の `bin/rails db:prepare` は変更していません。当該の仕事も成功しています |
| 規則辞書のテストが初期データと衝突しない版名を使います | 合格 | `spec/models/rule_dictionary_spec.rb` 9 行目の既定を `vspec.1` に変えています。個別に指定する `v1` `v2` も初期データの `v2026.08.1` と重なりません |

## 各基準への適合

| 基準 | 判定 | 備考 |
|---|---|---|
| CC | 合格 | 第三者の著作物・個人情報を含みません |
| OWASP10 | 合格 | 権限・秘密の設定を変えていません。追加した手順は接続情報を出力しません。テスト用データベースの用意を狭める方向の変更です |
| QC10 | 対象外 | 画面の変更を含みません |
| CRAP | 対象外 | 画面の変更を含みません |
| DP | 合格 | テストの前提を初期データから切り離し、失敗の原因を局所化しています。手順を足さずに置き換えるだけの最小の変更です |
| TM | 合格 | 単体テストの独立性を高める修正です。232 例が手元でも CI でも成功しています |

## プロジェクト固有の決まり

| 観点 | 判定 |
|---|---|
| 実装が `src/` 配下にあること | 合格（変更は CI 定義とテストのみで、実装の変更を含みません） |
| フォールバックが無いこと | 合格（失敗を握りつぶす分岐を足していません） |
| デバッグトレースが追えること | 合格（変更の理由を `ci.yml` 50 から 52 行目と spec 6 から 7 行目に書いています） |
| グローバル変数が無いこと | 合格 |
| 秘匿値がソースに書かれていないこと | 合格（接続情報は既存のとおり環境変数から読みます） |
| 絵文字を使っていないこと | 合格 |
| 時刻が JST、ファイルが UTF-8 であること | 合格（`ci.yml` の `TZ: Asia/Tokyo` を保っています） |
| コンテンツがですます調 | 合格（追加した注釈・PR 本文・セキュリティレビュー記録のいずれもですます調です） |
| スコープ外の変更が無いこと | 合格（`review-records/security-review/fix-issue-120-ci-seed.md` の追加は開発フローが求める記録であり、実装の変更ではありません） |

## 指摘

マージを妨げる指摘はありません。判定は合格です。

### 提案

- `spec/models/rule_dictionary_spec.rb` の `.current` を確かめる 3 例（70 から 89 行目）は、データベース全体から最新の公開済みの版を選ぶ書き方です。版名の衝突では落ちなくなりましたが、誤って初期データが入った状態では初期データの版が選ばれて落ちます。受け入れ条件は満たしていますので今回の修正は不要です。将来、テストの独立性をさらに高める場合は、その例が作った版だけを対象に確かめる書き方をご検討ください。
- CI から `Node.js 20 is deprecated`（`actions/checkout@v4` 等）の注意が出ています。この PR の範囲外ですので、別の Issue として起票することをご提案します。

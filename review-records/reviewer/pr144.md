# コードレビュー : PR #144

- 実施日時（JST）：2026-08-26 20:38
- 対象：issue #134 ／ issue #136
- ブランチ：`feature/issue-134-136-detection`（`1f1194b`）
- 比較元：`origin/main`（`3d6b1b4`）
- 確認の方法：一時的な作業ツリーで RSpec ・ RuboCop ・ 計測スクリプトを実行し、**レビュー担当が独自に用意した素材でも両方向に測りました。**

## 判定

合格

下の「要修正」の 3 件は、この PR のマージを妨げません。いずれも呼び出す側がまだ無く、利用者に影響しないためです。**ただし issue #55（生成リクエスト API）と issue #54（非同期ジョブ）でつなぐ前に片付けてください。**

## 実行した結果

| 検査 | 結果 |
|---|---|
| RSpec | 681 例、失敗 0 件 |
| RuboCop | 96 ファイル、指摘 0 件 |
| `forbidden_input_coverage.rb` | 止めるべき 13 件の取りこぼし 0 件、通すべき 15 件の誤検出 0 件 |
| `anti_ai_coverage.rb` | 通すべき 40 件の誤検出 0 件、止めるべき 33 件の取りこぼし 11 件、短い語での巻き込み 5 件 |

PR 本文と、セキュリティレビューの記録に書かれた数値を、いずれも再現できました。

## 受け入れ要件の確認

### issue #134

| 要件 | 判定 |
|---|---|
| 挙げられた 5 件を差し戻します | 達成。5 件とも RSpec に固定されています |
| 士業・デザイン業の言い回しを引き続き通します。誤検出を増やしません | **条件付き。** PR の 15 件では 0 件ですが、独自の素材で新規の誤検出を確認しました（要修正 1） |
| 「他社のロゴが入った看板」を取りこぼしたまま残します | 達成。実測で通ります |
| 通す側と止める側を対にした RSpec があります | 達成 |
| 両方向で測ります | 達成 |

**「が」を助詞に加えていない判断は妥当です。** 助詞の一覧は `[をとはも]` であり、「が」は入っていません。**取りこぼしを 1 件残す判断のほうが、権利と無関係のお客さまを締め出す誤検出より害が小さく、判断として支持できます。**

### issue #136

| 要件 | 判定 |
|---|---|
| 単数形と複数形の違いを吸収します | 達成 |
| 英国式と米国式のつづりを吸収します | 達成 |
| 通すべき素材 40 件の誤検出が 0 件のまま保たれます | 達成。**独自の 45 件でも誤検出 0 件でした** |
| `teal` が `stealth` を巻き込まない性質を保ちます | 達成 |
| 通す側と止める側を対にした RSpec があります | 達成 |
| 両方向で測ります | 達成 |

## 語の切れ目で見る性質（重点確認）

短い語だけを登録した規則辞書を作り、実測しました。

```
stealth -> nil    stealthy -> nil    artisan -> nil    smart -> nil
heartfelt -> nil  orbit -> nil       reshape -> nil    landscape -> nil
teals -> "teal"   arts -> "art"      orbs -> "orb"     shapes -> "shape"
```

**性質は壊れていません。** `WordForms.canonical` は語ごとに置き換え、語の並びを変えないため、語の切れ目の境界がそのまま効いています。

## 複数形でない語（重点確認）

`s` で終わる単数形の語を 88 語試しました。`lens` ・ `glass` ・ `focus` ・ `analysis` ・ `canvas` ・ `news` ほかはいずれも無傷でした。

**取りこぼしていた語です。** `chaos` ・ `cosmos` ・ `sans` ・ `pathos` ・ `kudos` ・ `hers` が形を崩されます。`sans`（`sans serif`）と `cosmos` はヒーローイメージの指示に現れ得ます。

## 指摘

### 要修正 1：助詞に「は」を加えたことで、打ち消しの文を新たに止めます

`origin/main` と同じ 10 文で比べたところ、**10 件中 7 件が新たに止まります。**

```
他社のロゴは使用しません。            他社の商標は掲載しておりません。
競合のロゴは載せないでください。       他社のロゴは配置しないでください。
他社のロゴは、当社の制作物には入れません。
他社のロゴは著作権法上そのまま使えません。  他社のロゴは使いません。
```

いずれも権利に配慮したお客さまの文であり、止めるべきものと正反対の内容です。「は」は主題を示す助詞で、打ち消しや対比と結び付きやすいため、影響が大きく出ます。

### 要修正 2：語の内側に記号がある語は、いまも語の切れ目で見ません

落とすのは語の**前後**の記号だけですので、内側に記号を持つ語は部分一致へ落ちます。

```
規則辞書に art's を登録 -> 「smart's clean layout」が丸ごと消えます
規則辞書に a/b を登録   -> 「a photographer of a/b tests」が丸ごと消えます
```

**片付いたのは前後の記号だけです。** 記録の書きぶりを実態へそろえるか、実装を直してください。

### 要修正 3：`WordForms` の検めの経路に、テストが 1 件もありません

`InvalidDefinitionError` は 4 箇所で投げられますが、テストがこの型を 1 度も参照していません。**記録には「テストで確認した」と書かれていますが、該当するテストが存在しません。**

## 提案

1. `singular_words` へ `chaos` ・ `cosmos` ・ `sans` ・ `pathos` ・ `kudos` を足してください。
2. `es` の扱いに揺れがあります（`lenses` -> `lense`）。取りこぼしの向きですので害は小さいものの、手当てを検討してください。
3. `forbidden_input_coverage.rb` に `# frozen_string_literal: true` がありません。
4. 計測スクリプトの 2 本は、手続きをトップレベルへ直接書いています。CLAUDE.md の「制御構文・条件構文以外はクラスまたは関数に書きます」に照らすと、関数へ入れる形が望ましいです。
5. `WordForms` の変換にトレースがありません。
6. `WordForms` の定義は遅延読み込みです。壊れていると、起動時ではなく最初の生成時に落ちます。

## 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | 適合。ただし要修正 1 が、権利に配慮したお客さまを締め出す向きに働きます |
| OWASP10 | 適合 |
| QC10 | 要修正 1 ・ 3 の 2 点を残します |
| CRAP | 対象となる画面の変更がありません |
| DP | 適合。`WordForms` を独立させた分割は妥当です |
| TM | 要修正 3 を残します。それ以外は両方向の計測を含めて良好です |

## 正規表現の危険

追加分に入れ子の量指定子も後方参照もありません。長さ 4,800 文字の入力を 30 回検査して、`main` が 0.127 秒、本 PR が 0.141 秒でした（**約 1.1 倍、1 回あたり約 4.7 ミリ秒**）。長さに対して線形で、爆発しません。**処理時間の観点で妨げはありません。**

---

# コードレビュー : PR #144（再確認）

- 実施日時（JST）：2026-08-26 22:10
- 対象：issue #134 ／ issue #136
- 確認した差分：`1f1194b..17ebf54`
- 比較元：`origin/main`（`3d6b1b4`）
- 確認の方法：一時的な作業ツリーで RSpec ・ RuboCop ・ 計測スクリプトを実行し、**レビュー担当が独自に用意した素材でも両方向に測りました。** 作業ツリーは `DELETE/review-worktrees/pr144b-20260826-reviewer/` へ移しました。

## 判定

**合格**

初回の指摘 3 件と提案 4 件が、いずれも片付いていることを実測で確認しました。**マージを妨げるものはありません。**

下の「要修正」1 件は、この PR のマージを妨げません。呼び出す側がまだ無く、利用者に影響しないためです。**issue #55（生成リクエスト API）でつなぐ前に、別の issue として片付けてください。**

## 実行した結果

| 検査 | 結果 |
|---|---|
| RSpec | 704 例、失敗 0 件 |
| RuboCop | 96 ファイル、指摘 0 件 |
| `forbidden_input_coverage.rb` | 止めるべき 13 件の取りこぼし 0 件、通すべき 25 件の誤検出 0 件 |
| `anti_ai_coverage.rb` | 通すべき 40 件の誤検出 0 件、止めるべき 33 件の取りこぼし 11 件、短い語での巻き込み 5 件 |

ご報告いただいた数値を、いずれも再現できました。

## 指摘への対応の確認

### 要修正 1（打ち消しの文の誤検出）— 対応を確認しました

`src/backend/config/forbidden_inputs.yml` の 96 行目・98 行目に、先読み `(?![^。、]{0,4}(?:ません|ないで|ず|禁止|不可|不要))` が入りました。**初回に示した 7 文は、10 文とも通るようになりました。**

**検出側が痩せていないかを、独自の 17 文で確かめました。** 止めるべき文のうち 14 文はそのまま止まります。

```
他社のロゴは必ず入れてください。              -> 止める
他社のロゴを、迷わず中央へ配置してください。       -> 止める
他社のロゴは遠慮せず大きく載せてください。        -> 止める
他社のロゴを配置しても差し支えありません。        -> 止める
他社のロゴは使用しない理由がありません。         -> 止める
競合のロゴを並べて配置してください。ロゴは小さくしません。 -> 止める
```

**「必ず」「迷わず」「遠慮せず」のように、打ち消しでない「ず」を含む文が止まることを確認しました。** 先読みが意図の語の直後の 4 文字に限られ、読点と句点を窓に含めない設計が効いています。窓を 4 文字に絞った判断、読点・句点を除いた判断は、いずれも妥当です。

### 要修正 2（語の内側の記号）— 対応を確認しました

`AntiAiRules::ASCII_TERM` が `/\A[ -~]+\z/` へ広がりました。実測しました。

```
規則辞書に art's を登録 -> 「smart's clean layout」を残します（直す前は消えていました）
規則辞書に art's を登録 -> 「an art's studio」を取り除きます
規則辞書に ,     を登録 -> 「a, b」を巻き込みません（直す前は include? で全件に当たっていました）
```

**記号だけの語を登録した場合の巻き込みまで、あわせて解消されています。** これは初回に指摘していない改善です。日本語を含む語は引き続き `include?` へ落ちますので、語の切れ目の無い言語の扱いは変わっていません。

### 要修正 3（`WordForms` の異常系のテスト）— 対応を確認しました

`spec/services/generation/word_forms_spec.rb` に、`InvalidDefinitionError` を確かめるテストが 9 件入りました。定義が読めない・対応表が無い・空・値が文字列でない・同じ語へ寄せる・英字以外が混ざる・一覧が一覧でない、の各経路を覆っています。**セキュリティレビューの記録も、実態へそろえる形で追記されています。** 記録の正確さが回復しました。

### 提案 1・2（語の形）— 対応を確認しました

`s` で終わる語を 88 語、あらためて試しました。

```
chaos -> chaos   cosmos -> cosmos   sans -> sans   pathos -> pathos   kudos -> kudos
lenses -> lens   glasses -> glass   focuses -> focus   analyses -> analysis
houses -> house
```

**`plural_forms` を先に引き、語尾の規則で見分けられる語は対応表へ置かない設計は妥当です。** 対応表が肥大しません。`lens` ・ `glass` ・ `focus` ・ `analysis` ・ `canvas` ・ `news` ・ `series` ・ `species` ・ `bias` ・ `iris` ・ `axis` ・ `virus` ・ `business` ・ `address` ・ `process` ・ `access` はいずれも無傷でした。

### 提案 3・4（計測スクリプト）— 対応を確認しました

2 本とも `# frozen_string_literal: true` を持ち、素材の一覧と測る処理がクラスへ入りました。CLAUDE.md の「制御構文・条件構文以外はクラスまたは関数に書きます」に適合します。

## 語の切れ目で見る性質（再確認）

短い語だけを登録した規則辞書で、あらためて実測しました。

```
stealth -> nil   stealthy -> nil   artisan -> nil   smart -> nil
heartfelt -> nil orbit -> nil      reshape -> nil   landscape -> nil
teals -> "teal"  arts -> "art"     orbs -> "orb"    lenses -> "lens"
```

**`ASCII_TERM` を広げても、性質は壊れていません。** 独自の 45 件の素材（`sans-serif signage` ・ `two lenses laid on a felt mat` ・ `a pair of reading glasses` ・ `a cosmos of soft bokeh` ・ `ceramics drying on a rack` などを含みます）で、誤検出は 0 件のままでした。止めるべき 7 件（全角・大文字・連続空白・単数複数の別書き）も、すべて当たります。

## 要修正：打ち消しの言い回しのうち、丁寧な形が残ります

`src/backend/config/forbidden_inputs.yml` の 96 行目・98 行目です。窓が 4 文字で、打ち消しの語が 6 つに限られるため、**丁寧な打ち消しの言い回しが取りこぼされます。** 独自の 10 文で `origin/main` と比べました。

| 文 | main | 本 PR |
|---|---|---|
| 他社のロゴは掲載できません。 | 通す | 通す |
| 他社のロゴを使用することはできません。 | 止める | 止める |
| 他社のロゴを配置することはございません。 | 止める | 止める |
| 他社のロゴは載せることができません。 | 通す | **止める** |
| 他社のロゴを使用した実績はございません。 | 止める | 止める |
| 他社のロゴは掲載を控えております。 | 通す | **止める** |
| 他社のロゴを使用するつもりはありません。 | 止める | 止める |
| 他社のロゴは配置いたしかねます。 | 通す | **止める** |
| 他社の商標は使用を避けております。 | 通す | **止める** |
| 他社のロゴを載せる予定はありません。 | 止める | 止める |

誤検出は main が 5 件、本 PR が 9 件です。**「は」を加えた分の増加が 4 件残っています。** いずれも「意図の語」と打ち消しの間が 4 文字を超える形（「することはできません」「ことができません」「を控えております」「いたしかねます」「を避けております」）です。

**これは新種の欠陥ではありません。** 打ち消しを見ない性質は `を` の形で main にもあり（10 文中 5 件）、regexp で日本語の打ち消しを網羅すること自体に限界があります。窓を広げると別の文の打ち消しまで拾うという、いただいた判断も正しいと考えます。

**issue #55 でつなぐ前に、別の issue として扱ってください。** 打ち消しの認識を規則の外へ出し（意図の語より後ろの文節を見る、または打ち消しの語尾の一覧を設定ファイルへ分離する）、上の 10 文を計測の素材へ足す形をお勧めします。

## 提案

1. 打ち消しの語のうち **`ず` は、単独では取り違えます。** 実測で `他社のロゴを配置ずみの版を再現してください。` が通るようになりました（main は止めます）。`ず(?:に|、|。)` または `ず(?![みつ])` の形にすると、`載せず、` と `入れずに` を除いたまま、`配置ずみ` を止められます。
2. `sunglasses -> sunglasse` ・ `eyeglasses -> eyeglasse` が残ります。`plural_forms` へ足すか、対応表の引きを語尾の一致まで広げてください。害は小さいものの、`glasses` を直したのと同じ理由が当てはまります。
3. **提案 5（`WordForms` のトレース）と提案 6（起動時の検め）は、issue #54 ・ #55 の段階で扱う判断に賛成します。別 issue としての起票をお勧めします。** どちらも「利用者の指定が黙って消えたときに追えるか」に関わり、呼び出す側の設計と一体で決めるのが妥当です。上の要修正 1 件と提案 1 件も、同じ issue にまとめて構いません。

## 各基準への適合

| 基準 | 判定 |
|---|---|
| CC | 適合 |
| OWASP10 | 適合 |
| QC10 | 適合。記録と実装の食い違いが解消されました |
| CRAP | 対象となる画面の変更がありません |
| DP | 適合。`plural_forms` を「語尾の規則で見分けられない語だけ」に限る分割は妥当です |
| TM | 適合。異常系 9 件が加わり、両方向の計測素材も 15 件から 25 件へ増えました |

## 正規表現の危険

足された先読み `(?![^。、]{0,4}(?:ません|ないで|ず|禁止|不可|不要))` は、窓が 4 文字に固定され、入れ子の量指定子も後方参照もありません。`ASCII_TERM` の `/\A[ -~]+\z/` は文字クラス 1 つの繰り返しで、あいまいさがありません。`matcher_for` は引き続き `Regexp.escape` を通します。

処理時間です。長さ 4,800 文字の入力を 30 回検査して、`main` が 0.127 秒、本 PR が 0.150 秒でした（**約 1.18 倍、1 回あたり約 5.0 ミリ秒**）。先読みの追加による増加は約 6 パーセントです。長さに対して線形で、爆発しません。**処理時間の観点で妨げはありません。**

## 連絡先

`info@rictaworks.jp`
